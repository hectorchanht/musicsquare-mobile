---
quick: 260606-4pn
slug: settings-lyrics-translation-default-quality-source-extras
mode: quick
created: 2026-06-06
---

# Quick Task 260606-4pn: settings — lyrics translation, default quality/source, extras

## Tasks
1. settings store (runes, localStorage `openmusic:settings:v1`, SSR-guarded): lyricsLang, translateMode, defaultQuality, defaultSource, accent, reduceMotion, autoExpandOnPlay; applyTheme(); loaded in (app)/+layout.
2. settings page controls (chips/segmented/toggles/swatches) writing to the store.
3. /api/translate (+server.ts) unofficial Google translate, batched + fallback; translate.ts client with caching.
4. NowPlaying lyrics translation per mode (replace / below), keep highlight + auto-scroll.
5. Wiring: accent + reduce-motion live on <html> (+ app.css rule); defaultSource → dedupeBest(preferred) across all lists + queue; autoExpandOnPlay → player.expanded.

## Constraints
Phase-1 music data layer + tests untouched (new translate route is isolated); no circular imports; SSR-guard localStorage; lucide icons; variant-C.

## must_haves
- Settings persist; accent + reduce-motion apply live.
- Lyrics translate to chosen language in chosen mode.
- defaultSource biases dedupe; autoExpand works.
- check 0/0, build ok, 58 tests, deployed; /api/translate returns translations.
