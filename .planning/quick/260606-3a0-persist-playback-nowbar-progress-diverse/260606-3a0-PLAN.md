---
quick: 260606-3a0
slug: persist-playback-nowbar-progress-diverse-cached-top-picks
mode: quick
created: 2026-06-06
---

# Quick Task 260606-3a0: Persist playback · nowbar progress · diverse cached Top Picks + Randomize

## Tasks
1. Move `<audio>` + `player.attach()` to root `+layout.svelte` so playback survives all client-side nav; remove duplicate from `(app)/+layout.svelte`.
2. Add a thin progress sliver to the top edge of the mini nowbar (currentTime/duration, guarded).
3. Home Top Picks = one hit from N distinct artists (artist pool + `Promise.allSettled(searchAll)`, dedupe by uid).
4. Cache picks in `localStorage` (`musicsquare:top-picks:v1`); render from cache on mount, fetch only on cold cache; add ↻ Randomize button (resample + refetch + overwrite cache). SSR-guard localStorage.

## Constraints
Reuse player/catalog/Track; no data-layer edits; no new deps; mobile-first; `pnpm check` 0/0 + build + deploy.

## must_haves
- Playback continues uninterrupted across home/search/artist navigation.
- Mini nowbar shows a live progress sliver.
- Home grid shows distinct artists; loads instantly from cache; Randomize fetches a fresh set.
- check 0/0, build ok, deployed.
