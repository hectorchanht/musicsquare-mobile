---
quick: 260606-3f6
slug: dedupe-cross-source-hide-source-draggable-subnav-smart-lyrics-autogrow
mode: quick
created: 2026-06-06
---

# Quick Task 260606-3f6: hide source · cross-source dedupe/best-quality · draggable subnav · smart lyrics · auto-grow queue

## Tasks
1. Remove source badges/labels from all user-facing lists (home/search/artist/NowPlaying); keep `track.source` internal; /spike unaffected.
2. `src/lib/services/dedupe.ts` → `dedupeBest()`: group by normalized title+artist, keep best quality (quality-rank → source tie-break), order-preserving. Apply to all shown lists + the queue. Do NOT touch catalog.ts.
3. `src/lib/services/picks.ts` → `buildDiversePicks(count, excludeUids)` (move artist-pool logic out of home; reuse dedupeBest). Home + queue auto-grow use it.
4. NowPlaying: draggable sub-nav sheet — drag up = full screen, drag down = restore now-playing (separate handle from cover-collapse gesture).
5. NowPlaying lyrics: auto-scroll active line to view; pause on user touch/scroll; resume + re-center ~2.5s after idle.
6. Player store: auto-advance on `ended`; `ensureAhead()` grows the queue (buildDiversePicks, exclude existing, dedupe) when within 2 of the end; `growing` guard.

## Constraints
Phase-1 data layer + tests untouched (58 green); no new deps; mobile-first; `pnpm check` 0/0 + build + deploy.

## must_haves
- No source labels render on user-facing pages.
- Duplicate songs across sources collapse to one (best quality).
- Sub-nav drags up to full / down to restore; lyrics smart-scroll; queue auto-advances + auto-grows.
- check 0/0, build ok, 58 tests, deployed.
