---
phase: quick-260611-fr9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/components/Nowbar.svelte
  - src/routes/(app)/+layout.svelte
  - src/lib/components/NowPlaying.svelte
autonomous: false
requirements: [FR9-NOWBAR-BLEND, FR9-NAV-ICONS, FR9-NP-TOP-PAD]
must_haves:
  truths:
    - "The nowbar and the bottom nav read as one continuous bottom surface — no visible gap between them."
    - "The nowbar's top corners are rounded; its bottom edge sits flush against the bottom nav (no gap)."
    - "On the NowPlaying full-screen page, content starts flush at the very top (no top padding/gap above the header)."
    - "The bottom-nav tab for the current route shows a FILLED icon; the other tabs show OUTLINE icons."
    - "On a cold reload, the nowbar still shows the last-played track (persisted current) and the now-playing queue tab still lists the persisted up-next."
  artifacts:
    - path: "src/lib/components/Nowbar.svelte"
      provides: "Docked nowbar restyled to blend flush with the tabbar, rounded top corners, no bottom gap"
    - path: "src/routes/(app)/+layout.svelte"
      provides: "Bottom-nav filled/outline active-state icons + contiguous nowbar↔tabbar surface"
    - path: "src/lib/components/NowPlaying.svelte"
      provides: "NowPlaying top padding removed (flush top)"
  key_links:
    - from: "src/routes/(app)/+layout.svelte (.tab.active)"
      to: "page.url.pathname"
      via: "fill prop on Lucide icon"
      pattern: "fill=\\{.*active"
---

<objective>
Restyle the docked nowbar (mini-player) so it visually blends into the bottom nav bar as a single continuous bottom surface (YouTube Music style): the nowbar sits flush directly on top of the tabbar with no gap between them, has rounded TOP corners, and the combined surface clears the iOS home indicator. Also remove the top padding on the full-screen NowPlaying page so content starts flush at the top, and make each bottom-nav tab icon FILLED for the current route / OUTLINE for inactive routes.

Purpose: The current nowbar is a separate floating bar with a visible gap above the tabbar. The target is an app-native, cohesive bottom surface plus clearer active-tab indication.

Output: Restyled `Nowbar.svelte` (docked variant only), updated `(app)/+layout.svelte` nav + spacing, NowPlaying flush top.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md

<!-- Primary restyle targets — already read by the planner; key facts embedded below. -->
@src/lib/components/Nowbar.svelte
@src/routes/(app)/+layout.svelte
@src/lib/components/NowPlaying.svelte

<grounding>
Verified facts (do not re-discover):

- This is Svelte 5 runes ($state/$derived/$props/$effect). Match existing idioms. NO new dependencies.
- Design tokens live in src/app.css :root — reuse them, never hardcode colors/radii:
  - `--nowbar-h: 64px`, `--tabbar-h: 56px`, `--color-bg`, `--color-border`, `--color-primary`,
    `--color-text`, `--color-text-muted`, `--radius-lg: 18px`.
- PERSISTENCE ALREADY EXISTS — this is a SURFACING-confirm task, NOT new infra:
  - `player.restore()` is already called from the ROOT layout `src/routes/+layout.svelte:25`
    (`void player.restore()`), rehydrating last-played `current` + `queue` + progress from
    localStorage key `openmusic:player:v1`.
  - `Nowbar.svelte` already renders `player.current ?? player.pendingTrack`, so the last-played
    track already surfaces in the nowbar on cold load.
  - The up-next queue already renders in `NowPlaying.svelte`'s queue tab (`player.queue`).
  - => Do NOT touch player.svelte.ts. Do NOT redesign the queue/persistence model. Task 3 only
    VERIFIES this still works after the restyle.

- Nowbar.svelte CURRENT docked styling (the `.nowbar` rule, NOT `.embed`):
  - `position: fixed; left: 8px; right: 8px; bottom: calc(var(--tabbar-h) + 6px);`
  - `border-radius: 14px;` (all four corners), `max-width: 704px; margin: 2px auto 0;` plus a
    stray `margin-top: 4px;`, `background: rgba(40,32,60,0.55)` + `backdrop-filter: blur(14px)`,
    `border: 1px solid rgba(255,255,255,0.08)`, `z-index: 20`, `overflow: hidden`.
  - There is a `.nowbar::before` blur pseudo-element (decorative); leave its blur logic intact
    but it must follow the same rounded-TOP / square-bottom shape as the bar (inherit
    border-radius or set explicitly) so no square corners peek out.
  - The `.embed` variant (used inside NowPlaying fullshrink) MUST stay position:static and is
    OUT OF SCOPE — only change rules scoped to docked `.nowbar` (not `.nowbar.embed`).

- (app)/+layout.svelte CURRENT bottom structure:
  - `.app` has `padding-bottom: calc(var(--nowbar-h) + var(--tabbar-h));` (keep content clear of
    the combined bottom surface).
  - `.tabbar` is `position: fixed; bottom: 0; height: var(--tabbar-h); background: var(--color-bg);
    border-top: 1px solid var(--color-border); padding-bottom: env(safe-area-inset-bottom);
    z-index: 21;` — the tabbar already owns the safe-area inset and sits ABOVE the nowbar (z21 > z20).
  - Nav markup (lines ~161-168): `{#each tabs as tab}` renders `<a class="tab" class:active={page.url.pathname === tab.href}>` with `<span class="ic"><Icon size={20} /></span>`. `tabs` = House `/`, Search `/search`, Library `/library` (imported from `@lucide/svelte`).

- Lucide is OUTLINE-ONLY (no separate filled House/Search/Library components). The established
  codebase idiom for a filled active state is the `fill` prop: NowPlaying.svelte:731 already does
  `<Heart ... fill={currentLiked ? 'currentColor' : 'none'} />`. Lucide's Icon.svelte spreads
  `...props` onto its `<svg>`, so `fill="currentColor"` fills the shape, `fill="none"` keeps it
  outline. => Use `fill={active ? 'currentColor' : 'none'}` per tab. Do NOT add a new icon library.
</grounding>

<interfaces>
From src/routes/(app)/+layout.svelte:
```ts
import { page } from '$app/state';            // page.url.pathname for active route
import { House, Search, Library } from '@lucide/svelte';
const tabs: { href: string; labelKey: TranslationKey; icon: Component }[];
// render: class:active={page.url.pathname === tab.href}
```
From src/lib/components/Nowbar.svelte:
```ts
let { variant = 'docked', onOpen }: { variant?: Variant; onOpen?: () => void } = $props();
// markup root: <div class="nowbar" class:embed={variant === 'embed'}> ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Blend the docked nowbar into the tabbar (rounded top, flush bottom)</name>
  <files>src/lib/components/Nowbar.svelte, src/routes/(app)/+layout.svelte</files>
  <action>
Restyle ONLY the docked `.nowbar` rule (leave `.nowbar.embed` untouched) so the nowbar reads as one continuous surface with the bottom nav, YouTube-Music style.

In src/lib/components/Nowbar.svelte `.nowbar`:
- Make it span full width flush to the tabbar: set `left: 0; right: 0;` (remove the 8px side insets) and `bottom: var(--tabbar-h);` so its bottom edge sits flush directly on top of the tabbar (the tabbar owns the safe-area inset, so do NOT add safe-area padding here — that would double-count). Remove the `+ 6px` gap.
- Rounded TOP corners only, square bottom (it touches the tabbar): replace `border-radius: 14px;` with `border-radius: var(--radius-lg) var(--radius-lg) 0 0;`.
- Remove the gap-producing margins: delete the stray `margin-top: 4px;` and change `margin: 2px auto 0;` to `margin: 0 auto;`. Keep `max-width: 704px;` so on wide/desktop the bar stays centered (the rounded top still reads fine centered above a full-width tabbar; acceptable per mobile-first scope).
- Make the surface read as the SAME material as the tabbar so they blend: keep the translucent blur look but ensure the bottom border does not draw a line between nowbar and tabbar — remove any `border-bottom` if a full `border` is present by switching `border: 1px solid rgba(255,255,255,0.08);` to top+sides only: `border-width: 1px 1px 0 1px; border-style: solid; border-color: rgba(255,255,255,0.08);` (Claude's discretion: if simpler, keep `border: 1px solid ...` and rely on the flush bottom + tabbar's own `border-top` — the visual goal is no double divider between the two).
- Fix `.nowbar::before` (the decorative blur pseudo) so it follows the new shape: give it `border-radius: inherit;` (or `var(--radius-lg) var(--radius-lg) 0 0`) so no square corner peeks past the rounded top. Leave the blur/z-index/pointer-events logic intact.

In src/routes/(app)/+layout.svelte:
- The nowbar now sits flush at `bottom: var(--tabbar-h)` and the tabbar at `bottom: 0`. Confirm `.app { padding-bottom: calc(var(--nowbar-h) + var(--tabbar-h)); }` still clears page content above the combined surface — it does (sum unchanged); leave it. (If the removed 6px gap leaves content slightly too close, this is fine — the combined surface height is unchanged.)
- Optionally (Claude's discretion) tighten the seam: ensure the tabbar's `border-top` is the only divider; do not add a second divider. No structural markup change required here for Task 1.

Do NOT alter `.np-prog`, `.np-open`, `.np-btn`, `.st-badge`, the `.embed` overrides, or any JS.
  </action>
  <verify>
    <automated>npm run check 2>&1 | tail -5</automated>
    <human-check>Run `npm run dev`, play a track on mobile viewport (DevTools device mode). Confirm: (1) the nowbar's bottom edge touches the tabbar with NO visible gap, reading as one continuous bottom surface; (2) the nowbar has rounded TOP corners and a square bottom; (3) no double divider line between nowbar and tabbar; (4) the iOS home-indicator safe area is still cleared (the tabbar's bottom padding is intact — no content clipped). Toggle light theme — surface still blends.</human-check>
  </verify>
  <done>`npm run check` passes (no new svelte-check errors). Docked nowbar is full-width, flush against the tabbar (no gap), rounded top corners only, single divider. `.embed` variant unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Filled/outline active-state bottom-nav icons + flush NowPlaying top</name>
  <files>src/routes/(app)/+layout.svelte, src/lib/components/NowPlaying.svelte</files>
  <action>
Two independent UI changes; both touch only styling/markup.

A) Bottom-nav filled-vs-outline icons (FR9-NAV-ICONS) — in src/routes/(app)/+layout.svelte:
- Lucide is outline-only; use the existing `fill` prop idiom (matches NowPlaying.svelte:731 `Heart fill={...}`). In the `{#each tabs}` block, compute the active flag once and pass it to the icon. Change the rendered icon from `<Icon size={20} />` to `<Icon size={20} fill={page.url.pathname === tab.href ? 'currentColor' : 'none'} />`. (The `class:active` on the `<a>` already drives the color via `.tab.active { color: var(--color-text); }`, so the filled icon adopts the active text color; inactive icons render as muted outlines via `.tab { color: var(--color-text-muted); }`.)
- If readability of the filled glyph needs it (Claude's discretion), nudge the active icon's `stroke-width` down slightly so the filled shape doesn't look heavy — e.g. pass `strokeWidth={page.url.pathname === tab.href ? 1.5 : 2}`. Optional; only if it looks heavy.
- Do NOT introduce a new icon set or separate filled component imports. Keep House/Search/Library.

B) NowPlaying flush top (FR9-NP-TOP-PAD) — in src/lib/components/NowPlaying.svelte:
- The `.np` rule (style block, ~line 831) is `padding: 0px 18px env(safe-area-inset-bottom);` — the top is already `0`. Confirm there is no other top gap above the header. The header `.bar` and `.np-top` follow immediately. If the requirement "no padding at the top" reveals a residual gap (e.g. the `.np.fullshrink .sheet.full { inset: calc(var(--nowbar-h) + 4px) 0 0 0; }` reserves space ONLY in fullshrink, which is correct), leave fullshrink alone. For the DEFAULT (closed/half) NowPlaying view, ensure content starts flush: the `.bar` should sit at the very top with no extra margin/padding above it. If any top inset exists, set it to 0. Add a comment noting top is intentionally flush per quick-260611-fr9.
- Respect existing safe-area: do NOT remove `env(safe-area-inset-bottom)` from `.np` padding. Only the TOP is flush.

Do NOT change the sheet snap machine, drag handlers, lyrics, or queue logic.
  </action>
  <verify>
    <automated>npm run check 2>&1 | tail -5</automated>
    <human-check>Run `npm run dev` (mobile viewport). (1) Navigate Home/Search/Library — the CURRENT tab's icon is FILLED (solid) and the other two are OUTLINE (stroke). Active tab label/icon use the bright text color; inactive are muted. (2) Expand the now-playing page — the header (ChevronDown / kebab) starts flush at the very top with no gap above it. Drag the sheet to half/full — the embedded top bar layout still looks correct (fullshrink unchanged).</human-check>
  </verify>
  <done>`npm run check` passes. Active route's nav icon is filled, others outline, via the `fill` prop (no new deps). NowPlaying default view content starts flush at the top; bottom safe-area inset preserved; fullshrink/sheet behavior unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Restyled docked nowbar blended flush with the bottom nav (rounded top, no bottom gap), filled/outline active-state nav icons, and flush NowPlaying top — all CSS/markup only, no store changes. Persisted last-played track + up-next queue surfacing was already wired (player.restore() in the root layout) and was NOT modified.</what-built>
  <how-to-verify>
1. `npm run dev`, open in mobile-sized viewport (DevTools device toolbar, e.g. iPhone).
2. Play a song. Confirm the nowbar + bottom nav read as ONE continuous bottom surface: nowbar sits flush on top of the tabbar, NO gap, rounded TOP corners, square bottom, no double divider line.
3. Hard-reload the page (cold load). Confirm the nowbar STILL shows the last-played song (title/artist/cover) without playing — this proves persisted `current` still surfaces after the restyle.
4. Tap the nowbar to expand NowPlaying → open the "Up Next" tab. Confirm the persisted queue still lists tracks (persisted up-next intact).
5. Confirm the NowPlaying page content starts FLUSH at the top (no gap above the ChevronDown/kebab header).
6. Tab through Home / Search / Library: the current tab's icon is FILLED, the other two are OUTLINE.
7. Verify iOS home-indicator clearance: the tabbar's bottom safe-area padding is intact (nothing clipped at the very bottom).
8. Toggle light theme (settings) and re-check the blend reads correctly in both themes.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks off (e.g. residual gap, divider line, icon not filling, top still padded).</resume-signal>
</task>

</tasks>

<verification>
- `npm run check` passes with no new svelte-check errors after both code tasks.
- No edits to src/lib/stores/player.svelte.ts (persistence untouched) — confirm via `git diff --stat`.
- Only `.nowbar` (docked) styling changed in Nowbar.svelte; `.nowbar.embed` rules are byte-identical.
- No new dependencies added (no package.json change).
</verification>

<success_criteria>
- The docked nowbar and the bottom nav render as one continuous bottom surface: flush (no gap), rounded top corners, square bottom, single divider.
- Each bottom-nav tab shows a FILLED icon when it is the active route and an OUTLINE icon otherwise, using the existing Lucide `fill` prop idiom (no new icon library).
- The NowPlaying full-screen page starts content flush at the top (no top padding) while preserving the bottom safe-area inset.
- On a cold reload the nowbar still shows the persisted last-played track and the now-playing queue tab still lists the persisted up-next (no persistence regression — that layer was not touched).
- iOS home-indicator safe area still cleared by the tabbar.
</success_criteria>

<output>
Create `.planning/quick/260611-fr9-restyle-nowbar-to-blend-with-bottom-nav-/260611-fr9-SUMMARY.md` when done.
</output>
