---
phase: 23-ux-audit-homepage-artist-polish
plan: 04
type: execute
wave: 2
depends_on: ["23-01", "23-03"]
files_modified:
  - src/lib/components/CompactRow.svelte
  - src/lib/components/CompactPager.svelte
  - src/routes/(app)/+page.svelte
  - src/routes/(app)/settings/home/+page.svelte
  - src/lib/i18n/en.ts
autonomous: true
requirements: [HOME-02, HOME-03, HOME-04]
must_haves:
  truths:
    - "Every home section renders compact by default (quick-picks pager: 4 stacked rows per column, ~90vw columns with scroll-snap)"
    - "A compact track-row: tap=play, trailing ⋮=open TrackMenu, long-press=open TrackMenu; a compact artist-row: round avatar, tap=artist page, no ⋮ and no long-press"
    - "Each section title is a full-row tap target with a trailing chevron that navigates to the correct destination (chart page / library tab / playlist detail)"
    - "A per-section density control in /settings/home flips individual sections back to comfortable (aria-pressed reflects state)"
    - "Compact item count = homeShelfSize rounded to a full column of 4; the home local toast is migrated to toast.show()"
  artifacts:
    - path: "src/lib/components/CompactRow.svelte"
      provides: "Compact track-row and artist-row variants (D-09)"
      contains: "MoreVertical"
    - path: "src/lib/components/CompactPager.svelte"
      provides: "Horizontal scroll-snap pager, 4 rows per column (D-05/D-06)"
      contains: "scroll-snap"
    - path: "src/routes/(app)/+page.svelte"
      provides: "Compact branch + title-row nav + toast migration"
      contains: "CompactPager"
    - path: "src/routes/(app)/settings/home/+page.svelte"
      provides: "Per-section density control (D-07)"
      contains: "homeSectionDensity"
  key_links:
    - from: "src/routes/(app)/+page.svelte"
      to: "src/lib/services/home-layout.ts"
      via: "resolveSectionDensity(id, settings.homeSectionDensity, 'compact')"
      pattern: "resolveSectionDensity"
    - from: "src/lib/components/CompactRow.svelte"
      to: "src/lib/components/TrackMenu.svelte"
      via: "⋮ and long-press open the track menu"
      pattern: "longpress|menuOpen"
---

<objective>
Ship the compact homepage (HOME-02/03): new `CompactRow` + `CompactPager` components, the home page compact branch (compact by default per D-07), the section-title navigation rows (HOME-04, D-14), the per-section density control in `/settings/home`, and migrate the home local toast to the global store.

Purpose: HOME-02/03/04 — per-section compact rows-of-4, ⋮/long-press menu on compact rows, and whole-title-row navigation to chart pages / library tabs / playlist detail.
Output: 2 new components, home page render branch + nav, settings control, new i18n keys.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

Tooling: prefix PATH with `$HOME/.nvm/versions/node/v22.22.0/bin` and use pnpm for every check/test command.
NOTE: long text in titles/subtitles MUST use `use:marquee` + `.marquee-inner` (project memory rule) — never static ellipsis. Skeletons must match loaded count/size/length exactly.
</execution_context>

<context>
@.planning/phases/23-ux-audit-homepage-artist-polish/23-CONTEXT.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-PATTERNS.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-01-SUMMARY.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CompactRow + CompactPager components (D-05/06/08/09, §4.1)</name>
  <files>src/lib/components/CompactRow.svelte, src/lib/components/CompactPager.svelte</files>
  <read_first>
    - src/routes/(app)/search/+page.svelte (lines 511-525 `.row` markup with `use:longpress onlongpress` + `onclick` + `use:lazyCover`; styles 596-607 `.art`; skeletonRows snippet 473-485)
    - src/routes/(app)/+page.svelte (`.albumrow` + `use:dragScroll` ~685; `tileMenu()` ~543; tile longpress/onclick ~648)
    - src/lib/components/TrackMenu.svelte (lines 222-223 marquee idiom; the menuTrack/menuOpen open pattern)
    - src/lib/actions/longpress.ts, src/lib/actions/marquee.ts (use:marquee + .marquee-inner)
    - src/lib/util/haptics.ts (tick() — call on long-press fire per §3.3)
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md §4.1 (exact geometry)
  </read_first>
  <action>Create `src/lib/components/CompactRow.svelte` with a `variant: 'track' | 'artist'` prop. Track variant: `display:flex; align-items:center; gap:8px;` min-height 44px; 40×40px art with `border-radius:6px` + `use:lazyCover` (search idiom) + fallback gradient; `.r-title` (14px/600) + `.r-sub` (12px/400 muted), both `min-width:0` with `use:marquee` + `.marquee-inner` on the inner span; a trailing `<button>` with `MoreVertical size={18}`, `≥44px` tap target, `aria-label={t('menu.options')}`. Interactions: `use:longpress onlongpress` (call `(e.currentTarget)?.blur()` MENU-03 guard + `haptics.tick()` + emit a `requestmenu` callback prop) and `onclick` (emit `play` callback prop); the ⋮ button emits `requestmenu`. Artist variant: round avatar (`border-radius:var(--radius-full)`, 40px), name only (single line `use:marquee`), `onclick` emits `open` (artist page), NO ⋮ button, NO longpress. Use callback props (`onplay`, `onrequestmenu`, `onopen`) — the host (+page) owns TrackMenu state. Create `src/lib/components/CompactPager.svelte` taking `items` + a `column` snippet/children; render a horizontal track `scroll-snap-type: x mandatory`, columns of `~90vw` width with `scroll-snap-align: start`, `gap:12px`, chunking items into `ceil(items.length / 4)` columns of 4 stacked rows. All net-new spacing from the 4/8/12/16 scale (§1). Import icons from `@lucide/svelte` (MoreVertical).</action>
  <verify>
    <automated>PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/components/CompactRow.svelte has a `variant` prop; track variant renders a `MoreVertical` ⋮ button with `aria-label={t('menu.options')}`; artist variant renders NO ⋮ button and no `use:longpress`
    - CompactRow uses `use:marquee` + `.marquee-inner` on title/subtitle (no static ellipsis)
    - CompactRow art is 40px with border-radius 6px (track) / var(--radius-full) (artist)
    - CompactPager CSS contains `scroll-snap-type: x mandatory` and per-column `scroll-snap-align`; columns are ~90vw and chunk items in groups of 4
    - `pnpm check` exits 0
  </acceptance_criteria>
  <done>Both compact components exist with the §4.1 geometry, correct track/artist interaction split, marquee long-text, and scroll-snap pager; pnpm check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Home compact branch + section-title nav + toast migration (D-05/07/10/14, HOME-04 §5.1/§5.2)</name>
  <files>src/routes/(app)/+page.svelte</files>
  <read_first>
    - src/routes/(app)/+page.svelte (section render loop ~665 `{#each resolveSectionOrder(...)}`; per-section `.albumrow` blocks; `.subhead` style ~831; the home local `toast()` copy ~217-222; tileMenu ~543; `goto` import line 3; section skeleton ~638-641)
    - src/lib/services/home-layout.ts (resolveSectionDensity from Plan 03; HOME_SECTIONS ids; clampShelfSize)
    - src/lib/stores/settings.svelte.ts (settings.homeSectionDensity from Plan 03; settings.homeShelfSize)
    - src/lib/stores/toast.svelte.ts (Plan 01 — toast.show)
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-01-SUMMARY.md, 23-03-SUMMARY.md
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md §5.1, §5.2 (redirect target table)
  </read_first>
  <action>In `+page.svelte`: (1) Migrate the local `toast()` copy (~217-222) to `import { toast } from '$lib/stores/toast.svelte'` and replace its call sites with `toast.show(...)`; remove the now-dead local toastMsg/toastTimer state and its inline `.toast` render (ToastHost in the layout now renders it). (2) For each section block, compute `const density = resolveSectionDensity(id, settings.homeSectionDensity, 'compact')` (pass `'compact'` as globalDefault to ship D-07 compact-by-default) and branch: when `density === 'compact'` render a `<CompactPager>` of `<CompactRow>`s instead of `.albumrow`; track sections use the track variant (onplay → player.play + setQueue like the existing tile; onrequestmenu → set menuTrack/menuOpen), artist sections (top-artists/fav-artists) use the artist variant (onopen → `goto('/artist/'+encodeURIComponent(name))`). Item count = `clampShelfSize(settings.homeShelfSize)` rounded UP to the nearest multiple of 4 (D-10). Add compact-row skeletons (40px art + 2 bars 62%/40%, 4 per column) gated on the section's loading flag. (3) Replace each static `<div class="subhead">{title}</div>` with `<button class="subhead-nav" aria-label={title + ', ' + t('home.seeAll')} onclick={() => goto(<dest>)}>`: full-width, left-aligned text, trailing `ChevronRight size={18}` muted via `margin-left:auto`, `≥44px` height, keeping the `.subhead` typography (0.95rem/700). Destinations per UI-SPEC §5.2: top-hits → `/charts/top`; top-artists → `/charts/top` (artists view — your discretion on exact top-level shape, kept under the per-type convention); tags shelf → `/charts/tags/{tag}`; countries shelf → `/charts/countries/{country}`; liked/downloads/history/fav-artists → `/library?tab={liked|downloads|history|fav-artists}`; per-playlist shelf → that playlist's detail deep-link in library (NOT the generic Playlists tab). Import CompactRow, CompactPager, ChevronRight.</action>
  <verify>
    <automated>PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - +page.svelte imports `toast` from `$lib/stores/toast.svelte` and contains no local `toastTimer`/inline `.toast` render
    - +page.svelte calls `resolveSectionDensity(` with `'compact'` as the third (globalDefault) argument
    - +page.svelte renders `<CompactPager` and `<CompactRow` for the compact branch
    - Each section title is a `<button class="subhead-nav"` with a `ChevronRight` and an aria-label including the see-all suffix
    - Library-mirror sections navigate to `/library?tab=...`; per-playlist shelves deep-link to the playlist detail (not the Playlists tab)
    - `pnpm check` exits 0
  </acceptance_criteria>
  <done>Home renders compact-by-default pagers with correct interactions, full-row title nav to the right destinations, count rounded to ×4, and uses the global toast.</done>
</task>

<task type="auto">
  <name>Task 3: Per-section density control in /settings/home + i18n keys (D-07, §7.1)</name>
  <files>src/routes/(app)/settings/home/+page.svelte, src/lib/i18n/en.ts</files>
  <read_first>
    - src/routes/(app)/settings/home/+page.svelte (existing per-section order/hide controls — add the density control next to each section row)
    - src/lib/stores/settings.svelte.ts (settings.homeSectionDensity field + its setter/save path from Plan 03)
    - src/lib/services/home-layout.ts (HomeSectionId, HomeDensity)
    - src/lib/i18n/en.ts (key structure to extend; the i18n parity test enforces all 15 dicts)
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md §4.2, §9 (new keys table)
  </read_first>
  <action>In `/settings/home/+page.svelte` add a per-section density control to each section row in the existing order/hide list: a comfortable/compact toggle (two buttons or a segmented control) that reads/writes `settings.homeSectionDensity[sectionId]` and persists via the existing settings save path. Each toggle button carries `aria-pressed` reflecting the active mode (compact vs comfortable) and an `aria-label`. Label the control with `t('settings.homeSectionDensity')`; options labelled `t('settings.densityCompact')` / `t('settings.densityComfortable')`. Add the new i18n keys to `src/lib/i18n/en.ts`: `menu.options` ("Options"), `home.seeAll` ("See all"), `charts.topTitle` ("Top hits"), `charts.tagTitle` ("{tag}"), `charts.countryTitle` ("{country}"), `settings.homeSectionDensity` ("Layout"), `settings.densityCompact` ("Compact"), `settings.densityComfortable` ("Comfortable"). NOTE: these keys must also be added to the other 14 locale dicts in Plan 06 OR here — add to all 15 now if practical, otherwise the i18n parity test (`pnpm check`/vitest) will fail; add the same keys to all `src/lib/i18n/*.ts` dictionaries (translate where you can, English fallback acceptable) so parity passes.</action>
  <verify>
    <automated>PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm vitest run src/lib/i18n 2>&1 | tail -8; PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - /settings/home/+page.svelte renders a per-section density toggle bound to `settings.homeSectionDensity` with `aria-pressed` on the active option
    - All 8 new keys (menu.options, home.seeAll, charts.topTitle, charts.tagTitle, charts.countryTitle, settings.homeSectionDensity, settings.densityCompact, settings.densityComfortable) exist in src/lib/i18n/en.ts AND all other 14 locale files
    - The i18n parity test passes (no missing-key failures)
    - `pnpm check` exits 0
  </acceptance_criteria>
  <done>Per-section density control persists overrides with aria-pressed; all 8 new i18n keys present across 15 dicts; parity test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| section data → goto() URL | section tag/country/playlist values build navigation URLs |
| settings → density render | per-section density from storage drives the render branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-08 | Tampering (open redirect / injection) | section-title goto destinations | mitigate | Tag/country/playlist values are `encodeURIComponent`-wrapped before building the in-app route; navigation is same-origin `goto()` to fixed `/charts/*` and `/library` paths only (no external URL). Acceptance: title-nav goto targets are encodeURIComponent-wrapped relative paths. |
| T-23-09 | Denial of Service | compact render on corrupt density | mitigate | resolveSectionDensity (Plan 03) returns the global default on garbage, so a corrupt override never blanks the home render. Acceptance: invalid stored density still renders (covered by Plan 03 tests). |
</threat_model>

<verification>
- `pnpm check` exits 0; i18n parity test passes.
- Compact pager renders 4 rows/column with scroll-snap; title rows navigate correctly.
- Home no longer has a local toast implementation (uses global store).
</verification>

<success_criteria>
- All home sections compact by default with the quick-picks pager; per-section override in settings.
- Compact track-row ⋮ + long-press open TrackMenu; artist rows tap to artist page with no ⋮.
- Each section title navigates to the correct chart page / library tab / playlist detail.
</success_criteria>

<output>
Create `.planning/phases/23-ux-audit-homepage-artist-polish/23-04-SUMMARY.md` when done.
</output>
