---
phase: 20-now-playing-surface-gestures
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/components/NowPlaying.svelte
autonomous: true
requirements: [NP-02, NP-04]
must_haves:
  truths:
    - "A running-line loader shows at the very top of the now-playing view while player.loading is true"
    - "The loader uses the nowbar's exact .np-prog.indet > i.sliver markup and np-indet keyframe"
    - "Scrolling the .panel in half-open never chains scroll to the page behind the sheet"
  artifacts:
    - path: "src/lib/components/NowPlaying.svelte"
      provides: "Top running-line loader (NP-04) mounted at the .np view top edge + overscroll-behavior-y:contain on .panel (NP-02)"
      contains: "np-prog indet"
  key_links:
    - from: "src/lib/components/NowPlaying.svelte (.np-prog.indet)"
      to: "player.loading"
      via: "class:indet={player.loading} / {#if player.loading} drives sliver visibility"
      pattern: "player\\.loading"
    - from: "src/lib/components/NowPlaying.svelte (.panel)"
      to: "front-layer scroll containment"
      via: "overscroll-behavior-y: contain CSS"
      pattern: "overscroll-behavior-y:\\s*contain"
---

<objective>
Two small, gesture-free additions to NowPlaying.svelte that are independent of the carousel work and land first so the heavy gesture plan (20-03) edits a settled file:

1. **NP-04 — Top running-line loader.** Mount a copy of the nowbar's exact running-line loader (`<div class="np-prog indet"><i class="sliver"></i></div>` + the `np-indet` keyframe) at the very top edge of the `.np` view, full-bleed across the width, below `env(safe-area-inset-top)` and above the `.bar` header, driven by `player.loading`. Default: show in all sheet states; suppress in `full` only if it visually duplicates the embedded Nowbar's own `.np-prog`.
2. **NP-02 — Scroll containment.** Add `overscroll-behavior-y: contain;` to the scrollable `.panel` so half-open scroll/bounce stops at the panel's edges and never chains to the page behind the sheet. Keep the panel's `pan-y` scroll (do NOT set `touch-action: none`).

Purpose: deliver two of the five Phase-20 criteria as low-risk, no-gesture edits, isolated from the pointer-arbitration work so they can ship in wave 1 without touching the `npTop*` handlers.
Output: modified `src/lib/components/NowPlaying.svelte` (markup + CSS only; no JS handler changes).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-now-playing-surface-gestures/20-CONTEXT.md
@.planning/phases/20-now-playing-surface-gestures/20-UI-SPEC.md
@.planning/phases/20-now-playing-surface-gestures/20-PATTERNS.md

<interfaces>
<!-- Exact markup + CSS to copy from the nowbar, and the existing NowPlaying anchors. -->
From src/lib/components/Nowbar.svelte (NP-04 loader — copy class names verbatim):
```svelte
<div class="np-prog" class:indet={player.loading}>
  {#if player.loading}<i class="sliver"></i>{:else}<i style:width={...}></i>{/if}
</div>
```
```css
.np-prog { position: absolute; top: 0; left: 0; right: 0; height: 3px; background: rgba(255,255,255,0.12); }
.np-prog > i { display: block; height: 100%; background: var(--color-primary); transition: width 0.25s linear; }
.np-prog.indet { overflow: hidden; }
.np-prog.indet > i.sliver { width: 35%; transition: none; animation: np-indet 1.1s ease-in-out infinite; }
@keyframes np-indet { 0% { transform: translateX(-110%); } 100% { transform: translateX(310%); } }
@media (prefers-reduced-motion: reduce) { .np-prog.indet > i.sliver { animation-duration: 2.2s; } }
```
> The NP-04 loader is the indeterminate variant only (always `indet`, only the sliver). It does NOT carry the determinate progress `<i style:width>` — that is the nowbar's seek progress, not wanted at the NP top.

From src/lib/components/NowPlaying.svelte (anchors):
- `.np` root (line ~647-654): `<section class="np" ...>`; CSS `.np { position: fixed; inset: 0; z-index: 50; ... padding: 0 18px env(safe-area-inset-bottom); overflow: hidden; }` (line ~831). It is `display:flex; flex-direction:column`.
- Embedded `<Nowbar variant="embed" .../>` renders only when `sheetState === 'full'` (line ~661) and carries its OWN `.np-prog`.
- `.bar` header is the next child (line ~663).
- `.panel { flex: 1; overflow-y: auto; }` (line ~901) — the scroller in both half and full.
- `player.loading` is the existing reactive flag from `$lib/stores/player.svelte` (already imported as `player`).
- `sheetState` is `$state<'closed'|'half'|'full'>` (line ~414).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount the NP-04 top running-line loader</name>
  <files>src/lib/components/NowPlaying.svelte</files>
  <read_first>
    - src/lib/components/NowPlaying.svelte (the `.np` section root ~647-654, the embedded Nowbar conditional ~661, the `.bar` header ~663, and the `.np` CSS ~831 — see the current state before editing)
    - src/lib/components/Nowbar.svelte lines 38-48 (loader markup) and 177-211 (`.np-prog` CSS + `np-indet` keyframe + reduced-motion override to copy verbatim)
    - .planning/phases/20-now-playing-surface-gestures/20-UI-SPEC.md (§6 Top running-line loader: 3px height, rail `rgba(255,255,255,0.12)`, sliver `var(--color-primary)`, 35% width / `np-indet 1.1s`, mount full-bleed under `env(safe-area-inset-top)` above `.bar`, z-index above cover/bar; "show in all states; suppress-in-full is the only permitted fallback")
    - .planning/phases/20-now-playing-surface-gestures/20-PATTERNS.md (Shared Patterns → Running-line loader markup + keyframe; NowPlaying.svelte modify section NP-04 paragraph)
  </read_first>
  <action>Add a top loader element as the FIRST child inside the `.np` `<section>` (before the embedded Nowbar conditional and the `.bar` header): `<div class="np-top-loader np-prog indet"><i class="sliver"></i></div>` rendered when `player.loading` is true (wrap in `{#if player.loading}` so the element only exists while loading). Reuse the nowbar class names `np-prog indet` + `sliver` verbatim so it inherits the shared keyframe; add the `np-prog`/`np-prog.indet`/`.np-prog.indet > i.sliver` CSS + the `@keyframes np-indet` + the reduced-motion `@media (prefers-reduced-motion: reduce)` override into NowPlaying.svelte's `<style>` block, copied byte-for-byte from Nowbar.svelte (3px height, rail `rgba(255,255,255,0.12)`, sliver `var(--color-primary)`, 35% width, `np-indet 1.1s ease-in-out infinite`, reduced-motion 2.2s). Add a `.np-top-loader` positioning rule: `position: absolute; top: env(safe-area-inset-top); left: 0; right: 0; z-index: 60;` so it sits full-bleed flush with the notch-safe top edge, above the cover and `.bar` (which are within the `z-index:50` `.np`) but below any modal. Since `.np` has horizontal padding (`0 18px`), make the loader full-bleed by countering it (`left: 0; right: 0;` on the absolute element relative to `.np` works because padding does not offset an absolutely-positioned child's left/right:0). NP-04 default per UI-SPEC: render in all sheet states. Implement the permitted fallback as a guard ONLY if duplication is observed in `full`: `{#if player.loading && !(sheetState === 'full')}` — but the DEFAULT is unconditional `{#if player.loading}`; document in a code comment which you chose and why. Do NOT modify any pointer handler or the `npTop*` region.</action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && grep -v '^\s*//' src/lib/components/NowPlaying.svelte | grep -c "np-prog indet" | grep -qv '^0$' && grep -q "np-indet" src/lib/components/NowPlaying.svelte && grep -q "env(safe-area-inset-top)" src/lib/components/NowPlaying.svelte && pnpm check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - NowPlaying.svelte contains `<i class="sliver"></i>` inside a `np-prog indet` element rendered under `{#if player.loading}` (behavior assertion: the loader exists exactly when `player.loading === true`).
    - The loader markup is the FIRST child of the `.np` section, positioned `top: env(safe-area-inset-top)` and `z-index` above the cover/`.bar` (source assertion: a `.np-top-loader` rule with `z-index` > 50 and `top: env(safe-area-inset-top)`).
    - The copied CSS matches the nowbar exactly: `height: 3px`, rail `rgba(255,255,255,0.12)`, sliver `var(--color-primary)`, sliver `width: 35%; animation: np-indet 1.1s ease-in-out infinite`, plus the `@keyframes np-indet` and the reduced-motion `2.2s` override.
    - `pnpm check` exits 0.
    - No edits to `npTopDown`/`npTopMove`/`npTopUp` or any pointer handler (this is the carousel plan's region; `git diff` shows only markup/CSS additions, not handler changes).
    - Zero new i18n keys added.
  </acceptance_criteria>
  <done>The indeterminate running-line loader mounts at the NP view's top edge, driven by player.loading, matching the nowbar's loader byte-for-byte; pnpm check is 0/0.</done>
</task>

<task type="auto">
  <name>Task 2: Add overscroll-behavior-y: contain to .panel (NP-02)</name>
  <files>src/lib/components/NowPlaying.svelte</files>
  <read_first>
    - src/lib/components/NowPlaying.svelte line ~901 (`.panel { flex: 1; overflow-y: auto; }` — the scroller in both half and full; current state)
    - .planning/phases/20-now-playing-surface-gestures/20-UI-SPEC.md (§7 Scroll containment: add `overscroll-behavior-y: contain` to `.panel`; keep `pan-y`; do NOT set `touch-action: none`; confirm against both half and full; iOS <16 best-effort, no JS scroll-lock)
    - .planning/phases/20-now-playing-surface-gestures/20-PATTERNS.md (NowPlaying modify section → Scroll containment paragraph)
  </read_first>
  <action>Edit the `.panel` CSS rule (currently `.panel { flex: 1; overflow-y: auto; }`) to add `overscroll-behavior-y: contain;` so over-scroll/bounce stops at the panel edges and never chains to the page behind the half-open sheet. Do NOT add `touch-action: none` — the panel must keep scrolling (`pan-y` stays with the browser). This is a single CSS-line addition; no markup, no JS. Add a short code comment noting NP-02 + the iOS <16 best-effort caveat (no JS scroll-lock workaround in this phase).</action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && grep -qE "overscroll-behavior-y:\s*contain" src/lib/components/NowPlaying.svelte && ! grep -A2 "\.panel\s*{" src/lib/components/NowPlaying.svelte | grep -q "touch-action: none" && pnpm check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `.panel` rule contains `overscroll-behavior-y: contain;` (source assertion via grep).
    - `.panel` does NOT contain `touch-action: none` (source assertion: the panel still scrolls — `pan-y` preserved).
    - `pnpm check` exits 0.
    - No JS scroll-locking added (NP-02 is CSS-only per the UI-SPEC; iOS <16 is documented best-effort).
  </acceptance_criteria>
  <done>`.panel` carries `overscroll-behavior-y: contain` and still scrolls; pnpm check is 0/0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This plan adds a CSS-driven loader element and one CSS property. No new input, no data flow, no network call, no persistence crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Information Disclosure | NP-04 loader / NP-02 CSS | accept | The loader renders a 3px animated bar driven by the existing already-trusted `player.loading` boolean; it renders no user data and no URL. `overscroll-behavior-y` is a layout-containment hint with no security relevance. Surface unchanged. |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — zero new dependencies. slopcheck N/A. |

Client-side, browser-only, no new XSS sink: the loader element is static markup (no interpolation of untrusted strings) and the CSS is verbatim from the existing nowbar. Threat surface is unchanged from Phase 18/19's nowbar loader.
</threat_model>

<verification>
- `grep -q "np-prog indet" src/lib/components/NowPlaying.svelte` and `grep -q "np-indet" ...` confirm the loader markup + keyframe exist.
- `grep -qE "overscroll-behavior-y:\s*contain" src/lib/components/NowPlaying.svelte` confirms NP-02.
- `pnpm check` exits 0.
- `git diff` shows no changes to `npTopDown/Move/Up` or any pointer handler.
</verification>

<success_criteria>
- The now-playing view shows a top running-line loader while a track loads, matching the nowbar's loader exactly (NP-04).
- The half-open panel contains its own scroll and never scrolls the page behind it (NP-02), with `pan-y` scroll preserved.
- pnpm check is 0/0; no gesture handler touched; zero new i18n keys.
</success_criteria>

<output>
Create `.planning/phases/20-now-playing-surface-gestures/20-02-SUMMARY.md` when done.
</output>
