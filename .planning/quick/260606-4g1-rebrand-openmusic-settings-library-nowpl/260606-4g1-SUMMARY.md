---
quick: 260606-4g1
slug: rebrand-openmusic-settings-library-nowplaying-options-lucide
status: complete
date: 2026-06-06
---

# Quick Task 260606-4g1 — Summary

Major UI/feature pass on the deployed app, now branded **openmusic**. Phase-1 data layer + 58 tests untouched. Live at openmusic.pages.dev.

## Shipped

1. **Rebrand → openmusic.** Top-nav wordmark, page `<title>`s, and visible strings now read "openmusic". (Repo stays `musicsquare-mobile`.)
2. **Lucide icons everywhere** (`@lucide/svelte` 1.17.0). Replaced all emoji — search, settings gear, transport (shuffle/prev/play/pause/next/repeat), bottom nav (House/Search/Library), grip handle, chevrons, and the 7 options-menu items. No emoji left in the UI.
3. **Settings page + entry** (`/settings`). Gear button top-right of Home. Real local actions: clear cached top picks, clear library (with confirm + live counts), language placeholder, about/version.
4. **Local library store** (`src/lib/stores/library.svelte.ts`, runes singleton, `openmusic:library:v1`, SSR-guarded): liked / playlists / downloads with toggleLike, create/add/remove playlist, addDownload.
5. **NowPlaying options menu** (⋮): **Download** (blob fetch → file save + records in Downloads; CORS fallback opens URL), **Like** (filled heart), **Add to playlist** (picker + "New playlist…"), **Go to album** (`/album/[name]`), **Go to artist**, **Share** (Web Share API or copy `/?play=<token>` link — Home decodes it and auto-plays), **Detail** (modal: title/artist/album/quality-kbps/source/uid/audioUrl, resolved first). Plus a toast.
6. **Library page** (`/library`) — Liked / Playlists / Downloads tabs (bottom-nav Library no longer 404s); tap to play (seeds queue); empty states.
7. **Album route** (`/album/[name]`) — derived from `searchAll(album)` (exact-album filter w/ fallback), dedupeBest.
8. **Grip handle** — sensitive (±24px drag toggles full/peek) AND click/tap toggles expand/collapse (tap vs drag via <8px move threshold).
9. **Share deep-link** — `/?play=<base64url stub>`; Home reconstructs a Track and plays, then clears the param.

## Verification
- `pnpm check` 0 errors / 0 warnings (3946 files incl lucide). `pnpm build` ok. `pnpm vitest run` 58/58 (data layer untouched).
- Deployed → https://openmusic.pages.dev. Live: `/`, `/search`, `/settings`, `/library`, `/artist/Adele`, `/album/25`, `/spike` all 200. Brand reads "openmusic".

## Notes
- New dep `@lucide/svelte` (user-sanctioned).
- Download honesty: a web app can't replay an arbitrary saved file offline — Downloads references the track and re-streams (documented in code).
- Album/artist remain derived (no real album/artist API in the adapters).
- shuffle/repeat still visual-only (real shuffle/repeat = Phase 7). Cross-source logic stays in presentation-layer utils; catalog/adapters/proxy unchanged.
