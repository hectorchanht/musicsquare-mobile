---
phase: 20-now-playing-surface-gestures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/actions/coverSwipe.ts
  - src/lib/actions/coverSwipe.test.ts
autonomous: true
requirements: [NP-01, NP-05]
must_haves:
  truths:
    - "A horizontal drag past the proportional commit on a swipe target fires onnext/onprev exactly once"
    - "A sub-slop tap never captures the pointer and never fires onnext/onprev, so the host's onclick still runs"
    - "A vertical-dominant drag yields (no capture) so the host's vertical-collapse handler keeps the gesture"
    - "A committed swipe arms a one-shot capture-phase click suppressor; a tap arms none"
    - "At a true boundary (hasPrev:false) a prev gesture springs back and never fires onprev, ignoring flick"
    - "Decisions covered: D-02 (boundary rubber-band resist), D-03 (direction mapping right=prev/left=next, reuse player.prev()/next()), D-08 (proportional 0.28xwidth commit + 0.5px/ms flick over swipeRemove/velocity mechanics)"
  artifacts:
    - path: "src/lib/actions/coverSwipe.ts"
      provides: "Reusable horizontal axis-locked prev/next swipe Svelte action with proportional commit, live dx callback, and boundary rubber-band"
      exports: ["coverSwipe", "CoverSwipeOpts"]
      min_lines: 90
    - path: "src/lib/actions/coverSwipe.test.ts"
      provides: "Headless node-style tests mirroring swipeRemove.test.ts proving the tap/vertical-yield/commit/flick/boundary/suppressor behaviours"
      min_lines: 120
  key_links:
    - from: "src/lib/actions/coverSwipe.ts"
      to: "src/lib/gestures/velocity.ts"
      via: "createVelocityTracker import for the 0.5px/ms flick read"
      pattern: "createVelocityTracker"
---

<objective>
Create the single reusable horizontal swipe action that both NP-01 (cover carousel) and NP-05 (nowbar slide-and-snap) build on. It is a structural mirror of `src/lib/actions/swipeRemove.ts` — same arm-on-down / commit-axis-after-slop / capture-only-in-move / trailing-click-suppressor idiom — with three deltas pinned by the UI-SPEC: (1) the distance commit is **proportional to a measured element width** (`0.28 × width`) instead of a flat 96px (D-08), (2) it exposes `onprev`/`onnext` plus a live per-frame `dx` callback so a host can drive a 3-cover carousel strip or a nowbar content slide, and (3) it applies an iOS rubber-band clamp at a true queue boundary (`hasPrev:false`) that ignores flick and always springs back (D-02).

Purpose: extracting the gesture into an action is the on-pattern choice (every gesture in this repo is a `use:` action) and gives a node-testable seam so the load-bearing Pitfall-7 invariant is proven without a DOM, exactly like `swipeRemove.test.ts`.
Output: `src/lib/actions/coverSwipe.ts` + `src/lib/actions/coverSwipe.test.ts`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-now-playing-surface-gestures/20-CONTEXT.md
@.planning/phases/20-now-playing-surface-gestures/20-UI-SPEC.md
@.planning/phases/20-now-playing-surface-gestures/20-PATTERNS.md

<interfaces>
<!-- The exact analog to mirror. coverSwipe.ts is a structural copy of swipeRemove.ts. -->
From src/lib/actions/swipeRemove.ts — Action shape to mirror:
```typescript
export interface SwipeRemoveOpts { onremove: () => void; threshold?: number; enabled?: boolean; }
export const swipeRemove: Action<HTMLElement, SwipeRemoveOpts> = (node, opts) => { ... };
```
Key constants in the analog (REUSE byte-for-byte): `SLOP = 8`, `FLICK_V = 0.5`, `FADE_DISTANCE = 120`, settle `transform 0.28s cubic-bezier(.22,1,.36,1)`. The analog's `down()` deliberately does NOT `setPointerCapture`; `move()` captures only after `Math.abs(ddx) >= SLOP` AND `Math.abs(ddx) > Math.abs(ddy)`; vertical-dominant goes passive (`dragging = false; return`); `up()` arms `suppressClick` (capture-phase, one-shot) ONLY when `captured`; `node.style.touchAction = 'pan-y'` on attach, cleared on `destroy()`.

From src/lib/gestures/velocity.ts:
```typescript
export interface VelocityTracker { sample(coord: number, timeStamp: number): void; velocity(): number; reset(): void; }
export function createVelocityTracker(): VelocityTracker;
```
`velocity()` is signed px/ms from the last two samples; positive = increasing coordinate; returns 0 with <2 samples or dt<=0.

From src/lib/actions/swipeRemove.test.ts — the headless test harness to copy verbatim:
`pe(clientX, clientY, timeStamp, pointerId=1)` synthetic PointerEvent factory; `makeNode()` returns `{ node, style, captureCalls, fire(type,e), has(type) }` with a `setPointerCapture` spy pushing into `captureCalls`; `mount(opts)` wires the action. Drive entirely headless (no jsdom).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for coverSwipe (RED)</name>
  <files>src/lib/actions/coverSwipe.test.ts</files>
  <read_first>
    - src/lib/actions/swipeRemove.test.ts (the headless harness + assertion style to mirror verbatim — pe(), makeNode(), mount())
    - src/lib/actions/swipeRemove.ts (the behaviour being mirrored: arm-on-down, commit-in-move, suppressor, touchAction)
    - .planning/phases/20-now-playing-surface-gestures/20-UI-SPEC.md (§3 Commit threshold: `dx ≥ 0.28 × width`, flick `0.5px/ms` with `|dx|>8`; §2 Rubber-band: hasPrev:false ignores flick + always springs back)
    - .planning/phases/20-now-playing-surface-gestures/20-PATTERNS.md (LOAD-BEARING INVARIANT block; the swipeRemove.ts:67-128 excerpts; Direction → action: `dx>0` (drag right) = onprev, `dx<0` (drag left) = onnext, per D-03)
  </read_first>
  <behavior>
    Copy the `pe`/`makeNode`/`mount` harness from swipeRemove.test.ts. Extend makeNode to let a test set a measured width (the action measures element width at down via `node.getBoundingClientRect().width` or a `width()` opt — see Task 2; the test supplies a fake `getBoundingClientRect` returning a fixed width, e.g. 300). Tests (all headless, no jsdom):
    - Sub-slop tap (ddx 4, ddy 2) → release: neither onprev nor onnext called; captureCalls length 0; no click suppressor armed (`has('click') === false`).
    - Vertical-dominant past slop (ddy 40 > ddx 4) → goes passive: no onprev/onnext, captureCalls length 0.
    - Horizontal drag RIGHT past `0.28 × 300` (= 84px), slow → onprev called once, onnext not called.
    - Horizontal drag LEFT past 0.28×300, slow → onnext called once, onprev not called.
    - Fast horizontal flick (|velocity| > 0.5px/ms, |dx| > 8, below distance commit) → commits (onnext/onprev once depending on sign).
    - Horizontal drag below distance commit + slow release (no flick) → springs back: no onprev/onnext; `style.transform === 'translateX(0)'`.
    - Capture happens ONLY in move() after horizontal commit, never on pointerdown (captureCalls length 0 right after pointerdown; equals [pointerId] after the committing move).
    - Committed swipe arms a one-shot capture-phase click suppressor that calls preventDefault + stopPropagation once and self-removes (`has('click') === false` after firing); a tap arms none.
    - touchAction is 'pan-y' on attach and '' after destroy().
    - enabled:false → inert (no commit, no capture).
    - Boundary: with `hasPrev:false`, a RIGHT (prev) drag well past the distance commit AND a hard flick → onprev NOT called, springs back to translateX(0). With `hasNext:false` (rare), a LEFT (next) drag is likewise resisted.
    - A live per-frame `ondrag` callback (if that is the chosen host API) receives the running dx during move(); assert it is called with a non-zero dx after a committing move. (If the host API is a returned-getter instead, assert that shape instead — keep the test aligned with Task 2's chosen signature.)
  </behavior>
  <action>Author `coverSwipe.test.ts` mirroring `swipeRemove.test.ts`'s headless harness exactly (synthetic PointerEvent `pe()`, `makeNode()` with a `setPointerCapture` spy + style sink + `getBoundingClientRect` returning a fixed width, `mount(opts)`). Cover every behavior listed above. The proportional commit basis is `0.28 × measuredWidth`; the flick rule is `|velocity| ≥ 0.5px/ms` with `|dx| > 8`; the boundary rule ignores flick entirely and always springs back. Direction mapping: drag right (`dx > 0`) = `onprev`; drag left (`dx < 0`) = `onnext` (D-03). Run the test FIRST and confirm it FAILS (no coverSwipe.ts yet). PATH note: prefix `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"` before pnpm (shell default node v16 breaks vitest).</action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && pnpm test --run src/lib/actions/coverSwipe.test.ts 2>&1 | grep -qE "Cannot find module|coverSwipe|FAIL|failed" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - coverSwipe.test.ts exists and imports `{ coverSwipe, type CoverSwipeOpts } from './coverSwipe'`.
    - The test FAILS before coverSwipe.ts exists (RED state proven).
    - Test harness is headless (no jsdom): uses a synthetic `pe()` PointerEvent and a `makeNode()` fake with a `setPointerCapture` spy, mirroring swipeRemove.test.ts.
    - There is an assertion that `setPointerCapture` is NOT called on `pointerdown` and IS called exactly once in `pointermove` after the horizontal commit (the no-setPointerCapture-on-pointerdown / axis-after-slop invariant — ROADMAP Pitfall 7).
    - There is an assertion that a sub-slop tap arms no click suppressor (`has('click') === false`) so the host onclick still reaches its handler.
    - There is a boundary assertion: `hasPrev:false` + hard prev flick → onprev NOT called + springs back to translateX(0) (flick ignored at a true boundary).
    - Direction asserted: drag right → onprev, drag left → onnext.
  </acceptance_criteria>
  <done>coverSwipe.test.ts written with all listed behaviours; the suite fails because coverSwipe.ts does not yet exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement coverSwipe action (GREEN)</name>
  <files>src/lib/actions/coverSwipe.ts</files>
  <read_first>
    - src/lib/actions/swipeRemove.ts (verbatim structural source: imports, *Opts interface, down/move/up triad, suppressClick, listener wiring, update(), destroy())
    - src/lib/actions/dragClose.ts (the vertical analog the cover's swipe must coexist with — confirms the same arm-on-down/no-capture idiom)
    - src/lib/gestures/velocity.ts (createVelocityTracker contract)
    - src/lib/components/NowPlaying.svelte lines 439-458 (measureOffsets: how the repo reads getBoundingClientRect at gesture start — mirror this for the proportional width)
    - .planning/phases/20-now-playing-surface-gestures/20-UI-SPEC.md (§1 1:1 lockstep strip; §2 rubber-band formula `offset = sign(dx)·maxPull·(1 − e^(−|dx|/maxPull))`, `maxPull = 0.18 × width`, spring-back 0.32s; §3 proportional 0.28× commit, flick 0.5px/ms)
    - .planning/phases/20-now-playing-surface-gestures/20-PATTERNS.md (Pattern Assignments → coverSwipe.ts section, all swipeRemove.ts excerpts)
  </read_first>
  <action>Create `src/lib/actions/coverSwipe.ts` as a structural mirror of `swipeRemove.ts`. Export `interface CoverSwipeOpts { onprev: () => void; onnext: () => void; ondrag?: (dx: number) => void; hasPrev?: boolean; hasNext?: boolean; enabled?: boolean; }` and `export const coverSwipe: Action<HTMLElement, CoverSwipeOpts>`. Reuse byte-for-byte: `const SLOP = 8`, `const FLICK_V = 0.5`, `createVelocityTracker()`, `node.style.touchAction = 'pan-y'` on attach, the capture-phase one-shot `suppressClick` armed in `up()` only when `captured`, and `destroy()` that drops the suppressor + resets inline styles + clears `touchAction`. Differences from the analog: (1) In `down()`, measure `const width = node.getBoundingClientRect().width` and compute `commitDist = 0.28 * width` and `maxPull = 0.18 * width` (mirror NowPlaying's measure-at-drag-start idiom). Do NOT `setPointerCapture` in `down()`. (2) In `move()`, the axis-dominance check claims horizontal only when `Math.abs(ddx) >= SLOP && Math.abs(ddx) > Math.abs(ddy)`; vertical-dominant (`Math.abs(ddy) > Math.abs(ddx)`) sets `dragging = false; return` (yields to the host's vertical-collapse handler); capture happens HERE, after the horizontal commit. (3) Determine the swipe direction sign to know which boundary applies: `dx > 0` is a prev gesture (drag right), `dx < 0` is a next gesture (drag left). When the relevant boundary flag is false (prev gesture && `hasPrev === false`, or next gesture && `hasNext === false`), clamp the applied translate to the rubber-band offset `Math.sign(dx) * maxPull * (1 - Math.exp(-Math.abs(dx) / maxPull))` and remember a `resisting` flag. (4) Always report the running dx to `ondrag?.(dx)` each move so the host can translate its carousel strip / content. (5) In `up()`, if `resisting`, IGNORE flick and ALWAYS spring back with `transform 0.32s cubic-bezier(.22,1,.36,1)` to `translateX(0)` (no onprev/onnext). Otherwise commit when `Math.abs(dx) >= commitDist || (Math.abs(vel.velocity()) > FLICK_V && Math.abs(dx) > SLOP)`: call `onprev()` for `dx > 0`, `onnext()` for `dx < 0` (D-03); below the commit + no flick → spring back `transform 0.28s cubic-bezier(.22,1,.36,1)` to `translateX(0)`. Do NOT add any advance logic — `onprev`/`onnext` are the host's `player.prev()`/`player.next()` (D-03). Add a top-of-file comment documenting the no-capture-on-pointerdown invariant verbatim from the PATTERNS LOAD-BEARING INVARIANT block.</action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && pnpm test --run src/lib/actions/coverSwipe.test.ts && pnpm check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test --run src/lib/actions/coverSwipe.test.ts` passes (GREEN) and `pnpm check` exits 0 (svelte-check clean).
    - coverSwipe.ts exports `coverSwipe` (Action) and `CoverSwipeOpts`; constants are `SLOP = 8`, `FLICK_V = 0.5`; commit distance = `0.28 * measuredWidth`, `maxPull = 0.18 * measuredWidth`.
    - LOAD-BEARING INVARIANT (source assertion): `down()` contains NO `setPointerCapture`; `setPointerCapture` appears ONLY inside `move()` after the `Math.abs(ddx) >= SLOP && Math.abs(ddx) > Math.abs(ddy)` horizontal-commit check. `grep -n setPointerCapture src/lib/actions/coverSwipe.ts` shows it only in the move path; sub-slop movement never reaches it so the host onclick fires.
    - Spring-back transitions assert the pinned values: committed/spring-back below-threshold uses `transform 0.28s cubic-bezier(.22,1,.36,1)`; the boundary rubber-band spring-back uses `transform 0.32s cubic-bezier(.22,1,.36,1)`.
    - Direction mapping is `dx > 0 → onprev`, `dx < 0 → onnext`; no new advance functions are added (only `onprev`/`onnext` callbacks are invoked).
    - `node.style.touchAction = 'pan-y'` set on attach and cleared in destroy(); a one-shot capture-phase `suppressClick` is armed in up() only when captured.
    - At a true boundary the flick is ignored and the gesture always springs back (verified by the boundary test passing).
  </acceptance_criteria>
  <done>coverSwipe.ts implemented; the full coverSwipe.test.ts suite is green and pnpm check is 0/0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user touch → action | The only input is pointer coordinates from the user's own touch on their own device; no network, no persistence, no cross-origin data crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | coverSwipe pointer math | accept | Pure client-side gesture math on the user's own coordinates; no trust decision, no data flow, no sink. A malicious local actor manipulating their own pointer events gains nothing beyond changing their own track. |
| T-20-02 | Denial of Service | capture / suppressor lifecycle | mitigate | `destroy()` drops the armed click suppressor and removes all pointer listeners; the one-shot suppressor self-removes after one click, preventing a leaked capture-phase handler from swallowing future genuine taps (the swipeRemove.ts WR-01 idiom, copied verbatim). |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — zero new dependencies (the action imports only the existing in-repo `velocity.ts`). slopcheck N/A; no install task exists. |

This is a client-side, browser-only gesture action that adds no new network calls, no new data flows, no auth, and no new XSS sink (it sets inline `transform`/`opacity`/`touchAction` styles only — never `innerHTML`, never a URL). The realistic threat surface is unchanged from the existing `swipeRemove.ts`.
</threat_model>

<verification>
- `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && pnpm test --run src/lib/actions/coverSwipe.test.ts` is green.
- `pnpm check` exits 0.
- `grep -n setPointerCapture src/lib/actions/coverSwipe.ts` confirms capture only inside the move() horizontal-commit branch, never in down().
</verification>

<success_criteria>
- A reusable `coverSwipe` action exists with `onprev`/`onnext`/`ondrag`/`hasPrev`/`hasNext`/`enabled` options and proportional (`0.28 × width`) commit + boundary rubber-band, fully node-tested.
- The Pitfall-7 invariant is enforced in code and asserted in tests.
- No new dependencies; zero new i18n keys.
</success_criteria>

<output>
Create `.planning/phases/20-now-playing-surface-gestures/20-01-SUMMARY.md` when done.
</output>
