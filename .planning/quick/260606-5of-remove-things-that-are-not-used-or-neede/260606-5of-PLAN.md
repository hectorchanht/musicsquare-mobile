---
quick_id: 260606-5of
description: remove things that are not used or needed for this svelte project
date: 2026-06-05
status: planned
---

# Quick Task 260606-5of: Remove unused / unneeded things

User-confirmed removal scope (all four groups selected via AskUserQuestion). Each group is an atomic commit.

## Discovery summary

- **Deps:** all `devDependencies` + `@lucide/svelte` are imported/used. No unused packages — package.json untouched.
- **Components/services/stores:** every file in `src/lib` has importers. No dead modules.
- **Kept:** `src/routes/spike/+page.svelte` — active Phase-1 egress test rig (deployed, success criterion #5), not dead code.

## Tasks (atomic commits)

### T1 — Remove dead app files
- `git rm src/lib/assets/favicon.svg` — orphan duplicate; live favicon is `static/favicon.svg` served as `/favicon.svg` (referenced in `src/app.html`). Not imported anywhere.
- Stage `docs/logo.png` deletion (already removed on disk; `docs/` had no other files).
- Fix `README.md:2` broken `<img src="docs/logo.png">` → repoint to in-repo `static/og.svg`.

### T2 — Remove legacy/ folder
- `git rm legacy/index.html legacy/pikachu.gif` (≈220KB).
- Note: `src/lib/**` retains `// legacy/index.html:NNNN` provenance comments. Harmless (comments only); rewriting them is out of scope.

### T3 — Remove Python issue-reply bot
- `git rm scripts/g4f_issue_reply.py .github/workflows/g4f-issue-reply.yml` — original-fork GitHub automation, not part of the svelte app. `scripts/` becomes empty.

### T4 — Remove inherited fork GitHub config
- `git rm -r .github/ISSUE_TEMPLATE` (14 files) + `git rm .github/FUNDING.yml` — inherited from charlespikachu/musicsquare fork.

## Verify
- `pnpm check` (svelte-check) passes after removals.
- `git status` clean except intended deletions.
