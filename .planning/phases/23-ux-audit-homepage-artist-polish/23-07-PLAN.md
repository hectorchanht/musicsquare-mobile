---
phase: 23-ux-audit-homepage-artist-polish
plan: 07
type: execute
wave: 2
depends_on: ["23-01", "23-02"]
files_modified:
  - src/routes/api/deezer/artist-albums/+server.ts
  - src/lib/services/deezer.ts
  - src/lib/services/deezer.test.ts
  - src/routes/(app)/artist/[name]/+page.svelte
autonomous: true
requirements: [ART-01, UX-01, UX-04, UX-05]
must_haves:
  truths:
    - "The artist page shows album-card skeletons until album track-counts are known, then renders ONLY non-empty albums; obvious stubs (empty/placeholder name) are dropped up front"
    - "A new Deezer proxy /api/deezer/artist-albums returns each album's nb_tracks natively, with the same never-throw / own-origin-CORS / edge-cache / safeImageUrl posture as /api/deezer/search"
    - "The artist hit-songs list rows support swipe-actions (queue/like) + long-press menu (D-01)"
  artifacts:
    - path: "src/routes/api/deezer/artist-albums/+server.ts"
      provides: "Deezer artist-albums proxy with nb_tracks (D-19 augment)"
      contains: "nb_tracks"
    - path: "src/lib/services/deezer.ts"
      provides: "deezerArtistAlbums client fn (never-throws, cached)"
      contains: "deezerArtistAlbums"
    - path: "src/routes/(app)/artist/[name]/+page.svelte"
      provides: "Trackless-album gate + skeleton + swipe on hit-songs"
      contains: "nb_tracks"
  key_links:
    - from: "src/routes/(app)/artist/[name]/+page.svelte"
      to: "src/lib/services/deezer.ts"
      via: "deezerArtistAlbums to get nb_tracks for filtering"
      pattern: "deezerArtistAlbums"
---

<objective>
Hide trackless albums on the artist page (ART-01, D-18) via the recommended AUGMENT path (D-19, UI-SPEC §8.2): add a thin Deezer `/api/deezer/artist-albums` proxy that returns each album's `nb_tracks` natively, a `deezerArtistAlbums` client fn, and gate the artist album render to skeleton → only non-empty albums. Also wire swipe-actions onto the artist hit-songs list (D-01).

Purpose: ART-01 verify-before-render UX; D-19 makes trackless filtering free with zero per-album fetches by using Deezer's native nb_tracks. Last.fm `getArtistTopAlbums` stays as the graceful fallback.
Output: new proxy route, deezer client fn + tests, artist page gate + swipe.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

Tooling: prefix PATH with `$HOME/.nvm/versions/node/v22.22.0/bin` and use pnpm for every check/test command.
NOTE: skeletons match loaded count/size/length. Never-throw resolver posture; only successful responses cached (WR-03 / T-17-13). No new infra deps.
</execution_context>

<context>
@.planning/phases/23-ux-audit-homepage-artist-polish/23-CONTEXT.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-PATTERNS.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-01-SUMMARY.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-02-SUMMARY.md

<interfaces>
From src/routes/api/deezer/search/+server.ts (copy posture VERBATIM):
- own-origin `corsHeaders(origin)`, `OPTIONS` 204 preflight, `edgeCache()` keyed by own-origin Request, `fetchWithRetry` + native `AbortSignal.timeout`, `safeImageUrl` host allow-list (`*.dzcdn.net`), reshape to a narrow client shape. Carries NO secret (Deezer public API).
From src/lib/services/deezer.ts:
- `cached(key, ttl, factory)` from './ttl-cache'; `combinedSignal(caller?)`; the never-throws + `.catch(()=>null)` posture used by deezerSearchTopN / deezerRelatedArtists.
From src/lib/services/lastfm.ts:
- `getArtistTopAlbums(artist, limit = 30): Promise<DiscoveryAlbum[]>` (line 264) — the FALLBACK album-list source; `DiscoveryAlbum` carries `{ name, image }` only (NO track count).
From src/lib/services/discovery.ts:
- `mapWithConcurrency` (line 110, cap ≤4, never-throws, sig.aborted guards) — for the fallback verification path only.
Artist page (artist/[name]/+page.svelte): race-guarded `albumsFor` effect (~194-203); `albumsLoading` flag; album-card skeleton (~334-348); album render (~350-360); hit-songs rows (~243/384 pattern with use:longpress + onclick).
swipeAction (Plan 02), toast + haptics (Plan 01) as in Plan 05.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /api/deezer/artist-albums proxy returning nb_tracks (D-19, §8.2)</name>
  <files>src/routes/api/deezer/artist-albums/+server.ts</files>
  <read_first>
    - src/routes/api/deezer/search/+server.ts (read in full — copy the proxy posture VERBATIM: corsHeaders, OPTIONS 204, edgeCache keyed by own-origin Request, fetchWithRetry + AbortSignal.timeout, safeImageUrl *.dzcdn.net allow-list, narrow reshape)
    - src/lib/proxy/http.ts (fetchWithRetry, corsHeaders signatures)
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md §8.2
  </read_first>
  <action>Create `src/routes/api/deezer/artist-albums/+server.ts` cloning the `/api/deezer/search` posture. It takes an artist name (query param `?q=`), resolves the Deezer artist id (search `artist` then read `id`, or use the existing artist search path), then GETs `https://api.deezer.com/artist/{id}/albums` and reshapes each album to a narrow client shape: `{ title, nb_tracks, cover (via safeImageUrl, *.dzcdn.net only) }`. Apply VERBATIM: own-origin `corsHeaders(origin)`, `OPTIONS` returning 204 preflight, `edgeCache()` keyed by the own-origin Request, `fetchWithRetry` + `AbortSignal.timeout`, never-throw → on any failure return an empty `{ data: [] }` (200, never 5xx leaking upstream errors). Carries NO secret. Validate the `q` param (trim; empty → empty result). Strip/clamp `nb_tracks` to a non-negative integer.</action>
  <verify>
    <automated>PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/api/deezer/artist-albums/+server.ts exports `GET` and `OPTIONS` handlers; OPTIONS returns 204 with corsHeaders
    - The reshaped album objects include a `nb_tracks` field and a `cover` passed through `safeImageUrl` (only *.dzcdn.net hosts)
    - On any upstream failure the handler returns a 200 with empty data (never throws / never 5xx-leaks)
    - The `q` param is validated (empty → empty result)
    - `pnpm check` exits 0
  </acceptance_criteria>
  <done>New artist-albums proxy mirrors the search proxy posture, returns nb_tracks + safe covers, never-throws.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deezerArtistAlbums client fn (D-19, §8.2)</name>
  <files>src/lib/services/deezer.ts, src/lib/services/deezer.test.ts</files>
  <read_first>
    - src/lib/services/deezer.ts (deezerSearchTopN ~182 and deezerRelatedArtists ~208 — copy the cached() + combinedSignal + never-throws + .catch(()=>fallback) posture; the WR-03 reject-inside-factory rule)
    - src/lib/services/deezer.test.ts (test harness mocking fetch / cache to extend)
    - src/lib/services/ttl-cache.ts (cached signature)
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md §8.2
  </read_first>
  <behavior>
    - deezerArtistAlbums('Jay Chou') resolves to an array of `{ title, nb_tracks, cover }`
    - On fetch failure / abort / malformed JSON it resolves to [] (never throws); the failure is NOT cached (rejects inside cached factory per WR-03)
    - A successful empty result ([]) is cached normally
  </behavior>
  <action>Add `export async function deezerArtistAlbums(name: string, signal?: AbortSignal): Promise<{ title: string; nb_tracks: number; cover: string | null }[]>` to `deezer.ts`. Implementation mirrors `deezerSearchTopN`: `cached(\`dz:artistalbums:${clean}\`, TTL_SEARCH (or a suitable existing TTL), async () => { const res = await fetch(apiUrl('/api/deezer/artist-albums?q='+encodeURIComponent(clean)), { signal: combinedSignal(signal) }); if (!res.ok) throw ...; return reshaped; })` wrapped so the outer call `.catch(() => [])` returns the empty sentinel (the throw inside the factory rejects so failures are never cached — WR-03 / T-17-13). Add tests in deezer.test.ts covering: success returns reshaped array, fetch failure returns [], malformed JSON returns [].</action>
  <verify>
    <automated>PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm vitest run src/lib/services/deezer.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/services/deezer.ts exports `function deezerArtistAlbums(` returning objects with `nb_tracks`
    - The fn never throws (failure/abort/malformed → []) and uses cached() so failures are not pinned for the TTL
    - deezer.test.ts asserts success-reshape, failure→[], malformed→[]; all pass
  </acceptance_criteria>
  <done>deezerArtistAlbums client fn exists with never-throws + WR-03 cache posture and passing tests.</done>
</task>

<task type="auto">
  <name>Task 3: Artist page trackless-album gate + skeleton + hit-songs swipe (ART-01 D-18, §8.1, D-01)</name>
  <files>src/routes/(app)/artist/[name]/+page.svelte</files>
  <read_first>
    - src/routes/(app)/artist/[name]/+page.svelte (race-guarded albumsFor effect ~194-203; albumsLoading flag ~67; album-card skeleton ~334-348; album render ~350-360; hit-songs rows with use:longpress + onclick ~243/384)
    - src/lib/services/deezer.ts (deezerArtistAlbums from Task 2)
    - src/lib/services/lastfm.ts (getArtistTopAlbums — fallback list source)
    - src/lib/services/discovery.ts (mapWithConcurrency — only if the fallback verification path is needed)
    - src/lib/actions/swipeAction.ts, src/lib/util/haptics.ts, src/lib/stores/toast.svelte.ts
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md §8.1, §8.2, §6
  </read_first>
  <action>(1) D-18 gate: in the race-guarded `albumsFor` effect (~194-203), source album track-counts via `deezerArtistAlbums(n)` (Task 2). After settle, render ONLY albums with `nb_tracks > 0`; drop obvious stubs (empty/whitespace `name`/`title`, placeholder names) UP FRONT before filtering. Keep the existing `albumsLoading` flag + race guard (assign only if `albumsFor === n`). The existing album-card skeleton (~334-348) stays up until counts are known; its shape already matches the render block. FALLBACK: if Deezer returns [] for this artist, fall back to Last.fm `getArtistTopAlbums(n)` for the list and verify counts with `mapWithConcurrency` (cap ≤4, never-throw, sig.aborted guard) via `deezerAlbum()` or `getAlbumTracklist` — hide any album resolving to 0/failed. Either path yields identical UX (skeleton → only non-empty albums). Map the Deezer album `title`/`cover` onto the existing album-card render (the card currently navigates to `/album/{name}?artist={name}` — preserve that nav using the album title). (2) Add `use:swipeAction` (right=queue / left=like-toggle + toast + tick, same wiring as Plan 05) to the artist hit-songs `.row` buttons, keeping existing use:longpress + onclick. No hex literals.</action>
  <verify>
    <automated>PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - artist/[name]/+page.svelte sources album track-counts (references `nb_tracks` / deezerArtistAlbums) and renders only albums with nb_tracks > 0
    - Obvious stubs (empty/whitespace/placeholder name) are dropped before render
    - The race guard (`albumsFor === n`) and `albumsLoading` skeleton remain; a Deezer-empty result falls back to Last.fm + capped verification
    - Hit-songs rows have `use:swipeAction` alongside use:longpress + onclick
    - `pnpm check` exits 0
  </acceptance_criteria>
  <done>Artist page skeletons albums then renders only non-empty ones (Deezer nb_tracks, Last.fm fallback); hit-songs rows swipe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/deezer/artist-albums (?q) | attacker-influenceable artist-name param forwarded to Deezer |
| Deezer upstream → proxy reshape → client | upstream album data incl. cover URLs and nb_tracks |
| proxy → client render (cover img) | cover URL becomes an <img src> |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-16 | Tampering / SSRF (upstream URL injection) | artist-albums proxy `?q` → Deezer URL | mitigate | `q` is sent as a Deezer query VALUE / used to resolve a numeric artist id; the upstream URL is built from a fixed `api.deezer.com` host + encoded params — never from raw user input as a host/path. Acceptance: proxy targets fixed api.deezer.com; q is encodeURIComponent'd and never concatenated into the host. (HIGH per security requirement.) |
| T-23-17 | Tampering (image/attribute injection) | cover URL reshape | mitigate | All cover URLs pass through `safeImageUrl` (https only, *.dzcdn.net host allow-list, no attribute-breaking chars) — identical to the search proxy. Acceptance: cover reshape calls safeImageUrl. |
| T-23-18 | Information Disclosure (upstream error leak) | proxy error path | mitigate | On any upstream failure the proxy returns a 200 empty body, never forwarding upstream status/body. Acceptance: failure path returns 200 empty (no 5xx, no upstream body). |
| T-23-19 | Cache poisoning | edge cache / client cache | mitigate | Edge cache keyed by the own-origin Request (no secret in key); client cached() pins only successful responses (WR-03), failures reject inside the factory. Acceptance: failures are not cached (tested in Task 2). |
</threat_model>

<verification>
- `pnpm check` exits 0; deezer.test.ts passes.
- Artist page renders only non-empty albums after skeleton; Deezer-empty falls back to Last.fm + capped verification.
- Proxy: OPTIONS 204, GET returns nb_tracks + safeImageUrl covers, never-throws, fixed api.deezer.com host (T-23-16/17/18).
- Hit-songs rows swipe (queue/like) coexisting with tap + long-press.
</verification>

<success_criteria>
- Trackless albums hidden via verify-before-render (Deezer nb_tracks augment, Last.fm fallback) — identical UX either path.
- New artist-albums proxy mirrors the secure search-proxy posture.
- Artist hit-songs rows support swipe-actions.
</success_criteria>

<output>
Create `.planning/phases/23-ux-audit-homepage-artist-polish/23-07-SUMMARY.md` when done.
</output>
