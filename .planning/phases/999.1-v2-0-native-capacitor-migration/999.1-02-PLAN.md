---
phase: 999.1-v2-0-native-capacitor-migration
plan: 02
type: execute
wave: 2
depends_on: ["999.1-01"]
files_modified:
  - svelte.config.js
  - src/routes/+layout.ts
  - capacitor.config.ts
  - package.json
  - .gitignore
  - android/   # `npx cap add android` output — committed
autonomous: false   # Task 3 is a device checkpoint (first APK boots/plays on a physical Android device)
requirements: []   # backlog phase — covers D-01 (dual build + SPA), D-12 (appId)
user_setup:
  - service: android-toolchain
    why: "Local debug APK build + on-device install require the Android SDK + JDK and a physical device with USB debugging"
    env_vars: []
    dashboard_config:
      - task: "Install JDK 21 (Temurin) and Android SDK / Android Studio Otter 2025.2.1+, accept SDK licenses, enable USB debugging on a physical Android device, and confirm `adb devices` lists it"
        location: "Local machine + Android device Developer Options"

must_haves:
  truths:
    - "Default web build (no BUILD_TARGET) still uses adapter-cloudflare — Cloudflare deploy output is unchanged (D-01)"
    - "BUILD_TARGET=native pnpm build produces build/index.html (SPA fallback) with SSR disabled (D-01)"
    - "capacitor.config.ts declares appId com.openmusic.app and webDir build (D-12, D-01)"
    - "A debug APK installs on a physical Android device, boots to the app, and can search + play a track in the foreground (foreground-only at this stage)"
  artifacts:
    - path: "svelte.config.js"
      provides: "BUILD_TARGET adapter switch (cloudflare default / static native)"
      contains: "BUILD_TARGET"
    - path: "src/routes/+layout.ts"
      provides: "ssr=false / prerender=false for the SPA static build"
      contains: "ssr"
    - path: "capacitor.config.ts"
      provides: "Capacitor app config — appId com.openmusic.app, webDir build"
      contains: "com.openmusic.app"
    - path: "android"
      provides: "cap add android platform output (committed)"
  key_links:
    - from: "capacitor.config.ts"
      to: "build/"
      via: "webDir points at adapter-static output"
      pattern: "webDir.*build"
    - from: "svelte.config.js"
      to: "@sveltejs/adapter-static"
      via: "native branch adapter import"
      pattern: "adapter-static"
---

<objective>
Add the dual-adapter build switch (D-01) and scaffold the Capacitor Android platform (D-12), producing the first installable debug APK that boots and plays in the foreground. This is the thin vertical slice: scaffold → dual build → APK that runs. Background audio, downloads, and CI signing are layered in later plans.

Purpose: Earliest possible installable APK (user's "quickly" priority). Proves the static SPA build + Capacitor shell + the Plan-01 API_BASE seam all work together end-to-end against the live deployed `/api/*` routes before investing in the harder device-gated work.

Output: dual-adapter svelte.config.js, SPA layout guard, capacitor.config.ts, committed `android/` platform, npm scripts, .gitignore entries, and a device-verified foreground-playing debug APK.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

**Tooling (MANDATORY):** prefix every node/pnpm/npx command with `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"` and use pnpm. Capacitor 8 toolchain (Pitfall 4): minSdk 24, compileSdk/targetSdk 36, Gradle 8.14.3, AGP 8.13.0, Kotlin 2.2.20, JDK 17+ (21 recommended), Node 22. Pin Capacitor 8.4.0 (NOT 7).
</execution_context>

<context>
@.planning/phases/999.1-v2-0-native-capacitor-migration/999.1-RESEARCH.md
@.planning/phases/999.1-v2-0-native-capacitor-migration/999.1-PATTERNS.md
@.planning/phases/999.1-v2-0-native-capacitor-migration/999.1-CONTEXT.md
@.planning/phases/999.1-v2-0-native-capacitor-migration/999.1-01-SUMMARY.md

<interfaces>
<!-- Extracted from codebase 2026-06-11. -->

svelte.config.js (current — single adapter; PRESERVE compilerOptions.runes exactly):
- `import adapter from '@sveltejs/adapter-cloudflare';`
- `compilerOptions.runes: ({ filename }) => filename.split(/[/\\]/).includes('node_modules') ? undefined : true`
- `kit: { adapter: adapter() }`

There is NO src/routes/+layout.ts (only +layout.svelte exists) — clean add.
`build/` is already gitignored (.gitignore:13 and :214 `/build`); `/.svelte-kit` gitignored (:213).
package.json: engines.node >=22, packageManager pnpm@8.15.5, scripts include build="vite build", test, check.

VERIFIED SPA-safe: no +page.server.ts anywhere, no `prerender = true` anywhere; src/routes/(app)/+page.ts load reads only url.searchParams (share-token OG path) → produces og:null in native build, no server fetch (RESEARCH Pattern 2).

Verified deployed Pages base for native API_BASE: https://openmusic.pages.dev (RESEARCH lines 227, 371).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dual-adapter build switch + SPA layout guard (D-01)</name>
  <files>svelte.config.js, src/routes/+layout.ts, package.json</files>
  <read_first>
    - svelte.config.js (the full current config — the compilerOptions.runes function MUST be preserved byte-for-byte)
    - src/routes/+layout.svelte (confirm there is no +layout.ts; understand existing layout)
    - src/routes/(app)/+page.ts (confirm SPA-safe: reads only url.searchParams, no server fetch)
    - package.json (scripts block, to add the native build script)
  </read_first>
  <action>
Modify `svelte.config.js`: import both `cloudflare from '@sveltejs/adapter-cloudflare'` and `staticAdapter from '@sveltejs/adapter-static'`. Compute `const native = process.env.BUILD_TARGET === 'native'`. Set `kit.adapter` to `native ? staticAdapter({ pages: 'build', assets: 'build', fallback: 'index.html', precompress: false, strict: false }) : cloudflare()`. PRESERVE `compilerOptions.runes` exactly as-is. Rationale baked in: `fallback: 'index.html'` is safe (no prerendered homepage to conflict, no +page.server.ts); `strict: false` is required for dynamic routes /album/[name], /artist/[name] that rely on the SPA fallback; default (BUILD_TARGET unset) stays cloudflare() so the web deploy is unchanged.

Create `src/routes/+layout.ts` exporting `export const ssr = false;` and `export const prerender = false;` so the static native build is full client-side SPA. (This applies to web dev too but the app already runs client-rendered; verify `pnpm check` and the full suite stay green — if SSR-disabling regresses any existing behavior, instead guard it on `process.env.BUILD_TARGET === 'native'` at config level. Default expectation per RESEARCH Pattern 2: a plain ssr=false layout is acceptable.)

Add to `package.json` scripts: `"build:native": "BUILD_TARGET=native VITE_API_BASE=https://openmusic.pages.dev vite build"` (so the native build bakes the deployed Pages base into the Plan-01 apiUrl seam). Install the new dev dep: run `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && pnpm add -D @sveltejs/adapter-static@3.0.10`.
  </action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && pnpm build:native && test -f build/index.html && echo "SPA-FALLBACK-OK" && pnpm test && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "BUILD_TARGET" svelte.config.js` shows the native adapter branch; `grep -n "adapter-static" svelte.config.js` present
    - `compilerOptions.runes` function is unchanged from the original (diff shows only adapter lines + imports changed)
    - `BUILD_TARGET=native ... vite build` exits 0 and `build/index.html` exists (SPA fallback)
    - With BUILD_TARGET unset, the default adapter remains adapter-cloudflare (grep shows cloudflare() on the else branch)
    - `src/routes/+layout.ts` exports `ssr = false` and `prerender = false`
    - `pnpm test && pnpm check` green (web build not regressed)
  </acceptance_criteria>
  <done>svelte.config.js switches adapters on BUILD_TARGET; native build emits build/index.html; web default unchanged; layout SPA guard added; suite + check green.</done>
</task>

<task type="auto">
  <name>Task 2: Capacitor scaffold — config, android platform, gitignore (D-01, D-12, Pitfall 4)</name>
  <files>capacitor.config.ts, .gitignore, package.json, android/</files>
  <read_first>
    - capacitor.config.ts (does not exist — RESEARCH "Code Examples" lines 333-344 is the template; PATTERNS line 215 locks appId)
    - .gitignore (lines 11-14, 211-214 — build/ and /.svelte-kit already ignored; android build intermediates need adding)
    - package.json (deps + scripts)
    - .planning/phases/999.1-v2-0-native-capacitor-migration/999.1-RESEARCH.md (Pitfall 4 toolchain versions; capacitor.config.ts + AndroidManifest examples)
  </read_first>
  <action>
Install Capacitor 8 deps (Node 22 + pnpm): `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && pnpm add @capacitor/core@8.4.0 @capacitor/app @capacitor/status-bar @capacitor/splash-screen && pnpm add -D @capacitor/cli@8.4.0 @capacitor/android@8.4.0`. (Do NOT install any background-audio plugin or @capacitor/filesystem here — those belong to Plans 03/04.)

Create `capacitor.config.ts` (typed `CapacitorConfig`): `appId: 'com.openmusic.app'` (D-12 LOCKED — overrides the com.hectorchanht.musicsquare placeholder in RESEARCH; changing it after first release breaks Obtainium continuity), `appName: 'MusicSquare'`, `webDir: 'build'` (adapter-static output), `android: { allowMixedContent: false }` (cleartext off — security V14). Leave server.androidScheme default ('https' → WebView origin https://localhost, which Plan 01 allowlisted).

Run `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && npx cap add android` then `npx cap sync android`. Commit the generated `android/` dir. Edit `android/app/build.gradle` to set `minSdkVersion 24`, `compileSdkVersion 36`, `targetSdkVersion 36` if `cap add` did not already set them (Pitfall 4); confirm Gradle wrapper 8.14.3 / AGP 8.13.0 / Kotlin 2.2.20 in the generated files, bumping if `cap add` emitted older pins.

Add `.gitignore` entries for Android build intermediates so only source is committed: `android/app/build/`, `android/build/`, `android/.gradle/`, `android/local.properties`, `android/app/release/`, `*.apk`, `*.keystore`, `*.jks`. Keep `android/` source (manifest, build.gradle, gradle wrapper) tracked. Add a `"cap:sync": "npx cap sync android"` convenience script to package.json.
  </action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && grep -q "com.openmusic.app" capacitor.config.ts && test -f android/app/src/main/AndroidManifest.xml && grep -q "targetSdkVersion 36\|targetSdk = 36\|targetSdkVersion = 36" android/app/build.gradle && echo "SCAFFOLD-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `capacitor.config.ts` exists, exports a CapacitorConfig with `appId: 'com.openmusic.app'`, `webDir: 'build'`, `android.allowMixedContent: false`
    - `android/app/src/main/AndroidManifest.xml` exists (cap add succeeded)
    - `android/app/build.gradle` shows minSdk 24, compileSdk 36, targetSdk 36
    - `.gitignore` ignores `android/app/build/`, `*.apk`, `*.keystore`/`*.jks`, `android/local.properties` (grep each)
    - `git status` shows `android/` source tracked but no build/ intermediates staged
    - @capacitor/core@8.4.0, @capacitor/cli@8.4.0, @capacitor/android@8.4.0 in package.json (NO background-audio plugin, NO @capacitor/filesystem yet)
  </acceptance_criteria>
  <done>capacitor.config.ts with locked appId; android/ platform added + synced + committed (intermediates gitignored); Cap-8 toolchain pinned.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device checkpoint — first debug APK boots and plays in foreground</name>
  <what-built>Dual-adapter static SPA build wrapped in a Capacitor 8 Android shell. The APK loads the SvelteKit SPA in the System WebView (origin https://localhost), and all /api/* metadata calls route cross-origin to the deployed Pages app (https://openmusic.pages.dev) via the Plan-01 API_BASE seam + Plan-01 CORS allowlist. Background audio, lock-screen controls, downloads, and CI signing are explicitly NOT in this APK yet (foreground-only slice).</what-built>
  <how-to-verify>
1. Build the debug APK on a machine with the Android SDK + JDK 21:
   `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && pnpm build:native && npx cap sync android && cd android && ./gradlew assembleDebug`
   Output: `android/app/build/outputs/apk/debug/app-debug.apk`
2. Install on a connected physical Android device: `adb install -r android/app/build/outputs/apk/debug/app-debug.apk`
3. Launch the app. Expect: it boots to the home/search UI (no blank screen, no SPA-fallback 404).
4. Search a song (try a Netease result specifically — it is the Pitfall-3 audio-URL case) and tap to play.
   Expect: audio plays in the FOREGROUND. Netease audio must NOT be silent (proves the apiUrl() audio-URL rewrite works in the APK).
5. Confirm cross-origin metadata works: search results from multiple sources populate (proves CORS allowlist accepted https://localhost).
6. Check `adb logcat` for CORS errors or `https://localhost/api/...` 404s — there should be none.
  </how-to-verify>
  <resume-signal>Type "approved" if the APK boots and plays Netease + at least one other source in the foreground with no CORS/404 errors. Otherwise describe: blank screen, silent source, CORS rejection, or logcat errors.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| APK WebView (https://localhost) → deployed Pages /api/* | Cross-origin metadata fetch (CORS allowlist from Plan 01 must accept the WebView origin) |
| APK ↔ source CDN | Direct audio byte stream over https (allowMixedContent:false enforces no cleartext) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-04 | Information Disclosure | cleartext traffic in WebView | mitigate | `android: { allowMixedContent: false }` + default androidScheme https; all /api + CDN over https |
| T-999.1-05 | Tampering | appId drift breaking update trust | accept | appId locked to com.openmusic.app (D-12) before any signed release; documented in capacitor.config.ts comment |
| T-999.1-06 | Information Disclosure | CORS relay reused by APK | mitigate | Relies on Plan-01 allowlist (T-999.1-01); device checkpoint confirms only https://localhost is accepted, not `*` |
</threat_model>

<verification>
- `pnpm test && pnpm check` green (web not regressed by the layout/adapter change).
- `BUILD_TARGET=native ... vite build` emits build/index.html.
- Device checkpoint (Task 3) is the gate: APK boots, Netease + other sources play in foreground, no CORS/404 in logcat. NOT node-testable.
</verification>

<success_criteria>
- Web default build unchanged (adapter-cloudflare); native build emits a static SPA.
- capacitor.config.ts locks appId com.openmusic.app; android/ committed with Cap-8 toolchain.
- A debug APK installs and foreground-plays (incl. Netease) on a real device — device-approved.
</success_criteria>

<output>
Create `.planning/phases/999.1-v2-0-native-capacitor-migration/999.1-02-SUMMARY.md` when done.
</output>
