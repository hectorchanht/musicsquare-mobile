---
phase: 23-ux-audit-homepage-artist-polish
plan: 06
type: execute
wave: 2
depends_on: ["23-01", "23-02"]
files_modified:
  - src/routes/(app)/charts/top/+page.svelte
  - src/routes/(app)/charts/tags/[tag]/+page.svelte
  - src/routes/(app)/charts/countries/[country]/+page.svelte
autonomous: true
requirements: [HOME-04, UX-01, UX-04, UX-05]
must_haves:
  truths:
    - "Visiting /charts/top, /charts/tags/[tag], /charts/countries/[country] shows a deep vertical list of ~50–100 standard track rows fetched from the matching Last.fm chart"
    - "Chart rows support tap-to-play, long-press menu, and swipe-actions (queue/like) — inherited from the standard row pattern (D-01)"
    - "Each chart page shows a deep .row skeleton (~12 rows, dwell-floored) before first paint, with no loading text; an empty result hides the list"
  artifacts:
    - path: "src/routes/(app)/charts/top/+page.svelte"
      provides: "Top hits / top artists deep chart page"
      contains: "getChartTopTracks"
    - path: "src/routes/(app)/charts/tags/[tag]/+page.svelte"
      provides: "Per-tag deep chart page"
      contains: "getTagTopTracks"
    - path: "src/routes/(app)/charts/countries/[country]/+page.svelte"
      provides: "Per-country deep chart page"
      contains: "getGeoTopTracks"
  key_links:
    - from: "src/routes/(app)/charts/tags/[tag]/+page.svelte"
      to: "src/lib/services/lastfm.ts"
      via: "getTagTopTracks(tag, limit, page)"
      pattern: "getTagTopTracks"
---

<objective>
Build the three per-type chart pages (HOME-04, D-11/D-12): `/charts/top`, `/charts/tags/[tag]`, `/charts/countries/[country]`. Each is a deep vertical list (~50–100 rows) of the standard `.row` pattern, reusing the search page's skeleton + dwell-floor + sentinel and inheriting swipe-actions + long-press menu for free.

Purpose: HOME-04 section-title navigation lands on these dedicated chart pages; UX-01 skeleton + UX-04/05 swipe/haptic come from reusing the standard row.
Output: 3 new route files modeled on the search deep-list page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

Tooling: prefix PATH with `$HOME/.nvm/versions/node/v22.22.0/bin` and use pnpm for every check/test command.
NOTE: skeletons match loaded count/size/length; long text uses use:marquee. Never-throw resolver posture — fetch failure → [] → list hides (no loading text persists).
</execution_context>

<context>
@.planning/phases/23-ux-audit-homepage-artist-polish/23-CONTEXT.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-PATTERNS.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-01-SUMMARY.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-02-SUMMARY.md

<interfaces>
From src/lib/services/lastfm.ts:
- `getChartTopTracks(limit = 30, page = 1): Promise<DiscoveryTrack[]>` (line 211)
- `getChartTopArtists(limit = 30): Promise<DiscoveryArtist[]>` (line 222)
- `getTagTopTracks(tag: string, limit = 30, page = 1): Promise<DiscoveryTrack[]>` (line 235)
- `getGeoTopTracks(country: string, limit = 30, page = 1): Promise<DiscoveryTrack[]>` (line 252)
- `interface DiscoveryTrack` (line 162), `interface DiscoveryArtist` (line 170)
- All never-throw → [] on failure; support `page` for load-more.
From src/lib/services/discovery.ts:
- `resolveStub(artist, title): Promise<Track | null>` (line 32) — re-search a discovery stub to a real playable Track (the home tap-to-play path).
swipeAction (Plan 02), toast + haptics (Plan 01), as in Plan 05.
New i18n keys (added in Plan 04 across all 15 dicts): `charts.topTitle`, `charts.tagTitle` ("{tag}"), `charts.countryTitle` ("{country}").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared chart row + skeleton, /charts/top page (D-11/D-12, §5.3)</name>
  <files>src/routes/(app)/charts/top/+page.svelte</files>
  <read_first>
    - src/routes/(app)/search/+page.svelte (the deep list: `.row` markup 511-525 with use:longpress + onclick + use:lazyCover; `{#snippet skeletonRows(count,label)}` 473-485; dwell-floor ~61-67/230-290; the infinite-scroll sentinel ~528-535; `.head h1` heading style 542)
    - src/lib/services/lastfm.ts (getChartTopTracks, getChartTopArtists, DiscoveryTrack/DiscoveryArtist)
    - src/lib/services/discovery.ts (resolveStub — the home tap-to-play resolution path)
    - src/lib/actions/swipeAction.ts, src/lib/util/haptics.ts, src/lib/stores/toast.svelte.ts
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md §5.3, §6
  </read_first>
  <action>Create `/charts/top/+page.svelte`. Fetch a long Top Hits chart via `getChartTopTracks(limit, page)` (limit ≥ 50, up to ~100 per D-12). Render a deep vertical list of the standard `.row` pattern copied from search (511-525): `use:lazyCover` art, `.r-title`/`.r-sub` with `use:marquee`, `use:longpress onlongpress` (menu, with `(e.currentTarget)?.blur()` MENU-03 guard + `haptics.tick()`), `onclick` (tap-to-play via `resolveStub(artist,title)` then `player.play` — the home discovery tap path), and `use:swipeAction` (right=queue / left=like-toggle + toast + tick, same wiring as Plan 05). Skeleton: reuse a `{#snippet skeletonRows(count)}` (~12 visible) with the 280ms dwell floor so a cache hit never flickers; replace any loading text. Heading uses the existing page-heading style with `t('charts.topTitle')`. Top Artists: include an artists view on this same route (a toggle/sub-section) OR document the chosen top-level split in the SUMMARY — your discretion under the per-type convention (UI-SPEC §5.2); artist rows tap to `/artist/{name}` (no swipe, no ⋮ — artists aren't tracks). Load-more vs single fetch is your discretion (reuse the search sentinel if load-more). Empty result → list hides (never-throw posture). No hex literals.</action>
  <verify>
    <automated>PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/(app)/charts/top/+page.svelte calls `getChartTopTracks(` with a limit ≥ 50
    - Track rows have `use:longpress`, `onclick` (resolveStub → player.play), and `use:swipeAction`
    - A skeleton snippet renders ~12 placeholder rows with the dwell-floor before first paint; no persistent loading text
    - Heading uses `t('charts.topTitle')`; empty result hides the list (no thrown error)
    - `pnpm check` exits 0
  </acceptance_criteria>
  <done>/charts/top renders a deep track list with tap/long-press/swipe and dwell-floored skeleton; top-artists handled per the documented split.</done>
</task>

<task type="auto">
  <name>Task 2: /charts/tags/[tag] and /charts/countries/[country] pages (D-11/D-12, §5.3)</name>
  <files>src/routes/(app)/charts/tags/[tag]/+page.svelte, src/routes/(app)/charts/countries/[country]/+page.svelte</files>
  <read_first>
    - src/routes/(app)/charts/top/+page.svelte (the row + skeleton + tap/swipe scaffold built in Task 1 — clone it)
    - src/lib/services/lastfm.ts (getTagTopTracks(tag, limit, page), getGeoTopTracks(country, limit, page))
    - SvelteKit route param access: `page.params.tag` / `page.params.country` via `$app/state` (used across routes)
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md §5.3
  </read_first>
  <action>Create `/charts/tags/[tag]/+page.svelte` and `/charts/countries/[country]/+page.svelte` cloning the Task-1 row/skeleton/tap/swipe scaffold. The tag page reads the `[tag]` route param (decode via `decodeURIComponent`), fetches `getTagTopTracks(tag, limit, page)` (limit ≥ 50), heading `t('charts.tagTitle')` interpolated with the tag. The country page reads `[country]`, fetches `getGeoTopTracks(country, limit, page)`, heading `t('charts.countryTitle')` interpolated with the country. Both: standard `.row` with tap-to-play (resolveStub), long-press menu, swipeAction (queue/like + toast + tick), dwell-floored skeleton (~12 rows), empty → hides. Validate/decode the param defensively (an undefined/garbage param → empty fetch → hidden list, never crash). No hex literals.</action>
  <verify>
    <automated>PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - charts/tags/[tag]/+page.svelte reads the `[tag]` param (decodeURIComponent) and calls `getTagTopTracks(`
    - charts/countries/[country]/+page.svelte reads the `[country]` param and calls `getGeoTopTracks(`
    - Both render the standard row with use:longpress + onclick (resolveStub→play) + use:swipeAction + dwell-floored skeleton
    - Both interpolate the param into the heading via charts.tagTitle / charts.countryTitle
    - `pnpm check` exits 0
  </acceptance_criteria>
  <done>Tag and country chart pages render deep lists with full row interactions and matched skeletons; param decode is defensive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route params ([tag], [country]) → Last.fm chart fetch | attacker-influenceable URL segments forwarded to the chart fetcher |
| Last.fm proxy response → row render | upstream data rendered as track rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-13 | Tampering (param/SSRF) | [tag]/[country] → getTagTopTracks/getGeoTopTracks | mitigate | Params are decodeURIComponent'd and passed as Last.fm query VALUES through the existing fixed proxy route (no URL is constructed from user input); the proxy targets a fixed Last.fm host. A garbage param yields an empty never-throw result → list hides. Acceptance: garbage [tag]/[country] renders an empty/hidden list without error. |
| T-23-14 | Tampering (XSS) | chart heading interpolation | mitigate | The tag/country is rendered as Svelte text ({...}, auto-escaped), never {@html}. Acceptance: chart pages contain no {@html}. |
| T-23-15 | Information Disclosure | resolveStub play path | accept | Reuses the existing home discovery resolve path (searchAll over CN sources); no new data surface. |
</threat_model>

<verification>
- `pnpm check` exits 0 for all three routes.
- Each chart page fetches the correct Last.fm chart and renders deep `.row` list with tap/long-press/swipe.
- Skeleton (~12 rows, dwell-floored) shows before first paint; empty result hides; no {@html}.
</verification>

<success_criteria>
- Three per-type chart pages exist with deep vertical lists, swipe + long-press inherited, dwell-floored skeletons.
- Section-title nav from home (Plan 04) lands on these routes correctly.
</success_criteria>

<output>
Create `.planning/phases/23-ux-audit-homepage-artist-polish/23-06-SUMMARY.md` when done.
</output>
