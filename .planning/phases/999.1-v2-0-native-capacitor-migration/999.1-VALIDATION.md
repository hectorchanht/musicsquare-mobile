---
phase: 999.1
slug: v2-0-native-capacitor-migration
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-11
---

# Phase 999.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.3 (+ svelte-check) |
| **Config file** | via `package.json` scripts; `pnpm test` = `vitest --run` |
| **Quick run command** | `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && pnpm test:unit -- <file>` |
| **Full suite command** | `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && pnpm test && pnpm check` |
| **Estimated runtime** | ~60 seconds (existing ~626-test suite) |

---

## Sampling Rate

- **After every task commit:** Run the relevant unit file (`pnpm test:unit -- <file>`)
- **After every plan wave:** Run full `pnpm test && pnpm check` (existing suite must stay green — web build must not regress)
- **Before `/gsd:verify-work`:** Full suite green + device-test sign-off (D-04, D-11) + one successful end-to-end CI release (tag → signed APK → installs → plays in background)
- **Max feedback latency:** 90 seconds (unit); device gates are manual

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | — | D-03 API_BASE prepend/empty-relative | — | N/A | unit | `pnpm test:unit -- api-base` | ❌ W0 | ⬜ pending |
| TBD | TBD | — | D-03 audio URL absolute under native env | — | N/A | unit | `pnpm test:unit -- netease` (extend) | ⚠️ extend | ⬜ pending |
| TBD | TBD | — | D-02 CORS allowlist + OPTIONS preflight | Open-CORS relay | ACAO only for allowlisted origins, never `*` | unit | `pnpm test:unit -- proxy` (extend) | ⚠️ extend | ⬜ pending |
| TBD | TBD | — | D-10 blob-store native branch | — | N/A | unit | `pnpm test:unit -- blob-store` | ❌ W0 | ⬜ pending |
| TBD | TBD | — | D-01 `BUILD_TARGET=native pnpm build` → `build/index.html` | — | N/A | build check | assert file exists | n/a | ⬜ pending |
| TBD | TBD | — | D-04/D-06 background audio + no-reload resume | Deep-link re-navigation | validate `appUrlOpen` URLs | **manual device** | device matrix below | n/a — DEVICE GATE | ⬜ pending |
| TBD | TBD | — | D-11 public-Music download playable offline | — | N/A | **manual device** | physical device | n/a — DEVICE GATE | ⬜ pending |
| TBD | TBD | — | D-07 tag → signed APK on Releases, Obtainium detects | Keystore leakage | base64 secret, masked, tag-scoped job | CI dry-run | push `v0.0.x-test` tag | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky. Task IDs filled by planner.*

---

## Wave 0 Requirements

- [ ] `src/lib/services/api-base.test.ts` — D-03 (base prepend, empty=relative, audio URL absoluteness)
- [ ] Extend `src/routes/api/proxy.test.ts` — D-02 (Capacitor origins allowlisted, OPTIONS preflight)
- [ ] `src/lib/services/blob-store.test.ts` — D-10 (native branch, mocked Capacitor)
- [ ] Background audio + downloads are device-validated, not node-testable — Wave 0 cannot close them; a documented manual test script (matrix below) is required instead.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Play → lock screen → wait 5+ min | D-04 | WebView/FGS behavior not node-testable | Audio keeps playing; notification persists with controls |
| Lock-screen prev/play/pause/next | D-04 | native MediaSession bridge | Each transport control drives `player.*` correctly |
| Background (home) → return | D-06 | activity lifecycle | WebView restored, NOT reloaded; playback state intact |
| Track auto-advance while locked | D-04 | end-to-end audio chain | Next track plays; notification metadata updates |
| OS kill (swipe from recents) | D-04 | lifecycle | Audio stops cleanly; relaunch restores last position |
| Android 14+ (targetSdk 36) device | D-04 | FGS type enforcement | No `MissingForegroundServiceTypeException`; notification shows |
| Download lands in public `Music/`, playable offline | D-11 | MediaStore native bridge | File visible in file manager; plays with network off |
| Obtainium detects new release | D-08 | external app | Add repo in Obtainium; bump tag; update offered |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
