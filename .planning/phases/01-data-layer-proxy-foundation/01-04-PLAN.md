---
phase: 01-data-layer-proxy-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
autonomous: false
requirements: [DATA-03, DATA-04, SRC-01]
files_modified:
  - src/lib/services/catalog.ts
  - src/lib/services/catalog.test.ts
  - src/routes/spike/+page.svelte
  - .planning/phases/01-data-layer-proxy-foundation/SPIKE-DECISION.md
user_setup:
  - service: cloudflare
    why: "The egress spike (success criterion #5) must run on a real Cloudflare edge — edge egress IP differs from dev IP, which is the risk being tested (D-07)"
    env_vars:
      - name: JOOX_TOKEN
        source: "Set the deployed secret via `wrangler secret put JOOX_TOKEN` (value from legacy/index.html:2165) or the CF dashboard for the openmusic project"
    dashboard_config:
      - task: "Create/confirm the openmusic Pages project on CF account f1868a071996e836eae6da2b65f37929"
        location: "Cloudflare dashboard f1868a071996e836eae6da2b65f37929 → Workers & Pages (D-05/D-06). D-08 is RESOLVED — wrangler login done + CLOUDFLARE_API_TOKEN in GitHub repo secret; the CI/Pages-via-Git path is preferred for the real-edge deploy."

must_haves:
  truths:
    - "A keyword search fans out across all 4 sources via Promise.allSettled and returns interleaved, deduped results"
    - "One deliberately-killed source (throwing adapter) leaves the other three sources' results intact"
    - "ensureTrackDetails dispatches by track.source through the registry (no source named in aggregation code) and honors the readiness guard audioUrl && (lrc || !lrcUrl)"
    - "The openmusic app is deployed to the real Cloudflare edge on account f1868a071996e836eae6da2b65f37929 with JOOX_TOKEN set as a deployed secret"
    - "The /spike harness has been run from the deployed edge against all 4 sources and a per-source browser-direct PASS/FAIL matrix is recorded, locking the metadata-proxy-vs-browser-direct-audio decision"
  artifacts:
    - path: "src/lib/services/catalog.ts"
      provides: "searchAll (allSettled fan-out + dedupe + interleave) + ensureTrackDetails registry dispatch"
      exports: ["searchAll", "ensureTrackDetails"]
    - path: "src/lib/services/catalog.test.ts"
      provides: "allSettled isolation + interleave/dedupe + ensureTrackDetails dispatch tests"
    - path: ".planning/phases/01-data-layer-proxy-foundation/SPIKE-DECISION.md"
      provides: "The locked egress-spike decision matrix (source × browser-direct × needs-Worker-fallback)"
      contains: "browser-direct"
  key_links:
    - from: "src/lib/services/catalog.ts"
      to: "src/lib/sources/registry.ts"
      via: "getEnabledAdapters / SOURCES"
      pattern: "getEnabledAdapters|SOURCES\\["
    - from: "src/lib/services/catalog.ts"
      to: "Promise.allSettled"
      via: "fan-out"
      pattern: "Promise\\.allSettled"
    - from: "src/routes/spike/+page.svelte"
      to: "src/lib/services/catalog.ts"
      via: "searchAll / ensureTrackDetails"
      pattern: "searchAll|ensureTrackDetails"
---

<objective>
Close the phase: build the aggregation layer that ties all four adapters together with per-source isolation (DATA-03), wire the full `/spike` harness to it, deploy `openmusic` to the real Cloudflare edge, and run the egress spike to lock the audio data-flow architecture (success criterion #5). This is the final vertical-slice thickening — after 01-02/01-03 all four adapters resolve audio, and now the catalog fans them out, isolates failures, and the deployed harness proves browser-direct playback per source from a real edge IP.

Purpose: DATA-03 (allSettled isolation), the DATA-04 acceptance test in aggregation (catalog names no source), and the spike decision gate that Phase 2 consumes as locked architecture.
Output: `catalog.ts` (searchAll + ensureTrackDetails) with tests, the live `/spike` harness, a deployed `openmusic` edge, and `SPIKE-DECISION.md` recording the per-source matrix.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-data-layer-proxy-foundation/01-CONTEXT.md
@.planning/phases/01-data-layer-proxy-foundation/01-RESEARCH.md
@.planning/phases/01-data-layer-proxy-foundation/01-PATTERNS.md
@.planning/phases/01-data-layer-proxy-foundation/01-VALIDATION.md
@.planning/phases/01-data-layer-proxy-foundation/01-01-SUMMARY.md
@.planning/phases/01-data-layer-proxy-foundation/01-02-SUMMARY.md
@.planning/phases/01-data-layer-proxy-foundation/01-03-SUMMARY.md

<interfaces>
<!-- All adapter + registry + proxy contracts now exist (01-01..01-03). Implement against them. -->
- Track, SourceId, SourceAdapter, SettledSourceResult, makeUid → src/lib/sources/types.ts
- SOURCES, getEnabledAdapters(prefs) → src/lib/sources/registry.ts
- All 4 client adapters (netease, qq, kuwo, joox) implemented → src/lib/sources/*.ts
- parseLRC, inferQualityFromUrl → src/lib/services/lrc.ts
- /spike harness shell (Netease-only) → src/routes/spike/+page.svelte  [wire to catalog, light up all 4]
- the deployed proxy /api/[source]/[...path]/+server.ts (already live)

SettledSourceResult (RESEARCH 303-308): { source; status:'ok'|'error'; tracks:Track[]; error?:string }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: catalog.ts — allSettled fan-out, dedupe, interleave, registry-dispatched ensureTrackDetails</name>
  <files>src/lib/services/catalog.ts, src/lib/services/catalog.test.ts</files>
  <read_first>
    - legacy/index.html lines 2216-2263 (searchAllSources — Promise.all + string-matched ladder to REPLACE), 2506-2513 (ensureTrackDetails switch + the readiness guard at 2507), 1691-1707 (getInterleavedSearchList round-robin), 1657 + 2008/2066/2132/2179 (trackMap dedup by uid)
    - .planning/phases/01-data-layer-proxy-foundation/01-RESEARCH.md (Pattern 3 lines 297-324 catalog.searchAll allSettled; Pattern 4 lines 327-336 ensureTrackDetails; §Anti-Patterns line 339 don't hard-code source list)
    - .planning/phases/01-data-layer-proxy-foundation/01-PATTERNS.md (§catalog.ts lines 219-257 — allSettled change, dedup Map by colon uid, generalize interleave to registry order, preserve readiness guard EXACTLY)
    - .planning/phases/01-data-layer-proxy-foundation/01-VALIDATION.md line 48 (allSettled test command `-t allSettled`)
    - src/lib/sources/registry.ts, src/lib/sources/types.ts (contracts to use)
  </read_first>
  <behavior>
    - Test 1 (allSettled isolation — runnable as `-t allSettled`): with adapters mocked so ONE source's `search` rejects, `searchAll` returns `perSource` with that source `status:'error'` (typed `error` message) AND the other three sources' tracks present in `interleaved` — the dead source does not blank the result set (DATA-03).
    - Test 2 (dedupe): two adapters returning a track with the same colon uid yields ONE entry in `interleaved` (Map-keyed by `${source}:${songid}`).
    - Test 3 (interleave): results round-robin in registry order (netease→qq→kuwo→joox by displayIndex), generalized from `Object.keys(SOURCES)` — NOT a hard-coded array.
    - Test 4 (ensureTrackDetails dispatch): `ensureTrackDetails(track)` calls `SOURCES[track.source].resolve` (mocked) — no source named in the function; and the readiness guard returns early when `detailsLoaded && audioUrl && (lrc || !lrcUrl)` (verbatim legacy 2507), so a Netease track with an unresolved `lrcUrl` still re-resolves.
    - Test 5 (no-source-named): a source assertion that `catalog.ts` contains no literal `'netease'|'qq'|'kuwo'|'joox'` string in branching (DATA-04 — aggregation names no source).
  </behavior>
  <action>
    Create `src/lib/services/catalog.ts`. Port `searchAllSources` (legacy 2216-2263) to RESEARCH Pattern 3: `searchAll(keyword, page, prefs)` iterates `getEnabledAdapters(prefs)` (NOT a hard-coded `if(s==='netease')` ladder — Anti-Pattern line 339), runs `Promise.allSettled` over `adapter.search(keyword, page, signal)`, partitions into `SettledSourceResult[]` (status ok/error + typed error message), then dedupes the flattened tracks via a `Map<uid, Track>` keyed by the colon uid and interleaves round-robin in REGISTRY order (port `getInterleavedSearchList` legacy 1691-1707 but generalize the hard-coded `order` array to `Object.keys(SOURCES)` so a new source needs no edit — DATA-04). Drop ALL DOM/render calls (`dom.searchStatus`, `renderMiniSearchList`, `playFromList` — those are Phase 4). Port `ensureTrackDetails` (legacy 2506-2513) to `ensureTrackDetails(track, signal)` → `SOURCES[track.source].resolve(track, signal)`, PRESERVING the readiness guard `track.detailsLoaded && track.audioUrl && (track.lrc || !track.lrcUrl)` EXACTLY (legacy 2507) so Netease's separate `lrcUrl` still triggers a lyrics re-resolve. Write `src/lib/services/catalog.test.ts` (Tests 1-5, adapters mocked via the registry).
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/services/catalog.test.ts && pnpm vitest run src/lib/services/catalog.test.ts -t allSettled && pnpm check 2>&1 | tail -3 && ! grep -nE "=== *'(netease|qq|kuwo|joox)'|== *'(netease|qq|kuwo|joox)'" src/lib/services/catalog.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/lib/services/catalog.test.ts -t allSettled` green: one rejecting source leaves the other three intact (DATA-03).
    - Source assertion: `grep "Promise.allSettled" src/lib/services/catalog.ts` present; `grep "getEnabledAdapters\|SOURCES\[" src/lib/services/catalog.ts` present.
    - Source assertion (DATA-04): no source-name equality branch in `catalog.ts` (grep gate above passes — aggregation names no source).
    - Behavior assertion: the readiness guard `audioUrl && (lrc || !lrcUrl)` is preserved verbatim (Test 4 re-resolves a Netease lrcUrl track).
    - `pnpm check` strict passes; full suite `pnpm vitest run` green across all modules.
  </acceptance_criteria>
  <done>Catalog fans out with allSettled isolation, dedupes by colon uid, interleaves in registry order, and dispatches details through the registry with no source named — DATA-03 + DATA-04 proven.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the full /spike harness to catalog (all 4 sources live)</name>
  <files>src/routes/spike/+page.svelte</files>
  <read_first>
    - src/routes/spike/+page.svelte (the shell from 01-01 — extend it)
    - .planning/phases/01-data-layer-proxy-foundation/01-RESEARCH.md (§The Egress Spike lines 466-507 — the 3 per-source measurements + decision-gate matrix; §Referrer suppression)
    - .planning/phases/01-data-layer-proxy-foundation/01-PATTERNS.md (§spike harness lines 350-357)
    - src/lib/services/catalog.ts (searchAll, ensureTrackDetails — now built)
  </read_first>
  <action>
    Extend `src/routes/spike/+page.svelte` to drive ALL FOUR sources through the now-built `catalog.searchAll` + `ensureTrackDetails`. Per source the harness: (a) searches a known-popular keyword (each source enabled individually so per-source isolation is visible), (b) resolves the top result's `audioUrl`, then runs the three measurements from RESEARCH lines 483-491 — Browser-direct playback (`new Audio(audioUrl)` with `referrerpolicy="no-referrer"`, listen for `playing` vs `error`/`stalled`, 8s timeout), CORS/fetch gating (ranged `fetch` `Range: bytes=0-1`, note status + whether it throws), and Range/206 support (check for `206` + `Content-Range`/`Accept-Ranges`). Render a per-source PASS/FAIL matrix (source × browser-direct × ranged-fetch × 206) live on the page. Do NOT use `{@html}` on any source-supplied field (Security V5/XSS — Svelte auto-escape only). This page is a deployed manual harness; it cannot be automated headlessly (VALIDATION Manual-Only Verifications).
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5 && grep -q "searchAll\|ensureTrackDetails" src/routes/spike/+page.svelte && grep -q "no-referrer" src/routes/spike/+page.svelte && ! grep -q "@html" src/routes/spike/+page.svelte</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` succeeds with the full harness compiled.
    - Key-link: `/spike` imports/calls `catalog.searchAll` + `ensureTrackDetails` (grep gate).
    - Source assertion: `referrerpolicy="no-referrer"` present on the `<audio>`/`Audio` usage; NO `{@html}` anywhere (grep gates).
    - The page renders four source rows with the three measurement columns (manual visual — verified on deploy in Task 3).
  </acceptance_criteria>
  <done>The /spike harness drives all 4 sources end-to-end and renders the per-source browser-direct measurement matrix, ready to run from the deployed edge.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy openmusic to the real edge + run the egress spike decision gate</name>
  <files>.planning/phases/01-data-layer-proxy-foundation/SPIKE-DECISION.md</files>
  <action>Confirm CF account access, deploy openmusic to the real Cloudflare edge, run the /spike harness from a deployed visit, and record the per-source browser-direct decision matrix — see how-to-verify for the exact gated steps.</action>
  <what-built>The full data layer + proxy is built and tested locally (4 adapters, allSettled catalog, the /spike harness). This checkpoint confirms Cloudflare account access, then deploys openmusic to the real edge and runs the egress spike — the locking decision for the audio data-flow architecture (success criterion #5).</what-built>
  <how-to-verify>
    1. ACCOUNT GATE (Pitfall 1 / D-08): run `wrangler whoami` and confirm the active account list includes `f1868a071996e836eae6da2b65f37929` (F147259). D-08 is RESOLVED (wrangler login done + CLOUDFLARE_API_TOKEN in GitHub repo secret), but the connected CF MCP token covers Flow + Frank.chan only — so you MUST confirm the ACTIVE account is f1868a071996e836eae6da2b65f37929 before deploying. If it is NOT, STOP and re-auth (do not deploy to Flow/Frank.chan).
    2. Set the deployed secret: `wrangler secret put JOOX_TOKEN` (value from legacy/index.html:2165) for the openmusic project, OR confirm it is set in the CF dashboard. Confirm `.dev.vars` is NOT what ships (it is gitignored).
    3. Deploy openmusic to the edge. Preferred (real-edge, per D-08): the CI / Cloudflare Pages-via-Git path using the `CLOUDFLARE_API_TOKEN` GitHub repo secret. Alternative: `wrangler deploy` / `wrangler pages deploy` locally. Confirm the project lands under account f1868a071996e836eae6da2b65f37929 and is reachable at `openmusic.pages.dev`.
    4. RUN THE SPIKE from a REAL browser visit to `https://openmusic.pages.dev/spike` (NOT `wrangler dev` — the edge-vs-home IP is the variable being tested). For each of the 4 sources, observe: does `<audio>` play browser-direct? does the ranged fetch return 206?
    5. Record the per-source results into `.planning/phases/01-data-layer-proxy-foundation/SPIKE-DECISION.md`: a matrix (source × browser-direct PASS/FAIL × needs-Worker-fallback × 206-support) plus the LOCKED decision per RESEARCH lines 493-500 — browser-direct for passing sources; document any source needing a stream-passthrough fallback (do NOT build the fallback now unless a source fails — that is the only trigger). This file is the architecture Phase 2 consumes.
    Expected (per all project research): browser-direct PASS for all four — but the spike exists precisely because JOOX/QQ CDN geo-binding from a CF edge could surprise us.
  </how-to-verify>
  <resume-signal>Type "approved" once openmusic is deployed to account f1868a071996e836eae6da2b65f37929 and SPIKE-DECISION.md records the per-source browser-direct matrix + locked decision — or describe what failed (e.g. "account access blocked", "JOOX 403s from edge").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client `catalog` ← per-source adapters | One source throwing must not crash the aggregation (isolation boundary) |
| deployed edge → audio CDN | The spike tests whether the CDN serves audio to a browser whose request originates from a CF edge region |
| operator → CF account | Deploy MUST target account f1868a071996e836eae6da2b65f37929, not Flow/Frank.chan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Denial of Service | one failing source blanks all search results | mitigate | `Promise.allSettled` isolation — typed per-source error, others intact (Task 1 Test 1, DATA-03) |
| T-01-10 | Spoofing/Elevation | deploy lands under the wrong CF account | mitigate | `wrangler whoami` account gate confirms f1868a071996e836eae6da2b65f37929 before any deploy (Task 3 step 1) |
| T-01-07 | Information Disclosure | JOOX token / signed audio URLs leak via deploy logs / `wrangler tail` | mitigate | Deployed secret via `wrangler secret put` (not in repo); no token logging; redact signed params (Security V7) |
| T-01-03 | Tampering (XSS) | source-supplied fields rendered on the deployed /spike | mitigate | No `{@html}`; Svelte auto-escape (Task 2 grep gate) |
| T-01-02 | Elevation/DoS | deployed /api/* as open music/CORS relay | mitigate | CORS scoped to own origin (built 01-01); the deployed proxy inherits it |
</threat_model>

<verification>
- `pnpm vitest run && pnpm check` — full suite green (lrc, registry, netease, qq, kuwo, joox, proxy, catalog) + strict typecheck (the phase gate per VALIDATION Sampling Rate).
- DATA-02 build-grep re-confirmed: `pnpm build && ! grep -rE "f84ao9lMF" .svelte-kit/output/client`.
- `wrangler whoami` shows account f1868a071996e836eae6da2b65f37929 before deploy.
- `openmusic.pages.dev/spike` reachable from a real browser; per-source matrix recorded.
- `.planning/phases/01-data-layer-proxy-foundation/SPIKE-DECISION.md` exists with the locked decision.
</verification>

<success_criteria>
- A keyword search fans out across all 4 sources via `Promise.allSettled`; one killed source leaves the rest intact (DATA-03 / criterion #1).
- Aggregation + dispatch name no source; adding a hypothetical source touches only new adapter files + the two registry imports (DATA-04 / criterion #3 — demonstrated across 01-02/01-03/01-04).
- A track resolves audioUrl + lyrics lazily through the proxy; JOOX returns the selected track after reorder (criterion #4, from 01-03; exercised end-to-end here).
- The spike has run on the real edge and the metadata-proxy-vs-browser-direct-audio decision is LOCKED in SPIKE-DECISION.md (criterion #5).
</success_criteria>

<output>
Create `.planning/phases/01-data-layer-proxy-foundation/01-04-SUMMARY.md` when done.
Also produce `.planning/phases/01-data-layer-proxy-foundation/SPIKE-DECISION.md` (Task 3 checkpoint).
</output>
