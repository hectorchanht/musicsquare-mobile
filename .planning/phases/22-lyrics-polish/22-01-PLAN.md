---
phase: 22-lyrics-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/lrc.ts
  - src/lib/services/lrc.test.ts
autonomous: true
requirements: [LYR-04, LYR-05, LYR-01]
nyquist_compliant: true

must_haves:
  truths:
    - "dominantScript('稻香') === 'han'; a kanji+kana JP line returns 'kana'; a hangul line 'hangul'; an EN line 'latin' (D-05)"
    - "reorderPairs moves the original (song-dominant-script) line ABOVE its translation within a same-timestamp group; pure-CN songs are returned unchanged (D-04/D-05)"
    - "splitParenLines recognizes all 9 bracket pairs （）()【】[]［］「」『』〈〉《》 and only splits a clause as fromParen when its script differs from the line's main text (D-07/D-08)"
    - "A same-script bracketed clause (backing vocals (oh oh), 「quote」) is NEVER split out and never becomes fromParen — never-drop guarantee (D-08/LYR-05)"
    - "A whole-line bracketed clause / section marker ([Chorus], 【副歌】) passes through unsplit as a normal line (D-09)"
    - "splitParenLines(reorderPairs(parseLRC(x))) is a deterministic pure function of x (idempotent order)"
    - "lineSeekFraction(time, duration) returns time/duration, guarded to null/undefined when duration <= 0 (LYR-01 seek math)"
  artifacts:
    - path: "src/lib/services/lrc.ts"
      provides: "dominantScript (NEW), reorderPairs (NEW), widened splitParenLines, lineSeekFraction (NEW); all exported, pure, DOM-free"
      contains: "export function dominantScript"
    - path: "src/lib/services/lrc.test.ts"
      provides: "Vitest fixtures for dominantScript, reorderPairs, widened splitParenLines, lineSeekFraction"
      contains: "describe('reorderPairs'"
  key_links:
    - from: "src/lib/services/lrc.ts splitParenLines"
      to: "src/lib/services/lrc.ts dominantScript"
      via: "script-mismatch gate per bracket match"
      pattern: "dominantScript\\("
    - from: "src/lib/services/lrc.ts reorderPairs"
      to: "src/lib/services/lrc.ts dominantScript"
      via: "song-dominant + per-line script comparison"
      pattern: "dominantScript\\("
---

<objective>
Land all pure-logic lyric changes in `src/lib/services/lrc.ts` as exported, DOM-free, fixture-tested functions: `dominantScript` (NEW), `reorderPairs` (NEW, D-04/D-05), widened/script-aware `splitParenLines` (D-07/D-08/D-09), and an optional pure `lineSeekFraction` seek-math helper (LYR-01). This is the dependency root for Phase 22 — the component plan (22-02) composes these.

Purpose: Fix the CN translation-above-original highlight bug (LYR-04) and the bracket-hiding never-drop requirement (LYR-05) entirely in node-testable pure functions, isolating zero-risk logic from the device-verified component work.
Output: Extended `lrc.ts` + extended `lrc.test.ts` with all VALIDATION.md Wave 0 fixtures, full lrc suite green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

ENVIRONMENT: shell default node is v16 and breaks vitest/svelte-check. ALWAYS prefix every node/pnpm command with:
`export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"` and use pnpm.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-lyrics-polish/22-CONTEXT.md
@.planning/phases/22-lyrics-polish/22-RESEARCH.md
@.planning/phases/22-lyrics-polish/22-PATTERNS.md
@.planning/phases/22-lyrics-polish/22-VALIDATION.md

<interfaces>
<!-- Current lrc.ts contract the executor extends. Extracted from src/lib/services/lrc.ts. -->
From src/lib/services/lrc.ts:
```typescript
export interface LyricLine {
  time: number;
  text: string;
  fromParen?: boolean;
}
export function parseLRC(txt: string): LyricLine[];          // strips [mm:ss.xxx], time-sorted — UNCHANGED
export function splitParenLines(lines: LyricLine[]): LyricLine[]; // WIDEN this (D-07/D-08); current parenRe = /[(（]([^()（）]+)[)）]/g
export function inferQualityFromUrl(url: string | null): QualityInfo; // UNCHANGED
```
NEW exports to add: `type Script = 'han' | 'kana' | 'hangul' | 'latin' | 'other'`, `dominantScript(text: string): Script`, `reorderPairs(lines: LyricLine[]): LyricLine[]`, `lineSeekFraction(time: number, duration: number): number | null`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dominantScript + reorderPairs (D-04/D-05) with fixtures</name>
  <files>src/lib/services/lrc.ts, src/lib/services/lrc.test.ts</files>
  <read_first>
    - src/lib/services/lrc.ts (current parseLRC/splitParenLines style, JSDoc convention, LyricLine interface)
    - src/lib/services/lrc.test.ts (existing describe('splitParenLines') Vitest fixture style to copy)
    - .planning/phases/22-lyrics-polish/22-RESEARCH.md (Pattern 2 dominantScript, Pattern 3 reorderPairs, Pitfall 5 \p{Script} u-flag + for...of, Assumption A1 kana rule)
    - .planning/phases/22-lyrics-polish/22-PATTERNS.md (lrc.ts assignment — exact JSDoc/export style, reorder-before-split ordering)
  </read_first>
  <behavior>
    - dominantScript: '稻香'→'han'; a JP line mixing kanji+kana (e.g. '君のため')→'kana' (any kana presence wins over Han count, A1); a hangul line→'hangul'; an EN line→'latin'; digits/punctuation-only→'other'; mixed line resolves by the kana>0 rule then max count
    - reorderPairs: a group with an EN original + CN translation at the same timestamp returns original first (matches song-dominant script); a pure-CN song (all 'han', no mismatched siblings) returns input order unchanged (no-op); a JP-kana original above/below its CN translation reorders original-first; a KR-hangul case likewise; a 3-lines-per-timestamp group preserves stable relative order of non-reordered lines
    - reorderPairs computes the song's dominant script across ALL line bodies (not per-line) to establish the original language baseline (A1 mitigation)
  </behavior>
  <action>
    Add `type Script = 'han' | 'kana' | 'hangul' | 'latin' | 'other'` and exported `dominantScript(text: string): Script` to lrc.ts, matching the existing multi-line JSDoc + export style. Iterate codepoints with `for (const ch of text)`; test each against `/\p{Script=Han}/u`, `/\p{Script=Hiragana}/u`/`/\p{Script=Katakana}/u`, `/\p{Script=Hangul}/u`, `/\p{Script=Latin}/u` (the `u` flag is mandatory — Pitfall 5). Apply the kana-presence rule: if any kana counted, return 'kana' regardless of Han count (A1). Otherwise return the max of han/hangul/latin; if all zero return 'other'.

    Add exported `reorderPairs(lines: LyricLine[]): LyricLine[]` between parseLRC and splitParenLines (MUST precede split — RESEARCH Pattern 1). Step 1: compute the song-dominant script over all line bodies. Step 2: walk consecutive same-`time` groups; within a group, if exactly one line's dominantScript === songDominant and a sibling's !== songDominant, move the matching (original) line to the front. Step 3: pure-CN (songDominant 'han', no mismatched siblings) returns the group unchanged. Step 4: stable — preserve relative order of non-reordered lines. No try/catch; match existing pure-transform style (inline guards only).

    Extend lrc.test.ts: import dominantScript, reorderPairs. Add `describe('dominantScript')` with han/kana(JP kanji+kana)/hangul/latin/other/mixed cases. Add `describe('reorderPairs')` with CN-above-EN→original-first, pure-CN no-op, JP-kana original, KR-hangul, 3-lines-per-timestamp stable-order, and an idempotency assertion that reorderPairs(reorderPairs(x)) equals reorderPairs(x).
  </action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm vitest run src/lib/services/lrc.test.ts -t "dominantScript" && pnpm vitest run src/lib/services/lrc.test.ts -t "reorderPairs"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function dominantScript" src/lib/services/lrc.ts` returns a match
    - `grep -n "export function reorderPairs" src/lib/services/lrc.ts` returns a match
    - `grep -c "Script=" src/lib/services/lrc.ts` >= 4 (Han, Hiragana/Katakana, Hangul, Latin property escapes present, all with /u)
    - reorderPairs is defined ABOVE splitParenLines in the file (line number of reorderPairs < line number of splitParenLines) — verify via grep -n
    - `describe('dominantScript'` and `describe('reorderPairs'` both present in lrc.test.ts
    - `pnpm vitest run src/lib/services/lrc.test.ts -t "dominantScript"` and `-t "reorderPairs"` both pass with 0 failures
  </acceptance_criteria>
  <done>dominantScript + reorderPairs exported and pure; both fixture suites green; reorderPairs precedes splitParenLines in file order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen splitParenLines to 9 bracket pairs with script-mismatch gate (D-07/D-08/D-09) + lineSeekFraction (LYR-01)</name>
  <files>src/lib/services/lrc.ts, src/lib/services/lrc.test.ts</files>
  <read_first>
    - src/lib/services/lrc.ts (current splitParenLines — keep the whole-line `if (!stripped)` passthrough branch unchanged for D-09)
    - src/lib/services/lrc.test.ts (existing splitParenLines describe block — extend it)
    - .planning/phases/22-lyrics-polish/22-RESEARCH.md (Pattern 4 widened split, Pitfall 4 bracket-pairing regex, never-drop guarantee)
    - .planning/phases/22-lyrics-polish/22-PATTERNS.md (splitParenLines WIDEN section — suggested regex + mixed-bracket fixture requirement)
  </read_first>
  <behavior>
    - splitParenLines recognizes all 9 bracket pairs: （） () 【】 [] ［］ 「」 『』 〈〉 《》
    - A bracketed clause is split out as { fromParen: true } ONLY when dominantScript(inner) !== dominantScript(stripped-main-text); a same-script clause (e.g. 'oh yeah (oh oh)', '愛「だよ」') stays inline in the main text and is NEVER emitted as fromParen (never-drop, LYR-05)
    - A Latin main line with a CJK bracketed clause (e.g. 'or never （我们可以一同去往世界各地）') splits the CJK clause out as fromParen — script mismatch
    - A whole-line bracket / section marker with empty stripped main text ([Chorus], 【副歌】, （我們可以...）) passes through unsplit as a normal line (D-09, existing branch kept)
    - A mixed-bracket line '愛（love）【chorus】' pairs each open bracket with its MATCHING close (no （…】 cross-pairing — Pitfall 4)
    - lineSeekFraction(time, duration): returns time/duration when duration > 0; returns null when duration <= 0 or not finite
  </behavior>
  <action>
    Widen `parenRe` in splitParenLines to the 9-pair set using a pair-aware regex that matches each open bracket with its matching close (RESEARCH-suggested: `/[（(【［「『〈《]([^）)】］」』〉》]+)[）)】］」』〉》]/g`). Per Pitfall 4, a mixed-bracket fixture must confirm correct pairing; if the suggested class-based close set proves to cross-pair on the `愛（love）【chorus】` fixture, switch to a pair-aware alternation (`（[^）]+）|\([^)]+\)|【[^】]+】|…`). Add the D-08 script-mismatch gate: compute `dominantScript(stripped)` once per line, and for each match compute `dominantScript(inner)` — only push `{ time, text: inner, fromParen: true }` when the two differ; when they match, leave the clause inline (do NOT strip it out of the main text). KEEP the existing `if (!stripped) { out.push(line); continue; }` whole-line passthrough branch byte-unchanged (D-09).

    Note: re-stripping must only remove the bracketed clauses that were actually split out (script-mismatched ones), so a same-script clause survives in the rendered main text. Adjust the stripped-text computation so same-script brackets remain part of `text`.

    Add exported `lineSeekFraction(time: number, duration: number): number | null` (LYR-01 seek math, consumed by 22-02): return `duration > 0 && Number.isFinite(duration) ? time / duration : null`. Pure, DOM-free, store-free.

    Extend lrc.test.ts splitParenLines describe block: add cases for all 9 bracket pairs recognized, script-mismatch split (Latin+CJK), same-script inline preserved (never-drop: 'oh yeah (oh oh)' stays one line, 'a「b」c' same-script stays inline), whole-line passthrough ([Chorus]/【副歌】), and the mixed-bracket pairing fixture '愛（love）【chorus】'. Add `describe('lineSeekFraction')` with duration>0, duration===0→null, negative/Infinity→null. Add a cross-function idempotency test: `splitParenLines(reorderPairs(parseLRC(x)))` applied twice yields equal output (Pitfall 2 action item).
  </action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm vitest run src/lib/services/lrc.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function lineSeekFraction" src/lib/services/lrc.ts` returns a match
    - The widened parenRe (or alternation) in splitParenLines includes 【 ［ 「 『 〈 《 open brackets — `grep -cE "【|［|「|『|〈|《" src/lib/services/lrc.ts` >= 1 in the split function region
    - splitParenLines calls dominantScript for the mismatch gate — `grep -n "dominantScript" src/lib/services/lrc.ts` shows a call inside splitParenLines (after the function's reorder use)
    - The whole-line passthrough branch `if (!stripped)` is still present in splitParenLines
    - New test cases present: a never-drop assertion (a same-script bracketed line is NOT split / has no fromParen entry), the mixed-bracket '愛（love）【chorus】' fixture, and `describe('lineSeekFraction'`
    - Full lrc suite passes: `pnpm vitest run src/lib/services/lrc.test.ts` reports 0 failures
  </acceptance_criteria>
  <done>splitParenLines handles 9 bracket pairs, splits only script-mismatched clauses (never drops same-script originals), keeps whole-line passthrough; lineSeekFraction exported; full lrc suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds only pure in-process string/array transforms. No input source, network, storage, or auth boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering/Information Disclosure | lrc.ts pure functions | accept | No new trust boundary. LRC text is already-fetched and rendered via Svelte auto-escaped interpolation (no {@html}) downstream in 22-02; ASVS V5 satisfied by default text escaping. No injection surface introduced by pure transforms. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH § Package Legitimacy Audit is N/A; all logic uses native JS (\p{Script}) + existing in-repo modules. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm vitest run src/lib/services/lrc.test.ts` — full lrc suite green (existing 13 tests + new fixtures)
- `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm check` — svelte-check 0 errors / 0 warnings (new exports type-clean)
</verification>

<success_criteria>
- dominantScript, reorderPairs, lineSeekFraction exported and pure (no Svelte/store imports)
- splitParenLines widened to 9 bracket pairs with script-mismatch gate and whole-line passthrough retained
- reorderPairs defined before splitParenLines (reorder-before-split ordering locked)
- All VALIDATION.md Wave 0 fixture items present and green; full lrc suite passes; `pnpm check` clean
</success_criteria>

<output>
Create `.planning/phases/22-lyrics-polish/22-01-SUMMARY.md` when done.
</output>
