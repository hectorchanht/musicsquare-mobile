---
quick: 260606-54l
slug: translate-song-artist-names-long-press-track-menu
status: complete
date: 2026-06-06
---

# Quick Task 260606-54l — Summary

Two features on the deployed openmusic app. Phase-1 music data layer + 58 tests untouched. Live at openmusic.pages.dev.

## A) Translate displayed song + artist names
- settings: added `nameLang` ('off' + 5 langs) + a Settings control "Translate song & artist names" (chips).
- NEW `src/lib/stores/names.svelte.ts` — reactive name-translation cache: `dn(text)` returns the translated name if cached else the original immediately, batches unseen strings (~160ms) into one `/api/translate` call per language, bumps a `$state` rev when results land so callers re-render. In-memory + localStorage (`openmusic:name-tr:<lang>`), SSR-safe.
- `names.dn(...)` applied to titles + artists across home tiles, search rows, library (all tabs), artist page (hero + rows + album names), album page (header + rows), NowPlaying (title/artist + queue/related rows), and the mini nowbar. Originals kept for nav/keys/uids. Verified live: 邓紫棋 → 鄧紫棋 (Simplified→Traditional).

## B) Long-press track context menu (reusable)
- NEW `src/lib/actions/longpress.ts` — `use:longpress` action firing `onlongpress` after ~450ms hold (cancels on >8px move / pointer up; suppresses native context menu).
- NEW `src/lib/components/TrackMenu.svelte` — extracted from NowPlaying's options menu, operates on a passed track, with **Play next** + **Add to queue** added to the prior 7 (Download, Like, Add to playlist [+new], Go to album, Go to artist, Share, Detail). Self-contained picker + detail modal + toast.
- player store: `playNext(t)` (insert right after current, de-duped) + `addToQueue(t)` (append, de-duped); both play immediately if nothing is playing.
- NowPlaying now uses `<TrackMenu>` (DRY) for its ⋮; long-press on any song tile/row across home, search, library, artist, album, and NowPlaying queue/related opens TrackMenu for that track. Tap still plays.

## Verification
- `pnpm check` 0 errors / 0 warnings (3953 files). `pnpm build` ok. `pnpm vitest run` 58/58 (music data layer untouched).
- Deployed → https://openmusic.pages.dev. `/api/translate` (zh-Hant) returns 鄧紫棋 for 邓紫棋. `/`, `/settings`, `/library`, `/spike` all 200.

## Notes
- Name translation reuses the same unofficial translate endpoint (cached, best-effort, falls back to originals).
- No circular imports (names + settings stores standalone). TrackMenu/longpress are presentation-layer; catalog/adapters/proxy + tests unchanged.
