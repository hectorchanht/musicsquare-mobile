---
phase: 18-sleep-timer
plan: 03
type: execute
wave: 3
depends_on: ["18-01", "18-02"]
files_modified:
  - src/lib/stores/sleepTimer.svelte.ts
  - src/lib/components/SleepTimerSheet.svelte
  - src/lib/components/TrackMenu.svelte
  - src/lib/components/Nowbar.svelte
  - src/lib/components/NowPlaying.svelte
  - src/routes/(app)/+layout.svelte
  - src/lib/i18n/en.ts
  - src/lib/i18n/zh-Hans.ts
  - src/lib/i18n/zh-Hant.ts
  - src/lib/i18n/ar.ts
  - src/lib/i18n/de.ts
  - src/lib/i18n/es.ts
  - src/lib/i18n/fr.ts
  - src/lib/i18n/hi.ts
  - src/lib/i18n/id.ts
  - src/lib/i18n/it.ts
  - src/lib/i18n/pt.ts
  - src/lib/i18n/ru.ts
  - src/lib/i18n/th.ts
  - src/lib/i18n/tr.ts
  - src/lib/i18n/vi.ts
autonomous: false
requirements: [TIMER-01]

must_haves:
  truths:
    - "The user can open a sleep-timer sheet from the track menu and pick 5/10/15/30/45/60 min or end-of-track — sub-sheet clones the pickerOpen precedent (D-10)"
    - "Setting a duration closes the sheet and the indicator appears (the countdown IS the confirmation, no toast — D-12)"
    - "An active-timer indicator (moon + live countdown) shows on BOTH the nowbar AND the expanded now-playing, both tappable to reopen the sheet (D-06/D-07/D-08)"
    - "With a timer active, the sheet header shows live remaining time, the active duration is highlighted, tapping a different duration restarts fresh, and a Cancel row appears (D-11)"
    - "The new i18n keys exist in all 15 locale dicts with no expiry-toast key (D-09)"
  artifacts:
    - path: "src/lib/components/SleepTimerSheet.svelte"
      provides: "Globally-mounted timer sub-sheet (3rd pickerOpen-style instance) with the open-only $effect overlay registration"
      contains: "trackmenu-timer"
    - path: "src/lib/stores/sleepTimer.svelte.ts"
      provides: "sheetOpen UI-trigger field so any surface can open the global sheet"
      contains: "sheetOpen"
    - path: "src/lib/components/Nowbar.svelte"
      provides: "Moon badge + countdown, tappable → opens the timer sheet"
      contains: "sleepTimer"
    - path: "src/lib/components/NowPlaying.svelte"
      provides: "Full countdown readout near the transport, tappable → opens the timer sheet"
      contains: "sleepTimer"
    - path: "src/lib/i18n/en.ts"
      provides: "menu.sleepTimer, timer.endOfTrack, timer.cancel, timer.minutes interpolation key"
      contains: "timer.minutes"
  key_links:
    - from: "src/lib/components/TrackMenu.svelte"
      to: "sleepTimer.sheetOpen"
      via: "the Sleep timer menu item sets sleepTimer.sheetOpen = true"
      pattern: "sleepTimer\\.sheetOpen"
    - from: "src/routes/(app)/+layout.svelte"
      to: "src/lib/components/SleepTimerSheet.svelte"
      via: "the sheet is mounted ONCE in the app layout (reachable in both collapsed + expanded states)"
      pattern: "SleepTimerSheet"
    - from: "src/lib/components/SleepTimerSheet.svelte"
      to: "src/lib/stores/overlays.svelte.ts"
      via: "$effect dep on sheetOpen ONLY, untrack(overlays.open/dismiss('trackmenu-timer'))"
      pattern: "trackmenu-timer"
---

<objective>
Build the sleep-timer UI: a globally-mounted timer sub-sheet, the "Sleep timer" track-menu item,
an active-timer indicator on both the nowbar and the expanded now-playing (each tappable to reopen
the sheet), and the minimal 15-locale i18n keys. The sheet is the third instance of the exact
`pickerOpen` overlay precedent already in TrackMenu.svelte.

Purpose: TIMER-01's user-facing surface — set/cancel/change from the menu, an always-visible
indicator that doubles as the fastest cancel/change path (D-08). Because the indicator must be
reachable from both the collapsed nowbar AND the expanded now-playing, the sheet is mounted ONCE in
the app layout (driven by a `sleepTimer.sheetOpen` UI flag) rather than living inside the
per-page-mounted, track-gated TrackMenu.

Output: 1 new sheet component + a store UI-flag field + 4 modified components/layout + 15 locale
dicts. Ends with a human-verify checkpoint (volume fade, indicator, sheet behavior are DOM/device).
Zero new packages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-sleep-timer/18-CONTEXT.md
@.planning/phases/18-sleep-timer/18-RESEARCH.md
@.planning/phases/18-sleep-timer/18-PATTERNS.md
@src/lib/stores/sleepTimer.svelte.ts
@src/lib/components/TrackMenu.svelte
@src/lib/components/Nowbar.svelte

<interfaces>
<!-- Contracts created in 18-01/18-02 that this plan consumes. -->

From src/lib/stores/sleepTimer.svelte.ts (18-01; this plan ADDS the sheetOpen field):
```typescript
export const sleepTimer: {
  mode: 'off' | 'minutes' | 'end-of-track';
  deadline: number | null;
  selectedMinutes: number | null;
  remaining: number;          // ms, ticked 1s while active — pass remaining/1000 to fmtTime
  get active(): boolean;
  set(mode: 'minutes', minutes: number): void;
  set(mode: 'end-of-track'): void;
  restart(): void;
  cancel(): void;
  // NEW (this plan): sheetOpen = $state(false) — UI flag any surface flips to open the global sheet
};
```

From src/lib/stores/player.svelte.ts:
```typescript
export function fmtTime(s: number): string;   // NaN/Infinity-safe mm:ss — :89
```

<!-- Existing seams (verified this session): -->
<!-- TrackMenu.svelte:185 the pickerOpen-trigger menu-item precedent (`<button class="mi" onclick={() => { pickerOpen = true; }}>`) -->
<!-- TrackMenu.svelte:194-203 the pickerOpen sub-sheet markup (scrim + .menu + transition:fly + use:dragClose) -->
<!-- TrackMenu.svelte:154-158 the overlay-registration $effect (dep on the bool ONLY, untrack the overlays calls) -->
<!-- Nowbar.svelte:58-62 the .np-btn play button (placement reference: a small badge to its LEFT) -->
<!-- NowPlaying.svelte:717-725 the .transport row (.t / class:on button idiom) -->
<!-- +layout.svelte:148-154 Nowbar/NowPlaying mounts (gated on player.expanded) — SleepTimerSheet mounts UNGATED here -->
<!-- i18n: en.ts:226-249 menu.* keys; en.ts:257 toast.skippedMany interpolation precedent; index.ts:106 t(key, params) API; 15 dicts: ar de en es fr hi id it pt ru th tr vi zh-Hans zh-Hant -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n keys (15 locales) + sheetOpen store flag + SleepTimerSheet component + layout mount</name>
  <read_first>
    - src/lib/i18n/en.ts (lines 226-260: the `menu.*` keys to slot alongside, and `toast.skippedMany: '{count} songs skipped'` at :257 — the SINGLE-interpolation-key precedent; en.ts defines TranslationKey so every key added here MUST be added to all 14 other dicts or svelte-check fails)
    - src/lib/i18n/index.ts (lines 67-72 interpolate, 106-108 `t(key, params)` — `t('timer.minutes', { n: min })` substitutes `{n}`)
    - src/lib/i18n/zh-Hans.ts and src/lib/i18n/zh-Hant.ts (the two CJK dicts — read the menu.* region to match house style for translations)
    - src/lib/stores/sleepTimer.svelte.ts (the store created in 18-01 — add the sheetOpen field without disturbing the deadline logic)
    - src/lib/components/TrackMenu.svelte (lines 1-40 the import + $state block, 154-158 the overlay-registration $effect to COPY EXACTLY, 194-203 the pickerOpen sub-sheet markup to clone, the .mi/.scrim/.menu/.menu-head CSS classes)
    - src/lib/stores/overlays.svelte.ts (open/dismiss invariants + the single-dismiss-path note — the sheet's $effect cleanup is the ONLY dismiss caller)
    - src/routes/(app)/+layout.svelte (lines 1-14 imports, 93-114 onMount/overlays.init, 148-154 the Nowbar/NowPlaying mounts — mount SleepTimerSheet UNGATED so it is reachable in both states)
    - .planning/phases/18-sleep-timer/18-RESEARCH.md (Code Examples → "Timer sheet (3rd pickerOpen instance)"; Pitfall 5 i18n parity; Pitfall 6 overlay $effect over-dependency)
    - .planning/phases/18-sleep-timer/18-PATTERNS.md (the TrackMenu.svelte MODIFIED section + "Overlay back-to-close registration" + "i18n 15-locale parity + single interpolation key" shared patterns)
  </read_first>
  <action>
    (1) Add four i18n keys to ALL 15 locale dicts (en, zh-Hans, zh-Hant, ar, de, es, fr, hi, id, it, pt, ru, th, tr, vi): `menu.sleepTimer` (the track-menu item label, e.g. EN 'Sleep timer'), `timer.endOfTrack` (e.g. EN 'End of track'), `timer.cancel` (e.g. EN 'Cancel timer'), and `timer.minutes` as a SINGLE interpolation key 'EN {n} min' (NOT six hardcoded strings — mirror `toast.skippedMany`'s `{count}` precedent). Add the EN entries to en.ts first (en defines TranslationKey), then add the same keys to the other 14 dicts with appropriate translations. Do NOT add any expiry-toast key (D-09 is silent). (2) In `src/lib/stores/sleepTimer.svelte.ts`, add a `sheetOpen = $state(false)` UI-trigger field (this is the ONLY change to that store — do not touch the deadline logic). It is a pure UI flag any surface flips to true to open the global sheet. (3) Create `src/lib/components/SleepTimerSheet.svelte` as a verbatim third instance of the `pickerOpen` precedent, but driven by `sleepTimer.sheetOpen` instead of a local boolean. Imports: `untrack` from svelte, `fly` from svelte/transition, `dragClose` from `$lib/actions/dragClose`, `overlays` from `$lib/stores/overlays.svelte`, `sleepTimer` from `$lib/stores/sleepTimer.svelte`, `fmtTime` from `$lib/stores/player.svelte`, `t` from `$lib/i18n`, and `Moon` from `@lucide/svelte`. Overlay-registration `$effect` (COPY the TrackMenu.svelte:154-158 shape EXACTLY — Pitfall 6): dep on `sleepTimer.sheetOpen` ONLY, `untrack(() => overlays.open('trackmenu-timer', () => (sleepTimer.sheetOpen = false)))` with cleanup `return () => untrack(() => overlays.dismiss('trackmenu-timer'))`. Visibility gated by `{#if sleepTimer.sheetOpen}`, NOT by the effect. Markup clones the :194-203 sub-sheet: a `.scrim` button (`onclick={() => (sleepTimer.sheetOpen = false)}`, `aria-label={t('menu.close')}`), then `<div class="menu" transition:fly={{ y: 240, duration: 200 }} use:dragClose={{ onclose: () => (sleepTimer.sheetOpen = false) }}>`. Header `.menu-head`: `{t('menu.sleepTimer')}` plus, when `sleepTimer.active && sleepTimer.mode === 'minutes'`, ` · {fmtTime(sleepTimer.remaining / 1000)}` (live remaining — D-11). Then a `{#each [5,10,15,30,45,60] as min (min)}` of `.mi` buttons with `class:on={sleepTimer.mode === 'minutes' && sleepTimer.selectedMinutes === min}` and label `{t('timer.minutes', { n: min })}`, each `onclick={() => { sleepTimer.set('minutes', min); sleepTimer.sheetOpen = false; }}` (D-12 — closing + the countdown is the confirmation, NO toast). An end-of-track `.mi` button with `class:on={sleepTimer.mode === 'end-of-track'}`, label `{t('timer.endOfTrack')}`, `onclick={() => { sleepTimer.set('end-of-track'); sleepTimer.sheetOpen = false; }}`. Finally `{#if sleepTimer.active}` a bottom Cancel `.mi` row: `onclick={() => { sleepTimer.cancel(); sleepTimer.sheetOpen = false; }}`, label `{t('timer.cancel')}`. Reuse the existing `.mi`/`.scrim`/`.menu`/`.menu-head` CSS — to keep them, either import or duplicate the minimal class rules from TrackMenu's `<style>` (the component is standalone, so include a small scoped `<style>` copying just `.scrim`, `.menu`, `.menu-head`, `.mi`, and `.mi.on` from TrackMenu.svelte). (4) Mount `<SleepTimerSheet />` ONCE in `src/routes/(app)/+layout.svelte`, UNGATED (sibling to the `{#if !player.expanded}<Nowbar />` block, around :148-154) so it is reachable whether the now-playing is collapsed or expanded — add the import alongside the existing Nowbar/NowPlaying imports.
  </action>
  <verify>
    <automated>npm run check && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` clean — proves all 15 dicts have the four new keys (a missing key in any dict is a svelte-check compile error against TranslationKey)
    - `grep -rc "timer.minutes" src/lib/i18n/*.ts | grep -v ':0' | wc -l` returns 15 (key present in every dict)
    - `grep -c "menu.sleepTimer\|timer.endOfTrack\|timer.cancel\|timer.minutes" src/lib/i18n/en.ts` returns 4
    - `grep -c "expir\|asleep" src/lib/i18n/en.ts` shows NO new expiry-toast key was added (D-09)
    - `grep -n "sheetOpen" src/lib/stores/sleepTimer.svelte.ts` matches
    - `grep -n "trackmenu-timer" src/lib/components/SleepTimerSheet.svelte` matches (overlay id) and the registration $effect depends on sheetOpen only
    - `grep -n "SleepTimerSheet" src/routes/(app)/+layout.svelte` matches (mounted, ungated)
    - `npm test` full suite green (no regression)
  </acceptance_criteria>
  <done>The four i18n keys exist in all 15 locales (no expiry key); the store has a sheetOpen flag; SleepTimerSheet is a correctly-registered third pickerOpen instance mounted once in the app layout.</done>
</task>

<task type="auto">
  <name>Task 2: TrackMenu "Sleep timer" item + Nowbar moon badge + NowPlaying countdown readout (all open the sheet)</name>
  <read_first>
    - src/lib/components/TrackMenu.svelte (lines 1-16 the import block to add Moon + sleepTimer; line 185 the exact pickerOpen-trigger menu-item button to clone as a sibling)
    - src/lib/components/Nowbar.svelte (full — the import block at :10-13, the .np-btn play button at :58-62 (placement: a small tappable badge to its LEFT), the .np-btn CSS at :188-201, the marquee/min-0 memory rule for any text row)
    - src/lib/components/NowPlaying.svelte (lines 6-12 imports incl. fmtTime already imported from player + overlays; lines 717-725 the .transport row with the .t / class:on button idiom; the marquee rules)
    - src/lib/stores/sleepTimer.svelte.ts (sheetOpen flag from Task 1 — every indicator sets it true)
    - .planning/phases/18-sleep-timer/18-PATTERNS.md (the Nowbar.svelte + NowPlaying.svelte MODIFIED sections — placement + the icon-only-if-tight allowance D-07)
    - MEMORY: long text rows use `use:marquee` + marquee-inner (parent locked flex/min-0/max), never static ellipsis — applies to any new countdown text row that could overflow
  </read_first>
  <action>
    (1) TrackMenu.svelte: add `Moon` to the `@lucide/svelte` import and `import { sleepTimer } from '$lib/stores/sleepTimer.svelte'`. Add a new `.mi` menu item as a sibling of the existing Add-to-playlist item (clone the :185 shape): `<button class="mi" onclick={() => { sleepTimer.sheetOpen = true; close(); }}><Moon size={18} /> {t('menu.sleepTimer')}</button>` — it opens the GLOBAL sheet (from Task 1) and closes the track menu (the sheet no longer lives inside TrackMenu, so there is no local timerOpen state and no new $effect here — Phase 19 will restyle the menu but keep this action). (2) Nowbar.svelte: add `Moon` to the `@lucide/svelte` import and `import { sleepTimer } from '$lib/stores/sleepTimer.svelte'` plus `import { fmtTime } from '$lib/stores/player.svelte'`. Add a small tappable indicator badge `{#if sleepTimer.active}` immediately to the LEFT of the play button (:58-62), inside the existing flex row: a `<button>` with a `Moon` icon and, when space allows, the countdown `{#if sleepTimer.mode === 'minutes'}{fmtTime(sleepTimer.remaining / 1000)}{/if}` (D-07 allows icon-only on the nowbar; end-of-track mode shows the moon only). `onclick={() => (sleepTimer.sheetOpen = true)}`, `aria-label={t('menu.sleepTimer')}`. Style it from a small variant of the `.np-btn` rules (smaller, transparent/subtle — it is a badge, not the primary play button). If the countdown text is rendered, keep its container flex/min-0 so it never breaks the nowbar layout (memory rule — prefer truncation-safe layout; the mm:ss is short so static is acceptable but the container must be min-0). (3) NowPlaying.svelte: add `Moon` to its `@lucide/svelte` import (fmtTime + overlays already imported) and `import { sleepTimer } from '$lib/stores/sleepTimer.svelte'`. Add a full countdown readout near the `.transport` region (:717-725) using the `.t` / `class:on` button idiom: `{#if sleepTimer.active}` a tappable button with a `Moon` icon and either `{fmtTime(sleepTimer.remaining / 1000)}` (minutes mode — D-07 NowPlaying shows the FULL mm:ss) or `{t('timer.endOfTrack')}` (end-of-track mode), `onclick={() => (sleepTimer.sheetOpen = true)}` (D-08), `aria-label={t('menu.sleepTimer')}`, `class:on={true}` so it reads as active. All three indicators converge on the SAME global sheet via `sleepTimer.sheetOpen = true` (D-08 — fastest cancel/change path).
  </action>
  <verify>
    <automated>npm run check && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sleepTimer.sheetOpen = true" src/lib/components/TrackMenu.svelte src/lib/components/Nowbar.svelte src/lib/components/NowPlaying.svelte` totals >= 3 (each surface opens the sheet)
    - `grep -n "menu.sleepTimer" src/lib/components/TrackMenu.svelte` matches (the menu item exists)
    - `grep -n "sleepTimer.active" src/lib/components/Nowbar.svelte` and `… src/lib/components/NowPlaying.svelte` both match (indicators gated on active)
    - `grep -n "fmtTime(sleepTimer.remaining" src/lib/components/Nowbar.svelte src/lib/components/NowPlaying.svelte` matches (live countdown reuses fmtTime — no new formatter)
    - `npm run check` clean
    - `npm test` full suite green
  </acceptance_criteria>
  <done>The track menu has a Sleep timer item; the nowbar shows a moon badge (+ countdown when space allows); the now-playing shows a full countdown/end-of-track readout; all three are tappable and open the same global sheet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify sleep-timer end-to-end (fade/indicator/sheet/expiry/edge cases)</name>
  <what-built>
    A sleep timer end-to-end: a "Sleep timer" track-menu item opens a duration sheet (5/10/15/30/45/60 + end-of-track); setting a duration shows a moon-badge indicator on the nowbar and a full countdown in now-playing; the indicator is tappable to reopen the sheet (which highlights the active duration, shows live remaining, and offers Cancel); playback pauses at expiry with a ~10s fade (volume-honoring platforms) or instant pause (iOS), surviving background-tab throttling via an absolute deadline.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open the app (mobile viewport / real device for the iOS + lock-screen items).
    1. Play a track. Open the track menu (⋮ or long-press) → tap "Sleep timer" → pick "5 min". The sheet closes; a moon + countdown badge appears on the nowbar AND in the expanded now-playing.
    2. Tap the nowbar moon badge → the sheet reopens, shows live remaining time in its header, and highlights "5 min". Tap "10 min" → the timer restarts fresh (remaining jumps to ~10:00).
    3. Tap the indicator again → tap "Cancel timer" → indicator disappears.
    4. Set a SHORT manual deadline to observe expiry quickly (or wait): at expiry on desktop/Android the volume fades over ~10s then pauses in place; tapping play resumes from the same position at full volume. On an iPhone it instant-pauses (no fade) — this is the correct fallback, not a bug.
    5. D-05: set a timer, then DURING the fade tap play/next/seek → the fade aborts, volume restores, playback continues, the timer clears.
    6. D-03: set "End of track" with repeat-one ON → at the natural track end playback STOPS (does not rewind/repeat, does not advance).
    7. D-04: pause manually, wait past a minutes-deadline → the indicator silently disappears, no toast, no fade.
    8. D-09 (device): at expiry the OS lock-screen/media UI reads paused.
    9. Background-tab (device): set a timer, lock the screen, confirm playback stops within seconds of the deadline (not minutes late).
    10. Confirm NO toast appears at expiry (D-09) and the language switcher shows the timer item/sheet translated (spot-check zh-Hant).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (fade behavior, indicator placement, sheet/highlight, expiry timing, D-03/D-04/D-05 edge cases)</resume-signal>
  <action>Pause for human verification — run the dev server and walk the how-to-verify steps. This is a verification checkpoint, not an implementation step; no code is written here.</action>
  <verify>
    <human-check>The how-to-verify steps all behave as described; the verifier types "approved".</human-check>
  </verify>
  <done>The verifier confirms the sleep timer end-to-end (set/indicator/reopen/cancel/change, fade-or-instant expiry, D-03/D-04/D-05/D-09 edge cases, 15-locale translation) and types "approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap → sheet/store | Durations are chosen from a fixed button enum; the only state written is mode/minutes (validated-by-construction). No free text, no network. |
| sheet → overlays/history | The timer sheet pushes one history entry via overlays.open; the single-dismiss-path invariant (history depth == stack depth) must hold or Back desyncs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Denial of Service (UX) | SleepTimerSheet overlay $effect | mitigate | The registration $effect depends on `sleepTimer.sheetOpen` ONLY, with `untrack` around overlays.open/dismiss and visibility gated by `{#if}` (NOT the effect). Copying the pickerOpen precedent exactly prevents the history-depth desync (Pitfall 6 / Phase-19 blocker) that would otherwise stick or double-pop the Back gesture. |
| T-18-07 | Tampering | i18n key parity | mitigate | All four keys added to all 15 dicts; en.ts defines TranslationKey so any omission is a svelte-check compile error (verified by `npm run check`). Static literal keys only — no dynamic key construction. |
| T-18-08 | Information disclosure | sheetOpen SSR state | accept | sheetOpen is an in-memory client UI flag (mirrors the accepted overlays posture); no persistence, no PII, no secrets. |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed (RESEARCH "Package Legitimacy Audit: Not applicable"). `@lucide/svelte` (Moon icon) is already installed and imported across the codebase. |
</threat_model>

<verification>
- `npm run check` clean (proves 15-locale parity for the four new keys + no type errors in the new component/store field)
- `npm test` full suite green (no regression to the ~171 existing tests)
- Indicator/sheet wiring greps: all three surfaces set `sleepTimer.sheetOpen = true`; SleepTimerSheet registers `trackmenu-timer` with an open-only $effect; the sheet is mounted ungated in the app layout
- Human-verify checkpoint passes (fade, indicator placement, sheet highlight/restart/cancel, expiry timing, D-03/D-04/D-05/D-09 edge cases — all DOM/device-only)
</verification>

<success_criteria>
- A "Sleep timer" track-menu item opens a duration sheet with 5/10/15/30/45/60 + end-of-track
- Setting a duration closes the sheet and shows the indicator (no toast — the countdown is the confirmation)
- The indicator shows on both the nowbar (moon + optional countdown) and now-playing (full countdown), both tappable to reopen the sheet
- The active sheet shows live remaining, highlights the active duration, restarts fresh on a different pick, and offers Cancel
- Four i18n keys exist in all 15 locales with no expiry-toast key (D-09); `npm run check` + full suite green
- Human-verify checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/18-sleep-timer/18-03-SUMMARY.md` when done.
</output>
