---
phase: 23-ux-audit-homepage-artist-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/stores/toast.svelte.ts
  - src/lib/components/ToastHost.svelte
  - src/lib/actions/inflightGuard.ts
  - src/lib/util/haptics.ts
  - src/routes/(app)/+layout.svelte
autonomous: true
requirements: [UX-02, UX-05]
must_haves:
  truths:
    - "A single global toast store exists; pages call toast.show(msg) and one mounted ToastHost renders it (D-15)"
    - "An async action button ignores taps while its action runs and re-enables on settle (resolve or throw)"
    - "haptics.tick() fires a ~15ms vibrate where supported and no-ops safely on iOS Safari"
  artifacts:
    - path: "src/lib/stores/toast.svelte.ts"
      provides: "Global runes-singleton toast store with show(msg)"
      contains: "show"
    - path: "src/lib/components/ToastHost.svelte"
      provides: "Single mounted toast renderer with aria-live"
      contains: "aria-live"
    - path: "src/lib/actions/inflightGuard.ts"
      provides: "Pure node-testable in-flight decision helper (D-16)"
    - path: "src/lib/util/haptics.ts"
      provides: "Commit-tier haptic tick (D-17)"
      contains: "navigator.vibrate"
  key_links:
    - from: "src/routes/(app)/+layout.svelte"
      to: "src/lib/components/ToastHost.svelte"
      via: "component mount alongside NowPlaying/SleepTimerSheet"
      pattern: "ToastHost"
---

<objective>
Build the consolidated feedback layer primitives — a global toast store (D-15), a reusable double-click in-flight guard (D-16), and a commit-tier haptics helper (D-17) — and mount the single ToastHost in the app layout. These are leaf utilities consumed by every downstream plan in this phase.

Purpose: UX-02 (toast on click + double-click resistance) and UX-05 (haptics) rest entirely on these primitives. Consolidating the three local toast copies into one store removes drift.
Output: 4 new files + ToastHost mounted in the layout. No call-site migrations yet (downstream plans migrate their own surfaces).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

Tooling: shell default node is v16 and breaks tooling. Prefix PATH with `$HOME/.nvm/versions/node/v22.22.0/bin` and use pnpm for every check/test command.
</execution_context>

<context>
@.planning/phases/23-ux-audit-homepage-artist-polish/23-CONTEXT.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-PATTERNS.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Global toast store + ToastHost component, mounted in layout (D-15)</name>
  <files>src/lib/stores/toast.svelte.ts, src/lib/components/ToastHost.svelte, src/routes/(app)/+layout.svelte</files>
  <read_first>
    - src/lib/stores/settings.svelte.ts (runes-singleton + `browser` SSR-guard posture to mirror)
    - src/lib/components/TrackMenu.svelte (the local `toast()` copy ~lines 28-34; the `.toast` style ~line 342; the render+transition `{#if toastMsg}` ~line 306 — copy style/transition byte-identical)
    - src/routes/(app)/+layout.svelte (lines 13-15 imports, lines 154/159 where NowPlaying and SleepTimerSheet mount)
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md §3.1
  </read_first>
  <behavior>
    - toast.show('hi') sets the visible message to 'hi'
    - A second toast.show('bye') before 2000ms replaces the message with 'bye' and resets the timer (no stacking — one message at a time)
    - After 2000ms with no further calls, the message clears to ''
    - Store is SSR-guarded (browser-only timer) and in-memory (no localStorage)
  </behavior>
  <action>Create `src/lib/stores/toast.svelte.ts` as a runes singleton `export const toast = new Toast()` exposing reactive `msg: string` (via `$state`) and `show(msg: string): void`. `show()` assigns the message, clears any existing timer, and sets a new `setTimeout(() => msg = '', 2000)`. Guard the timer behind `browser` from `$app/environment` (SSR-safe). The 2000ms timeout is locked (matches all three existing local copies). No stacking/queue. Create `src/lib/components/ToastHost.svelte` that imports the store and renders `{#if toast.msg}<div class="toast" role="status" aria-live="polite" transition:fly={{ y: -20, duration: 180 }}>{toast.msg}</div>{/if}`. Copy the `.toast` CSS byte-identical from TrackMenu line ~342 (position:fixed; left:50%; transform:translateX(-50%); top:calc(env(safe-area-inset-top,0px)+14px); z-index:90; background:#000; color:#fff; padding:10px 16px; border-radius:999px; font-size:13px; box-shadow:var(--shadow-lg)). These grandfathered values are exempt from the 8pt scale per UI-SPEC §1. Import `fly` from `svelte/transition`. In `src/routes/(app)/+layout.svelte` add `import ToastHost from '$lib/components/ToastHost.svelte';` next to the NowPlaying import (line 13) and mount `<ToastHost />` alongside `<SleepTimerSheet />` (line ~159). Do not migrate the three existing local toast copies in this plan — downstream plans own their surfaces.</action>
  <verify>
    <automated>PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/stores/toast.svelte.ts exports `const toast` with a `show(` method and a reactive `msg` field
    - src/lib/components/ToastHost.svelte contains `role="status"` and `aria-live="polite"`
    - ToastHost `.toast` CSS contains `top: calc(env(safe-area-inset-top, 0px) + 14px)` and `border-radius: 999px`
    - src/routes/(app)/+layout.svelte renders `<ToastHost`
    - `pnpm check` exits 0
  </acceptance_criteria>
  <done>Toast store + host exist, host is mounted once in the layout, role/aria-live present, pnpm check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: inflightGuard pure helper (D-16) + haptics tick (D-17)</name>
  <files>src/lib/actions/inflightGuard.ts, src/lib/util/haptics.ts, src/lib/actions/inflightGuard.test.ts, src/lib/util/haptics.test.ts</files>
  <read_first>
    - src/lib/components/track-menu-gate.ts (the canonical pure `shouldStartResolve(inFlight, key)` seed — mirror its node-testable posture)
    - src/lib/components/track-menu-gate.test.ts (node-test style to match)
    - src/lib/services/color.ts and src/lib/gestures/velocity.ts (leaf-util posture: zero imports, pure, SSR-safe — model haptics on this)
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md §3.2 and §3.3
  </read_first>
  <behavior>
    - inflightGuard: `shouldRun(inFlight, key)` returns true when key not in the Set, false when already present
    - inflightGuard: a `withGuard` style helper (if added) re-enables the key in `finally` on both resolve and throw — never leaves a stuck key
    - haptics: `tick()` calls `navigator.vibrate(15)` when present; does NOT throw when `navigator.vibrate` is undefined (iOS Safari)
  </behavior>
  <action>Create `src/lib/actions/inflightGuard.ts` exporting a pure `shouldRun(inFlight: Set<string>, key: string): boolean` (returns `!inFlight.has(key)`) mirroring `shouldStartResolve` in `track-menu-gate.ts` so it is node-testable without a DOM. The `new Set(...).add(key)` reassign-for-reactivity and the `finally`-delete stay in the consuming components (per PATTERNS.md §3.2) — keep the helper a pure decision only. Create `src/lib/util/haptics.ts` exporting `tick(): void` that calls `navigator.vibrate?.(15)` wrapped in `try { } catch {}` so it never throws and no-ops where unsupported (iOS Safari). 15ms is the locked commit-tier duration (D-17). Add node tests `inflightGuard.test.ts` (covering key-present/absent) and `haptics.test.ts` (stub `navigator.vibrate` to assert it is called with 15; delete it to assert no throw). Both follow the node-only Vitest project posture used by track-menu-gate.test.ts.</action>
  <verify>
    <automated>PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm vitest run src/lib/actions/inflightGuard.test.ts src/lib/util/haptics.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/actions/inflightGuard.ts exports `function shouldRun(` and returns `!inFlight.has(key)`
    - src/lib/util/haptics.ts exports `function tick(` and references `navigator.vibrate`
    - haptics.ts wraps the vibrate call so calling tick() with no navigator.vibrate does not throw
    - The two test files pass under pnpm vitest run
  </acceptance_criteria>
  <done>Both pure helpers exist with passing node tests; haptics no-ops safely without navigator.vibrate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → toast text | callers pass message strings; rendered into DOM text |
| browser API → haptics | navigator.vibrate may be absent or throw |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering (XSS) | ToastHost message render | mitigate | Render `toast.msg` as text content only (`{toast.msg}` in Svelte auto-escapes); never use `{@html}`. Acceptance: ToastHost source contains no `{@html`. |
| T-23-02 | Denial of Service | haptics.tick | accept | navigator.vibrate is a benign no-op-or-throw API; wrapped in try/catch so a throwing/absent API cannot crash callers. Low risk, no PII. |
| T-23-03 | Information Disclosure | toast store | accept | In-memory only, no persistence, no secrets; transient UI string. |
</threat_model>

<verification>
- `pnpm check` exits 0 (no type errors from new files or the layout edit).
- `pnpm vitest run` for the two new test files passes.
- ToastHost source contains no `{@html` (T-23-01).
</verification>

<success_criteria>
- toast.show() shows a transient message via a single mounted ToastHost with role="status"/aria-live="polite".
- inflightGuard.shouldRun is a pure node-tested decision helper.
- haptics.tick() vibrates 15ms where supported and no-ops on iOS Safari.
</success_criteria>

<output>
Create `.planning/phases/23-ux-audit-homepage-artist-polish/23-01-SUMMARY.md` when done.
</output>
