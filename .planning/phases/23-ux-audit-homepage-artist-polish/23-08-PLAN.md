---
phase: 23-ux-audit-homepage-artist-polish
plan: 08
type: execute
wave: 2
depends_on: ["23-01", "23-02"]
files_modified:
  - src/lib/components/NowPlaying.svelte
  - src/lib/components/TrackMenu.svelte
  - src/lib/components/SleepTimerSheet.svelte
autonomous: true
requirements: [UX-02, UX-05, UX-06]
must_haves:
  truths:
    - "The NowPlaying flash() and TrackMenu toast() local copies are migrated to the global toast store; transport like/repeat toggles carry aria-pressed reflecting state"
    - "TrackMenu, NowPlaying sheet, and SleepTimerSheet trap focus while open and restore focus to the trigger on close (use:focusTrap)"
    - "TrackMenu async actions show aria-busy while in-flight (the inFlight guard, already present, is surfaced to AT); haptic tick fires on like toggle / addQueue / long-press menu open"
  artifacts:
    - path: "src/lib/components/TrackMenu.svelte"
      provides: "toast migration + focusTrap + aria-busy + haptics on addQueue/like"
      contains: "focusTrap"
    - path: "src/lib/components/NowPlaying.svelte"
      provides: "flash→toast migration + aria-pressed toggles + focusTrap + haptic on like"
      contains: "aria-pressed"
    - path: "src/lib/components/SleepTimerSheet.svelte"
      provides: "focusTrap on the sheet"
      contains: "focusTrap"
  key_links:
    - from: "src/lib/components/TrackMenu.svelte"
      to: "src/lib/actions/focusTrap.ts"
      via: "use:focusTrap on the menu container"
      pattern: "focusTrap"
---

<objective>
Complete the a11y + feedback consolidation on the two remaining local-toast surfaces and the overlays (UX-02/05/06): migrate NowPlaying `flash()` and TrackMenu `toast()` to the global store, add `aria-pressed` to transport toggles, wire `use:focusTrap` onto TrackMenu / NowPlaying sheet / SleepTimerSheet (with focus restore), surface `aria-busy` on TrackMenu in-flight actions, and add commit-tier `haptics.tick()` to the like/addQueue/long-press-menu events on these surfaces.

Purpose: UX-06 (aria-pressed toggles, focus-trap in sheets), UX-02 (global toast), UX-05 (haptics on the remaining commit events). This finishes the feedback/a11y pass started by Plans 01/02.
Output: toast migration + aria-pressed + focusTrap + aria-busy + haptics across NowPlaying, TrackMenu, SleepTimerSheet.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

Tooling: prefix PATH with `$HOME/.nvm/versions/node/v22.22.0/bin` and use pnpm for every check/test command.
NOTE: focusTrap manages focus ONLY — never open/close state (preserve the Phase 19 overlay `$effect` history invariant: dep `open`-only, untrack() around overlay calls, single dismiss path, {#if open && track}). Do not change the existing inFlight guard logic — only surface aria-busy.
</execution_context>

<context>
@.planning/phases/23-ux-audit-homepage-artist-polish/23-CONTEXT.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-PATTERNS.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-01-SUMMARY.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-02-SUMMARY.md

<interfaces>
toast (Plan 01): `import { toast } from '$lib/stores/toast.svelte'; toast.show(t('...'))`. ToastHost is mounted in the layout — the local `.toast`/`.np-toast` renders are removed.
focusTrap (Plan 02): `use:focusTrap` on the open sheet/menu container; focuses first focusable on mount, cycles Tab, restores focus on destroy. Manages focus only.
haptics (Plan 01): `import { tick } from '$lib/util/haptics'; tick()` on commit.
TrackMenu existing: local `toast(m)` ~line 30; `inFlight` Set + `shouldStartResolve` guard ~42-57; `addQueue()` ~64; `like()` ~76; overlay $effect invariant ~180-208; `.row-spinner` precedent ~254/262/273.
NowPlaying existing: `flash(m)` ~39 → `npToast` render ~981 / `.np-toast` style ~1161; like toggle button ~873; repeat toggle ~879 (`class:on={player.repeatMode !== 'off'}`); options/collapse buttons ~768/770.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TrackMenu — toast migration + focusTrap + aria-busy + haptics (UX-02/05/06)</name>
  <files>src/lib/components/TrackMenu.svelte</files>
  <read_first>
    - src/lib/components/TrackMenu.svelte (local toast() ~30 and all its call sites; inFlight guard ~42-57; addQueue ~64; like ~76; overlay $effect ~180-208; .row-spinner ~254/262/273; the menu container / scrim markup ~306+)
    - src/lib/stores/toast.svelte.ts, src/lib/actions/focusTrap.ts, src/lib/util/haptics.ts
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md §3.1, §3.2, §3.3, §7.3
  </read_first>
  <action>(1) Toast migration: replace the local `toast(m)` function (~30) and its inline render with `import { toast } from '$lib/stores/toast.svelte'`; change every `toast(...)` call site to `toast.show(...)` (addQueue/playNext/like/download/share/remix/addToPlaylist/noAudio calls). Remove the local toastMsg/toastTimer state and the inline `.toast` render (ToastHost renders globally now). (2) focusTrap: add `use:focusTrap` to the open menu container element (the panel inside `{#if open && track}`), so focus traps + restores to the trigger on close. Do NOT touch the overlay `$effect` open/close invariant (~180-208) — focusTrap manages focus only. Keep the existing scrim `aria-label={t('menu.close')}`. (3) aria-busy: on the action buttons that go through the inFlight guard (the gated run path ~45-57), set `aria-busy={inFlight.has(key)}` so AT announces the in-flight state; the `.row-spinner` already shows visually. Do NOT change the guard logic. (4) haptics: call `tick()` inside `addQueue()` and `like()` (commit-tier per §3.3). The long-press that OPENS this menu fires from the row's onlongpress (Plans 04/05/06 add tick there) — do not add a second tick on open here.</action>
  <verify>
    <automated>PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm check 2>&1 | tail -5; PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm vitest run src/lib/components/track-menu-gate.test.ts 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - TrackMenu.svelte imports `toast` from `$lib/stores/toast.svelte` and has no local `toast(`/`toastTimer`/inline `.toast` render
    - The menu container carries `use:focusTrap`; the overlay $effect (~180-208) is unchanged (dep open-only)
    - The gated action buttons set `aria-busy={inFlight.has(...)}`
    - `addQueue()` and `like()` call `tick()`
    - `pnpm check` exits 0 and track-menu-gate.test.ts still passes
  </acceptance_criteria>
  <done>TrackMenu uses the global toast, traps focus, surfaces aria-busy, and haptics on addQueue/like; guard + overlay invariant unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: NowPlaying — flash→toast migration + aria-pressed + focusTrap + haptic on like (UX-02/05/06)</name>
  <files>src/lib/components/NowPlaying.svelte</files>
  <read_first>
    - src/lib/components/NowPlaying.svelte (flash() ~39 + its call sites incl. like ~47; npToast render ~981 + .np-toast style ~1161; like toggle ~873; repeat toggle ~879; the sheet/panel container; options/collapse ~768/770)
    - src/lib/stores/toast.svelte.ts, src/lib/actions/focusTrap.ts, src/lib/util/haptics.ts
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md §3.1, §3.3, §7.1, §7.3
  </read_first>
  <action>(1) Toast migration: replace `flash(m)` (~39) and the `npToast` inline render (~981) + `.np-toast` style (~1161) with `import { toast } from '$lib/stores/toast.svelte'`; change `flash(...)` call sites to `toast.show(...)` (the like toast ~47 etc.). Remove the local npToast state and `.np-toast` CSS. (2) aria-pressed: add `aria-pressed={currentLiked}` to the like transport button (~873) and `aria-pressed={player.repeatMode !== 'off'}` to the repeat toggle (~879); add `aria-pressed` to the shuffle/play-mode toggle if present (§7.1) reflecting its active state. (3) focusTrap: add `use:focusTrap` to the now-playing sheet/panel container when expanded so focus traps + restores; keep the existing collapse/options aria-labels. Do not alter the sheet's open/collapse state machine — focusTrap manages focus only. (4) haptic: call `tick()` inside `toggleCurrentLike` (the like commit, §3.3); do NOT add haptics to play/pause/seek/prev/next (silent per §3.3).</action>
  <verify>
    <automated>PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - NowPlaying.svelte imports `toast` from `$lib/stores/toast.svelte` and has no local `flash(`/`npToast`/`.np-toast` render
    - The like button has `aria-pressed={currentLiked}`; the repeat toggle has `aria-pressed={player.repeatMode !== 'off'}`
    - The now-playing sheet container carries `use:focusTrap`; the sheet open/collapse state machine is unchanged
    - `toggleCurrentLike` calls `tick()`; play/pause/seek/prev/next do NOT call tick()
    - `pnpm check` exits 0
  </acceptance_criteria>
  <done>NowPlaying uses the global toast, aria-pressed on like/repeat (and shuffle if present), traps focus on the sheet, haptic on like only.</done>
</task>

<task type="auto">
  <name>Task 3: SleepTimerSheet focusTrap (UX-06 §7.3)</name>
  <files>src/lib/components/SleepTimerSheet.svelte</files>
  <read_first>
    - src/lib/components/SleepTimerSheet.svelte (the sheet container / open guard; the existing overlay pattern)
    - src/lib/actions/focusTrap.ts
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md §7.3
  </read_first>
  <action>Add `use:focusTrap` to the SleepTimerSheet's open sheet container so focus traps while open and restores to the trigger on close. Manage focus ONLY — do not touch the sheet's open-only `$effect` overlay invariant or its `sleepTimer.sheetOpen` state. Confirm the scrim/close control retains its `aria-label`. If any icon-only control in the sheet lacks an `aria-label`, add one (audit-and-fill).</action>
  <verify>
    <automated>PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SleepTimerSheet.svelte carries `use:focusTrap` on the open sheet container
    - The sheet's open-only $effect / sleepTimer.sheetOpen state is unchanged
    - Any icon-only control in the sheet has an aria-label
    - `pnpm check` exits 0
  </acceptance_criteria>
  <done>SleepTimerSheet traps + restores focus; overlay invariant untouched; icon buttons labelled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard focus → trapped sheet | focus management could strand keyboard users if mis-wired |
| toast text → DOM | migrated toast strings rendered as text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-20 | Denial of Service (focus stranding) | focusTrap on sheets | mitigate | focusTrap restores focus to the trigger on destroy and never owns open/close; the existing scrim/Escape dismissal paths remain. Acceptance: focus restores on close; overlay $effect unchanged in all three files. |
| T-23-21 | Tampering (XSS) | migrated toast render | mitigate | All toast text routes through ToastHost (text-only `{toast.msg}`, no {@html}); the local raw renders are removed. Acceptance: NowPlaying/TrackMenu contain no inline `.toast`/`.np-toast` raw HTML render. |
</threat_model>

<verification>
- `pnpm check` exits 0; track-menu-gate.test.ts still passes.
- NowPlaying/TrackMenu use the global toast (no local copies); aria-pressed on like/repeat; aria-busy on in-flight actions.
- TrackMenu / NowPlaying sheet / SleepTimerSheet trap + restore focus; overlay invariants unchanged.
- Haptic fires on like/addQueue commits; silent on play/pause/seek/nav.
</verification>

<success_criteria>
- All three local toast copies (home in Plan 04; NowPlaying + TrackMenu here) now use the global store.
- aria-pressed on every transport/menu toggle; focus-trap on every sheet/menu with restore.
- Commit-tier haptics complete the §3.3 event set.
</success_criteria>

<output>
Create `.planning/phases/23-ux-audit-homepage-artist-polish/23-08-SUMMARY.md` when done.
</output>
