---
phase: 17-up-next-sourcing-settings-plumbing
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/lib/services/color.ts
  - src/lib/services/color.test.ts
  - src/lib/stores/settings.svelte.ts
  - src/lib/stores/settings.svelte.test.ts
  - src/routes/(app)/settings/appearance/+page.svelte
autonomous: true
requirements: [UX-03, UX-07]

must_haves:
  truths:
    - "D-11: The text-size sliders span 50%–200% (all five: title, artist, lyrics, NP-title, NP-artist) — the clamp only widens, so previously-persisted 70–160 values stay valid"
    - "D-12: Each slider's demo text reads 'example {name}' sourced from the current/last-played track — title sliders show the song name, artist sliders the artist name; static example fallback when nothing has played"
    - "D-13: The accent-color setting visibly recolors progress bars/active tabs/chips/buttons app-wide including hover surfaces — applyTheme derives and sets --color-primary-hover automatically from the chosen accent (the dead-wiring root cause is fixed; no second picker)"
  artifacts:
    - path: "src/lib/services/color.ts"
      provides: "Pure darken(hex, amount) helper (zero-dep) for accent hover derivation"
      contains: "export function darken"
    - path: "src/lib/services/color.test.ts"
      provides: "Unit tests: darken correctness + malformed-input passthrough"
      contains: "darken"
    - path: "src/lib/stores/settings.svelte.ts"
      provides: "FONT_SCALE_MIN=50 / FONT_SCALE_MAX=200; applyTheme sets --color-primary-hover via darken(accent)"
      contains: "color-primary-hover"
    - path: "src/routes/(app)/settings/appearance/+page.svelte"
      provides: "Dynamic demo text from player.current (read in page, settings stays leaf); widened slider range"
      contains: "demoTitle"
  key_links:
    - from: "src/lib/stores/settings.svelte.ts applyTheme"
      to: "--color-primary-hover CSS var"
      via: "setProperty('--color-primary-hover', darken(this.accent, 0.12))"
      pattern: "color-primary-hover"
    - from: "src/routes/(app)/settings/appearance/+page.svelte demo text"
      to: "player.current"
      via: "$derived(player.current?.title ?? fallback) read in page (not in settings store)"
      pattern: "player.current"
    - from: "src/lib/stores/settings.svelte.ts applyTheme"
      to: "src/lib/services/color.ts darken"
      via: "import darken (pure util, settings stays leaf)"
      pattern: "import \\{ darken \\}"
---

<objective>
Land the milestone's appearance/settings changes: widen the text-size range to 50–200% for all five sliders, drive the demo text from the actual current/last-played track, and fix the dead accent-hover wiring by deriving `--color-primary-hover` from the chosen accent in `applyTheme()`.

Purpose: UX-03 (text-size 50–200% + live demo text) and UX-07 (accent visibly applies, hover-wiring fixed). The accent root cause is confirmed: `applyTheme()` sets `--color-primary` but never `--color-primary-hover` (pinned at `#6a48f0` in app.css:10), so hover surfaces stay the wrong color.
Output: New pure `color.ts` (`darken`) + test, widened FONT_SCALE consts + accent-hover derivation in settings, dynamic demo text on the appearance page, extended settings test.

Depends on Plan 01: shares `settings.svelte.ts` and `settings.svelte.test.ts` (Plan 01 added upnext fields + effectiveUpnextMode there); this plan edits the FONT_SCALE consts and applyTheme in the same file. The `settings.demoPrefix` i18n key is created by Plan 01 Task 4 — this plan only CONSUMES it via `t(...)` and does NOT edit any locale file.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-up-next-sourcing-settings-plumbing/17-CONTEXT.md
@.planning/phases/17-up-next-sourcing-settings-plumbing/17-RESEARCH.md
@.planning/phases/17-up-next-sourcing-settings-plumbing/17-PATTERNS.md
@.planning/phases/17-up-next-sourcing-settings-plumbing/17-01-SUMMARY.md

<phase_invariants>
- Settings is a LEAF store: it must NOT import the player. The demo-text current-track read happens in the appearance PAGE (which already imports both), not in the settings store (Pitfall 6 / SSR-leak rule).
- `color.ts` is a PURE util — no `browser` reads, no DOM, no store imports — so it is trivially testable and safe to import from settings.
- FONT_SCALE widening only widens the clamp; persisted 70–160 values stay valid (D-11). `clampInt` already handles this.
- Zero new dependencies — hover derivation is inline pure math, never a color library (Anti-Pattern in research).
- The `settings.demoPrefix` i18n key is owned/created by Plan 01 Task 4. This plan must NOT edit any locale file — only reference the key via `t('settings.demoPrefix', { name })`.
- Marquee rule: any long demo/preview text uses `use:marquee` + marquee-inner, never static ellipsis (only if the existing demo span lacks it — match existing `.prev` styling).
</phase_invariants>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure darken(hex, amount) helper + unit test</name>
  <files>src/lib/services/color.ts, src/lib/services/color.test.ts</files>
  <read_first>
    - src/lib/i18n/detect.ts (a pure, SSR-safe, unit-tested helper — the style/shape to mirror: no side effects, no DOM)
    - src/lib/stores/settings.svelte.ts (clampInt lines 60-64 — the existing clamp idiom)
    - .planning/phases/17-up-next-sourcing-settings-plumbing/17-PATTERNS.md (section "src/lib/services/color.ts" — the 6-line clamp-and-darken pattern with the exact function body)
  </read_first>
  <behavior>
    - Test: darken('#7c5cff', 0.12) returns a valid #rrggbb that is darker than the input on each channel (approximately #6d51e0-range; assert each channel decreased and stays within 0..255).
    - Test: darken('#ffffff', 0.5) returns '#808080' (or within rounding) — channels halved.
    - Test: darken('#000000', 0.5) returns '#000000' (clamped at 0).
    - Test: darken accepts both '#rrggbb' and 'rrggbb' (leading # optional).
    - Test: malformed input (e.g. 'red', '#fff', '') returns the input unchanged (never throws).
    - Test: amount 0 returns the same color; amount 1 returns '#000000'.
  </behavior>
  <action>
    Create `color.ts` exporting a pure `darken(hex: string, amount: number): string`: regex-match `/^#?([0-9a-f]{6})$/i` on the trimmed input; on no match return the input unchanged (never throws). Parse to int, scale each channel by `(1 - amount)` clamped to 0..255 and rounded, reassemble as `'#' + [r,g,b].map(c => c.toString(16).padStart(2,'0')).join('')`. No `browser` reads, no DOM, no imports from stores — keep it pure so settings can import it without breaking leaf-store discipline.
    Create `color.test.ts` covering the 6 behavior cases. RED first, then implement to GREEN.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/services/color.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function darken" src/lib/services/color.ts` matches.
    - `grep -v '^//' src/lib/services/color.ts | grep -cE "browser|document|window|import.*store"` returns 0 (pure, no DOM/store deps).
    - `pnpm vitest run src/lib/services/color.test.ts` passes all 6 cases including malformed-input passthrough.
  </acceptance_criteria>
  <done>color.ts exports a pure darken(hex, amount) that darkens valid hex per-channel (clamped) and passes malformed input through unchanged; unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen FONT_SCALE + accent-hover derivation in applyTheme + settings test</name>
  <files>src/lib/stores/settings.svelte.ts, src/lib/stores/settings.svelte.test.ts</files>
  <read_first>
    - src/lib/stores/settings.svelte.ts (FONT_SCALE_MIN/MAX consts lines 52-53, clampInt usages lines 208-212, applyTheme lines 312-330 esp. the `--color-primary` set at line 315; Plan 01's upnext fields are also in this file now)
    - src/lib/services/color.ts (the darken helper from Task 1)
    - src/lib/stores/settings.svelte.test.ts (existing tests + Plan 01's effectiveUpnextMode additions — extend, do not replace)
    - app.css (lines 9-10: `--color-primary: #7c5cff`, `--color-primary-hover: #6a48f0` — the static hover var being replaced at runtime)
    - .planning/phases/17-up-next-sourcing-settings-plumbing/17-PATTERNS.md (section "src/lib/stores/settings.svelte.ts" edits (1) and (3))
  </read_first>
  <behavior>
    - Test: FONT_SCALE_MIN === 50 and FONT_SCALE_MAX === 200.
    - Test: a persisted fontScale value of 160 (previously valid) still loads unchanged through clampInt (within new 50..200 bounds).
    - Test: a persisted value of 250 clamps to 200; a value of 30 clamps to 50.
    - Test (applyTheme hover): after setting accent and calling applyTheme in a browser-stubbed env, the documentElement style receives --color-primary = accent AND --color-primary-hover = darken(accent, 0.12). (If applyTheme is hard to invoke in jsdom, assert the derivation indirectly by asserting darken(accent,0.12) is the value passed; mirror how existing settings.svelte.test.ts handles applyTheme — extend that harness.)
  </behavior>
  <action>
    Change the two consts (lines 52-53): `FONT_SCALE_MIN = 50` (was 70), `FONT_SCALE_MAX = 200` (was 160). The clampInt calls (208-212) and the appearance sliders pick the new bounds up automatically; persisted 70–160 stay valid (only the clamp widened).
    Import `{ darken }` from `$lib/services/color` (pure util — settings stays leaf). In `applyTheme()` immediately after `r.style.setProperty('--color-primary', this.accent)` (line 315) add `r.style.setProperty('--color-primary-hover', darken(this.accent, 0.12))` — the ~12% darken matches today's `#7c5cff`→`#6a48f0` relationship (A3). This is the UX-07 root-cause fix (the hover var was never set at runtime).
    Extend settings.svelte.test.ts with the FONT_SCALE bounds/clamp cases and the applyTheme hover-derivation case. Keep Plan 01's effectiveUpnextMode tests intact.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/stores/settings.svelte.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "FONT_SCALE_MIN = 50" src/lib/stores/settings.svelte.ts` and `grep -n "FONT_SCALE_MAX = 200" src/lib/stores/settings.svelte.ts` both match.
    - `grep -n "color-primary-hover" src/lib/stores/settings.svelte.ts` matches inside applyTheme (the runtime hover set).
    - `grep -n "import { darken }" src/lib/stores/settings.svelte.ts` matches.
    - `grep -v '^//' src/lib/stores/settings.svelte.ts | grep -c "import.*player.svelte"` returns 0 (settings stays leaf).
    - `pnpm vitest run src/lib/stores/settings.svelte.test.ts` passes all new + existing cases.
  </acceptance_criteria>
  <done>FONT_SCALE bounds are 50/200 (persisted values stay valid), applyTheme derives and sets --color-primary-hover from the accent via darken, settings stays leaf; settings tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Dynamic demo text on the appearance page (consumes settings.demoPrefix)</name>
  <files>src/routes/(app)/settings/appearance/+page.svelte</files>
  <read_first>
    - src/routes/(app)/settings/appearance/+page.svelte (read the five `.ctl` slider blocks + their `.prev` demo spans lines 41-69 — the static `Stargazing`/`Myles Smith` literals at lines 44,50,56,62,68; the `min/max` slider bindings; the `.prev` span styling lines 101-102)
    - src/lib/stores/player.svelte.ts (the `current` field shape — title/artist — for the demo read; the page imports player directly)
    - src/lib/i18n/en.ts (confirm `settings.demoPrefix` exists — created by Plan 01 Task 4; this task only references it via t())
    - .planning/phases/17-up-next-sourcing-settings-plumbing/17-PATTERNS.md (section "settings/appearance/+page.svelte")
  </read_first>
  <action>
    NOTE: This task consumes the `settings.demoPrefix` key created in Plan 01 Task 4 — do NOT edit any locale file.
    In appearance/+page.svelte import `{ player }` from `$lib/stores/player.svelte` (the page may import both stores — settings stays leaf because this read is in the PAGE, Pitfall 6). Add `const demoTitle = $derived(player.current?.title ?? 'Stargazing')` and `const demoArtist = $derived(player.current?.artist ?? 'Myles Smith')` (static fallback when nothing has played). Replace the static demo literals: title-type sliders (title, NP-title) show `t('settings.demoPrefix', { name: demoTitle })`; artist-type sliders (artist, NP-artist) show `t('settings.demoPrefix', { name: demoArtist })`; the lyrics slider previews the lyrics-type text per D-12 (use the song title as the representative name, or keep an existing lyrics-appropriate literal as fallback). The slider `min/max` bindings already reference FONT_SCALE_MIN/MAX (now 50/200 from Task 2) — no per-slider numeric change needed. Keep the `.prev` span styling unchanged; if the demo span can overflow, ensure it uses the existing marquee/ellipsis treatment already on `.prev`.
  </action>
  <verify>
    <automated>pnpm svelte-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "demoTitle\|demoArtist" "src/routes/(app)/settings/appearance/+page.svelte"` shows the $derived reads from player.current.
    - `grep -n "settings.demoPrefix" "src/routes/(app)/settings/appearance/+page.svelte"` shows the interpolation used under the sliders.
    - `grep -n "import { player }" "src/routes/(app)/settings/appearance/+page.svelte"` confirms the page (not the store) reads player.
    - `pnpm svelte-check` exits clean (0 errors) — confirms the consumed demoPrefix key resolves.
  </acceptance_criteria>
  <done>Each appearance slider shows "example {name}" sourced from the current/last-played track (title vs artist per slider type) with a static fallback; the slider range is the widened 50–200%; svelte-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-picked accent hex → CSS var | Arbitrary hex string flows into darken() and a CSS custom property |
| localStorage → FONT_SCALE persisted values | User-controllable persisted numbers re-clamped on load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Tampering | darken() malformed/hostile input | mitigate | Strict `/^#?[0-9a-f]{6}$/i` match; non-match returns input unchanged (never throws, no injection — output is always a literal #rrggbb or the passthrough string set via the typed CSS API). |
| T-17-08 | Tampering | FONT_SCALE persisted value | mitigate | clampInt re-clamps every load to 50..200 with a sane default; out-of-range/NaN → default 100. |
| T-17-09 | Info Disclosure | settings→player import cycle (SSR leak) | mitigate | player.current is read in the appearance PAGE, never inside the settings store; settings stays leaf (asserted by grep gate). |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase (RESEARCH Package Legitimacy Audit: zero new deps). slopcheck N/A. |
</threat_model>

<verification>
- `pnpm vitest run src/lib/services/color.test.ts` green (darken correctness + malformed passthrough).
- `pnpm vitest run src/lib/stores/settings.svelte.test.ts` green (FONT_SCALE bounds/clamp + applyTheme hover derivation; Plan 01 cases intact).
- `pnpm svelte-check` clean (appearance page; consumed demoPrefix key resolves).
- `pnpm vitest run` full suite green at wave merge.
- Manual (per VALIDATION.md): change accent → progress bars/tabs/chips/buttons recolor including hover; drag each slider to 50%/200% → demo text shows the current song/artist name.
</verification>

<success_criteria>
- UX-03: all five sliders span 50–200% with demo text "example {name}" from the current/last-played track; persisted values stay valid.
- UX-07: accent visibly applies app-wide including hover (--color-primary-hover derived from accent); dead wiring fixed.
- No regression: full vitest suite green; svelte-check clean.
</success_criteria>

<output>
Create `.planning/phases/17-up-next-sourcing-settings-plumbing/17-03-SUMMARY.md` when done.
</output>
</output>
