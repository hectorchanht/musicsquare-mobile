---
phase: 20-now-playing-surface-gestures
plan: 04
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/lib/components/Nowbar.svelte
autonomous: false
requirements: [NP-05]
must_haves:
  truths:
    - "A horizontal swipe on the nowbar content past the proportional commit (or a 0.5px/ms flick) changes track"
    - "Drag left→right plays previous, right→left plays next (same direction as the cover)"
    - "A sub-slop tap on the nowbar still expands the now-playing surface (tap-to-expand preserved)"
    - "The .np-prog loader rail stays visually pinned while the .np-open content slides"
    - "On the first track with currentTime ≤ 3, a prev swipe rubber-bands and springs back with no track change"
    - "Decisions covered: D-06 (nowbar track & snap, no multi-cover carousel), D-07 (tap-to-expand preserved via sub-slop tap), D-08 (proportional 0.28xwidth commit + 0.5px/ms flick on the nowbar)"
  artifacts:
    - path: "src/lib/components/Nowbar.svelte"
      provides: "Slide-and-snap prev/next swipe on the .np-open content row via coverSwipe (NP-05), tap-to-expand preserved, loader rail kept pinned"
      contains: "coverSwipe"
  key_links:
    - from: "src/lib/components/Nowbar.svelte"
      to: "src/lib/actions/coverSwipe.ts"
      via: "use:coverSwipe on the .np-open content row"
      pattern: "use:coverSwipe"
    - from: "src/lib/components/Nowbar.svelte"
      to: "player.prev() / player.next()"
      via: "onprev → player.prev(), onnext → player.next()"
      pattern: "player\\.(prev|next)\\(\\)"
---

<objective>
NP-05 — apply the same horizontal swipe to the nowbar mini-player, but lighter than the cover: the `.np-open` content (thumb art + text) slides 1:1 with the finger and springs back, or slides off and swaps on commit (D-06 — no multi-cover carousel, the art is too small to read a peek). Reuse the `coverSwipe` action from 20-01 with the same `0.28 × contentWidth` proportional commit and `0.5px/ms` flick (D-08), the same direction mapping (left→right = prev, right→left = next), and the same boundary rubber-band (only prev on index 0). Tap-to-expand (sub-slop tap → `player.expand()`) MUST keep working (D-07), and the `.np-prog` loader rail at the top of the bar must NOT slide with the content (it sits outside `.np-open`).

Purpose: deliver the fifth Phase-20 criterion (NP-05) in a different file from the cover work so it parallelizes, reusing the same node-tested action and the same Pitfall-7 invariant.
Output: modified `src/lib/components/Nowbar.svelte` (coverSwipe wired to `.np-open`, slide motion, tap-expand preserved, loader rail pinned).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-now-playing-surface-gestures/20-CONTEXT.md
@.planning/phases/20-now-playing-surface-gestures/20-UI-SPEC.md
@.planning/phases/20-now-playing-surface-gestures/20-PATTERNS.md
@.planning/phases/20-now-playing-surface-gestures/20-01-SUMMARY.md

<interfaces>
<!-- The coverSwipe action from 20-01, and the existing nowbar surfaces this layers onto. -->
From src/lib/actions/coverSwipe.ts (created in 20-01):
```typescript
export interface CoverSwipeOpts {
  onprev: () => void;  onnext: () => void;
  ondrag?: (dx: number) => void;
  hasPrev?: boolean;  hasNext?: boolean;  enabled?: boolean;
}
export const coverSwipe: Action<HTMLElement, CoverSwipeOpts>;
```
Commit = `0.28 × measuredWidth` (measured from the attached node's getBoundingClientRect) OR `|velocity| > 0.5px/ms` with `|dx| > 8`; sets `touch-action: pan-y` on attach; never setPointerCapture on pointerdown; arms a one-shot click suppressor on a committed swipe; boundary clamp when `hasPrev:false`. Direction: drag right (`dx>0`) → onprev; drag left (`dx<0`) → onnext.

From src/lib/components/Nowbar.svelte (existing):
```svelte
<div class="nowbar" class:embed={variant === "embed"}>
  <div class="np-prog" class:indet={player.loading}> ... </div>   <!-- OUTSIDE .np-open: must NOT slide -->
  <button class="np-open" aria-label={t("nowbar.openNowPlaying")} disabled={resolving} onclick={handleOpen}>
    <span class="np-art" ...></span>
    <span class="np-meta"> ...title / artist... </span>
  </button>
  ... sleep-timer badge, play/pause button ...
</div>
```
`handleOpen()` (lines ~32-35): `if (onOpen) onOpen(); else player.expand();` — the sub-slop tap that MUST keep firing (D-07).
`const np = $derived(player.current ?? player.pendingTrack)`; `const resolving = $derived(!player.current && !!player.pendingTrack)`.

From src/lib/stores/player.svelte (already imported as `player`):
```typescript
player.current: Track | null;  player.queue: Track[];  player.prev(): void;  player.next(): void;
```
Neighbor / boundary lookup (same as the cover): `const i = player.queue.findIndex(t => t.uid === player.current?.uid)`; boundary is `i === 0` (prev on first track; player.prev() restarts when currentTime > 3 so the resist case is narrow).

From src/lib/components/NowPlaying.svelte: the embedded `<Nowbar variant="embed" onOpen={() => {...collapse...}} />` overrides `onOpen` — so when `onOpen` is provided the tap collapses the sheet rather than `player.expand()`. The swipe behaves identically in both variants (it always changes track); only the TAP differs by variant via the existing `handleOpen`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire coverSwipe slide-and-snap onto .np-open (NP-05)</name>
  <files>src/lib/components/Nowbar.svelte</files>
  <read_first>
    - src/lib/components/Nowbar.svelte (current state: the `.np-prog` rail ~38-48, the `.np-open` content button ~49-70, `handleOpen` ~32-35, `np`/`resolving` deriveds ~26-27, the `.nowbar`/`.np-open` CSS ~107-216)
    - src/lib/actions/coverSwipe.ts (the action from 20-01 — CoverSwipeOpts, ondrag, the optional opacity fade idiom it inherits from swipeRemove)
    - src/lib/actions/swipeRemove.ts (the lighter single-node slide + optional `FADE_DISTANCE = 120` opacity fade the nowbar may reuse)
    - src/lib/stores/player.svelte.ts lines 1386-1412 (prev/next), ~160/~99 (queue/current)
    - .planning/phases/20-now-playing-surface-gestures/20-UI-SPEC.md (§5 Nowbar slide-and-snap: content travels 1:1, optional fade across 120px, commit slides off over `0.28s cubic-bezier(.22,1,.36,1)` then fire prev/next + store-swap repaint, spring-back `0.28s` to translateX(0)/opacity 1, boundary = same rubber-band but lighter 0.28s, tap-to-expand preserved, loader rail must NOT slide; §3 proportional `0.28 × nowbarContentWidth` same fraction as cover)
    - .planning/phases/20-now-playing-surface-gestures/20-PATTERNS.md (Nowbar modify section: attach swipe to `.np-open` only; loader rail outside; commit motion; same no-capture + suppressor invariant)
  </read_first>
  <action>Attach `use:coverSwipe` to the `.np-open` content `<button>` ONLY (not the `.nowbar` shell and not the `.np-prog` rail — the rail must stay pinned while content slides, UI-SPEC §5 last row). Wire: `onprev: () => player.prev()`, `onnext: () => player.next()` (same direction mapping as the cover — left→right prev, right→left next), `hasPrev: player.queue.findIndex(t => t.uid === player.current?.uid) !== 0` (the prev-on-first-track boundary; derive reactively), `hasNext: true` (ensureAhead keeps a neighbor), and `enabled: !resolving` (don't fight the resolving spinner state). Use `ondrag` to drive a local `$state` `slideX` bound to the `.np-open` `transform: translateX(...)` so the content follows the finger 1:1 (the action sets the inline transform itself via the node — confirm whether the action drives `node.style.transform` directly as swipeRemove does, in which case `ondrag` is only needed if you want an additional fade; if the action drives the transform, you do NOT need a separate `slideX` binding — just let the action translate the node). The OPTIONAL opacity fade across `FADE_DISTANCE = 120` is inherited from the swipeRemove idiom — keep it subtle (D-06 is "track & snap-back," fade is secondary). On commit the action calls onprev/onnext → the store swaps `player.current` → the `np` derived repaints the content (no fly-in needed, UI-SPEC §5). The commit slide-off + spring-back use `0.28s cubic-bezier(.22,1,.36,1)` (the lighter surface uses 0.28s, NOT the 0.32s cover settle). Honor reduced motion (springs collapse to instant; track change still happens). Do NOT touch `handleOpen` — the existing `onclick={handleOpen}` is the tap-to-expand path (D-07) that must keep firing; `coverSwipe`'s no-capture-on-pointerdown + one-shot suppressor guarantee a sub-slop tap reaches it and a committed swipe does not replay it. Keep `disabled={resolving}` and the `aria-label` on `.np-open` intact.</action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && grep -A6 "class=\"np-open\"" src/lib/components/Nowbar.svelte | grep -q "use:coverSwipe" && grep -qE "onprev:.*player\.prev\(\)" src/lib/components/Nowbar.svelte && grep -qE "onnext:.*player\.next\(\)" src/lib/components/Nowbar.svelte && grep -q "onclick={handleOpen}" src/lib/components/Nowbar.svelte && pnpm check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `use:coverSwipe` is attached to the `.np-open` button (NOT the `.nowbar` shell, NOT `.np-prog`), wired `onprev: () => player.prev()` / `onnext: () => player.next()` (source assertion via grep). No new advance function introduced.
    - The `.np-prog` loader rail is OUTSIDE `.np-open` and carries no swipe (behavior assertion: the rail stays pinned while content slides; source assertion: `use:coverSwipe` does not appear on `.np-prog`).
    - `hasPrev` is false exactly when the current track is at queue index 0 (the prev boundary, D-02); `hasNext` is true; `enabled` is `!resolving`.
    - Tap-to-expand preserved: `onclick={handleOpen}` is unchanged on `.np-open`, and the swipe relies on `coverSwipe` never `setPointerCapture`-ing on `pointerdown` + arming a one-shot click suppressor on a committed swipe (LOAD-BEARING INVARIANT) so a sub-slop tap reaches `handleOpen` and a committed swipe does not. (Verified by 20-01's tests + this wiring not overriding capture.)
    - Commit slide-off + spring-back use `0.28s cubic-bezier(.22,1,.36,1)` (the lighter surface; NOT the 0.32s cover settle) — the action's pinned values are reused unchanged.
    - `disabled={resolving}` and the `aria-label` on `.np-open` remain.
    - `pnpm check` exits 0; zero new i18n keys.
  </acceptance_criteria>
  <done>The nowbar content slides with the finger and changes track on commit/flick, rubber-bands at the prev boundary, keeps tap-to-expand, and keeps the loader rail pinned; pnpm check is 0/0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Device-verify the nowbar swipe vs tap-to-expand</name>
  <files>src/lib/components/Nowbar.svelte</files>
  <action>Run the app on a real phone (or device emulation in DevTools with touch) against the docked nowbar, and walk the how-to-verify steps to confirm the swipe-changes-track vs sub-slop-tap-expands arbitration, the pinned loader rail, and the prev-boundary rubber-band — see how-to-verify for the exact gated steps. No code change in this task; it is the human gate after the automated suite + pnpm check pass.</action>
  <what-built>The docked nowbar mini-player's content row (`.np-open`) now changes track on a horizontal swipe (slide-and-snap, lighter than the cover carousel — no peek), with the same proportional commit + flick + prev-boundary rubber-band, while the sub-slop tap still expands now-playing. The `.np-prog` loader rail stays pinned. Wired through the node-tested `coverSwipe`; `pnpm check` passes. Tap-vs-swipe arbitration and slide feel are device-only.</what-built>
  <how-to-verify>
    On a phone (or device emulation), with a track playing and a non-empty up-next queue, on the DOCKED nowbar at the bottom:
    1. Swipe the nowbar content RIGHT→LEFT past ~28% of its width → next track; LEFT→RIGHT → previous. A quick flick also commits. The thumb art + text slide with the finger and snap.
    2. A short nudge (under ~28%, no flick) springs the content back with no track change.
    3. Single-TAP the nowbar (not a swipe) → now-playing expands (tap-to-expand still works); a committed swipe never also expands.
    4. While a track is loading, the running-line loader rail at the top of the bar stays pinned and does NOT slide with the swiped content.
    5. On the very first track (currentTime ≤ 3), swipe LEFT→RIGHT (prev) → content rubber-bands and springs back, no track change (even on a hard flick).
    6. Reduce-motion on → track change still happens without the slide animation.
    7. (If easy) In the now-playing FULL state, the embedded nowbar tap still collapses the sheet (its `onOpen` override) and the swipe still changes track.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any misbehavior (tap eaten by swipe, loader rail sliding, boundary committing a track, expand firing after a swipe).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user touch → nowbar gesture → player.prev()/next() | Pointer coordinates from the user's own touch drive the EXISTING prev/next transport. No new network call, no new input, no new data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Tampering | nowbar gesture → player.prev/next | accept | The swipe only invokes the existing `player.prev()`/`player.next()`; no new advance logic, no new trust decision. A user manipulating their own pointer events only changes their own playback. |
| T-20-08 | Denial of Service | tap-vs-swipe arbitration / leaked suppressor | mitigate | coverSwipe never captures on pointerdown and arms a one-shot capture-phase suppressor only on a committed swipe; `destroy()` drops it — preventing a leaked handler from eating future taps and keeping tap-to-expand reliable (the swipeRemove.ts WR-01 idiom). |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — zero new dependencies (consumes the in-repo `coverSwipe` action + `player` store). slopcheck N/A. |

Client-side, browser-only gesture layering on the mini-player. No new network calls, no new data flows, no auth, no new XSS sink (the nowbar already renders the same `np.cover` background and title/artist text; this plan adds only inline `transform`/`opacity` motion). Threat surface is unchanged from the existing nowbar.
</threat_model>

<verification>
- `pnpm check` exits 0; `pnpm test --run src/lib/actions/coverSwipe.test.ts` green (the action this consumes).
- `grep` confirms: `use:coverSwipe` on `.np-open` wired to `player.prev()`/`player.next()`; `onclick={handleOpen}` unchanged; `use:coverSwipe` absent from `.np-prog` (loader rail pinned).
- Human checkpoint (Task 2) confirms swipe-vs-tap arbitration, pinned loader rail, and prev-boundary rubber-band on a device.
</verification>

<success_criteria>
- A horizontal swipe on the nowbar changes track (NP-05), with tap-to-expand preserved and the loader rail pinned.
- Same proportional commit + flick + prev-boundary rubber-band as the cover, lighter slide-and-snap (no peek).
- pnpm check 0/0; zero new i18n keys; the Pitfall-7 invariant holds via the shared coverSwipe action.
</success_criteria>

<output>
Create `.planning/phases/20-now-playing-surface-gestures/20-04-SUMMARY.md` when done.
</output>
