---
quick_id: 260606-6p7
type: execute
autonomous: true
files_modified:
  - src/lib/i18n/index.ts
  - src/lib/i18n/en.ts
  - src/lib/i18n/zh-Hant.ts
  - src/lib/i18n/zh-Hans.ts
  - src/lib/i18n/i18n.test.ts
  - src/lib/stores/settings.svelte.ts
  - src/routes/(app)/settings/+page.svelte
  - src/routes/(app)/+layout.svelte
  - src/routes/(app)/+page.svelte
  - src/routes/(app)/search/+page.svelte
  - src/routes/(app)/library/+page.svelte
  - src/lib/components/NowPlaying.svelte
  - src/lib/components/TrackMenu.svelte
  - src/routes/(app)/artist/[name]/+page.svelte
  - src/routes/(app)/album/[name]/+page.svelte
must_haves:
  truths:
    - "Settings page shows an App language selector (English / 繁體中文 / 简体中文)"
    - "Selecting a language re-renders all UI chrome reactively, with no page reload"
    - "The chosen App language persists across reloads (stored in openmusic:settings:v1)"
    - "On a first visit (no saved appLang) the language auto-detects from navigator.language; afterwards the saved choice always wins"
    - "English renders the exact current on-screen text (no visual change when en is active)"
    - "nameLang and lyricsLang are unchanged by switching App language (content-translation settings stay independent)"
    - "All visible chrome — body text, button labels, toasts, menu labels, empty states, placeholders, aria-labels — is sourced from t() across all 12 in-scope files"
  artifacts:
    - path: "src/lib/i18n/index.ts"
      provides: "AppLang type, t(key, params?) reactive helper, dict registry, lookupKey() pure resolver with en->raw-key fallback, detectAppLang()"
    - path: "src/lib/i18n/en.ts"
      provides: "English dictionary (source/reference locale = current verbatim UI text)"
    - path: "src/lib/i18n/zh-Hant.ts"
      provides: "Traditional Chinese dictionary, identical key set to en"
    - path: "src/lib/i18n/zh-Hans.ts"
      provides: "Simplified Chinese dictionary, identical key set to en"
    - path: "src/lib/i18n/i18n.test.ts"
      provides: "Unit tests: all three locales share identical keys, param interpolation, missing-key fallback to en then raw key"
    - path: "src/lib/stores/settings.svelte.ts"
      provides: "appLang $state field + load/save/first-visit detect, mirroring lyricsLang"
  key_links:
    - from: "src/lib/i18n/index.ts t()"
      to: "settings.appLang"
      via: "reactive $state read on each call (same model as names.dn + rev)"
      pattern: "settings\\.appLang"
    - from: "src/routes/(app)/settings/+page.svelte"
      to: "settings.appLang"
      via: "selector chips that set settings.appLang then settings.save()"
      pattern: "settings\\.appLang ="
    - from: "all 12 in-scope .svelte files"
      to: "src/lib/i18n"
      via: "import { t } and t('key') in templates"
      pattern: "t\\('"
---

<objective>
Add an **App language** selector to the settings page and internationalize ALL static UI-chrome strings across the app (~74 strings, 12 `.svelte` files). Language switching is reactive (no reload) and persisted, using a custom lightweight runes-based `t()` — NO new dependency.

Purpose: Let users read the interface in English, Traditional Chinese, or Simplified Chinese, independent of the existing content-translation settings (lyrics/name language).
Output: New `src/lib/i18n/` module (registry + 3 locale dicts + unit test), an `appLang` field on the settings store, a settings-page selector, and every in-scope `.svelte` file rewired to `t(...)`.
</objective>

<context>
@.planning/quick/260606-6p7-add-app-language-selector-to-settings-i1/260606-6p7-CONTEXT.md
@./CLAUDE.md

<interfaces>
Settings store integration point — add appLang the SAME way as lyricsLang.
From src/lib/stores/settings.svelte.ts:
- `const KEY = 'openmusic:settings:v1';`
- `class Settings { lyricsLang = $state<LyricsLang>('off'); nameLang = $state<LyricsLang>('off'); ... private loaded = false; load() {...} save() {...} }`
- `load()`: early-returns if `this.loaded || !browser`; parses localStorage KEY; assigns each field with `?? default`; calls `applyTheme()`.
- `save()`: early-returns if `!browser`; writes JSON of ALL fields to KEY; calls `applyTheme()`.
- `export const settings = new Settings();`

Reactivity reference — rune read on every call so callers re-render when state changes.
From src/lib/stores/names.svelte.ts `dn(text)`: reads `settings.nameLang` ($state) and `void this.rev;` at the top, then returns. Any template that called it re-evaluates when the rune changes.

Vitest server project (vite.config.ts): node env, includes `src/**/*.{test,spec}.ts`, EXCLUDES `*.svelte.{test,spec}.ts`, and `requireAssertions: true`.
=> the i18n unit test MUST be a plain `.test.ts` (node project) and MUST NOT import anything that pulls in `$state` (runes do not compile under the node test project). Therefore keep the PURE lookup logic (`lookupKey`, `interpolate`, `detectAppLang`, the dicts) importable WITHOUT touching settings/$state; the reactive `t()` wrapper is the only thing that reads `settings.appLang`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: i18n infrastructure — registry, t(), 3 locale dicts, unit test</name>
  <files>src/lib/i18n/index.ts, src/lib/i18n/en.ts, src/lib/i18n/zh-Hant.ts, src/lib/i18n/zh-Hans.ts, src/lib/i18n/i18n.test.ts</files>
  <behavior>
    - lookupKey('nav.home', 'en') returns the English value.
    - lookupKey on a key missing in zh-Hant falls back to the en value (never blank).
    - lookupKey on a key missing in ALL dicts returns the raw key string itself.
    - interpolate('{count} tracks', { count: 3 }) === '3 tracks'; unknown tokens are left intact.
    - All three dictionaries (en, zh-Hant, zh-Hans) expose IDENTICAL key sets (test compares sorted key lists for equality across all three).
    - detectAppLang('zh-TW') === 'zh-Hant'; detectAppLang('zh-CN') === 'zh-Hans'; detectAppLang('zh') === 'zh-Hans'; detectAppLang('en-US') === 'en'; detectAppLang(undefined) === 'en'.
  </behavior>
  <action>
    Create `src/lib/i18n/`. Per CONTEXT.md (LOCKED): custom runes-based, NO new dependency.

    `en.ts`, `zh-Hant.ts`, `zh-Hans.ts`: each `export default` a record under DOT-NAMESPACED keys. en values are the CURRENT verbatim on-screen text (en must be a visual no-op). All three files MUST share an identical key set. Suggested namespaces (final keys derived from the strings extracted in Tasks 4-6): `nav.*` (Home/Search/Library), `common.*` (Back/Go/retry/loading), `settings.*` (every settings section heading, chip/segment label, muted note, toggle label, About/Data text, confirm/flash messages), `home.*`, `search.*`, `library.*`, `nowplaying.*` (Now Playing, Up Next, Lyrics, Related, empty states, aria-labels), `menu.*` (Play next, Add to queue, Download, Like/Liked, etc.), `toast.*` (Playing next, Added to queue, Preparing download, Downloaded · added to Library, Share link copied, Added to playlist, Playlist created), `artist.*`, `album.*`. Include keys for ALL aria-labels and input placeholders too.

    `index.ts`:
      - `export type AppLang = 'en' | 'zh-Hant' | 'zh-Hans';`
      - A `dicts` registry mapping each AppLang to its dictionary, plus a typed `TranslationKey` (keyof en) so missing keys are a compile error at `t()` call sites.
      - `interpolate(str, params?)`: replace `{token}` occurrences with `String(params[token])`; leave unknown tokens intact. Pure, exported.
      - `lookupKey(key, lang)`: pure resolver — try `dicts[lang][key]`; else `dicts.en[key]`; else return `key` (the raw key, NEVER blank). Exported (this is what the unit test drives — no $state involved).
      - `detectAppLang(navLang?: string): AppLang`: `zh-TW`/`zh-Hant` -> `zh-Hant`; `zh-CN`/`zh-Hans`/`zh` -> `zh-Hans`; else `en`. Pure, exported, takes the language string as an argument (no `navigator` access here — keep it SSR-safe and unit-testable).
      - `t(key, params?)`: reads `settings.appLang` (the reactive $state rune — import settings from `$lib/stores/settings.svelte`) on EVERY call so any template/`$derived` re-renders on language change (same model as names.dn). Returns `interpolate(lookupKey(key, settings.appLang), params)`.

    `i18n.test.ts`: plain Vitest `.test.ts` (NOT `.svelte.test.ts`) so it runs in the node project. Import ONLY the pure exports (`lookupKey`, `interpolate`, `detectAppLang`, the dicts) — do NOT import `t` (it pulls in `$state` via settings). Cover every bullet in <behavior>. Each `it` must assert (requireAssertions is on).

    Do NOT add svelte-i18n / Paraglide / any package. Do NOT modify package.json.
  </action>
  <verify>
    <automated>pnpm test --run src/lib/i18n/i18n.test.ts</automated>
  </verify>
  <done>i18n module exists; all three locales share identical key sets; param interpolation, en-then-raw-key fallback, and detectAppLang all proven by the unit test (green). No new dependency added.</done>
</task>

<task type="auto">
  <name>Task 2: settings store — appLang field + load/save + first-visit detect</name>
  <files>src/lib/stores/settings.svelte.ts</files>
  <action>
    Add `appLang = $state<AppLang>('en')` to the `Settings` class, importing `AppLang` and `detectAppLang` from `$lib/i18n`. Mirror `lyricsLang` exactly:
      - In `load()`: read `v.appLang` from the parsed JSON. If a saved value exists, use it. If NO `appLang` key was saved (first visit), set `this.appLang = detectAppLang(navigator.language)` (load() already early-returns when `!browser`, so `navigator` is safe here). Do NOT change how lyricsLang/nameLang are loaded.
      - In `save()`: include `appLang: this.appLang` in the serialized object alongside the existing fields.
    Per CONTEXT.md (LOCKED): default `en`; first-visit-only auto-detect; persist in the SAME key `openmusic:settings:v1`. Keep `nameLang`/`lyricsLang` INDEPENDENT — do not touch them.
    Circular-import note: i18n `index.ts` imports `settings`, and settings imports `detectAppLang` from i18n. `detectAppLang` is a pure leaf (no $state), so this resolves fine at value level. If `pnpm check` flags a cycle, move `AppLang`/`detectAppLang` into a tiny `src/lib/i18n/lang.ts` leaf and re-export from index; prefer keeping it in index.ts unless check complains.
  </action>
  <verify>
    <automated>pnpm check 2>&1 | grep -v '^#' | grep -c 'error' | grep -qx 0; pnpm test --run 2>&1 | tail -3</automated>
  </verify>
  <done>settings.appLang exists, defaults to 'en', auto-detects from navigator.language only on first visit, persists to openmusic:settings:v1 in both load() and save(); svelte-check reports 0 errors; existing + i18n tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Settings page — App language selector + extract settings-page strings</name>
  <files>src/routes/(app)/settings/+page.svelte</files>
  <action>
    Add a new App-language section that mirrors the existing chip selectors (e.g. the `Default music source` chips). Import `t`, `type AppLang` from `$lib/i18n`. Add an `appLangs: { v: AppLang; label: string }[]` array: `{ v: 'en', label: 'English' }`, `{ v: 'zh-Hant', label: '繁體中文' }`, `{ v: 'zh-Hans', label: '简体中文' }` (these labels are language ENDONYMS — render them literally, NOT through t()). Add `function setAppLang(v: AppLang) { settings.appLang = v; settings.save(); }`. Render the section near the top (it controls the whole UI) with chips bound to `settings.appLang` and a `t('settings.appLanguage')` heading using the `Languages` (or another lucide) icon.

    Then replace EVERY hardcoded UI string on this page with `t('settings.*')` keys (add the matching keys to all three dicts from Task 1): page `<title>` ("Settings · openmusic"), `Back` aria-label, `Settings` h1, the section headings (`Lyrics translation`, `Translate song & artist names`, `Translate mode`, `Default song quality`, `Default music source`, `Accent color`, `Playback & motion`, `Data`, `About`), the segment/chip option labels that are ENGLISH WORDS (`Off`, `Show below`, `Replace`, `Auto`, `Lossless`, `Auto (all)`, `Auto-expand now-playing on play`, `Reduce motion`, `Clear cached top picks`, `Clear library`), the About line, the muted helper notes, the `{liked} liked · {playlists} playlists · {downloads} downloads` line via param interpolation, and the flash/confirm strings: `Cached top picks cleared.`, `Library cleared.`, `Clear all liked songs, playlists and downloads?`. Keep proper-noun source labels (`NetEase`, `QQ`, `Kuwo`, `JOOX`) and quality tokens (`320k`, `128k`) literal. Keep the language endonym labels (`繁體中文` etc.) literal.

    Do NOT couple appLang to lyricsLang/nameLang. Do NOT touch any file other than this one and the dict files.
  </action>
  <verify>
    <automated>grep -q "settings.appLang" "src/routes/(app)/settings/+page.svelte"; grep -q "t('settings." "src/routes/(app)/settings/+page.svelte"; pnpm check 2>&1 | grep -v '^#' | grep -c 'error' | grep -qx 0</automated>
  </verify>
  <done>Settings page shows an App-language chip selector wired to settings.appLang + save(); all settings-page chrome (incl. title, aria-label, headings, toggles, notes, flash/confirm) goes through t(); svelte-check 0 errors.</done>
</task>

<task type="auto">
  <name>Task 4: Extract chrome in layout, home, and search</name>
  <files>src/routes/(app)/+layout.svelte, src/routes/(app)/+page.svelte, src/routes/(app)/search/+page.svelte</files>
  <action>
    Import `t` from `$lib/i18n` in each file; replace every hardcoded visible string with `t('...')` and add the matching keys to all three dicts.

    `+layout.svelte`: the nav tab labels (`Home`, `Search`, `Library` -> `nav.home`/`nav.search`/`nav.library`); aria-labels `Open now playing`, `Play/pause`; the inline `loading…` text on the now-bar. The `tabs` array currently hardcodes label strings — store a translation KEY per tab (e.g. `labelKey`) and call `t(tab.labelKey)` in the template so switching language re-renders the nav reactively.

    `+page.svelte` (home): the `openmusic` brand wordmark stays literal (proper noun). Translate: `Settings` aria-label, the search-pill text — IMPORTANT: its English value MUST be the current "Search songs, artists across all sources" (an uncommitted user edit; do NOT revert it), `Top picks` heading, `Loading…`/`Randomize` button label, the `no results — sources may be unavailable` error and the generic error display, and `retry`.

    `search/+page.svelte`: `Search` h1; the input `placeholder` — its English value MUST be the current "Search songs, artists across all sources" (uncommitted user edit; do NOT revert); the `Go`/`…` submit label; `Some sources didn't respond — showing the rest.`; `Searching all sources…`; `No results.`. Keep `autocomplete`/`autocapitalize` attribute values literal.
  </action>
  <verify>
    <automated>grep -q "t('nav.home')" "src/routes/(app)/+layout.svelte"; grep -q "Search songs, artists across all sources" src/lib/i18n/en.ts; pnpm check 2>&1 | grep -v '^#' | grep -c 'error' | grep -qx 0</automated>
  </verify>
  <done>Nav labels, home, and search chrome all routed through t(); the search placeholder/pill English value is exactly "Search songs, artists across all sources" (user edit preserved); svelte-check 0 errors.</done>
</task>

<task type="auto">
  <name>Task 5: Extract chrome in NowPlaying and TrackMenu (heaviest — toasts, menu labels, aria)</name>
  <files>src/lib/components/NowPlaying.svelte, src/lib/components/TrackMenu.svelte</files>
  <action>
    Import `t` from `$lib/i18n` in both; replace every visible string with `t('...')` and add keys to all three dicts.

    `NowPlaying.svelte`: aria-labels `Collapse`, `Options`, `Album art — drag down to minimize`, `Seek`, `Shuffle`, `Previous`, `Play/pause`, `Next`, `Repeat`, `Lyrics`, `Reorder track`, and the dynamic `Collapse panel`/`Expand panel` grip label; the `Now Playing` context label; subnav tabs `Up Next`, `Lyrics`, `Related`; empty/status states `No queue yet.`, `translating…`, `No lyrics for this track.`, `Loading related…`. Keep `--:--`, time displays, and `names.dn(...)` content calls untouched (content translation stays independent). The now-bar `loading…` literal lives in +layout.svelte (handled in Task 4) — do not duplicate it here.

    `TrackMenu.svelte`: menu-head is `names.dn` content (leave it). Menu item labels: `Play next`, `Add to queue`, `Download`, the dynamic `Liked`/`Like`, `Add to playlist`, `Go to album`, `Go to artist`, `Share`, `Detail`, the picker heading `Add to playlist`, `New playlist…`, and the detail modal labels (`Track detail`, `Title`, `Artist`, `Album`, `Quality`, `Source`, `UID`, `Audio URL`, `(not resolved)`, `unknown`; the `—` placeholder may stay literal). aria-labels: `Close menu`, `Close`. Toasts (all -> `toast.*`): `Playing next`, `Added to queue`, `Preparing download…`, `No audio available`, `Downloaded · added to Library`, `Opened audio · added to Library`, `Share link copied`, `Added to playlist`, `Playlist created`. The `prompt('New playlist name')` label -> `t('menu.newPlaylistPrompt')`. Keep `detailTrack.source`/`.uid`/`.audioUrl` VALUES literal (data, not chrome).
  </action>
  <verify>
    <automated>grep -q "t('toast.playingNext')" src/lib/components/TrackMenu.svelte; grep -q "t('nowplaying." src/lib/components/NowPlaying.svelte; pnpm check 2>&1 | grep -v '^#' | grep -c 'error' | grep -qx 0</automated>
  </verify>
  <done>All NowPlaying + TrackMenu chrome (labels, toasts, empty states, aria-labels, prompt) routed through t(); content (names.dn / track data) left untouched; svelte-check 0 errors.</done>
</task>

<task type="auto">
  <name>Task 6: Extract chrome in library, artist, and album pages + final whole-app verify</name>
  <files>src/routes/(app)/library/+page.svelte, src/routes/(app)/artist/[name]/+page.svelte, src/routes/(app)/album/[name]/+page.svelte</files>
  <action>
    Import `t` from `$lib/i18n` in each; replace every visible string with `t('...')` and add keys to all three dicts.

    `library/+page.svelte`: `<title>` ("Library · openmusic"), `Library` h1, tab labels `Liked`/`Playlists`/`Downloads`, empty states (`No liked songs yet. Tap ♥ in the player.`, `No playlists. Create one from a song's ⋮ menu.`, `No downloads. Use Download in a song's ⋮ menu.`, `Empty playlist.`), aria-labels `Delete playlist`/`Remove`, and the downloads `note` paragraph. Keep `pl.name` (user data) literal; counts via interpolation if any count text is added.

    `artist/[name]/+page.svelte`: `‹ Back` link text, `Derived from cross-source search · {count} tracks` (param), `Loading {name}…` (the word "Loading" + ellipsis is chrome; pass `name` as a content param via names.dn or raw), `Albums` heading, the `{n} track`/`{n} tracks` pluralization (use a count param; for en pick singular/plural, for zh use the single form), `Hit songs` heading, `No songs found for {name}.` (param). Keep `names.dn(name)` content untouched.

    `album/[name]/+page.svelte`: `<title>`, `Back` aria-label, `Album · {count} tracks (derived from search)` (param), `Loading album…`, `No tracks found for "{name}".` (param). Keep `names.dn(name)` content untouched.

    FINAL whole-app pass: grep the 12 in-scope files (EXCLUDING `src/routes/spike/+page.svelte`, which is OUT OF SCOPE) for any remaining hardcoded user-facing chrome you missed; fix before finishing. Confirm en values are verbatim the originals (en = visual no-op).
  </action>
  <verify>
    <automated>grep -q "t('library." "src/routes/(app)/library/+page.svelte"; grep -q "t('artist." "src/routes/(app)/artist/[name]/+page.svelte"; grep -q "t('album." "src/routes/(app)/album/[name]/+page.svelte"; pnpm check 2>&1 | grep -v '^#' | grep -c 'error' | grep -qx 0; pnpm test --run 2>&1 | tail -3</automated>
  </verify>
  <done>Library/artist/album chrome routed through t(); final grep sweep of the 12 in-scope files finds no leftover hardcoded chrome (spike page excluded); svelte-check 0 errors; all tests green; en is a visual no-op.</done>
</task>

</tasks>

<verification>
- `pnpm check` -> 0 errors (svelte-check strict).
- `pnpm test --run` -> existing tests stay green + new `src/lib/i18n/i18n.test.ts` passes (identical key sets, interpolation, fallback, detectAppLang).
- Manual smoke (en visual no-op): with appLang='en' the UI text is identical to before.
- Switch language on the settings page -> nav, now-playing, menus, toasts, empty states all re-render with no reload; reload preserves the choice; lyricsLang/nameLang unchanged.
</verification>

<success_criteria>
- App-language selector on the settings page (English / 繁體中文 / 简体中文), wired to `settings.appLang` + `save()`.
- All ~74 in-scope chrome strings across the 12 `.svelte` files (excluding `src/routes/spike/+page.svelte`) sourced from `t()`; en values verbatim the current text.
- Reactive switching (no reload), persisted in `openmusic:settings:v1`, first-visit auto-detect from `navigator.language`.
- `nameLang`/`lyricsLang` untouched and independent.
- No new dependency; `pnpm check` 0 errors; `pnpm test --run` green incl. the i18n unit test.
</success_criteria>

<working_tree_hygiene>
CRITICAL — the tree has UNRELATED in-flight edits that this task MUST NOT touch or commit:
`src/app.d.ts`, `src/lib/proxy/proxy-types.ts` (LASTFM_SECRET), `.planning/research/FEATURES.md`, and untracked `.planning/research/*-lastfm.md`.

- NEVER run `git add -A` or `git add .`. Stage ONLY the files this task edits, by explicit path.
- The uncommitted edit in `src/routes/(app)/search/+page.svelte` (placeholder -> "Search songs, artists across all sources") is intentional — its English i18n value MUST be that exact text; do NOT revert it.
- Do NOT modify README.md.
- Each task is independently committable; stage only that task's files when committing.
</working_tree_hygiene>

<output>
Mark quick task 260606-6p7 complete when all six tasks pass verification.
</output>
