---
phase: 20-now-playing-surface-gestures
plan: 03
type: execute
wave: 2
depends_on: ["20-01", "20-02"]
files_modified:
  - src/lib/components/NowPlaying.svelte
autonomous: false
requirements: [NP-01, NP-03]
must_haves:
  truths:
    - "A horizontal drag past 0.28×coverWidth (or a 0.5px/ms flick) left→right plays previous, right→left plays next"
    - "During the drag the prev/next album art peeks in 1:1 and the strip snaps to the committed track on release"
    - "On the very first track with currentTime ≤ 3, a prev swipe rubber-bands and springs back with no track change"
    - "A plain sub-slop tap on the cover in the half state collapses the sheet to closed"
    - "A plain sub-slop tap still reaches the cover's onclick; a committed swipe does not replay onclick"
    - "A vertical-dominant drag still triggers the existing sheet collapse (npTop handlers keep working)"
    - "Decisions covered: D-01 (3-cover carousel peek with snap), D-02 (prev-boundary rubber-band on the cover), D-04 (sub-slop tap in half collapses sheet to closed), D-05 (three-gesture axis arbitration: tap/horizontal-carousel/vertical-collapse, no setPointerCapture on pointerdown)"
  artifacts:
    - path: "src/lib/components/NowPlaying.svelte"
      provides: "3-cover carousel strip on .cover driven by coverSwipe (NP-01), boundary rubber-band, axis-arbitration with npTop* vertical collapse, tap-cover-collapses-in-half (NP-03)"
      contains: "coverSwipe"
  key_links:
    - from: "src/lib/components/NowPlaying.svelte"
      to: "src/lib/actions/coverSwipe.ts"
      via: "use:coverSwipe on the cover container wiring onprev/onnext/ondrag/hasPrev/hasNext"
      pattern: "use:coverSwipe"
    - from: "src/lib/components/NowPlaying.svelte"
      to: "player.prev() / player.next()"
      via: "onprev → player.prev(), onnext → player.next() (D-03, no new advance fns)"
      pattern: "player\\.(prev|next)\\(\\)"
    - from: "src/lib/components/NowPlaying.svelte carousel neighbors"
      to: "player.queue / player.current"
      via: "queue.findIndex(t => t.uid === current.uid) → queue[i±1] covers"
      pattern: "player\\.queue"
---

<objective>
The two NowPlaying.svelte gestures that share the `.cover` / `.np-top` region and MUST arbitrate by axis with the existing vertical-collapse handlers — built last, after 20-01 (the `coverSwipe` action) and 20-02 (loader + scroll containment) have settled the file:

1. **NP-01 — Cover carousel swipe (prev/next).** A horizontal swipe on the cover drives a 3-cover strip (`queue[i-1]` / current / `queue[i+1]`) that follows the finger 1:1 (full-bleed, no gutter, no parallax, no fade, no scale), then animates the committed neighbor to center over `0.32s cubic-bezier(.22,1,.36,1)` and swaps via `player.prev()`/`player.next()` (D-03). Commit at `0.28 × coverWidth` or a `0.5px/ms` flick. At a true boundary (prev on index 0 with `currentTime ≤ 3`) the strip rubber-bands (`maxPull = 0.18 × coverWidth`, `0.32s` spring-back) and never changes track (D-02). Wire `use:coverSwipe` from 20-01.
2. **NP-03 — Tap cover collapses subnav (half only).** A sub-slop tap on the cover, only when `sheetState === 'half'`, collapses the sheet to `closed` via the EXISTING closed-snap path (no new animation). The tap must reach `onclick`; a committed swipe must not (the trailing-click suppressor in coverSwipe handles this).

The two coexist with the existing `npTopDown/npTopMove/npTopUp` vertical-collapse handlers by axis dominance: `|dy|>|dx|` → vertical collapse (existing), `|dx|>|dy|` → carousel, sub-slop on both → the cover's onclick (tap-collapse). The two must NOT both capture.

Purpose: deliver the highest-risk interaction in v1.2 (ROADMAP Pitfall 7) correctly, reusing the proven slop/axis/flick idiom and the new `coverSwipe` action rather than a gesture library.
Output: modified `src/lib/components/NowPlaying.svelte` (carousel strip markup + CSS, neighbor derivation, coverSwipe wiring, tap-collapse onclick; arbitration with npTop*).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-now-playing-surface-gestures/20-CONTEXT.md
@.planning/phases/20-now-playing-surface-gestures/20-UI-SPEC.md
@.planning/phases/20-now-playing-surface-gestures/20-PATTERNS.md
@.planning/phases/20-now-playing-surface-gestures/20-01-SUMMARY.md

<interfaces>
<!-- The coverSwipe action from 20-01, and the existing NowPlaying surfaces this layers onto. -->
From src/lib/actions/coverSwipe.ts (created in 20-01):
```typescript
export interface CoverSwipeOpts {
  onprev: () => void;        // fired on a committed drag-right (left→right) — D-03
  onnext: () => void;        // fired on a committed drag-left (right→left) — D-03
  ondrag?: (dx: number) => void; // per-move running dx for driving the strip translate
  hasPrev?: boolean;         // false → prev gesture rubber-bands (D-02 boundary)
  hasNext?: boolean;         // false → next gesture rubber-bands
  enabled?: boolean;
}
export const coverSwipe: Action<HTMLElement, CoverSwipeOpts>;
```
Commit = `0.28 × measuredWidth` OR `|velocity| > 0.5px/ms` with `|dx| > 8`; boundary clamp = `sign(dx)·maxPull·(1−e^(−|dx|/maxPull))`, `maxPull = 0.18 × width`, ignores flick. NEVER setPointerCapture on pointerdown; arms a one-shot click suppressor on a committed swipe; sets `touch-action: pan-y` on attach.

From src/lib/stores/player.svelte (already imported as `player`):
```typescript
player.current: Track | null;
player.queue: Track[];              // public $state
player.loading: boolean;
player.prev(): void;                // restarts the song if audio.currentTime > 3, else steps to queue[i-1] (or restarts if i===0)
player.next(): void;                // steps to queue[i+1], else ensureAhead() grows + advances
```
Neighbor lookup (host computes; indexOf is private):
```typescript
const i = player.queue.findIndex(t => t.uid === player.current?.uid);
const prevCover = i > 0 ? player.queue[i - 1] : null;            // null → hasPrev:false (rubber-band)
const nextCover = i >= 0 && i + 1 < player.queue.length ? player.queue[i + 1] : null; // almost always present (ensureAhead)
```

From src/lib/components/NowPlaying.svelte (existing — DO NOT race these):
- `npTopDown/npTopMove/npTopUp` on `.np-top` (lines ~377-406): `DRAG_SLOP = 8`; capture only after `dy > DRAG_SLOP && |dy| > |dx|`; `npTopUp` calls `player.collapse()` when `dragY > 120`; the `!wasDragging` tap path lets onclick run.
- `.np-top` markup (lines ~673-689): the `role="group"` container with the 4 pointer handlers; inside it the `.cover` `role="button" tabindex="0" bind:this={coverEl} aria-label style:background-image`.
- `.cover` CSS (line ~839): `width: min(72vw, 320px)`, reflow `0.32s cubic-bezier(.22,1,.36,1)`; `.np.reflow .cover` (~844): `width:auto; height:30vh; margin:0 -18px` (half/full reflow).
- `.np-top` CSS (line ~838): `touch-action: pan-x;` (the comment anticipates exactly this swipe — keep pan-x / let the action own X).
- `sheetState` (`$state<'closed'|'half'|'full'>`, line ~414); the grip TAP single-step (line ~511) shows the `sheetState = ...` reassignment that animates the `.sheet` transform (`0.28s cubic-bezier(.22,1,.36,1)`, line ~749) + `.cover` reflow (`0.32s`, line ~839) concurrently.
- `effectiveCover` (`$derived`, line ~309): `swappedCover ?? player.current?.cover ?? null`; `fallbackCover(track)` returns a gradient string.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carousel strip + coverSwipe wiring + boundary (NP-01)</name>
  <files>src/lib/components/NowPlaying.svelte</files>
  <read_first>
    - src/lib/components/NowPlaying.svelte (current state: the `npTop*` handlers ~361-406, the `.np-top`/`.cover` markup ~673-689, `effectiveCover`/`fallbackCover` ~58-309, the `.cover`/`.np-top` CSS ~838-844, `sheetState` ~414, the grip TAP closed-snap ~489-513)
    - src/lib/actions/coverSwipe.ts (the action created in 20-01 — its CoverSwipeOpts contract and the ondrag callback)
    - src/lib/stores/player.svelte.ts lines 1386-1412 (next/prev semantics — prev restarts when currentTime > 3) and ~160/~99 (queue, current public state)
    - .planning/phases/20-now-playing-surface-gestures/20-UI-SPEC.md (§1 Cover carousel peek: 1:1 lockstep rigid strip, full-bleed no gutter at ±coverWidth, no parallax/scale/fade, overflow:hidden track, commit settle `0.32s cubic-bezier(.22,1,.36,1)`; §2 Rubber-band formula + `maxPull = 0.18×coverWidth` + 0.32s + ignore-flick; §3 commit `0.28 × coverWidth`; cross-browser `touch-action` reconcile row)
    - .planning/phases/20-now-playing-surface-gestures/20-PATTERNS.md (NowPlaying modify section: arbitration plan Option A vs B; the npTop* coexistence excerpt; neighbor lookup; Measure-at-drag-start)
  </read_first>
  <action>Inside the existing `.cover` box (which currently shows a single `style:background-image`), build a 3-cell carousel strip: a wrapper with `overflow: hidden` and an inner rigid track holding three full-bleed cells (prev / current / next) sized exactly at the cover width so each neighbor sits edge-to-edge at `±coverWidth` (no gutter). Derive the neighbors reactively: `const ci = $derived(player.queue.findIndex(t => t.uid === player.current?.uid)); const prevCover = $derived(ci > 0 ? player.queue[ci-1] : null); const nextCover = $derived(ci >= 0 && ci+1 < player.queue.length ? player.queue[ci+1] : null);`. Each cell's background-image: current cell uses `effectiveCover ?? fallbackCover(player.current)`; prev/next cells use `neighbor.cover ?? fallbackCover(neighbor)` (or empty when the neighbor is null). The track's resting transform centers the current cell (`translateX(-coverWidth)` if you lay out prev|current|next, or `translateX(0)` with the current cell first and neighbors absolutely positioned at ±coverWidth — choose whichever keeps it a single rigid strip; UI-SPEC §1 requires 1:1 lockstep, neighbors move at the same rate as current). Attach `use:coverSwipe` to the `.cover` (or the `.np-top` container — see arbitration in Task 3) wiring: `onprev: () => player.prev()`, `onnext: () => player.next()` (D-03 — reuse existing, add NO new advance fn), `hasPrev: prevCover !== null && !(ci === 0)` (the true boundary is prev on index 0; note player.prev() itself restarts when currentTime > 3 so a non-boundary prev is always safe), `hasNext: true` (ensureAhead keeps a neighbor; pass `nextCover !== null || true` effectively true), and `ondrag: (dx) => { dragX = dx }` updating a local `$state` that drives the strip's live `transform: translateX(...)`. On commit the action calls onprev/onnext which swaps `player.current`; the strip re-derives and the committed neighbor becomes the new current. Apply the commit-settle transition `transform 0.32s cubic-bezier(.22,1,.36,1)` on the strip when not actively dragging (mirror the inline `transition: none` during drag idiom from swipeRemove). Keep the `.cover` `role="button" tabindex="0" aria-label` and `bind:this={coverEl}` intact (UI-SPEC accessibility + the existing keyboard ArrowLeft/Right + reflow re-measure depend on them). Do NOT add accent/color/glow on arm — feedback is positional only (UI-SPEC §3 commit feedback). Honor reduced motion: under `@media (prefers-reduced-motion: reduce)` / `:root[data-reduce-motion]` the strip transition collapses to instant (no animation); track change still happens.</action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && grep -q "use:coverSwipe" src/lib/components/NowPlaying.svelte && grep -q "player.queue.findIndex" src/lib/components/NowPlaying.svelte && grep -qE "onprev:.*player\.prev\(\)" src/lib/components/NowPlaying.svelte && grep -qE "onnext:.*player\.next\(\)" src/lib/components/NowPlaying.svelte && grep -q "0.32s cubic-bezier(.22,1,.36,1)" src/lib/components/NowPlaying.svelte && pnpm check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `use:coverSwipe` is attached and wired with `onprev: () => player.prev()` and `onnext: () => player.next()` — no new advance function is introduced (D-03; source assertion via grep).
    - Neighbors are derived from the public `player.queue` via `findIndex(t => t.uid === player.current?.uid)` and `queue[i±1]`; `hasPrev` is false exactly when `ci === 0` / `prevCover === null` (the true boundary — D-02).
    - The carousel renders up to 3 full-bleed cells in a rigid strip with `overflow: hidden`, 1:1 lockstep (no parallax/scale/fade on the cover); the commit settle uses `transform 0.32s cubic-bezier(.22,1,.36,1)` and the below-threshold spring-back / boundary spring-back follow the coverSwipe action's pinned values (0.28s / 0.32s).
    - LOAD-BEARING INVARIANT: the gesture relies on `coverSwipe` which never `setPointerCapture` on `pointerdown` and commits axis only after the 8px slop + `|dx|>|dy|` dominance check; a sub-slop tap still reaches the cover's `onclick`; a committed swipe arms the one-shot click suppressor so it does not replay onclick. (Verified by 20-01's tests + this wiring not overriding capture.)
    - The `.cover` keeps `role="button"`, `tabindex="0"`, `aria-label`, and `bind:this={coverEl}` (source assertion: those attributes still present on the cover element).
    - No accent/color hint on arm (UI-SPEC §3): the carousel cells carry no `--color-primary` fill/ring/glow.
    - `pnpm check` exits 0.
  </acceptance_criteria>
  <done>The cover renders a 3-cover carousel that follows the finger, commits prev/next via player.prev()/next(), rubber-bands at the prev boundary, and keeps its button/aria/ref attributes; pnpm check is 0/0.</done>
</task>

<task type="auto">
  <name>Task 2: Tap-cover-collapses-in-half (NP-03)</name>
  <files>src/lib/components/NowPlaying.svelte</files>
  <read_first>
    - src/lib/components/NowPlaying.svelte (the `.cover` element + the grip TAP closed-snap path ~489-513 showing the `sheetState = 'closed'` reassignment idiom, and `npTopUp`'s `!wasDragging` tap path ~399-405; current state after Task 1)
    - .planning/phases/20-now-playing-surface-gestures/20-UI-SPEC.md (§4 Tap cover → collapse to closed: trigger = sub-slop tap on `.cover` only when `sheetState === 'half'`; reuse the existing grip-down → closed snap; no-op in closed/full)
    - .planning/phases/20-now-playing-surface-gestures/20-PATTERNS.md (NowPlaying modify section → Tap-cover → collapse paragraph; the closed-snap idiom reference)
  </read_first>
  <action>Add an `onclick` handler on the `.cover` element that, ONLY when `sheetState === 'half'`, snaps the sheet to `closed` by reusing the EXISTING closed-snap reassignment used by the grip TAP path: set `sheetState = 'closed'` (and reset the transient drag state the same way the grip path does, e.g. `sheetDragging = false; sheetDragY = 0;` / `sheetDragY = offsetFor('closed')` consistent with how the existing closed snap is driven). Do NOT author a new animation — the `.sheet` transform transition (`0.28s`) + the `.cover` reflow (`0.32s`) already run concurrently on a `sheetState` change. No-op in `closed` and `full` (D-04 scopes this to half only — guard with `if (sheetState !== 'half') return;`). Because the carousel swipe lives on `coverSwipe` (which arms a one-shot click suppressor on a committed swipe and never captures on a sub-slop tap), this `onclick` fires ONLY on a genuine tap, never after a committed swipe — do NOT add any extra movement guard beyond the `sheetState === 'half'` check; the no-capture-on-pointerdown invariant + suppressor are what make the tap reliable.</action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && grep -A3 "class=\"cover\"" src/lib/components/NowPlaying.svelte | grep -q "onclick" && grep -qE "sheetState\s*===\s*'half'" src/lib/components/NowPlaying.svelte && pnpm check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The `.cover` element has an `onclick` that sets `sheetState = 'closed'` ONLY when `sheetState === 'half'` (behavior assertion: tap-in-half → closed; tap-in-closed/full → no-op via the `if (sheetState !== 'half') return;` guard).
    - The collapse reuses the existing closed-snap path (no new `@keyframes` or transition authored — the `.sheet` 0.28s + `.cover` 0.32s run as they already do on state change; source assertion: no new keyframe added for this).
    - LOAD-BEARING INVARIANT: the tap reaches `onclick` because `coverSwipe` never `setPointerCapture`s on `pointerdown`; a committed swipe does not replay `onclick` because `coverSwipe` arms the one-shot capture-phase suppressor on commit. No extra movement guard is added beyond the state check.
    - `pnpm check` exits 0.
    - Zero new i18n keys.
  </acceptance_criteria>
  <done>Tapping the cover in the half state collapses the sheet to closed via the existing snap; closed/full taps are unaffected; pnpm check is 0/0.</done>
</task>

<task type="auto">
  <name>Task 3: Axis arbitration with the existing vertical collapse</name>
  <files>src/lib/components/NowPlaying.svelte</files>
  <read_first>
    - src/lib/components/NowPlaying.svelte (the `npTopDown/npTopMove/npTopUp` handlers ~361-406 and the `.np-top` markup ~673-681 + `.np-top { touch-action: pan-x }` ~838; current state after Tasks 1-2)
    - src/lib/actions/coverSwipe.ts (its move() vertical-yield branch — `|ddy| > |ddx|` goes passive without capturing — and its `touch-action: pan-y` on attach)
    - .planning/phases/20-now-playing-surface-gestures/20-UI-SPEC.md (cross-browser `touch-action` axis-yield row + the `setPointerCapture + trailing click (Pitfall 7)` row + Reduced-motion row)
    - .planning/phases/20-now-playing-surface-gestures/20-PATTERNS.md (LOAD-BEARING INVARIANT block; the npTop* coexistence excerpt; Arbitration plan Option A vs B — coverSwipe attached to .cover is Option B)
  </read_first>
  <action>Reconcile the two pointer paths so the horizontal carousel (coverSwipe on `.cover`) and the existing vertical-collapse (`npTop*` on `.np-top`) arbitrate by axis dominance and NEVER both capture. Use Option B (the on-pattern action approach): `coverSwipe` lives on `.cover` and owns the horizontal axis; `npTop*` stays on `.np-top` and owns the vertical axis. Verify/set `touch-action` so the browser hands the horizontal axis to the action and the vertical axis to the collapse: the `.cover` (where coverSwipe attaches) gets `touch-action: pan-y` set BY the action on attach (it already does this); keep `.np-top { touch-action: pan-x }` so the wrapper yields horizontal pan. Confirm `coverSwipe`'s move() yields on vertical dominance (`|ddy| > |ddx|` → passive, no capture) so a vertical drag started on the cover flows up to `npTopMove` and triggers the existing collapse; and confirm `npTopMove` only captures on `dy > DRAG_SLOP && |dy| > |dx|` (already the case) so it never steals a horizontal swipe. The result: `|dy|>|dx|` past slop → existing sheet collapse; `|dx|>|dy|` past slop → carousel; sub-slop → the cover's onclick (tap-collapse, NP-03). Add a code comment at the `.np-top`/`.cover` boundary documenting this axis split and the no-double-capture rule. Make NO change that causes either handler to `setPointerCapture` on `pointerdown`.</action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && grep -q "touch-action: pan-x" src/lib/components/NowPlaying.svelte && ! grep -A2 "function npTopDown" src/lib/components/NowPlaying.svelte | grep -q "setPointerCapture" && grep -A6 "function npTopMove" src/lib/components/NowPlaying.svelte | grep -qE "Math.abs\(dy\) > Math.abs\(dx\)" && pnpm test --run src/lib/actions/coverSwipe.test.ts && pnpm check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - LOAD-BEARING INVARIANT (the highest-risk interaction): neither `npTopDown` nor any pointerdown path calls `setPointerCapture`; `npTopMove` captures only after `dy > DRAG_SLOP && Math.abs(dy) > Math.abs(dx)`; `coverSwipe` captures only after the horizontal commit; sub-slop movement reaches the cover's `onclick`. (Source assertions via grep + the green coverSwipe.test.ts.)
    - Axis split is explicit: `.np-top { touch-action: pan-x }` retained (wrapper yields horizontal) and `coverSwipe` sets `touch-action: pan-y` on `.cover` on attach; a vertical-dominant drag started on the cover yields from coverSwipe (passive, no capture) and is claimed by `npTopMove` (behavior assertion: vertical drag still collapses the sheet — `dragY > 120` path intact).
    - The two handlers never both capture (no double-capture): coverSwipe yields on `|ddy|>|ddx|`; npTopMove yields on `|dx|>=|dy|`.
    - `pnpm test --run src/lib/actions/coverSwipe.test.ts` is green and `pnpm check` exits 0.
    - A code comment documents the axis-arbitration contract at the `.np-top`/`.cover` boundary.
  </acceptance_criteria>
  <done>The carousel and vertical-collapse arbitrate cleanly by axis with no double-capture and no pointerdown capture; vertical collapse + tap-collapse + horizontal carousel all coexist; coverSwipe tests green; pnpm check 0/0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Device-verify the cover gesture arbitration</name>
  <files>src/lib/components/NowPlaying.svelte</files>
  <action>Run the app on a real phone (or device emulation in DevTools with touch) against the cover, and walk the how-to-verify steps to confirm the tap-vs-h-swipe-vs-v-collapse arbitration, the carousel peek/snap, and the prev-boundary rubber-band — see how-to-verify for the exact gated steps. No code change in this task; it is the human gate after the automated suite + pnpm check pass.</action>
  <what-built>The cover now arbitrates three gestures: horizontal swipe → 3-cover carousel prev/next (with rubber-band on the first track), vertical drag → existing sheet collapse, and a sub-slop tap in half → collapse to closed. All wired through the node-tested `coverSwipe` action with the no-capture-on-pointerdown invariant. The automated suite (coverSwipe.test.ts) and `pnpm check` pass, but the tap-vs-swipe-vs-collapse arbitration and the carousel feel are device-only.</what-built>
  <how-to-verify>
    On a phone (or device emulation), with a track playing and a non-empty up-next queue:
    1. Open now-playing to full, then half. In HALF, single-tap the cover → the sheet collapses to `closed` (immersive cover, no subnav). Tapping in closed/full does nothing new.
    2. Drag the cover LEFT→RIGHT past ~28% of its width → previous track plays; the prev album art peeks in 1:1 and snaps to center. RIGHT→LEFT → next track. A quick flick in either direction also commits.
    3. A short horizontal nudge (under ~28%, no flick) springs back with no track change.
    4. Skip to the very FIRST track in the queue (or restart so currentTime ≤ 3 on index 0); drag LEFT→RIGHT (prev) → the cover rubber-bands and springs back with NO track change (even on a hard flick).
    5. Drag the cover straight DOWN → the existing sheet collapse still works (no carousel hijack).
    6. A plain sub-slop tap on the cover (in half) still collapses — it is never swallowed by the swipe; a committed swipe never also triggers tap-collapse.
    7. Turn on reduce-motion (OS or app setting) → track change still happens, just without the slide animation.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any gesture that mis-fires (tap eaten, swipe collapses the sheet, boundary commits a track, double-capture jitter).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user touch → cover gesture → player.prev()/next() | Pointer coordinates from the user's own touch drive the EXISTING prev/next transport. No new network call, no new input field, no new data flow. |
| queue Track → carousel cell background-image | Album-art URLs rendered in the 3 carousel cells come from the existing already-trusted `player.queue` Track objects (the same URLs the cover/nowbar already render). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Tampering | cover gesture → player.prev/next | accept | The swipe only invokes the existing `player.prev()`/`player.next()`; it adds no new advance logic and no new trust decision. A user manipulating their own pointer events only changes their own playback. |
| T-20-05 | Information Disclosure / Injection | carousel cell `style:background-image=url(...)` | accept | Neighbor cover URLs come from the same already-trusted `player.queue` Track objects rendered today by `.cover` and the nowbar; no NEW untrusted source is introduced and the value is used in a CSS `url()` background, not `innerHTML`. No new XSS sink. Surface unchanged from the existing cover render. |
| T-20-06 | Denial of Service | double-capture / leaked suppressor | mitigate | Axis arbitration guarantees coverSwipe and npTop* never both capture; coverSwipe's `destroy()` drops the armed suppressor and the suppressor is one-shot, preventing a leaked capture-phase handler from eating future taps. |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — zero new dependencies (consumes the in-repo `coverSwipe` action + `player` store). slopcheck N/A. |

Client-side, browser-only gesture layering. No new network calls, no new data flows, no auth, no new XSS sink (carousel cells use CSS `url()` backgrounds sourced from the existing trusted queue, never `innerHTML` and never a new untrusted URL). The realistic high-severity surface is the gesture-collision correctness (ROADMAP Pitfall 7), addressed as a load-bearing acceptance criterion, not a security threat.
</threat_model>

<verification>
- `pnpm test --run src/lib/actions/coverSwipe.test.ts` green; `pnpm check` exits 0.
- `grep` confirms: `use:coverSwipe` wired to `player.prev()`/`player.next()`; neighbors from `player.queue.findIndex`; `.np-top { touch-action: pan-x }` retained; `npTopDown` has no `setPointerCapture`; `npTopMove` keeps the `|dy| > |dx|` dominance check; tap-collapse guarded by `sheetState === 'half'`.
- Human checkpoint (Task 4) confirms the tap/h-swipe/v-collapse arbitration and the boundary rubber-band on a device.
</verification>

<success_criteria>
- Swiping the cover left→right plays previous, right→left plays next, axis-locked so vertical collapse and plain taps keep working (NP-01).
- Tapping the cover in half collapses the subnav (NP-03).
- The prev-on-first-track boundary rubber-bands with no track change; the carousel peeks 1:1 and snaps.
- No double-capture; the Pitfall-7 invariant holds; pnpm check 0/0; zero new i18n keys.
</success_criteria>

<output>
Create `.planning/phases/20-now-playing-surface-gestures/20-03-SUMMARY.md` when done.
</output>
