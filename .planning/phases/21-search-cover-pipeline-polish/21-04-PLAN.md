---
phase: 21-search-cover-pipeline-polish
plan: 04
type: execute
wave: 2
depends_on: ["21-01", "21-02"]
files_modified:
  - src/routes/(app)/search/+page.svelte
autonomous: false
requirements: [SRCH-01, SRCH-02, SRCH-03, COVER-02]
must_haves:
  truths:
    - "After every dedupeBest in run()/onPartial/loadMore, the list is fully re-sorted by scoreMatch(query, candidate, setContext) descending — display order = score order (D-01/D-02)"
    - "The score+sort runs INSIDE the existing race guards (myAc.signal.aborted || kw !== q.trim()); a superseded partial never clobbers a newer query (Pitfall 3)"
    - "The list persisted to searchSession is the ALREADY-SORTED list; restore assigns it order-preserving and does NOT re-sort (Pitfall 6 — scroll restore intact)"
    - "Search result rows carry use:lazyCover so empty/broken covers resolve on scroll-into-view and repaint via a reactive resolved-cover map, never refetching (SRCH-02/COVER-02)"
    - "Empty-query autofocus is verified intact: a fresh empty visit focuses the input + opens recents; a restored prior session never steals focus (SRCH-03/D-17/D-19)"
    - "The iOS keyboard restriction is accepted — success criterion is the focused input (ring + caret), no gesture-chained nav hack is added (D-18)"
    - "The 試聽 penalty, short-title and cross-source-artist boosts visibly re-order the live search list for a real query"
  artifacts:
    - path: "src/routes/(app)/search/+page.svelte"
      provides: "score+sort wired into run/onPartial/loadMore; use:lazyCover on result rows; resolved-cover reactive map; autofocus verified"
      contains: "computeSetContext"
  key_links:
    - from: "src/routes/(app)/search/+page.svelte"
      to: "src/lib/services/score-match.ts + score-context.ts"
      via: "rankList = sort by scoreMatch(q, cand, computeSetContext(rows,q))"
      pattern: "scoreMatch"
    - from: "src/routes/(app)/search/+page.svelte"
      to: "src/lib/actions/lazyCover.ts"
      via: "use:lazyCover on the .art span of each result row"
      pattern: "use:lazyCover"
---

<objective>
Wire the Plan-01 scoring brain and Plan-02 lazyCover action into the search page: re-sort the result list by score after every dedupe (inside the race guards), attach lazyCover to result rows so empty/broken covers resolve on scroll, and verify-and-harden the already-shipped empty-query autofocus.

Purpose: SRCH-01 (live re-sort), SRCH-02 + COVER-02 (search-row lazy covers), SRCH-03 (autofocus verify). Depends on Plan 01 (scoreMatch + computeSetContext) and Plan 02 (lazyCover). Coordinates with the already-committed ql0 typeahead + D-05 recents on this page.
Output: edited `search/+page.svelte` only.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

ENVIRONMENT: Shell default node is v16 and breaks vitest/svelte-check. EVERY node command MUST be prefixed with:
`export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"` and use pnpm.

DEPENDS ON: Plan 01 (scoreMatch 3-arg + computeSetContext) and Plan 02 (lazyCover action). Read 21-01-SUMMARY.md and 21-02-SUMMARY.md for the exact exports before starting.

SEQUENCING: ql0 autocomplete + D-05 recents are ALREADY COMMITTED on this page (commit 40e08c4; lines ~72-111, ~403-432). There is NO uncommitted conflict (git status shows only .planning/HANDOFF.json). Build ON the committed code — do NOT clobber the typeahead/recents blocks. Re-`git diff` the file at the start in case it changed since planning.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-search-cover-pipeline-polish/21-CONTEXT.md
@.planning/phases/21-search-cover-pipeline-polish/21-RESEARCH.md
@.planning/phases/21-search-cover-pipeline-polish/21-PATTERNS.md
@.planning/phases/21-search-cover-pipeline-polish/21-01-SUMMARY.md
@.planning/phases/21-search-cover-pipeline-polish/21-02-SUMMARY.md

<interfaces>
From src/lib/services/score-match.ts (Plan 01):
  export function scoreMatch(query: { artist: string; title: string }, candidate: Track, ctx?: SetContext): number
From src/lib/services/score-context.ts (Plan 01):
  export function computeSetContext(rows: Track[], query: string): SetContext
From src/lib/actions/lazyCover.ts (Plan 02):
  export const lazyCover: Action<HTMLElement, { track: Track; onResolved: (uid: string, url: string) => void }>

Current search/+page.svelte landmarks (re-verify line numbers at edit time):
  - run() onPartial guard + dedupe: ~221-223  `if (myAc.signal.aborted || kw !== q.trim()) return; results = dedupeBest(partial.interleaved, settings.preferredSource);`
  - run() final dedupe: ~225  `results = dedupeBest(interleaved, settings.preferredSource);`
  - run() Deezer-boost post-paint swap: ~237-241 (guard + results = boosted + persistSession)
  - loadMore() dedupe: ~275  `const merged = dedupeBest(interleaved, settings.preferredSource);`
  - persistSession() call sites: ~233, 240, 285
  - onMount restore (hasPrior gate): ~303-315  (assigns results = searchSession.results directly)
  - autofocus (ALREADY SHIPPED): ~316-320  `if (!q.trim()) queryInputEl?.focus();`
  - result row render: ~479-489  `<span class="art" style:background-image={t.cover ? `url(${t.cover})` : fallbackCover(t)}></span>` inside `{#each results as t (t.uid)}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Score+sort step in run/onPartial/loadMore (inside race guards, sort-before-persist)</name>
  <files>src/routes/(app)/search/+page.svelte</files>
  <read_first>
    - src/routes/(app)/search/+page.svelte (the run()/onPartial/loadMore dedupe sites ~221-289, the Deezer-boost swap ~237-241, persistSession ~233/240/285, the onMount restore ~303-315)
    - src/lib/services/score-match.ts (the 3-arg scoreMatch from Plan 01)
    - src/lib/services/score-context.ts (computeSetContext from Plan 01)
    - src/lib/services/dedupe.ts (dedupeBest — runs BEFORE the sort; its appearance order is the tie-break)
  </read_first>
  <action>
    Add a pure local helper `rankList(rows: Track[], query: string): Track[]` that computes `computeSetContext(rows, query)` once, then returns a sorted copy: `[...rows].sort((a, b) => scoreMatch(qObj, b, ctx) - scoreMatch(qObj, a, ctx))` descending. Per researcher Q2/A4: build `qObj` by passing the raw trimmed keyword into BOTH artist and title (`{ artist: kw, title: kw }`) — the similarity term degrades to token-overlap (still useful) and the new short-title/artist-frequency boosts + 試聽 penalty are the dominant search-list signals. The sort must be stable in practice: scoreMatch is deterministic, so equal scores keep dedupeBest's appearance order.

    Wrap EVERY existing `results = dedupeBest(...)` assignment with rankList, KEEPING it inside the existing race guard (D-02/Pitfall 3):
    - onPartial (~222): `results = rankList(dedupeBest(partial.interleaved, settings.preferredSource), kw);` — still after `if (myAc.signal.aborted || kw !== q.trim()) return;`
    - run() final (~225): `results = rankList(dedupeBest(interleaved, settings.preferredSource), kw);`
    - Deezer-boost swap (~239): `results = rankList(boosted, kw);` inside its existing guard
    - loadMore() (~275): `const merged = rankList(dedupeBest(interleaved, settings.preferredSource), kw);`

    Sort-before-persist (Pitfall 6): because `results` is now always the sorted list, every `persistSession()` already stores the sorted order — confirm persistSession reads the live `results`. The onMount restore path (~305-315) assigns `results = searchSession.results` directly and MUST NOT call rankList (keep it order-preserving so the restored scroll offset lands correctly).

    Do not touch the ql0 typeahead or recents blocks.
  </action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'rankList' src/routes/(app)/search/+page.svelte` matches at the 4 commit sites + the helper definition
    - `grep -n 'computeSetContext' src/routes/(app)/search/+page.svelte` matches (used inside rankList)
    - Every rankList call sits inside the existing `myAc.signal.aborted || kw !== q.trim()` guard (verify the onPartial + boost sites still return early before assigning)
    - The onMount restore (`results = searchSession.results`) does NOT call rankList (grep the restore block — no rankList)
    - `pnpm check` clean
  </acceptance_criteria>
  <done>The search list re-sorts by score after every dedupe inside the race guards; persisted list is sorted; restore stays order-preserving; ql0/recents untouched.</done>
</task>

<task type="auto">
  <name>Task 2: use:lazyCover on result rows + reactive resolved-cover map</name>
  <files>src/routes/(app)/search/+page.svelte</files>
  <read_first>
    - src/routes/(app)/search/+page.svelte (the result row `{#each results as t (t.uid)}` + the `.art` span ~479-489; the existing fallbackCover helper; the dedupeBestWithDeezer post-paint reactive-swap pattern ~237-241 as the reactive-repaint precedent)
    - src/lib/actions/lazyCover.ts (the action signature + onResolved callback from Plan 02)
  </read_first>
  <action>
    Add a reactive map for lazily-resolved covers: `let resolvedCovers = $state<Record<string, string>>({});` (or a `$state(new Map())` reassigned for reactivity — match the repo's reactive-collection idiom). Attach `use:lazyCover={{ track: t, onResolved: (uid, url) => { resolvedCovers = { ...resolvedCovers, [uid]: url }; } }}` to the `.art` span of each result row (or the row element). Change the `.art` background-image to prefer a resolved cover: `style:background-image={ (resolvedCovers[t.uid] ?? t.cover) ? `url(${resolvedCovers[t.uid] ?? t.cover})` : fallbackCover(t) }`. The resolved value written by lazyCover is already a SOLID https URL (Plan 02 isSolidCover gate) so the existing background-image render is safe (T-0bb-01 — do not widen to a less-safe path; resolved covers are https-only).

    The action handles cache-first read, broken-URL Image() probe (D-15), and the tier chain internally — the page only supplies the track + the onResolved repaint. Keep the row's existing `use:longpress` + onclick handlers intact (lazyCover stacks on the `.art` span or the row alongside them).
  </action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'use:lazyCover' src/routes/(app)/search/+page.svelte` matches on the result row
    - A reactive `resolvedCovers` state exists and is reassigned in onResolved (grep both)
    - The `.art` background-image prefers `resolvedCovers[t.uid] ?? t.cover` (grep the render expression)
    - The existing `use:longpress` + onclick on the row are preserved (grep both still present)
    - `pnpm check` clean
  </acceptance_criteria>
  <done>Search result rows resolve empty/broken covers lazily on scroll and repaint via a reactive map, reusing the Plan-02 action; existing row handlers intact.</done>
</task>

<task type="auto">
  <name>Task 3: Verify-and-harden empty-query autofocus (SRCH-03)</name>
  <files>src/routes/(app)/search/+page.svelte</files>
  <read_first>
    - src/routes/(app)/search/+page.svelte (the onMount block ~303-321: hasPrior restore THEN `if (!q.trim()) queryInputEl?.focus();`; the inputFocused/recents D-05 wiring; the ql0 typeahead block)
    - src/lib/stores/searchSession.svelte.ts (hasPrior — the restore-vs-fresh gate, D-17)
  </read_first>
  <action>
    SRCH-03 is ALREADY SHIPPED (commits 78b585d/f5cdab7) — this is verify-and-harden, NOT a rebuild. Confirm the autofocus stays correct after the Task 1/2 edits:
    - The focus call lives in onMount AFTER the hasPrior restore, gated on `!q.trim()` (a restored prior query makes q non-empty so focus is not stolen — D-17). It must remain in onMount, NOT a `$effect` keyed on q (clearing the input mid-session must NOT re-grab focus — Anti-pattern "re-grabbing focus on q clear").
    - Confirm focusing sets `inputFocused = true` (or that focusing the input triggers the existing focus handler) so the D-05/D-19 recents suggestions open on a fresh empty visit. If the programmatic `.focus()` does not fire the focus handler that sets inputFocused, set `inputFocused = true` alongside the focus call so D-19 holds.
    - Reconcile with the committed ql0 typeahead: a fresh empty visit shows recents (D-19), not stale typeahead state. Do not regress the typeahead.
    Accept the iOS keyboard restriction (D-18) — the success criterion is the input being focused (ring + caret), not that the soft keyboard always rises. Do not add a gesture-chained nav hack.
  </action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The `!q.trim()` focus call remains inside onMount AFTER the hasPrior restore (grep: focus call sits after the restore block, not in a $effect)
    - There is NO `$effect` keyed on `q` that calls `.focus()` (grep confirms autofocus is onMount-only)
    - On a fresh empty visit `inputFocused` becomes true so recents open (grep: inputFocused set true on the autofocus path, or the focus handler is confirmed to do so)
    - `pnpm check` clean
  </acceptance_criteria>
  <done>Empty-query autofocus is confirmed onMount-only + post-restore + recents-opening, surviving the Task 1/2 edits and the committed ql0 typeahead; no rebuild, no q-keyed $effect.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify search scoring, lazy covers, and autofocus live</name>
  <what-built>Score-based full re-sort of the search list (per partial, race-guarded), use:lazyCover on result rows resolving empty/broken covers on scroll with a reactive repaint, and the verified empty-query autofocus + recents.</what-built>
  <how-to-verify>
    1. `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm dev` and open /search.
    2. SRCH-03: fresh visit with an empty query → the input is focused (caret/ring) and the recent-searches list shows. Search something, navigate to another tab, come back → the prior query + results restore with scroll intact and the input is NOT auto-focused.
    3. SRCH-01: search a query like 稻香 → confirm a clean short title (稻香) ranks above a long cover variant (稻香 (翻唱版)/DJ版). Search a query where a QQ 試聽 sub-60s clip appears → confirm it sinks to the bottom (never above a full track). Watch the brief reshuffle as sources stream in (D-02 accepted).
    4. SRCH-02 / COVER-02: search a query whose results have empty covers (QQ rows return cover: null) → scroll; covers resolve as rows enter view. Scroll the same rows back → no refetch (check the network tab — no repeat cover fetches for already-resolved uids).
    5. Confirm no broken images flash; rows without a resolvable cover keep the gradient.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what ranked/rendered wrong (scoring order, cover not resolving, refetch on re-scroll, focus stolen on restore).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream search results → ranked + rendered list | untrusted source rows cross into scoring + cover render; resolved covers must stay https-only |
| superseded query partial → live results | a stale async partial must not clobber the current query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08 | Tampering | superseded partial re-sort | mitigate | rankList runs INSIDE the existing `myAc.signal.aborted || kw !== q.trim()` guard at every commit site (Pitfall 3); a stale partial returns early before assigning |
| T-21-03 (T-0bb-01) | Tampering / XSS | lazyCover resolved cover render | mitigate | resolvedCovers only holds SOLID https URLs (Plan 02 isSolidCover gate); rendered via the existing background-image path for the source cover — resolved values are not routed through any less-safe sink |
| T-21-05 | DoS | lazyCover fan-out on scroll | mitigate | per-row action: unobserve-after-first + in-flight de-dupe + cache-first read (Plan 02); naturally viewport-bounded, no Promise.all over the list |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (zero new deps — RESEARCH.md audit "Not applicable"); no install task to gate |
</threat_model>

<verification>
- `pnpm check` clean after each task
- Full suite green at wave merge (especially score-match/discovery/fallback D-07 regression set)
- Manual checkpoint (Task 4): scoring order correct, 試聽 clips sink, lazy covers resolve + cache (no refetch), autofocus fresh-vs-restored correct
</verification>

<success_criteria>
- SRCH-01 wired live: score-based full re-sort per partial inside race guards, sort-before-persist, order-preserving restore
- SRCH-02 + COVER-02: result rows resolve empty/broken covers on scroll, cached uid-first/name, no refetch
- SRCH-03: empty-query autofocus verified onMount-only + post-restore + recents-opening (D-17/D-18/D-19), ql0 typeahead intact
</success_criteria>

<output>
Create `.planning/phases/21-search-cover-pipeline-polish/21-04-SUMMARY.md` when done. Record the query→{artist,title} mapping used (raw keyword into both) and the observed live scoring behavior on 稻香 / a 試聽-clip query.
</output>
