---
phase: 17-up-next-sourcing-settings-plumbing
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/lib/actions/swipeRemove.ts
  - src/lib/actions/swipeRemove.test.ts
  - src/lib/stores/player.svelte.ts
  - src/lib/stores/player.svelte.test.ts
  - src/lib/components/NowPlaying.svelte
autonomous: true
requirements: [QUEUE-05, QUEUE-02]

must_haves:
  truths:
    - "The user can swipe a track out of up-next with a full-row horizontal swipe (axis-locked; vertical drag still reorders, taps still play)"
    - "A swipe past the distance threshold OR a fast flick removes the row; below threshold it springs back"
    - "The user can clear the whole queue via a 'Clear' button in the Up-Next header; only the currently-playing track survives and manual pins are reset"
    - "After clear the queue stays empty until the current track nears its end, then the exhaust engine generates more (no immediate refill)"
    - "A swipe-removed track is session-excluded from auto-generation (does not reappear via regen), reset on a fresh play"
  artifacts:
    - path: "src/lib/actions/swipeRemove.ts"
      provides: "Horizontal axis-locked swipe-to-remove Svelte action (capture-after-slop, tap-preserving, flick + distance threshold)"
      contains: "swipeRemove"
    - path: "src/lib/actions/swipeRemove.test.ts"
      provides: "Unit tests: slop axis-lock, flick threshold, distance threshold, tap-preserve"
      contains: "swipeRemove"
    - path: "src/lib/stores/player.svelte.ts"
      provides: "removeFromQueue(uid), clearQueue(), removedUids Set threaded into regenerate/ensureAhead exclude, removedUids reset on fresh play"
      contains: "removeFromQueue"
    - path: "src/lib/components/NowPlaying.svelte"
      provides: "use:swipeRemove on Up-Next rows + Clear button in the Up-Next header"
      contains: "swipeRemove"
  key_links:
    - from: "src/lib/components/NowPlaying.svelte Up-Next row"
      to: "player.removeFromQueue"
      via: "use:swipeRemove onremove handler"
      pattern: "swipeRemove=\\{\\{ onremove"
    - from: "src/lib/components/NowPlaying.svelte Up-Next header"
      to: "player.clearQueue"
      via: "Clear button onclick"
      pattern: "player\\.clearQueue\\(\\)"
    - from: "src/lib/stores/player.svelte.ts regenerate/ensureAhead"
      to: "removedUids exclusion"
      via: "union removedUids into the existing exclude/have Set"
      pattern: "removedUids"
---

<objective>
Add direct queue management to up-next: a horizontal axis-locked swipe-to-remove gesture, a Clear button, and the two new player methods (`removeFromQueue`, `clearQueue`) plus a session-scoped `removedUids` exclusion set so a swiped-away song does not regenerate back in.

Purpose: QUEUE-05 — the user can curate the queue directly. Combined with the never-stop posture: clear keeps the current track and re-grows only near end-of-track (D-08/D-09); a removed song is session-excluded from generation (D-10) so it does not feel broken.
Output: New `swipeRemove.ts` action + test, `removeFromQueue`/`clearQueue`/`removedUids` on the player (threaded into the existing exclude sets), swipe + Clear UI in NowPlaying.

Depends on Plan 01: shares `player.svelte.ts` and `player.svelte.test.ts` (queueContext/regenerate/ensureAhead landed there); this plan extends those same seams. The `nowplaying.clearQueue` i18n key is created by Plan 01 Task 4 — this plan only CONSUMES it via `t(...)` and does NOT edit any locale file.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-up-next-sourcing-settings-plumbing/17-CONTEXT.md
@.planning/phases/17-up-next-sourcing-settings-plumbing/17-RESEARCH.md
@.planning/phases/17-up-next-sourcing-settings-plumbing/17-PATTERNS.md
@.planning/phases/17-up-next-sourcing-settings-plumbing/17-01-SUMMARY.md

<phase_invariants>
- NEVER `setPointerCapture` on `pointerdown` (retargets the trailing click — taps stop firing). Capture only in `pointermove` after slop, once the horizontal axis is committed (Pitfall 2 / D-06). Sub-slop movement must still deliver `onclick` (tap-to-play).
- The horizontal swipe must yield to the existing vertical GripVertical reorder: when vertical movement dominates, the action goes passive so the grip/scroll runs.
- `removedUids` is a plain `Set` (NOT `$state`) — an internal exclusion budget like `manualUids`, never reactive (Anti-Pattern in research). Keep Track objects clean.
- Every queue replacement routes through ONE method that re-reads `this.queue` at write-time and re-merges manual pins + current track (Pitfall 1 — queue-mutation race). Never assign `this.queue` from a closed-over snapshot.
- After clear, do NOT immediately regenerate/ensureAhead — the exhaust engine refills only when the current track nears its end (D-09).
- The `nowplaying.clearQueue` i18n key is owned/created by Plan 01 Task 4. This plan must NOT edit any locale file — only reference the key via `t('nowplaying.clearQueue')`.
</phase_invariants>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: swipeRemove horizontal axis-locked action + unit test</name>
  <files>src/lib/actions/swipeRemove.ts, src/lib/actions/swipeRemove.test.ts</files>
  <read_first>
    - src/lib/actions/dragClose.ts (read in full — the capture-after-slop, tap-preserving idiom; esp. the "Do NOT setPointerCapture on pointerdown" rationale comment lines 60-63, the down/move/up handlers lines 52-95, listeners + update/destroy lines 97-117). This is the structural template to MIRROR on the X axis.
    - src/lib/gestures/velocity.ts (createVelocityTracker — sample(coord, timeStamp)/velocity()/reset(); it is axis-agnostic despite the `y` field name — pass clientX)
    - src/lib/actions/dragReorder.test.ts (test structure to mirror — synthesize PointerEvents, assert behavior)
    - .planning/phases/17-up-next-sourcing-settings-plumbing/17-PATTERNS.md (section "src/lib/actions/swipeRemove.ts" — the full annotated mirror with down/move/up code and the axis-arbitration step)
  </read_first>
  <behavior>
    - Test: a pointerdown then sub-slop movement (< 8px) then pointerup does NOT call onremove and leaves the element transform reset (tap preserved — onclick still reachable).
    - Test: vertical-dominant movement (|dy| > |dx|) past slop goes passive (dragging cancelled), does NOT call onremove, does NOT setPointerCapture (lets the grip/scroll run).
    - Test: horizontal-dominant drag past the distance threshold then pointerup calls onremove once.
    - Test: a fast horizontal flick (velocity > FLICK_V) past slop but below the distance threshold still calls onremove.
    - Test: horizontal drag below threshold + slow release springs back (onremove NOT called; transform transitions to translateX(0)).
    - Test: capture happens only in move() after the horizontal commit, never on pointerdown.
  </behavior>
  <action>
    Create `swipeRemove.ts` as a structural X-axis mirror of `dragClose.ts`. Export `interface SwipeRemoveOpts { onremove: () => void; threshold?: number; enabled?: boolean; }` and `export const swipeRemove: Action<HTMLElement, SwipeRemoveOpts>`. State: `dragging`, `captured`, `startX`, `startY`, `dx`; constants `SLOP = 8` (dragClose's DRAG_START), `FLICK_V = 0.5` (dragClose's FLICK_V px/ms), a `FADE_DISTANCE` (~120) for opacity, default `threshold` ~96. Use `createVelocityTracker` seeded on `e.clientX`. Set `node.style.touchAction = 'pan-y'` (keep vertical scroll, yield horizontal). down(): record start only, reset velocity, seed sample, transition='none' — NO capture (copy the dragClose tap-preservation comment). move(): compute ddx/ddy; if !captured: below slop on BOTH axes → return (tap preserved); |ddy|>|ddx| → `dragging=false; return` (vertical yields to grip); else `node.setPointerCapture(e.pointerId); captured=true` (horizontal commit). Then translateX(dx) + opacity fade. up(): if `|dx|>threshold || (|velocity|>FLICK_V && |dx|>SLOP)` call `onremove()`, else spring back (transition transform→0, opacity→1). Copy listeners + update()/destroy() verbatim from dragClose (pointerdown/move/up/cancel; destroy resets inline styles + clears touchAction). Respect `enabled` (default true).
    Create `swipeRemove.test.ts` mirroring `dragReorder.test.ts`: a fake node with stubbed `setPointerCapture`/`style`, dispatch synthetic PointerEvents with clientX/clientY/timeStamp, assert the 6 behavior cases. RED first, then implement to GREEN.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/actions/swipeRemove.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "setPointerCapture" src/lib/actions/swipeRemove.ts` appears ONLY inside the move() handler, never in down().
    - `grep -n "touchAction = 'pan-y'" src/lib/actions/swipeRemove.ts` matches.
    - `grep -n "Math.abs(ddy) > Math.abs(ddx)" src/lib/actions/swipeRemove.ts` matches (vertical-yields axis arbitration).
    - `pnpm vitest run src/lib/actions/swipeRemove.test.ts` passes all 6 behavior cases.
  </acceptance_criteria>
  <done>swipeRemove action exists with capture-after-slop horizontal arbitration, flick + distance threshold removal, spring-back, tap-preservation, and vertical-yields-to-grip; unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: removeFromQueue + clearQueue + removedUids exclusion on the player</name>
  <files>src/lib/stores/player.svelte.ts, src/lib/stores/player.svelte.test.ts</files>
  <read_first>
    - src/lib/stores/player.svelte.ts (read manualUids declaration line 459, playNext/addToQueue filter idiom lines 708-724, regenerate exclude set lines 1041-1052, ensureAhead `have` set lines 730-744, fresh-play branch lines 1020-1023, current track field, persist(); also Plan 01's queueContext additions already present in this file)
    - src/lib/services/similar.ts (buildSimilarQueue(track, excludeUids) — confirm the exclude Set param)
    - src/lib/services/picks.ts (buildDiversePicks(count, excludeUids) — confirm the `have`/exclude param)
    - src/lib/stores/player.svelte.test.ts (existing tests + Plan 01's queueContext additions — extend, do not replace)
    - .planning/phases/17-up-next-sourcing-settings-plumbing/17-PATTERNS.md (section "src/lib/stores/player.svelte.ts" — removeFromQueue, clearQueue, removedUids threading, reset-on-fresh-play)
  </read_first>
  <behavior>
    - Test: removeFromQueue(uid) removes the matching entry from queue, adds uid to removedUids, and deletes it from manualUids.
    - Test: clearQueue() leaves queue = [current] when a current track exists (and [] when none), and clears manualUids.
    - Test: clearQueue() does NOT trigger an immediate regenerate/ensureAhead (queue stays at [current]).
    - Test: a uid in removedUids is excluded from regenerate's buildSimilarQueue exclude set and ensureAhead's buildDiversePicks set.
    - Test: a fresh play resets removedUids to empty (new session starts clean).
  </behavior>
  <action>
    Declare `private removedUids = new Set<string>()` beside `manualUids` (line 459) — a plain Set, NOT $state.
    Add `removeFromQueue(uid: string)`: `this.removedUids.add(uid); this.manualUids.delete(uid); this.queue = this.queue.filter((t) => t.uid !== uid); this.persist();` — re-read `this.queue` at write-time (Pitfall 1), never a closed snapshot.
    Add `clearQueue()`: `this.queue = this.current ? [this.current] : []; this.manualUids.clear(); this.persist();` — D-08 keep current + reset pins; D-09 NO regenerate/ensureAhead call here.
    Thread `removedUids` into the existing exclude sets: in `regenerate` (line 1041) union `...this.removedUids` into the `exclude` Set before `buildSimilarQueue(seed, exclude)`; in `ensureAhead` (line 730) union `...this.removedUids` into the `have` Set before `buildDiversePicks(8, have)`. Both fns already accept the exclude/have set.
    Reset `removedUids` on a fresh play: in the `opts?.fresh` branch (near line 1020, before regenerate) add `this.removedUids.clear();` so a new listening session starts clean (D-10 — session-scoped, NOT persisted; do NOT serialize removedUids).
    Extend player.svelte.test.ts with the 5 behavior cases. Keep Plan 01's queueContext tests intact.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/stores/player.svelte.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "removeFromQueue\|clearQueue" src/lib/stores/player.svelte.ts` shows both methods.
    - `grep -n "removedUids" src/lib/stores/player.svelte.ts` appears in the declaration, removeFromQueue, the regenerate exclude set, the ensureAhead have set, and the fresh-play reset.
    - `grep -n "new Set<string>()" src/lib/stores/player.svelte.ts` confirms removedUids is a plain Set (not $state).
    - `pnpm vitest run src/lib/stores/player.svelte.test.ts` passes all new + existing cases.
    - removedUids does not appear inside the persist/serialize block (not persisted).
  </acceptance_criteria>
  <done>Player exposes removeFromQueue (filters + session-excludes) and clearQueue (keeps current, clears pins, no immediate refill); removedUids is threaded into both generators and reset on fresh play; player tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire swipe-remove on Up-Next rows + Clear button in the Up-Next header</name>
  <files>src/lib/components/NowPlaying.svelte</files>
  <read_first>
    - src/lib/components/NowPlaying.svelte (read the Up-Next list lines 733-759: the `.q-row` button line 743 with `onclick={() => player.play(track, { fresh: true })}`, the GripVertical reorder child button lines 747-755 with its `onclick={(e) => e.stopPropagation()}`, and the subnav header lines 726-731; also the `.np-top { touch-action: pan-x }` precedent line 804)
    - src/lib/actions/swipeRemove.ts (the action from Task 1 — its signature/opts)
    - src/lib/i18n/en.ts (confirm `nowplaying.clearQueue` exists — created by Plan 01 Task 4; this task only references it via t())
    - .planning/phases/17-up-next-sourcing-settings-plumbing/17-PATTERNS.md (section "src/lib/components/NowPlaying.svelte")
  </read_first>
  <action>
    NOTE: This task consumes the `nowplaying.clearQueue` key created in Plan 01 Task 4 — do NOT edit any locale file.
    Import `swipeRemove` from `$lib/actions/swipeRemove` and `Trash2` from `@lucide/svelte`. On the existing Up-Next `.q-row` button add `use:swipeRemove={{ onremove: () => player.removeFromQueue(track.uid) }}` alongside the existing `use:longpress` and `onclick`. The GripVertical child button keeps its own pointer handlers + `stopPropagation` so the swipe's slop/axis-lock yields vertical to the grip (do not remove the grip). Ensure `.q-row` has `touch-action: pan-y` (the action sets it inline; mirror the existing `.np-top { touch-action: pan-x }` CSS precedent if a stylesheet declaration is preferred).
    In the Up-Next subnav header (lines 726-731) add a Clear button beside the tab buttons, gated on a non-trivial queue: `{#if tab === 'queue' && player.queue.length > 1}<button class="clear" onclick={() => player.clearQueue()} aria-label={t('nowplaying.clearQueue')}><Trash2 size={16} /></button>{/if}`. Use the same `tab` variable the existing subnav header uses to know the queue tab is active.
  </action>
  <verify>
    <automated>pnpm svelte-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "use:swipeRemove" src/lib/components/NowPlaying.svelte` shows the action on the Up-Next row with `player.removeFromQueue(track.uid)`.
    - `grep -n "player.clearQueue()" src/lib/components/NowPlaying.svelte` shows the Clear button handler.
    - `grep -n "nowplaying.clearQueue" src/lib/components/NowPlaying.svelte` shows the aria-label binding.
    - `pnpm svelte-check` exits clean (0 errors) — confirms the consumed clearQueue key resolves.
  </acceptance_criteria>
  <done>Up-Next rows accept the horizontal swipe-remove gesture (grip reorder + tap-to-play preserved), the queue tab header shows a Clear button when the queue has more than the current track; svelte-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM pointer events → swipeRemove action | Untrusted pointer sequences arbitrate gesture intent |
| NowPlaying handler → player queue mutation | UI intent flips queue state via player methods |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | DoS (UX) | swipeRemove gesture arbitration | mitigate | Capture-after-slop + vertical-yields rule prevents stuck/hijacked gestures; sub-slop reaches onclick (no dead taps); pointercancel handled (iOS). Verified by unit test. |
| T-17-05 | DoS (memory) | removeFromQueue/clearQueue | mitigate | Methods only mutate the `queue` array + reset Sets; introduce NO `createObjectURL`. Verify no new blob/object-URL. |
| T-17-06 | Tampering | queue-mutation race vs async regenerate/ensureAhead | mitigate | removeFromQueue/clearQueue re-read `this.queue` at write-time and route through one method; never assign from a closed-over snapshot (Pitfall 1). |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase (RESEARCH Package Legitimacy Audit: zero new deps). slopcheck N/A. |
</threat_model>

<verification>
- `pnpm vitest run src/lib/actions/swipeRemove.test.ts` green (axis-lock/flick/threshold/tap/spring-back/capture-only-in-move).
- `pnpm vitest run src/lib/stores/player.svelte.test.ts` green (removeFromQueue/clearQueue/removedUids exclusion/reset-on-fresh-play).
- `pnpm svelte-check` clean (NowPlaying wiring; consumed clearQueue key resolves).
- `pnpm vitest run` full suite green at wave merge.
- Manual (device, per VALIDATION.md): swipe partial → spring back; swipe full → removes; flick → removes; tap → plays; vertical grip drag → still reorders; Clear → only current survives, then re-grows near end-of-track.
</verification>

<success_criteria>
- QUEUE-05: swipe-to-remove + clear-all work; removed track is session-excluded (D-10); clear keeps only current and re-grows near end-of-track (D-08/D-09).
- QUEUE-02: exhaust auto-generation honored, now excluding removed uids.
- No regression: full vitest suite green; svelte-check clean.
</success_criteria>

<output>
Create `.planning/phases/17-up-next-sourcing-settings-plumbing/17-02-SUMMARY.md` when done.
</output>
