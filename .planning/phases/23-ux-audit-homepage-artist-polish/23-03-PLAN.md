---
phase: 23-ux-audit-homepage-artist-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/home-layout.ts
  - src/lib/services/home-layout.test.ts
  - src/lib/config/defaults.ts
  - src/lib/stores/settings.svelte.ts
  - src/lib/stores/settings.svelte.test.ts
autonomous: true
requirements: [HOME-02]
must_haves:
  truths:
    - "resolveSectionDensity(sectionId, perSectionMap, globalDefault) returns 'compact' or 'comfortable', falling back to the global default for unknown/garbage input (never blanks)"
    - "Home sections render compact BY DEFAULT (compact-by-default ships per D-07) while a persisted homeSectionDensity per-section override map can flip individual sections back to comfortable"
    - "homeSectionDensity persists in openmusic:settings:v1 with the object-not-array load guard and resets with resetHome()"
  artifacts:
    - path: "src/lib/services/home-layout.ts"
      provides: "resolveSectionDensity pure resolver (D-07)"
      contains: "resolveSectionDensity"
    - path: "src/lib/config/defaults.ts"
      provides: "homeSectionDensity default in HOME_DEFAULTS"
      contains: "homeSectionDensity"
    - path: "src/lib/stores/settings.svelte.ts"
      provides: "homeSectionDensity field + load guard + reset"
      contains: "homeSectionDensity"
  key_links:
    - from: "src/lib/stores/settings.svelte.ts"
      to: "src/lib/config/defaults.ts"
      via: "HOME_DEFAULTS.homeSectionDensity init"
      pattern: "homeSectionDensity"
---

<objective>
Add the per-section density plumbing (D-07, HOME-02): a pure `resolveSectionDensity` resolver in `home-layout.ts`, a persisted `homeSectionDensity` override map via the Phase 17 settings pattern, and the compact-by-default behavior. Pure-logic plan — no UI; the home page (Plan 04) and /settings/home UI (also Plan 04) consume this.

Purpose: HOME-02 requires per-section compact mode with settings override; D-07 locks "all compact by default". This is the data/resolution layer everything compact builds on.
Output: extended home-layout resolver + tests, settings field + load guard + reset + tests, defaults entry.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

Tooling: prefix PATH with `$HOME/.nvm/versions/node/v22.22.0/bin` and use pnpm for every check/test command.
</execution_context>

<context>
@.planning/phases/23-ux-audit-homepage-artist-polish/23-CONTEXT.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-PATTERNS.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md

<interfaces>
From src/lib/services/home-layout.ts:
- `export const HOME_SECTIONS = ['liked','downloads','top-hits','top-artists','fav-artists','tags','countries','playlists','history'] as const;`
- `export type HomeSectionId = (typeof HOME_SECTIONS)[number];`
- `export type HomeDensity = 'comfortable' | 'compact';`
- `export function resolveSectionOrder(saved: string[] | undefined): HomeSectionId[]` (line 131 — corrupt-input fallback posture to mirror)
- `export function resolveSubset(saved, pool)` (line 165 — unknown-key-dropped posture)

From src/lib/config/defaults.ts:
- `HOME_DEFAULTS` group (lines 117-127) including `homeDensity: 'comfortable' as HomeDensity` (line 124), `homeShelfSize`, `homeSectionOrder`
- `enabledSources: {} as Partial<Record<SourceId, boolean>>` (line 82 — object-not-array map precedent)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolveSectionDensity resolver + compact-by-default (D-07)</name>
  <files>src/lib/services/home-layout.ts, src/lib/services/home-layout.test.ts</files>
  <read_first>
    - src/lib/services/home-layout.ts (resolveSectionOrder ~131-150, resolveSubset ~165-180, HomeDensity type ~206 — mirror the corrupt-input fallback posture)
    - src/lib/services/home-layout.test.ts (node-test style to extend)
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-PATTERNS.md "resolveSectionDensity" section
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md §4.2
  </read_first>
  <behavior>
    - resolveSectionDensity('tags', { tags: 'comfortable' }, 'compact') === 'comfortable' (per-section override wins)
    - resolveSectionDensity('tags', {}, 'compact') === 'compact' (falls to global default)
    - resolveSectionDensity('tags', { tags: 'garbage' as any }, 'compact') === 'compact' (invalid value → global default)
    - resolveSectionDensity('tags', undefined, 'compact') === 'compact' (missing map → global default; never blanks)
  </behavior>
  <action>Add `export function resolveSectionDensity(sectionId: HomeSectionId, perSection: Partial<Record<HomeSectionId, HomeDensity>> | undefined, globalDefault: HomeDensity): HomeDensity` to `home-layout.ts`. Implementation: `const v = perSection?.[sectionId]; return v === 'comfortable' || v === 'compact' ? v : globalDefault;` — mirroring resolveSubset's "unknown/garbage → fallback, never blank" posture. The compact-by-default requirement (D-07 "all compact by default") ships by the home page passing `'compact'` as the `globalDefault` argument (Plan 04 wires this; this plan provides the resolver). Do NOT change the persisted `homeDensity` field semantics — it stays as-is. Add tests to `home-layout.test.ts` covering the four behavior cases above.</action>
  <verify>
    <automated>PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm vitest run src/lib/services/home-layout.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/services/home-layout.ts exports `function resolveSectionDensity(`
    - resolveSectionDensity returns the per-section value only when it is exactly 'comfortable' or 'compact', else the globalDefault
    - home-layout.test.ts has new cases for override-wins, missing-map-fallback, and garbage-value-fallback; all pass
  </acceptance_criteria>
  <done>resolveSectionDensity is a pure, node-tested resolver with corrupt-input fallback; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: homeSectionDensity settings plumbing (Phase 17 pattern, D-07)</name>
  <files>src/lib/config/defaults.ts, src/lib/stores/settings.svelte.ts, src/lib/stores/settings.svelte.test.ts</files>
  <read_first>
    - src/lib/config/defaults.ts (HOME_DEFAULTS lines 117-127; enabledSources object default line 82)
    - src/lib/stores/settings.svelte.ts (the WR-10 3-touch-point pattern: field init ~169-189, load guard ~221-224 enabledSources object-not-array guard, save() JSON ~341-350, resetHome() ~451-465, KEY line 56)
    - src/lib/stores/settings.svelte.test.ts (load-guard + reset test style to extend)
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-PATTERNS.md "settings.svelte.ts + defaults.ts" section
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md §4.2
  </read_first>
  <behavior>
    - Fresh load (no stored value) → homeSectionDensity === {} (empty override map)
    - Stored `{ tags: 'comfortable' }` → loaded as `{ tags: 'comfortable' }`
    - Stored homeSectionDensity that is an Array or non-object → coerced to {} (object-not-array guard)
    - resetHome() restores homeSectionDensity to {}
  </behavior>
  <action>In `defaults.ts` HOME_DEFAULTS add `homeSectionDensity: {} as Partial<Record<HomeSectionId, HomeDensity>>,` (ensure HomeSectionId/HomeDensity imports exist — they back homeSectionOrder/homeDensity already). In `settings.svelte.ts` apply the documented 3-touch-point WR-10 pattern: (1) field init `homeSectionDensity = $state<Partial<Record<HomeSectionId, HomeDensity>>>({ ...HOME_DEFAULTS.homeSectionDensity });`; (2) load guard copying the enabledSources object-not-array guard verbatim — `this.homeSectionDensity = v.homeSectionDensity && typeof v.homeSectionDensity === 'object' && !Array.isArray(v.homeSectionDensity) ? (v.homeSectionDensity as Partial<Record<HomeSectionId, HomeDensity>>) : {};`; (3) add `homeSectionDensity` to the save() JSON block and to resetHome(). Persists under the existing `KEY` (`openmusic:settings:v1`). Add tests to settings.svelte.test.ts covering: default {}, valid map load, array/non-object coercion to {}, resetHome restores {}.</action>
  <verify>
    <automated>PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm vitest run src/lib/stores/settings.svelte.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/config/defaults.ts HOME_DEFAULTS contains `homeSectionDensity:`
    - src/lib/stores/settings.svelte.ts declares a `homeSectionDensity` $state field, a load guard rejecting Array/non-object values, and resets it in resetHome()
    - settings.svelte.test.ts asserts default {}, valid-map load, array→{} coercion, and reset→{}; all pass
  </acceptance_criteria>
  <done>homeSectionDensity is plumbed through defaults → field → load guard → save → reset, persists in openmusic:settings:v1, with passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → settings load | persisted homeSectionDensity is attacker-influenceable (user-editable storage) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06 | Tampering | settings load (corrupt homeSectionDensity) | mitigate | Object-not-array load guard rejects Array/non-object stored values, coercing to {}; resolveSectionDensity rejects non-enum values per section. Acceptance: array/non-object stored value loads as {} (tested); garbage per-section value resolves to globalDefault (tested). |
| T-23-07 | Denial of Service | resolveSectionDensity | mitigate | Resolver never throws and never blanks the render (returns globalDefault on any invalid input). Acceptance: undefined map and garbage value both return globalDefault. |
</threat_model>

<verification>
- `pnpm check` exits 0.
- home-layout.test.ts and settings.svelte.test.ts pass.
- Stored Array/non-object homeSectionDensity loads as {} (T-23-06).
</verification>

<success_criteria>
- resolveSectionDensity pure resolver with corrupt-input fallback (node-tested).
- homeSectionDensity persists/loads/resets via the Phase 17 pattern with object-not-array guard.
- Compact-by-default is achievable by passing 'compact' as globalDefault (consumed by Plan 04).
</success_criteria>

<output>
Create `.planning/phases/23-ux-audit-homepage-artist-polish/23-03-SUMMARY.md` when done.
</output>
