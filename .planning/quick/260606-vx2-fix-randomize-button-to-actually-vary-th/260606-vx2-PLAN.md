---
phase: quick-260606-vx2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/discovery.ts
  - src/lib/services/discovery.test.ts
  - src/routes/(app)/+page.svelte
autonomous: false
requirements: [VX2-RANDOMIZE]
must_haves:
  truths:
    - "Pressing Randomize on the home page changes the visible discovery surface (different tiles and/or different shelf order) on consecutive presses"
    - "Randomize ignores the existing shelf cache and fetches a fresh, varied arrangement, then saves the new arrangement to cache"
    - "A pure shuffle/variation helper is unit-tested for correct permutation behavior"
    - "WR-02 (button stays enabled during background revalidate), WR-04 (refreshGen generation guard), and never-throws posture are all preserved"
    - "The no-key buildDiversePicks fallback path is unchanged"
  artifacts:
    - path: "src/lib/services/discovery.ts"
      provides: "Pure shuffle() + pickRandomPage() variation helpers; page-param plumbing on discovery builders"
      contains: "export function shuffle"
    - path: "src/lib/services/discovery.test.ts"
      provides: "Unit tests for shuffle (permutation invariants) and pickRandomPage (bounded integer)"
      contains: "shuffle"
    - path: "src/routes/(app)/+page.svelte"
      provides: "Randomize wired to varied refresh (random page + tile shuffle + shelf-order reshuffle, cache-bypass)"
      contains: "refresh(true, false, true)"
  key_links:
    - from: "src/routes/(app)/+page.svelte"
      to: "src/lib/services/discovery.ts"
      via: "import shuffle + page-param builders"
      pattern: "shuffle"
    - from: "src/lib/services/lastfm.ts builders"
      to: "/api/lastfm/discovery ?page="
      via: "optional page arg → existing edge passthrough"
      pattern: "page"
---

<objective>
Fix the 隨機推薦 / Randomize button on the home page so it genuinely VARIES the discovery surface on each press. Currently `refresh(true)` re-fetches the SAME deterministic Last.fm endpoints with FIXED inputs (`getChartTopTracks(PER_SHELF)`, fixed `DISCOVERY_TAGS`/`DISCOVERY_COUNTRIES` fan-out), so every press re-renders identical tiles → the button appears broken.

Purpose: Make Randomize a real "give me something new" action — different tracks and/or different ordering on every press — without touching caching robustness, the WR-02/WR-04 guards, the never-throws posture, or the edge security boundary. Keep this self-contained so the SEPARATE upcoming "user-configurable DISCOVERY_TAGS/COUNTRIES + layout reorder/hide" task does not collide.

Output: A pure, tested variation helper in `discovery.ts`; an optional `page` param threaded through the discovery builders (the edge already accepts `page`); and a `randomize` path in `+page.svelte`'s `refresh()` that varies inputs, bypasses the shelf cache, and saves the new arrangement.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md

# The home page — Randomize button (~337 onclick), refresh() flow (~202-279),
# cache load/save/apply (~165-191), constants PER_SHELF=18 / FANOUT_CAP=4.
@src/routes/(app)/+page.svelte

# Discovery service — existing Fisher-Yates pattern lives in picks.ts sample();
# this file already owns DISCOVERY_TAGS / DISCOVERY_COUNTRIES / mapWithConcurrency
# and has a sibling test (discovery.test.ts). Natural home for the variation helper.
@src/lib/services/discovery.ts
@src/lib/services/discovery.test.ts

# The discovery builders to extend with an optional `page` param.
@src/lib/services/lastfm.ts

# The edge proxy ALREADY reads + passes through `page` (lines 239, 249) — NO edge
# change required. Confirm before assuming; do not widen the allow-list.
@src/routes/api/lastfm/discovery/+server.ts

<interfaces>
<!-- Contracts the executor needs. Already verified against the codebase. -->

From src/lib/services/lastfm.ts (current signatures — extend with optional trailing page):
```typescript
export async function getChartTopTracks(limit = 30): Promise<DiscoveryTrack[]>;
export async function getChartTopArtists(limit = 30): Promise<DiscoveryArtist[]>;
export async function getTagTopTracks(tag: string, limit = 30): Promise<DiscoveryTrack[]>;
export async function getGeoTopTracks(country: string, limit = 30): Promise<DiscoveryTrack[]>;
// fetchList<T>(params: Record<string, string>) → only includes a param key when truthy.
```

From src/routes/api/lastfm/discovery/+server.ts (ALREADY supports page — no change):
```typescript
const page = url.searchParams.get('page') ?? '';
if (page) upstream += `&page=${encodeURIComponent(page)}`;  // bounded passthrough
```

From src/lib/services/picks.ts (existing Fisher-Yates to mirror — pure, no Math.random seed):
```typescript
function sample<T>(arr: T[], n: number): T[] { /* Fisher-Yates then slice(0,n) */ }
```

From src/routes/(app)/+page.svelte (current refresh signature + Randomize call):
```typescript
let refreshGen = 0;
async function refresh(seedQueue = true, background = false): Promise<void>;
// Randomize button: onclick={() => refresh(true)}
// Builders called with FIXED inputs inside Promise.all (lines 209-218).
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure variation helpers + page-param plumbing</name>
  <files>src/lib/services/discovery.ts, src/lib/services/discovery.test.ts, src/lib/services/lastfm.ts</files>
  <behavior>
    shuffle&lt;T&gt;(arr): returns a NEW array (input not mutated) that is a permutation of the input — same length, same multiset of elements. Empty array → empty array; single element → equal single-element array. Uses Fisher-Yates with Math.random (mirrors picks.ts sample()).
    pickRandomPage(max): returns an INTEGER in [1, max] inclusive (Last.fm pages are 1-based). max&lt;=1 → always 1. Never returns 0, never &gt; max, never a fraction.
    getChartTopTracks/getTagTopTracks/getGeoTopTracks: when called with a page argument &gt; 1, the request carries that page; page omitted or 1 → no page key in the query (preserves the existing default request + edge cache key for cold/background loads).
  </behavior>
  <action>
    In `src/lib/services/discovery.ts`, add two PURE exported helpers next to `mapWithConcurrency`:
    (1) `shuffle<T>(arr: T[]): T[]` — copy-then-Fisher-Yates (identical algorithm to picks.ts `sample()` but returns the full permutation, no slice). MUST NOT mutate the input. (2) `pickRandomPage(max: number): number` — return `Math.max(1, Math.floor(Math.random() * Math.max(1, Math.floor(max))) + 1)`; clamp so the result is always an integer in [1, max]. Document both as the Randomize variation primitives (used to vary tile order, shelf order, and which Last.fm chart page is fetched). Do NOT add seeding, do NOT pull in any dependency.
    In `src/lib/services/lastfm.ts`, add an OPTIONAL trailing `page = 1` parameter to `getChartTopTracks`, `getTagTopTracks`, and `getGeoTopTracks` (NOT getChartTopArtists — chart.gettopartists varies less usefully by page and artist tiles are reshuffled client-side; keep it limit-only). Thread it through `fetchList` ONLY when `page > 1` (so a default/cold call produces the EXACT same request + edge cache key as today — preserves the charts-1h / tags-6h cache and never breaks the absent-key empty path). The edge already reads/passes `page` (verify +server.ts lines 239/249 — no edge change). Keep every builder's never-throws / `[] on failure` posture intact.
    In `src/lib/services/discovery.test.ts`, add a `describe('shuffle / pickRandomPage variation helpers')` block: shuffle returns a same-length permutation with the same multiset of elements and does NOT mutate the input (assert input array order unchanged after call); shuffle of `[]` → `[]` and `[x]` → `[x]`; pickRandomPage stays an integer within [1, max] across many iterations (e.g. loop 200x asserting `Number.isInteger` && `>=1` && `<=max`), and `pickRandomPage(1)` / `pickRandomPage(0)` always return 1. Do NOT inline implementation code in the action — write the tests in the test file directly.
  </action>
  <verify>
    <automated>npx vitest run src/lib/services/discovery.test.ts</automated>
  </verify>
  <done>shuffle + pickRandomPage exported and tested (permutation + bounded-integer invariants green); the three top-track builders accept an optional page that only alters the request when &gt;1; existing discovery.test.ts cases still pass; no edge change.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Randomize to a varied, cache-bypassing refresh</name>
  <files>src/routes/(app)/+page.svelte</files>
  <action>
    Add a third parameter to `refresh`: `async function refresh(seedQueue = true, background = false, randomize = false)`. When `randomize` is true, vary the discovery inputs inside the existing `Promise.all` (lines ~209-218) WITHOUT changing the never-throws structure:
    (1) Pick a fresh random page per chart/tag/geo call using `pickRandomPage` — choose a small bound so pages still have data (e.g. `pickRandomPage(5)` for charts; the same or a per-shelf draw for tag/geo). Pass it as the new trailing `page` arg to `getChartTopTracks` / `getTagTopTracks` / `getGeoTopTracks`. On a NON-randomize call, pass nothing (default page 1) so cold-start + background revalidate keep today's cache-friendly request.
    (2) After building the `tags` and `countries` Shelf arrays, when `randomize` is true: `shuffle()` the ORDER of `tagShelves` and `countryShelves` (shelf order only — NOT which tags/countries; the tag/country LISTS stay the fixed constants, leaving the upcoming user-config task untouched), and `shuffle()` the `tracks` array WITHIN each shelf plus `topHits` and `topArtists` so even a page that returns overlapping tracks renders in a visibly different order.
    (3) Import `shuffle` and `pickRandomPage` from `$lib/services/discovery` (alongside the existing `mapWithConcurrency, DISCOVERY_TAGS, DISCOVERY_COUNTRIES` import).
    Wire the Randomize button: change `onclick={() => refresh(true)}` (~line 337) to `onclick={() => refresh(true, false, true)}` — `background=false` is REQUIRED here so the press is NOT silent, BUT keep the button enabled: the button is currently `disabled={loading}`; a Randomize press sets `loading=true` only briefly during the fetch. To preserve WR-02 (button stays usable / not locked into a permanent "Loading…"), pass `background=false` so the user sees the refresh complete and `loading` flips back in the `finally`. Do NOT change the error retry button (keep `refresh(true)`). The shuffle/page variation happens regardless of the cache because `refresh` always FETCHES — Randomize does not read `loadCache()`; it overwrites the cache via the existing `saveCache(...)` call with the freshly-shuffled arrangement. Confirm the existing `saveCache` writes the post-shuffle `topHits/topArtists/tagShelves/countryShelves` (it already saves the assigned state vars — ensure the shuffle is applied to those vars BEFORE saveCache runs, or shuffle the local `hits/artists/tags/countries` before both assignment and save so cache + UI match). Leave the `else` D-06 buildDiversePicks fallback branch UNCHANGED (it already randomizes via its own sample()). Preserve the `refreshGen` (WR-04) guard exactly — the `gen !== refreshGen` early-returns must remain.
  </action>
  <verify>
    <automated>npx vitest --run && npx svelte-check --tsconfig ./tsconfig.json</automated>
  </verify>
  <done>Randomize calls `refresh(true, false, true)`; randomize varies chart/tag/geo page + shuffles shelf order + within-shelf track order + topHits/topArtists; non-randomize (cold/background) calls keep page 1 and the existing request; saveCache persists the shuffled arrangement; WR-04 guard and never-throws structure unchanged; full test suite + svelte-check clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Randomize (隨機推薦) button now fetches a random Last.fm chart/tag/geo page and shuffles shelf + tile order on each press, bypassing the identical cached arrangement and saving the new one. Tile-order shuffle guarantees visible variation even when a page returns overlapping tracks.</what-built>
  <how-to-verify>
    Tests-green is NOT sufficient proof (the v7k cover task looked done in tests but needed runtime confirmation — Last.fm calls only work with LASTFM_KEY set locally or on the deployed site).
    1. Run the app against a Last.fm key: either `LASTFM_KEY=<key> npx wrangler pages dev .svelte-kit/cloudflare` after `npm run build`, OR test on https://openmusic.pages.dev (prod has the key).
    2. Open the home page; let the discovery shelves load.
    3. Press 隨機推薦 / Randomize. Confirm the visible tiles AND/OR the order of shelves visibly CHANGE.
    4. Press it 3-4 more times — each press should produce a noticeably different surface (not the identical grid every time).
    5. Confirm the Randomize button does NOT get stuck on "Loading…" / stays usable between presses (WR-02).
    6. Reload the page — the LAST randomized arrangement should render from cache (saveCache persisted it), then a background revalidate runs without locking the button.
    7. (Optional no-key check) With no LASTFM_KEY, confirm the home page still shows the buildDiversePicks fallback grid and Randomize still reshuffles it (unchanged path).
  </how-to-verify>
  <resume-signal>Type "approved" if consecutive Randomize presses visibly vary the home surface and the button stays usable, or describe what still looks identical / broken.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/lastfm/discovery | client passes a `page` query value that the edge interpolates into the upstream Last.fm URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-vx2-01 | Tampering | `page` query passthrough on /api/lastfm/discovery | mitigate | `pickRandomPage` produces a BOUNDED positive integer (clamped [1,max]); the edge already `encodeURIComponent`s `page` (line 249) and only appends it — no command construction, allow-list of methods unaffected. Client never sends an attacker-controlled string. |
| T-vx2-02 | Information disclosure | LASTFM_KEY on the edge | accept | No change to the edge key handling; the key is still injected server-side and never reaches the client. Adding a `page` builder arg touches client builders only. |
| T-vx2-03 | Denial of service | extra Last.fm fan-out from random pages | accept | Randomize fetches the SAME number of shelves (FANOUT_CAP=4 unchanged); a random page is one request per existing shelf, edge-cached per (method+params+page). No new fan-out width. |
</threat_model>

<verification>
- `npx vitest --run` — full suite green (existing + new shuffle/pickRandomPage cases).
- `npx svelte-check --tsconfig ./tsconfig.json` — 0 errors.
- Runtime (checkpoint): consecutive Randomize presses visibly vary the home discovery surface against a Last.fm key; button stays usable; reload renders the last randomized arrangement from cache.
- No edge endpoint change (page passthrough pre-existing); no new npm dependency; no settings/config system introduced.
</verification>

<success_criteria>
- Pressing Randomize produces a visibly different discovery surface on consecutive presses (random page + shelf-order shuffle + within-shelf tile shuffle).
- Randomize bypasses the identical cached arrangement (always fetches) and saves the freshly-shuffled arrangement to cache.
- Pure `shuffle` + `pickRandomPage` helpers are unit-tested (permutation invariants + bounded integer).
- WR-02 (button usable), WR-04 (refreshGen guard), never-throws (builders → []), and the D-06 buildDiversePicks fallback are all preserved.
- Scope stays self-contained: DISCOVERY_TAGS/COUNTRIES lists are NOT made configurable here (left for the separate upcoming task); only shelf/tile ORDER and chart page vary.
</success_criteria>

<output>
Create `.planning/quick/260606-vx2-fix-randomize-button-to-actually-vary-th/260606-vx2-SUMMARY.md` when done.
</output>
