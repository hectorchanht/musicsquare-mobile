# Phase 999.1: v2.0 Native (Capacitor) migration — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-11
**Phase:** 999.1-v2-0-native-capacitor-migration
**Areas discussed:** Wrapper strategy, Background audio, Build & signing, Downloads/offline
**Invocation note:** User narrowed phase scope at invocation: "lets make a android app quickly now, just apk, no play store" → Android-only quick-APK slice, iOS deferred.

---

## Wrapper strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Remote URL shell | Capacitor server.url → deployed Pages site; APK in hours; needs network, no offline | |
| Bundled static + existing API | adapter-static SPA bundled in APK; API_BASE → existing Pages /api routes + CORS; ~2-4 days; no worker extraction | ✓ |
| Full assessment plan | API extraction to Hono worker + dual-build; ~1-2 weeks | |

**User's choice:** Bundled static + existing API

---

## Background audio

| Option | Description | Selected |
|--------|-------------|----------|
| Must work, full (Recommended) | Foreground service + media notification plugin, lock-screen controls; +3-5 days | ✓ |
| Must keep playing, minimal | Keep audio alive on lock, skip media notification for v0; ~1-2 days | |
| Ship v0 without, test first | Plain APK first, decide after device testing | |

**User's choice:** Must work, full

---

## Build & signing

| Option | Description | Selected |
|--------|-------------|----------|
| Local build first (Recommended) | Android Studio/gradle locally, self-signed keystore, CI deferred | |
| GitHub Actions from day one | Tag → build + sign (keystore in secrets) → APK on GitHub Releases | ✓ |
| Local now, CI later in phase | Prove local, add Actions as final task | |

**User's choice:** GitHub Actions from day one

### Updates channel

| Option | Description | Selected |
|--------|-------------|----------|
| Manual reinstall (Recommended) | Rebuild + reinstall when wanted; zero infra | |
| Obtainium via GitHub Releases | Auto-detects new APK versions; needs CI + version discipline | ✓ |
| In-app update check | App pings Releases API — new capability | |

**User's choice:** Obtainium via GitHub Releases

---

## Downloads/offline

| Option | Description | Selected |
|--------|-------------|----------|
| Keep IndexedDB (Recommended) | Zero work, blob-store.ts unchanged; swap deferred | |
| Filesystem swap now | @capacitor/filesystem + capacitor-blob-writer behind blob-store.ts; +2-3 days | ✓ |

**User's choice:** Filesystem swap now

### File location

| Option | Description | Selected |
|--------|-------------|----------|
| App-private storage (Recommended) | Data dir, no permissions, invisible to file manager | |
| Public Music folder | User-visible, other apps can read; MediaStore/scoped-storage work | ✓ |

**User's choice:** Public Music folder

---

## Claude's Discretion

- SPA fallback/prerender details, CORS implementation approach
- Media-controls/foreground-service plugin choice (research)
- Version scheme, min SDK, icon/splash, safe-area

## Deferred Ideas

- iOS sideload/AltStore slice
- API extraction to standalone Hono worker
- In-app update check
