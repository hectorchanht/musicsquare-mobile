---
phase: 999.1-v2-0-native-capacitor-migration
plan: 03
type: execute
wave: 3
depends_on: ["999.1-02"]
files_modified:
  - src/lib/services/blob-store.ts
  - src/lib/services/blob-store.test.ts
  - package.json
autonomous: false   # Task 1 is a blocking storage-location decision checkpoint (public Music vs app-private — D-11)
requirements: []   # backlog phase — covers D-10 (filesystem swap) and D-11 (download storage location)
user_setup:
  - service: android-device
    why: "The native filesystem download path (write → playable-offline → survives backgrounding) can only be validated on a physical Android device; unit tests cover the platform-switch branch with mocked Capacitor, not real file I/O."
    env_vars: []
    dashboard_config:
      - task: "Connect a physical Android device with USB debugging; have a file-manager app installed to confirm a download lands in the chosen directory and is visible (D-11 public-Music) or app-private (fallback)."
        location: "Android device Developer Options + a file-manager app"

must_haves:
  truths:
    - "On web (Capacitor.isNativePlatform() false), blob-store keeps the EXACT existing IndexedDB backend — web download/offline-play behavior is byte-identical to today (D-10)"
    - "On native, blob-store put/get/del route to capacitor-blob-writer + @capacitor/filesystem behind the SAME put/get/del signatures and the SAME never-throws posture (resolve false/null/void, never reject) (D-10)"
    - "The blobStore namespace export shape is unchanged so all 4 consumers (player.svelte.ts x4 call sites, library.svelte.ts, TrackMenu.svelte, player.svelte.test.ts) compile and pass without edits (D-10)"
    - "The storage-location decision (public Music D-11 via a Kotlin MediaStore bridge vs the zero-dep app-private fallback) is explicitly re-confirmed by the user before any MediaStore plugin is adopted (D-11, RESEARCH Pitfall 2)"
    - "The existing ~626-test suite stays green and pnpm check passes (web must not regress)"
  artifacts:
    - path: "src/lib/services/blob-store.ts"
      provides: "Platform-switched blob backend — web IDB unchanged / native filesystem"
      contains: "isNativePlatform"
    - path: "src/lib/services/blob-store.test.ts"
      provides: "Unit coverage: native branch calls filesystem backend, never-throws sentinel on every error path (Wave 0 gap)"
  key_links:
    - from: "src/lib/services/blob-store.ts"
      to: "capacitor-blob-writer"
      via: "native put() writes the Blob to the filesystem via blob-writer"
      pattern: "blob-writer|write_blob|writeFile"
    - from: "src/lib/services/blob-store.ts"
      to: "@capacitor/core"
      via: "Capacitor.isNativePlatform() platform branch"
      pattern: "isNativePlatform"
---

<objective>
Swap the `blob-store.ts` backend behind a platform branch (D-10): web keeps the existing IndexedDB store byte-for-byte; native writes downloaded audio Blobs to the filesystem via `capacitor-blob-writer` + `@capacitor/filesystem`, preserving the identical `put/get/del` signatures and never-throws posture so all 4 consumers are untouched. Before committing to a storage directory, re-confirm the D-11 public-Music decision with the user — RESEARCH Pitfall 2 found that public `Music/` on Android 10+ requires a custom Kotlin MediaStore bridge (no maintained npm plugin), while the app-private dir is zero-dep and ships immediately.

Purpose: Downloaded songs must play offline inside the APK. The web path must not regress. The public-Music requirement is disproportionately expensive relative to the "quick APK" goal, so the user must consciously re-confirm it (or accept the documented fallback) before that cost is incurred.

Output: platform-switched `blob-store.ts` + native-branch unit tests; a recorded storage-location decision.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

**Tooling (MANDATORY on every command):** prefix every node/pnpm/npx command with `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"` and use pnpm. Single-file test: `pnpm test:unit -- <name>`. Full gate: `pnpm test && pnpm check`. Pin `@capacitor/filesystem@8.1.2`, `capacitor-blob-writer@1.1.20`.
</execution_context>

<context>
@.planning/phases/999.1-v2-0-native-capacitor-migration/999.1-RESEARCH.md
@.planning/phases/999.1-v2-0-native-capacitor-migration/999.1-PATTERNS.md
@.planning/phases/999.1-v2-0-native-capacitor-migration/999.1-CONTEXT.md
@.planning/phases/999.1-v2-0-native-capacitor-migration/999.1-02-SUMMARY.md

<interfaces>
<!-- Extracted from codebase 2026-06-11 — executor uses these directly, no exploration needed. -->

src/lib/services/blob-store.ts — the seam to preserve (never-throws + lazy-cached-open):
- `import { browser } from '$app/environment';`
- module consts: `DB_NAME='openmusic-blobs'`, `STORE='tracks'`, `VERSION=1`
- `function openDb(): Promise<IDBDatabase | null>` — `if (!browser) return null`; `if (typeof indexedDB === 'undefined') return null`; lazy + cached (`openPromise`); resolve(null) on every error path, never reject
- `export async function put(uid: string, blob: Blob): Promise<boolean>` — false on `!uid`/no-db/any failure; true if write landed
- `export async function get(uid: string): Promise<Blob | null>` — null on miss/error
- `export async function del(uid: string): Promise<void>` — silent on success/miss/failure
- `export const blobStore = { put, get, del };` — THE export consumers import

Consumers that MUST keep compiling unchanged (do not alter the signature/namespace):
- src/lib/stores/player.svelte.ts:363,433,1301,1395 — all `blobStore.get(uid).catch(() => null)`
- src/lib/stores/library.svelte.ts:169 — `void blobStore.del(uid)`
- src/lib/components/TrackMenu.svelte:124 — `await blobStore.put(r.uid, blob)`
- src/lib/stores/player.svelte.test.ts:26 — mocks `blobStore: { get, put, del }`

Native backend libs (RESEARCH Standard Stack, VERIFIED on npm 2026-06-11):
- `@capacitor/filesystem@8.1.2` (peer @capacitor/core >=8) — file writes
- `capacitor-blob-writer@1.1.20` (peer filesystem >=1) — streams Blob → disk WITHOUT base64 (avoids 33% bloat + memory spike; @capacitor/filesystem.writeFile base64-encodes)
- `Capacitor.isNativePlatform()` from `@capacitor/core` — platform branch (PATTERNS "SSR / platform feature-detection guard")

Test analog: src/lib/services/deezer.test.ts (vi.mock + vi.stubGlobal + never-throws asserts); header-comment style deezer.test.ts:1-13.

STORAGE-DIR (D-11, RESEARCH Pitfall 2 — the decision point):
- PUBLIC Music/ (D-11 locked, user re-confirmed 2026-06-11): Android 10+ scoped storage forbids direct public-dir writes; @capacitor/filesystem Directory.Documents/ExternalStorage only work on Android 9 and older. Needs a MediaStore API bridge. `capacitor-mediastore` (odion-cloud) is NOT on npm → unverified git/source dependency (no slopcheck), single-maintainer — supply-chain risk. Alternatively a custom Kotlin MediaStore bridge in android/.
- APP-PRIVATE fallback (zero-dep): Directory.Data / Directory.External (app-scoped). No MediaStore, no permissions on any Android version. capacitor-blob-writer + @capacitor/filesystem handle it directly. Files deleted on uninstall, NOT user-visible in other apps. Shortest path to a working download feature.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Re-confirm download storage location — public Music (D-11) vs app-private fallback</name>
  <decision>Where do downloaded audio files land on Android, and therefore which native backend does blob-store implement?</decision>
  <context>D-11 is LOCKED to the public `Music/` folder and the user re-confirmed it on 2026-06-11 after research. RESEARCH Pitfall 2 then established the concrete cost: on Android 10+ (the entire realistic install base), writing to public `Music/` requires the MediaStore API, which neither `@capacitor/filesystem` nor `capacitor-blob-writer` expose. The only options are (a) an unverified non-npm community plugin (`capacitor-mediastore`, odion-cloud — not on npm, no slopcheck, single maintainer) or (b) a custom Kotlin MediaStore bridge added to `android/`. Both contradict the "quick APK" bias. The prompt explicitly authorized surfacing the app-private fallback "if disproportionately painful." It is. This checkpoint forces a conscious re-confirmation BEFORE the native backend is built, because the chosen path determines Task 2's implementation and a later device plan's scope.</context>
  <read_first>
    - .planning/phases/999.1-v2-0-native-capacitor-migration/999.1-RESEARCH.md (Pitfall 2 lines 289-296; Assumptions A3; Open Questions 2)
    - .planning/phases/999.1-v2-0-native-capacitor-migration/999.1-CONTEXT.md (D-11, lines 33-34 — the locked decision + 2026-06-11 re-confirmation)
  </read_first>
  <options>
    <option id="public-music-mediastore">
      <name>Public Music/ via a custom Kotlin MediaStore bridge (honors D-11 as locked)</name>
      <pros>User-visible in file managers and other apps; survives uninstall; matches the locked, re-confirmed decision. No unverified npm dependency if the bridge is hand-written in android/.</pros>
      <cons>Requires net-new Kotlin (MediaStore insert + write + scoped-storage handling + per-Android-version quirks) — a dedicated device-gated plan of its own; largest scope add in the phase; contradicts the "quick APK" speed goal.</cons>
    </option>
    <option id="public-music-community-plugin">
      <name>Public Music/ via the community capacitor-mediastore plugin (git/source dep)</name>
      <pros>Less hand-written native code than the Kotlin bridge.</pros>
      <cons>NOT on the npm registry — git/source dependency, no slopcheck coverage, single maintainer, unverified Cap-8 compat: a real supply-chain + maintenance risk on a download feature.</cons>
    </option>
    <option id="app-private-fallback">
      <name>App-private external dir (Directory.Data/External — zero-dep fallback)</name>
      <pros>Zero extra plugins; capacitor-blob-writer + @capacitor/filesystem handle it directly; no permissions on any Android version; ships now; aligns with "quick APK."</pros>
      <cons>Files NOT user-visible in other apps; deleted on uninstall; does not honor the literal D-11 public-Music requirement — requires the user to consciously relax D-11.</cons>
    </option>
  </options>
  <resume-signal>Select: public-music-mediastore (Task 2 writes the filesystem put/get/del against an app-private dir now AND a follow-up Kotlin-MediaStore device plan is added), public-music-community-plugin (gate the git dep behind a separate checkpoint:human-verify before adopt), or app-private-fallback (Task 2 targets Directory.Data/External, no MediaStore work, D-11 relaxed — confirm the relaxation explicitly).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Platform-switch blob-store backend (web IDB unchanged / native filesystem) + native-branch tests (D-10)</name>
  <files>src/lib/services/blob-store.ts, src/lib/services/blob-store.test.ts, package.json</files>
  <read_first>
    - src/lib/services/blob-store.ts (the FULL existing module — every put/get/del path + the never-throws/lazy-open posture must be preserved on the web branch byte-for-byte)
    - src/lib/services/deezer.test.ts (vi.mock + vi.stubGlobal + afterEach restore + never-throws assertion style to mirror)
    - src/lib/stores/player.svelte.test.ts:26 (the existing blobStore mock shape — the namespace export must not change or this breaks)
    - .planning/phases/999.1-v2-0-native-capacitor-migration/999.1-RESEARCH.md (Pitfall 2 for the directory choice resolved in Task 1; capacitor-blob-writer rationale lines 270)
  </read_first>
  <behavior>
    - With Capacitor.isNativePlatform() mocked TRUE: put(uid, blob) calls the capacitor-blob-writer/filesystem write backend (not IndexedDB) and resolves true on success
    - With Capacitor.isNativePlatform() mocked TRUE: get(uid) reads the file back as a Blob, returns null on a miss
    - With Capacitor.isNativePlatform() mocked TRUE: del(uid) deletes the file and resolves void even when the file is absent
    - Every native error path (write/read/delete throws) resolves to the sentinel (false / null / void) and NEVER rejects — identical posture to the web branch
    - With Capacitor.isNativePlatform() mocked FALSE: the existing IndexedDB code path runs unchanged
  </behavior>
  <action>
Install the native backend deps (Node 22 + pnpm): `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && pnpm add @capacitor/filesystem@8.1.2 capacitor-blob-writer@1.1.20`.

Modify `src/lib/services/blob-store.ts`: at the top of each of `put`/`get`/`del`, branch on `Capacitor.isNativePlatform()` (import `{ Capacitor } from '@capacitor/core'`). When NOT native, fall through to the EXISTING IndexedDB code path verbatim — do not touch `openDb`, the consts, or any IDB logic. When native, route to a small filesystem backend (a private `nativePut/nativeGet/nativeDel` set in the same module): `put` writes the Blob via `capacitor-blob-writer` (`write_blob({ path, blob, directory, recursive: true })`) into the directory chosen in Task 1 under a deterministic filename derived from `uid` (e.g. `downloads/${uid}` — sanitize the uid for filesystem-safe characters); `get` reads it back via `@capacitor/filesystem` and returns a Blob (read as binary, not base64 string into memory where avoidable) or null on miss; `del` deletes it, swallowing not-found. Mirror the web branch's never-throws contract exactly: wrap every native call so it resolves `false`/`null`/void and NEVER rejects. Keep `export const blobStore = { put, get, del };` and all three signatures byte-identical so the 4 consumers are untouched. Use the directory decided in Task 1 (app-private Directory.Data/External, or — if public-music-mediastore was chosen — write to the app-private dir here and leave a clearly-commented TODO marker that the public-Music MediaStore bridge is delivered by the follow-up device plan).

Create `src/lib/services/blob-store.test.ts` mirroring `deezer.test.ts`'s header + `afterEach(() => { vi.restoreAllMocks(); vi.unstubAllGlobals(); })`. `vi.mock('@capacitor/core', ...)` to control `isNativePlatform()`, and `vi.mock('capacitor-blob-writer')` + `vi.mock('@capacitor/filesystem')` to assert the native branch calls them and that every thrown error resolves to the sentinel. Cover all five behaviors above. Do not require a real browser/IDB — the web-branch assertion can rely on the existing SSR guard (`browser` false in the node test env makes openDb resolve null → put returns false without IDB), which proves the web path is reached, not the native one, when isNativePlatform() is false.
  </action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" && pnpm test:unit -- blob-store && pnpm test:unit -- player.svelte && pnpm test && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isNativePlatform" src/lib/services/blob-store.ts` shows the platform branch in put/get/del
    - `grep -n "blob-writer\|capacitor-blob-writer" src/lib/services/blob-store.ts` shows the native write uses capacitor-blob-writer (not base64 writeFile for the audio Blob)
    - `export const blobStore = { put, get, del }` is unchanged; the three function signatures are byte-identical to the original (diff shows only added native branches + imports)
    - blob-store.test.ts asserts all five <behavior> cases (native put/get/del call the FS backend, every error path returns the sentinel, web branch unchanged) and passes
    - `pnpm test:unit -- player.svelte` stays green (consumers untouched) and the full suite + `pnpm check` are green
    - @capacitor/filesystem@8.1.2 + capacitor-blob-writer@1.1.20 in package.json
  </acceptance_criteria>
  <done>blob-store branches on isNativePlatform(); web IDB path verbatim, native path uses capacitor-blob-writer/@capacitor/filesystem with identical never-throws signatures; namespace export unchanged; native-branch tests + full suite + check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView JS → native filesystem (Capacitor bridge) | Audio Blobs written/read across the JS↔native bridge to on-device storage |
| downloaded file → other apps (public Music only) | If public Music/ is chosen, files become readable by any app on the device |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-07 | Tampering | unverified non-npm MediaStore plugin (if public-music-community-plugin chosen) | mitigate | Gate any git/source MediaStore plugin behind a `checkpoint:human-verify` before adopt (RESEARCH Pitfall 2); prefer a hand-written Kotlin bridge or the app-private fallback to avoid the unscanned dependency |
| T-999.1-08 | Information Disclosure | public Music/ files readable by other apps (if public-music chosen) | accept | Downloaded audio is non-sensitive media the user explicitly chose to expose (D-11); no PII; user re-confirmed the public-visibility tradeoff |
| T-999.1-09 | Denial of Service | filesystem write failures crashing playback | mitigate | Native branch preserves the never-throws sentinel posture (resolve false/null/void) so a failed download degrades to CDN playback, never crashes the player (parity with the IDB branch) |
| T-999.1-SC | Tampering | npm installs (@capacitor/filesystem, capacitor-blob-writer) | mitigate | Both are slopcheck `[OK]` / `Approved` in RESEARCH Package Legitimacy Audit (ionic-team + diachedelic, mature) — no `[ASSUMED]`/`[SUS]` checkpoint required |
</threat_model>

<verification>
- `pnpm test && pnpm check` green (existing ~626-test suite + new blob-store native-branch cases) — web build must not regress.
- The download write→playable-offline→survives-backgrounding behavior is DEVICE-GATED (RESEARCH Validation, D-11 manual row) and is verified in the device-test plan, not here.
</verification>

<success_criteria>
- Storage location consciously re-confirmed (Task 1 decision recorded).
- blob-store native branch implemented with identical signatures + never-throws posture; web IDB path unchanged; consumers untouched.
- Native-branch unit tests + full suite + svelte-check green.
</success_criteria>

<output>
Create `.planning/phases/999.1-v2-0-native-capacitor-migration/999.1-03-SUMMARY.md` when done.
</output>
