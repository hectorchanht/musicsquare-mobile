---
phase: quick-260606-v7k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/itunes-cover.ts
  - src/lib/services/itunes-cover.test.ts
  - src/lib/services/cover-cache.ts
  - src/lib/services/cover-backfill.ts
  - src/lib/services/cover-backfill.test.ts
  - src/routes/(app)/+page.svelte
autonomous: false
requirements: [COVER-FB-01, COVER-ARTIST-01]
must_haves:
  truths:
    - "Western-catalog home track tiles (top hits / tag / country shelves) that miss CN-source + CAA show a real album cover via the iTunes fallback instead of a color gradient"
    - "熱門歌手 (top artist) circle tiles show a real artist/album image instead of always a gradient"
    - "A cover that cannot be resolved by any source still degrades to the existing color gradient (never a broken image, never blocks first paint)"
    - "No secret/key is introduced; the new fetch is a direct client-side call to a public no-auth CORS API, concurrency-capped and timeout-bounded"
    - "Covers appear progressively as background resolves land (coverVer bump), with cached covers shown instantly on a warm/second visit (zero re-fetch)"
  artifacts:
    - path: "src/lib/services/itunes-cover.ts"
      provides: "Pure iTunes Search cover resolver: URL builder, 100->600 artwork upgrade, song + artist lookups, never-throws fetch wrapper"
      exports: ["itunesSongCover", "itunesArtistCover", "buildItunesSearchUrl", "upgradeArtwork"]
    - path: "src/lib/services/itunes-cover.test.ts"
      provides: "Unit tests for URL build, artwork upgrade, fallback ordering, abort/timeout, never-throws"
    - path: "src/lib/services/cover-cache.ts"
      provides: "Artist-only cache key (distinct from artist|title track key) + get/set for artist covers"
      contains: "artistCoverCacheKey"
    - path: "src/lib/services/cover-backfill.ts"
      provides: "Track resolveOne extended with iTunes fallback step; new backfillArtistCovers pass"
      contains: "backfillArtistCovers"
    - path: "src/routes/(app)/+page.svelte"
      provides: "Artist-tile cover branch in tileCover + artist rows added to the post-paint backfill"
  key_links:
    - from: "src/lib/services/cover-backfill.ts"
      to: "src/lib/services/itunes-cover.ts"
      via: "fallback after CN searchAll miss"
      pattern: "itunesSongCover"
    - from: "src/routes/(app)/+page.svelte"
      to: "src/lib/services/cover-backfill.ts"
      via: "scheduleBackfill pushes artist rows + calls backfillArtistCovers"
      pattern: "backfillArtistCovers"
    - from: "src/routes/(app)/+page.svelte"
      to: "src/lib/services/cover-cache.ts"
      via: "tileCover artist branch reads getCachedArtistCover"
      pattern: "getCachedArtistCover"
---

<objective>
Make the home "精選推薦" discovery tiles show REAL covers instead of color-block gradients, for the two cases the existing pipeline structurally cannot cover:

- **RC1 — Western catalog gap:** the home shelves are Last.fm GLOBAL charts/tags (overwhelmingly Western). The existing backfill resolves covers only via the CN-source `searchAll → dedupeBest[0].cover` path, which has poor cover coverage for Western tracks → most track tiles stay gradient. Add a Western-friendly, no-auth, CORS-open **iTunes Search API** fallback that runs ONLY when CN-source searchAll AND CAA-by-mbid both miss.
- **RC2 — Artist tiles never backfilled:** `scheduleBackfill` only pushes TRACK rows; artist tiles (熱門歌手) are excluded and Last.fm artist images are deprecated (filtered to null) → artist circles are STRUCTURALLY always gradient. Resolve artist images via the same no-auth iTunes source on a capped, cached, post-paint pass with an artist-ONLY cache key.

Purpose: the prior `rvy` attempt wired the pipeline correctly but did not deliver covers because the cover SOURCE (CN-only) misses Western catalog and never touches artists. This closes both source gaps without a new endpoint, new dependency, or any secret.

Output: a pure, tested `itunes-cover.ts` resolver; an extended `cover-cache.ts` (artist key) + `cover-backfill.ts` (iTunes fallback step + artist pass); and a `+page.svelte` artist-tile cover branch + artist backfill wiring. The color gradient remains the absolute last resort.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<!-- Grounded root-cause analysis (orchestrator already traced the full chain): the
     onMount → applyCache/refresh → scheduleBackfill → backfillCovers → searchAll →
     setCachedCover + coverVer++ → tileCover → <img src> wiring is CORRECT. The edge
     already filters the Last.fm grey-star placeholder (GREY_STAR_HASH) so item.image is
     null for placeholder-only items. cover-cache keys (matchKey) match writer↔reader.
     The failure is a cover-SOURCE gap (CN-only misses Western + never touches artists),
     NOT a wiring/reactivity/cache-key bug. Do NOT replan the pipeline — extend it. -->

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

From src/lib/services/lastfm.ts (the row shapes the backfill consumes):
```typescript
export interface DiscoveryTrack { artist: string; title: string; image: string | null; mbid: string | null; }
export interface DiscoveryArtist { name: string; image: string | null; mbid: string | null; }
```

From src/lib/services/cover-cache.ts (existing track-cover store — extend, do not rewrite):
```typescript
export function coverCacheKey(artist: string, title: string): string; // matchKey(artist,title)
export function getCachedCover(artist: string, title: string): string | null;
export function setCachedCover(artist: string, title: string, url: string): void; // no-op on empty url, never throws
// stored shape: flat Record<string,string> at localStorage 'openmusic:cover-cache:v1'
// uses: import { matchKey } from './match-key';  (matchKey(artist, title) — artist-first, folded)
```

From src/lib/services/cover-backfill.ts (existing track backfill — extend resolveOne, add artist pass):
```typescript
export interface CoverNeed { artist: string; title: string; }
export interface BackfillOpts { signal?: AbortSignal; onResolved?: (key: string, url: string) => void; max?: number; }
export async function backfillCovers(items: CoverNeed[], opts?: BackfillOpts): Promise<void>;
// internals to reuse: CAP=3, DEFAULT_MAX=24, mapWithConcurrency pool, getCachedCover-skip,
// resolveOne = searchAll(`${artist} ${title}`,1) → dedupeBest(...,settings.preferredSource)[0]?.cover
```

From src/lib/services/discovery.ts (capped async pool — pure reuse):
```typescript
export async function mapWithConcurrency<T, R>(items: T[], limit: number, fn: (item: T) => Promise<R>): Promise<R[]>;
// at most `limit` fn() in flight, order-preserving, NEVER throws (per-item reject → undefined slot)
```

From src/lib/services/cover-art.ts (the CAA-by-mbid builder — step 2, unchanged):
```typescript
export function caaReleaseGroupCover(mbid: string | null | undefined): string | null;
```

From src/routes/(app)/+page.svelte (the consumer — extend tileCover + scheduleBackfill):
```typescript
// tileCover order today: item.image → caaReleaseGroupCover(mbid) → getCachedCover(artist,title) → null
// scheduleBackfill today: pushes only TRACK rows with !image && !mbid, then void backfillCovers(rows,{onResolved:()=>coverVer++,max:24})
// coverVer is a $state(0) bumped in onResolved so tileCover() recomputes <img src> as covers land
// hideOnError(e) hides the <img> so the gradient underneath shows (onerror→gradient)
// artist tiles render: <img class="al-cover-img" src={tileCover(a)} ...> where a is DiscoveryArtist (no title)
```
</interfaces>

iTunes Search API reference (chosen over Deezer — see Task 1 rationale):
- Song cover: `https://itunes.apple.com/search?term=<artist title>&entity=song&limit=1` → `results[0].artworkUrl100`
- Artist art: `https://itunes.apple.com/search?term=<artist>&entity=musicArtist&limit=1` returns NO artwork field for the artist entity; instead resolve the artist's top album art via `entity=album&attribute=artistTerm&limit=1` → `results[0].artworkUrl100` (an album cover, the standard "artist image" proxy). Validate this during Task 1 and pick whichever returns artwork; document the choice.
- Artwork upgrade: `artworkUrl100` ends in `…/100x100bb.jpg`; replace `100x100bb` → `600x600bb` for a high-res tile. Do the replacement defensively (only when the `100x100bb` token is present; otherwise return the raw URL).
- No key, CORS-enabled (`Access-Control-Allow-Origin: *`), no rate-limit header contract — so still cap concurrency (reuse CAP=3 + max cap) and bound each call with `AbortSignal.timeout`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure iTunes cover resolver (itunes-cover.ts) + tests</name>
  <files>src/lib/services/itunes-cover.ts, src/lib/services/itunes-cover.test.ts</files>
  <behavior>
    - buildItunesSearchUrl(term, entity, attribute?) → an itunes.apple.com/search URL with term encodeURIComponent'd, entity + limit=1, optional attribute; no raw spaces/special chars leak into the query.
    - upgradeArtwork(url) → replaces the `100x100bb` token with `600x600bb` when present; returns the URL unchanged when the token is absent; returns null for empty/whitespace/null/undefined.
    - itunesSongCover(artist, title, signal?) → fetches entity=song, returns upgradeArtwork(results[0].artworkUrl100) or null; returns null on empty results, non-ok response, malformed JSON, abort, or any throw (never throws).
    - itunesArtistCover(artist, signal?) → resolves an artist image (musicArtist artwork if present, else top-album art via entity=album&attribute=artistTerm), upgraded; null on miss; never throws.
    - All network calls bounded by AbortSignal.timeout (e.g. 6000ms) AND honor a caller-passed signal (combine or short-circuit on signal.aborted).
    - Tests stub global fetch (vi.stubGlobal('fetch', ...)) like discovery.test.ts: assert URL shape, 100→600 upgrade, results[0] selection, empty-results→null, non-ok→null, malformed-JSON→null, thrown-fetch→null, aborted-signal→null. NO live network in tests.
  </behavior>
  <action>Create src/lib/services/itunes-cover.ts as a PURE, testable client-side resolver — the Western-catalog + artist fallback cover SOURCE. Choose **iTunes Search API** over Deezer: no key, CORS `*` open, strongest Western catalog plus decent CN coverage, trivial artwork-size upgrade by string token replacement; Deezer needs object-key artwork selection and weaker text-match on the chart-heavy Western titles this task targets. Document this rationale in the file header (mirror the cover-art.ts / cover-backfill.ts header-comment style: state the source, the no-auth/CORS posture, that it only builds URLs + does a bounded fetch, never throws, never blocks paint).

Export: `buildItunesSearchUrl(term, entity, attribute?)`, `upgradeArtwork(url)`, `itunesSongCover(artist, title, signal?)`, `itunesArtistCover(artist, signal?)`. Build the search term as `${artist} ${title}` (song) / `${artist}` (artist), trimmed, encodeURIComponent'd via URLSearchParams. Bound every fetch with `AbortSignal.timeout(6000)` and respect a caller signal (return null immediately if `signal?.aborted`). On any non-ok / empty `results` / malformed JSON / abort / throw → return null (per-item miss → gradient; mirror the never-throws contract of resolveStub + mapWithConcurrency). For the artist path, during implementation verify whether `entity=musicArtist` returns any artwork; if not, fall back to `entity=album&attribute=artistTerm&limit=1` album art and note the chosen approach in the header. The returned URL is a plain string consumed ONLY as an `<img src>` attribute downstream (never CSS url()), so NO host allow-list / safeImageUrl change is needed (parity with caaReleaseGroupCover, T-nza-02). Do NOT add any npm dependency.

Write itunes-cover.test.ts mirroring cover-art.test.ts (pure URL assertions) + discovery.test.ts (vi.stubGlobal('fetch', ...) for the fetch paths). Cover: URL build + encoding, 100→600 upgrade (and no-token passthrough + empty→null), song happy-path, artist happy-path, empty-results→null, non-ok→null, malformed-JSON→null, fetch-throws→null, aborted-signal→null.</action>
  <verify>
    <automated>cd /Users/laichan/code/tung/musicsquare-mobile && pnpm test -- --run itunes-cover 2>&1 | tail -20</automated>
  </verify>
  <done>itunes-cover.ts exports the four functions; all itunes-cover.test.ts cases pass (URL build/upgrade/fallback-order + never-throws + abort), no new dependency added, no CSS url() interpolation introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire iTunes fallback into track backfill + add artist-cover cache key & pass</name>
  <files>src/lib/services/cover-cache.ts, src/lib/services/cover-backfill.ts, src/lib/services/cover-backfill.test.ts</files>
  <behavior>
    - cover-cache.ts gains an ARTIST-ONLY key distinct from the artist|title track key (so 'Drake' the artist never collides with a 'Drake'|'<title>' track): add artistCoverCacheKey(artist), getCachedArtistCover(artist), setCachedArtistCover(artist,url) reusing the same flat Record store + try/catch null/no-op + matchKey-based folding (e.g. matchKey(artist,'') or a stable 'artist:'-prefixed key — pick one and pin it in a test).
    - cover-backfill resolveOne (track path): after CN searchAll → dedupeBest[0].cover MISSES (null/empty), fall back to itunesSongCover(artist,title,signal). Cache + onResolved fire for whichever resolves. CN path stays the primary cheap step; iTunes is the fallback only.
    - new backfillArtistCovers(names, opts): mirrors backfillCovers — skip already-cached (getCachedArtistCover), de-dupe, cap to max via mapWithConcurrency CAP=3, resolveOne = itunesArtistCover(name,signal) → setCachedArtistCover + onResolved(artistCoverCacheKey(name), url). Never throws; respects signal.
    - Tests (extend/observe cover-backfill behavior): stub searchAll + itunes-cover fns. Assert (a) track resolveOne uses CN cover when present and does NOT call iTunes; (b) when CN misses, iTunes fallback is called and its cover is cached + onResolved fired; (c) artist key is distinct from the track key for the same name; (d) backfillArtistCovers caches via the artist key and skips already-cached artists; (e) never throws on a thrown resolver.
  </behavior>
  <action>Extend src/lib/services/cover-cache.ts: add `artistCoverCacheKey(artist)` (distinct from coverCacheKey — e.g. `'artist:' + matchKey(artist, '')`, pin the exact form in a test so it can never silently collide with the track key), plus `getCachedArtistCover(artist)` / `setCachedArtistCover(artist, url)` reusing the existing readRecord/write + empty-url no-op + try/catch graceful-null. Same flat `openmusic:cover-cache:v1` Record (artist + track entries coexist; the `artist:` prefix keeps them disjoint).

Extend src/lib/services/cover-backfill.ts:
1. In `resolveOne` (track), after computing the CN `cover` from `dedupeBest(...)[0]?.cover`, if it is null/empty call `await itunesSongCover(item.artist, item.title, signal)` as the FALLBACK; use whichever is non-empty. Keep the existing `setCachedCover` + `onResolved(coverCacheKey(...), cover)` + signal.aborted guards + try/catch swallow. Update the header comment: CN searchAll stays the primary cheap path, iTunes is the no-auth Western/CN-miss fallback, CAA-by-mbid remains a tileCover-level step (not in backfill). Note iTunes is still capped by the existing CAP=3 pool + max cap + timeout (no self-DoS, Pitfall 11 / T-rvy-02).
2. Add `backfillArtistCovers(names: string[], opts: BackfillOpts = {})` mirroring `backfillCovers`: de-dupe names, skip `getCachedArtistCover`-hits, slice to `max`, run through `mapWithConcurrency(work, CAP, resolveOneArtist)` where `resolveOneArtist` = signal-guarded `itunesArtistCover(name, signal)` → on a non-empty url `setCachedArtistCover(name, url)` + `onResolved?.(artistCoverCacheKey(name), url)`; per-item try/catch swallow (miss → gradient). Never throws.

Extend src/lib/services/cover-backfill.test.ts (create it if absent, modeled on discovery.test.ts using vi.spyOn(catalog,'searchAll') + vi.spyOn on the itunes-cover module, with the cover-cache MemStorage stub from cover-cache.test.ts): cover the five behaviors above. Keep all assertions node-runnable (no jsdom, no live network).</action>
  <verify>
    <automated>cd /Users/laichan/code/tung/musicsquare-mobile && pnpm test -- --run cover-backfill cover-cache 2>&1 | tail -25</automated>
  </verify>
  <done>cover-cache exports the three artist-cover fns with a key provably distinct from the track key; resolveOne uses CN-first then iTunes-fallback; backfillArtistCovers resolves+caches+skips-cached via the artist key; all cover-backfill + cover-cache tests pass; never-throws preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Home wiring — artist tile cover branch + artist backfill pass + reactivity</name>
  <files>src/routes/(app)/+page.svelte</files>
  <action>Wire the new resolvers into the home page WITHOUT touching the (correct) shelf-fetch / cache / coverVer machinery:

1. `tileCover`: add an artist branch. The function already takes `{ image, mbid, artist?, title? }`. For an artist tile (`item.image` null, `item.mbid` null or CAA-miss, and `title` ABSENT i.e. a DiscoveryArtist where `item.artist`/name carries the artist), read `getCachedArtistCover(name)`. Concretely: keep steps 1 (image) and 2 (CAA-by-mbid). Then if a track key (`artist && title`) → `getCachedCover(artist, title)` (unchanged). Add: else if an artist name is present (no title) → `getCachedArtistCover(name)`. The artist `<button>` currently calls `tileCover(a)` where `a` is `DiscoveryArtist {name,image,mbid}` — pass the name through so the artist branch can read it (e.g. call `tileCover({ image: a.image, mbid: a.mbid, artist: a.name })` is AMBIGUOUS with the track key; instead pass a dedicated artist field — extend tileCover's param to accept an optional `artistName?: string` and have the artist tile pass `{ image: a.image, mbid: a.mbid, artistName: a.name }`, reading `getCachedArtistCover(artistName)`; keep `void coverVer` first so it stays reactive). Import `getCachedArtistCover` from cover-cache.

2. `scheduleBackfill`: after pushing the TRACK rows (unchanged), also collect the top-artist names needing art (`topArtists` entries where `!a.image` — and since CAA-by-mbid is attempted in tileCover, optionally also require CAA likely-miss; simplest correct rule: push every `topArtists` name whose `image` is null) into a `string[]`, and fire `void backfillArtistCovers(artistNames, { onResolved: () => coverVer++, max: 12 })` alongside the existing `void backfillCovers(...)`. Reuse the same `coverVer++` onResolved so artist covers appear progressively. Import `backfillArtistCovers` from cover-backfill.

3. Reactivity + render: the artist `<img>` already exists (`{#if tileCover(a)}<img ... src={tileCover(a)} onerror={hideOnError} />`) — just update the `tileCover(a)` call site to the new artist-aware form. Covers render as `<img src>` over the gradient with onerror→hide (existing pattern); confirm NO new CSS `url()` interpolation is introduced anywhere (the gradient stays the only background-image). Keep the `coverVer` bump as the single reactive trigger.

Do not change refresh(), the shelf cache, the edge endpoint, or playStub.</action>
  <verify>
    <automated>cd /Users/laichan/code/tung/musicsquare-mobile && pnpm check 2>&1 | tail -15 && pnpm test 2>&1 | tail -8</automated>
  </verify>
  <done>tileCover resolves artist tiles via getCachedArtistCover; scheduleBackfill fires backfillArtistCovers for null-image top artists with coverVer++ onResolved; covers render as <img> over gradient (onerror→gradient, no new CSS url()); pnpm check is clean (0 errors) and the full test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Real album/song covers now resolve for Western-catalog home TRACK tiles (top hits / tag / country shelves) via an iTunes fallback after CN-source + CAA both miss, and ARTIST circle tiles (熱門歌手) now resolve a real image via iTunes — both capped, cached, post-paint, and degrading to the gradient on any miss.</what-built>
  <how-to-verify>
    The prior rvy attempt "looked correct" but did NOT deliver covers — so a runtime visual check is REQUIRED; code-passes-tests is NOT sufficient.

    1. Run `pnpm dev` and open the home page (`/`) in a browser. IMPORTANT: covers come from the live Last.fm key + live iTunes API. If running locally without `LASTFM_KEY`, the page shows the buildDiversePicks fallback grid (real Tracks, already have covers) — to exercise the discovery shelves you need the key set (or verify on the deployed openmusic.pages.dev which has the prod key). Note which surface you are viewing.
    2. To force a cold resolve, clear the cover cache first: in DevTools console run `localStorage.removeItem('openmusic:cover-cache:v1')` then reload.
    3. On the "Top hits" / a tag / a country shelf, confirm Western tracks (e.g. Ariana Grande / Olivia Rodrigo / Drake-type chart entries) now show a real album cover within a second or two of load (covers land progressively as the capped backfill resolves) — NOT a flat color gradient. A few genuine misses staying gradient is acceptable; the majority should now have art.
    4. On the "熱門歌手" (top artists) row, confirm the circle tiles now show real artist/album images, not gradients.
    5. Confirm there are NO broken-image icons anywhere (a 404/miss must show the gradient, not a broken img).
    6. Reload once more WITHOUT clearing the cache: covers should appear instantly (served from the cover cache, no re-fetch flicker).
    7. In the DevTools Network tab, confirm requests go to `itunes.apple.com/search` only as a fallback (you should see far fewer than one-per-tile, capped ≤3 concurrent), and that no request carries a secret/key.
  </how-to-verify>
  <resume-signal>Type "approved" once real covers render on both track and artist tiles with no broken images, or describe what is still gradient/broken.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → iTunes Search API | New untrusted external response (artwork URL strings) crosses into the client |
| client → CN proxies (existing) | Unchanged — searchAll reuse |
| resolved URL → DOM | Cover URL strings are written into `<img src>` attributes (never CSS url()) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7k-01 | Information disclosure | iTunes fetch | accept | Public no-auth endpoint; query carries only artist/title text (already public Last.fm data), no secret/key/PII. No new env var. |
| T-v7k-02 | Denial of service (self-DoS / rate-limit) | iTunes fan-out | mitigate | Reuse mapWithConcurrency CAP=3 + total max cap (24 tracks / 12 artists), skip-already-cached, and `AbortSignal.timeout(6000)` per call; fired post-paint (void), never blocks first render (Pitfall 11). |
| T-v7k-03 | Tampering (injection via cover URL) | tileCover render | mitigate | Resolved URLs are consumed ONLY as `<img src>` attributes (never CSS url()), so no safeImageUrl/host allow-list change is needed (T-nza-02 parity). If ANY new cover URL is ever interpolated into CSS url(), it MUST be host/character-validated. |
| T-v7k-04 | Information disclosure (broken image) | <img> onerror | mitigate | A 404/miss hides the img via the existing hideOnError → gradient shows; never a broken-image icon. resolveOne never throws (per-item miss → gradient). |
| T-v7k-SC | Tampering | npm/pip/cargo installs | mitigate | NONE — this plan adds NO new dependency (constraint). No install task; legitimacy gate not triggered. |
</threat_model>

<verification>
- `pnpm test` — full suite green, including new itunes-cover + extended cover-backfill/cover-cache tests (URL build, 100→600 upgrade, CN-first→iTunes-fallback ordering, artist-key distinctness, never-throws, abort).
- `pnpm check` — 0 svelte-check errors after the +page.svelte wiring.
- Runtime visual check (Task 4 checkpoint): real covers render on Western track tiles AND artist tiles on the running app; misses degrade to gradient; no broken images; warm reload serves cached covers instantly; iTunes called only as a capped fallback with no secret.
</verification>

<success_criteria>
- Western-catalog home TRACK tiles that miss CN-source + CAA now show a real iTunes-resolved cover (RC1 closed).
- 熱門歌手 ARTIST tiles now show a real image via iTunes on a capped, cached, post-paint pass with an artist-only cache key (RC2 closed).
- Gradient remains the graceful last resort; never a broken image; first paint never blocked.
- No secret/key/dependency introduced; concurrency capped + timeout-bounded; no CSS url() interpolation of any new cover host.
- Covers appear progressively (coverVer) and instantly on warm visits (cache); `pnpm check` clean; full test suite passes.
</success_criteria>

<output>
Create `.planning/quick/260606-v7k-resolve-real-covers-for-home-discovery-t/260606-v7k-SUMMARY.md` when done.
</output>
