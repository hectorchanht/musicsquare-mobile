---
phase: 21-search-cover-pipeline-polish
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/sources/types.ts
  - src/lib/sources/qq.ts
  - src/lib/proxy/qq.ts
  - src/lib/services/score-context.ts
  - src/lib/services/score-context.test.ts
  - src/lib/services/score-match.ts
  - src/lib/services/score-match.test.ts
autonomous: true
requirements: [SRCH-01]
must_haves:
  truths:
    - "A sub-60s 試聽 clip with a known duration sinks below a full track of the same song, even when the clip has every available boost (D-04 penalty-dominance)"
    - "A track whose duration is undefined/null/0 is NEVER penalized (D-03 unknown-neutrality)"
    - "An artist appearing in results from 2+ distinct sources is boosted; an artist appearing many times from ONE source is not (D-05 cross-source presence, not row count)"
    - "A title whose length is close to the query length outranks a longer variant title of the same song (D-06 short-title proximity)"
    - "resolveStub and tryFallback (the 2-arg scoreMatch callers) behave identically to before — existing score-match/discovery/fallback suites stay green (D-07)"
    - "QQ search rows carry a numeric duration (seconds) when the upstream tang response reports one (interval); the 試聽 penalty is demonstrable end-to-end for QQ"
  artifacts:
    - path: "src/lib/services/score-context.ts"
      provides: "Pure computeSetContext(rows, query) → { artistSources: Map<string,Set<SourceId>>, queryLen }"
      exports: ["computeSetContext", "SetContext"]
    - path: "src/lib/services/score-match.ts"
      provides: "scoreMatch extended with optional 3rd SetContext arg + 試聽 penalty + short-title/artist-frequency boosts; PREVIEW_PENALTY/SHORT_CLIP_SEC consts"
      contains: "SHORT_CLIP_SEC"
    - path: "src/lib/sources/types.ts"
      provides: "Track.duration optional field"
      contains: "duration?: number"
  key_links:
    - from: "src/lib/services/score-match.ts"
      to: "src/lib/services/score-context.ts"
      via: "import type { SetContext }"
      pattern: "SetContext"
    - from: "src/lib/sources/qq.ts"
      to: "Track.duration"
      via: "search() maps interval seconds onto duration"
      pattern: "duration"
---

<objective>
Fold three new search-scoring signals into the pure `scoreMatch` brain and plumb `Track.duration` end-to-end for at least one real source (QQ) so the 試聽 sub-60s penalty is demonstrable, not dormant.

Purpose: SRCH-01 — result scoring must boost shorter (non-cover) titles and cross-source artists while heavily penalizing sub-60s preview clips, without falsely penalizing sources that report no duration. This is the pure-logic + data-plumbing foundation that Plan 04 (search page) wires into the live result set.
Output: extended `score-match.ts` (additive optional `SetContext` 3rd arg), new pure `score-context.ts`, `Track.duration` field + QQ duration mapping, and regression-guarded test suites (existing 2-arg callers unchanged).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

ENVIRONMENT: Shell default node is v16 and breaks vitest/svelte-check. EVERY node command in this plan MUST be prefixed with:
`export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"` and use pnpm.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-search-cover-pipeline-polish/21-CONTEXT.md
@.planning/phases/21-search-cover-pipeline-polish/21-RESEARCH.md
@.planning/phases/21-search-cover-pipeline-polish/21-PATTERNS.md

<interfaces>
<!-- Current scoreMatch signature (2-arg) — resolveStub/tryFallback callers MUST keep working unchanged -->
From src/lib/services/score-match.ts:
  export function scoreMatch(query: { artist: string; title: string }, candidate: Track): number
  // internal consts: SIM_EXACT = 10, SIM_ARTIST = 3, SIM_TITLE = 3, SIM_TOKEN = 2, VARIANT_WEIGHT = 4
  // score = similarity(query, candidate) − variantPenalty(query, candidate)
  export const VARIANT_KEYWORDS: string[]

From src/lib/services/match-key.ts:
  export function matchKey(artist: string, title: string): string   // artist-first '|' joined

From src/lib/sources/types.ts:
  export type SourceId = 'netease' | 'qq' | 'kuwo' | 'joox' | 'fivesing' | 'jamendo'
  export interface Track { ...; cover: string | null; tags?: string[]; bio?: string; ... }  // Last.fm fields are the additive-optional precedent
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Track.duration field + plumb QQ interval end-to-end</name>
  <files>src/lib/sources/types.ts, src/lib/sources/qq.ts, src/lib/proxy/qq.ts</files>
  <read_first>
    - src/lib/sources/types.ts (the Track interface; the Last.fm `tags?`/`bio?` block lines 59-63 is the additive-optional precedent to mirror)
    - src/lib/sources/qq.ts (QQSearchItem shape lines 18-24; the search() push block lines 145-178 where tracks are built)
    - src/lib/proxy/qq.ts (the QQ search proxy reshape — confirm whether the upstream tang search body exposes an interval/duration field that survives the reshape)
  </read_first>
  <behavior>
    - Test: a Track with `duration` omitted type-checks (field is optional).
    - Test: a QQ search row whose upstream item has `interval: 215` produces a Track with `duration === 215`.
    - Test: a QQ search row with no interval produces a Track with `duration` undefined (NOT 0, NOT penalized later).
  </behavior>
  <action>
    Add `duration?: number;` (seconds) to the `Track` interface in types.ts, placed in the source-specific extras block with a comment that undefined means "source did not report duration" (D-03 — undefined is never penalized). Mirror the additive-optional style of the existing `tags?`/`bio?` Last.fm fields — do NOT make it required, do NOT add it to any serialize whitelist.

    In qq.ts: add `interval?: number;` to the `QQSearchItem` interface (the tang search row exposes track length in seconds as `interval` in the QQ catalogue shape — confirm the exact field name against the live `/api/qq/search` response during execution; if the field is named differently in the reshaped body, map whatever numeric-seconds field exists). In the `list.forEach` push block (~line 153), set `duration: typeof it.interval === 'number' && it.interval > 0 ? it.interval : undefined` on the pushed Track.

    In proxy/qq.ts: if the search reshape strips fields to a whitelist, ensure the numeric `interval` (or equivalent) seconds field is preserved through to the client adapter. If the proxy passes the body through untouched, no change is needed — verify by reading the reshape and only edit if it would drop the field.

    Per the researcher Assumption A1: the absence of duration was verified in adapter interfaces but NOT against the live upstream payload. During execution, run a real `/api/qq/search` query and confirm the seconds field name. If QQ genuinely does not expose duration, plumb the cheapest source that does (Netease/Kuwo song detail) instead — but at least ONE source must end-to-end carry duration so the penalty is demonstrable (do NOT ship the penalty dormant for every source).
  </action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/sources/types.ts` contains `duration?: number` on the `Track` interface (grep: `grep -n 'duration?: number' src/lib/sources/types.ts` returns a match)
    - `src/lib/sources/qq.ts` maps a numeric upstream seconds field onto `duration` in the search() push block, guarded so a missing/zero value yields `undefined` (grep: `grep -n 'duration' src/lib/sources/qq.ts` returns a match in the push block)
    - `pnpm check` passes (0 errors)
    - The SUMMARY records the confirmed upstream field name from the live `/api/qq/search` response, or — if QQ does not expose it — which alternate source was plumbed instead and why
  </acceptance_criteria>
  <done>Track has an optional numeric `duration` (seconds); QQ search rows carry it when upstream reports it and leave it undefined otherwise; `pnpm check` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: New pure score-context.ts (cross-source artist map) + test</name>
  <files>src/lib/services/score-context.ts, src/lib/services/score-context.test.ts</files>
  <read_first>
    - src/lib/services/score-match.ts (the pure-function + import-light discipline: only matchKey + Track/SourceId types; no $state, no $app/*, no I/O)
    - src/lib/services/score-match.test.ts (the node-Vitest test structure to mirror)
    - src/lib/services/cover-cache.ts (artistCoverCacheKey lines 38-40 — `matchKey(artist, '')` artist-only key precedent)
  </read_first>
  <behavior>
    - Test: computeSetContext over rows where artist "周杰倫" appears from sources 'qq' AND 'netease' yields an entry whose Set has size 2 for that artist's matchKey.
    - Test: an artist appearing in 5 rows but all from a SINGLE source yields a Set of size 1 (D-05 — distinct sources, not row count).
    - Test: queryLen equals the trimmed query length.
    - Test: the returned artistSources keys on `matchKey(artist, '')` (artist-only), so two different titles by the same artist collapse to one entry.
  </behavior>
  <action>
    Create `src/lib/services/score-context.ts` as a pure node-testable module. Export an interface `SetContext { artistSources: Map<string, Set<SourceId>>; queryLen: number }` and a function `computeSetContext(rows: Track[], query: string): SetContext`. Build artistSources by keying each row on `matchKey(r.artist, '')` (artist-only, mirroring `artistCoverCacheKey`) and adding `r.source` to that key's Set. Set `queryLen = query.trim().length`. Import only `matchKey` from `$lib/services/match-key` and `SourceId`/`Track` types from `$lib/sources/types`. No I/O, no runes — keep it node-Vitest-testable exactly like `score-match.ts`.

    Create `score-context.test.ts` mirroring `score-match.test.ts` structure with the four behaviors above.
  </action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm test -- score-context 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `score-context.test.ts` passes with the four behaviors (cross-source size-2, single-source size-1, queryLen, artist-only key collapse)
    - `computeSetContext` imports ONLY matchKey + types (grep the file: no `$state`, no `$app/`, no `fetch`)
    - `SetContext` and `computeSetContext` are exported
  </acceptance_criteria>
  <done>computeSetContext is a pure, tested function producing a distinct-source artist map keyed artist-only, plus queryLen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend scoreMatch with optional SetContext + 試聽 penalty + boosts + regression guard</name>
  <files>src/lib/services/score-match.ts, src/lib/services/score-match.test.ts</files>
  <read_first>
    - src/lib/services/score-match.ts (full file — the 2-arg signature line 155, tuning consts lines 63-67, variantPenalty/similarity that must NOT regress, CR-01/CR-02/WR-02 invariants)
    - src/lib/services/score-match.test.ts (existing assertions that must stay green)
    - src/lib/services/score-context.ts (the SetContext interface from Task 2 — import it as a type)
    - src/lib/services/discovery.ts (resolveStub — a 2-arg scoreMatch caller that must be unaffected)
    - src/lib/services/fallback.ts (tryFallback — a 2-arg scoreMatch caller that must be unaffected)
  </read_first>
  <behavior>
    - Test (D-03): scoreMatch(q, cand-with-duration-undefined) returns the SAME value with and without a SHORT_CLIP context — unknown duration is never penalized.
    - Test (D-03): scoreMatch(q, cand-with-duration-0) is not penalized (0 = unknown).
    - Test (D-04 penalty-dominance): a candidate with duration=30 (sub-60s) carrying every boost (short title + cross-source artist + exact similarity) scores STRICTLY LESS than a clean full-length candidate with NO boosts. Assert PREVIEW_PENALTY > SIM_EXACT + maxShortTitleBoost + maxArtistBoost as a derived invariant.
    - Test (D-06): with ctx, a title whose length ≈ queryLen scores higher than a longer same-song variant title.
    - Test (D-05): with ctx, a candidate whose artist is in 2+ sources scores higher than an identical candidate whose artist is in 1 source.
    - Test (D-07 regression): scoreMatch called with 2 args (no ctx) returns identical values to the pre-change behavior for the existing fixtures.
  </behavior>
  <action>
    Extend `scoreMatch` to accept an optional 3rd arg `ctx?: SetContext` (import the type from `score-context.ts`). Keep the existing 2-arg call shape working byte-identically for resolveStub/tryFallback — the new behavior fires ONLY when ctx is passed and/or duration is present.

    Add named tuning consts in the existing weight block (lines 63-67 style): `SHORT_CLIP_SEC = 60`, `PREVIEW_PENALTY`, and the boost consts (e.g. `SHORT_TITLE_BOOST_MAX`, `ARTIST_FREQ_BOOST`). Compute/assert PREVIEW_PENALTY strictly greater than `SIM_EXACT + SHORT_TITLE_BOOST_MAX + ARTIST_FREQ_BOOST` so D-04 holds for any boost combination — express it as a derived constant or add an invariant assertion in the test rather than two independently-chosen magnitudes (Pitfall 2).

    Penalty (D-03/D-04): subtract PREVIEW_PENALTY ONLY when `typeof candidate.duration === 'number' && candidate.duration > 0 && candidate.duration < SHORT_CLIP_SEC`. undefined/null/0 = unknown = no penalty. Apply it as a large flat subtraction so no boost can lift a clip above a full track.

    Boosts (only when ctx provided): `shortTitleBoost` rewards a candidate title whose length is close to `ctx.queryLen` (D-06 — proximity, NOT "shorter is always better"; a long title the user actually typed must not be punished). `artistFrequencyBoost` adds `ARTIST_FREQ_BOOST` only when `ctx.artistSources.get(matchKey(candidate.artist,'')).size >= 2` (D-05 — cross-source presence, not row count). Keep `similarity` and `variantPenalty` (CR-01 word boundaries, CR-02 substring de-dupe, WR-02 title-only) byte-unchanged.

    Extend `score-match.test.ts` with the six behaviors above; keep all existing assertions green.
  </action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm test -- score-match discovery fallback 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- score-match discovery fallback` all green (the D-07 regression set stays passing)
    - `grep -n 'SHORT_CLIP_SEC' src/lib/services/score-match.ts` matches; `grep -n 'PREVIEW_PENALTY' src/lib/services/score-match.ts` matches
    - The penalty-dominance invariant is asserted in a test (a sub-60s fully-boosted clip scores strictly less than a clean unboosted full track)
    - The 2-arg `scoreMatch(query, candidate)` signature is preserved (resolveStub/tryFallback unchanged — grep `discovery.ts`/`fallback.ts` show no new arg passed)
    - A test proves undefined and 0 duration are NOT penalized (D-03)
  </acceptance_criteria>
  <done>scoreMatch carries the 試聽 penalty + short-title + cross-source-artist boosts behind an additive optional ctx arg with D-04 penalty-dominance and D-03 unknown-neutrality both test-asserted; 2-arg callers untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream tang `/api/qq/search` → QQ adapter | untrusted upstream JSON (duration field) crosses into the Track model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | QQ duration mapping (qq.ts) | mitigate | Coerce to a finite positive number or `undefined` (`typeof === 'number' && > 0`); a non-numeric/negative upstream value never becomes a duration, so a hostile/garbage payload cannot forge a penalty bypass |
| T-21-02 | DoS | scoreMatch boost compute | accept | Pure synchronous arithmetic over an already-bounded result set; no I/O, no fan-out — negligible cost |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (zero new deps — verified against RESEARCH.md Package Legitimacy Audit: "Not applicable"); no install task to gate |
</threat_model>

<verification>
- `pnpm check` clean (Track.duration type-safe)
- `pnpm test -- score-match score-context discovery fallback` all green
- Full suite green at wave merge: `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm test`
</verification>

<success_criteria>
- SRCH-01 scoring logic complete: 試聽 penalty (demonstrable for QQ), short-title proximity boost, cross-source artist boost, all behind an additive optional ctx arg
- D-03 (unknown-neutrality), D-04 (penalty-dominance), D-05 (cross-source not row-count), D-06 (query-length proximity), D-07 (2-arg regression) all test-asserted
- No existing test regressed (score-match, discovery, fallback green)
</success_criteria>

<output>
Create `.planning/phases/21-search-cover-pipeline-polish/21-01-SUMMARY.md` when done. Record the confirmed upstream duration field name (or the alternate source plumbed) and the final PREVIEW_PENALTY / boost constant values.
</output>
