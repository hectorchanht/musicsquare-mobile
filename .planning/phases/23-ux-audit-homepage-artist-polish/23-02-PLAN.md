---
phase: 23-ux-audit-homepage-artist-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/actions/swipeAction.ts
  - src/lib/actions/swipeAction.test.ts
  - src/lib/actions/focusTrap.ts
autonomous: true
requirements: [UX-04, UX-06]
must_haves:
  truths:
    - "A directional swipe action reveals a per-direction icon during drag, commits past threshold (or on flick), then always springs the row back to translateX(0) — never removed"
    - "Swipe coexists with the row's onclick (tap-to-play) and use:longpress (menu): sub-slop movement reaches onclick, vertical movement yields to scroll"
    - "A focusTrap action focuses the first focusable element on mount, cycles Tab/Shift+Tab within the container, and restores focus to the trigger on destroy"
  artifacts:
    - path: "src/lib/actions/swipeAction.ts"
      provides: "Directional swipe-action gesture generalized from swipeRemove (D-02)"
      contains: "onSwipeRight"
    - path: "src/lib/actions/focusTrap.ts"
      provides: "Hand-rolled focus-trap Svelte action (UX-06 §7.3)"
      contains: "destroy"
  key_links:
    - from: "src/lib/actions/swipeAction.ts"
      to: "src/lib/gestures/velocity.ts"
      via: "createVelocityTracker for flick commit"
      pattern: "velocity|createVelocityTracker"
---

<objective>
Build the two gesture/a11y primitives consumed by the row surfaces and chart pages: a directional `swipeAction` action (generalized from `swipeRemove.ts`, D-01/D-02) and a hand-rolled `focusTrap` action (UX-06 §7.3). New files only — application to surfaces happens in downstream plans.

Purpose: UX-04 swipe-to-queue/like and UX-06 focus-trapping both depend on these. Generalizing the proven swipeRemove mechanics preserves the load-bearing Phase 15/20 gesture invariants.
Output: 2 new action files + swipeAction node tests. No surface wiring in this plan.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

Tooling: prefix PATH with `$HOME/.nvm/versions/node/v22.22.0/bin` and use pnpm for every check/test command.
</execution_context>

<context>
@.planning/phases/23-ux-audit-homepage-artist-polish/23-CONTEXT.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-PATTERNS.md
@.planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: swipeAction directional gesture, generalized from swipeRemove (D-02)</name>
  <files>src/lib/actions/swipeAction.ts, src/lib/actions/swipeAction.test.ts</files>
  <read_first>
    - src/lib/actions/swipeRemove.ts (read in full — copy gesture mechanics verbatim, change only commit semantics)
    - src/lib/actions/swipeRemove.test.ts (test harness/style to mirror — pointer event simulation)
    - src/lib/gestures/velocity.ts (createVelocityTracker — px/ms flick tracker reused)
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-PATTERNS.md "swipeAction.ts" section (the up() commit pseudocode)
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md §6
  </read_first>
  <behavior>
    - Horizontal drag right past `threshold` (default 96px) OR a right flick (velocity > FLICK_V with dx > SLOP) calls onSwipeRight() once, then the node springs back to translateX(0)
    - Horizontal drag left past threshold OR left flick calls onSwipeLeft() once, then springs back
    - Sub-slop movement (|dx| < 8px) does NOT commit and does NOT suppress the trailing click (tap-to-play preserved)
    - Vertical-dominant movement yields (no horizontal capture; scroll/longpress proceed)
    - On a committed drag, a trailing click is suppressed once (WR-01)
    - enabled:false disables all gesture handling
  </behavior>
  <action>Create `src/lib/actions/swipeAction.ts` as `Action<HTMLElement, SwipeActionOpts>`. Inherit verbatim from swipeRemove.ts: `SLOP = 8`, `FLICK_V = 0.5`, `createVelocityTracker()` seeded on X, `node.style.touchAction = 'pan-y'`, NO `setPointerCapture` on pointerdown (capture only after horizontal commit in move()), sub-slop reaches onclick, vertical-dominant goes passive, and the `suppressClick` trailing-click handler armed only on a committed drag. Opts shape: `{ onSwipeRight?: () => void; onSwipeLeft?: () => void; threshold?: number; enabled?: boolean }` with `threshold` default 96. CHANGE from swipeRemove: in up(), compute `committed = Math.abs(dx) > threshold || (Math.abs(v) > FLICK_V && Math.abs(dx) > SLOP)`; if committed call `dx > 0 ? onSwipeRight?.() : onSwipeLeft?.()`; then ALWAYS set `node.style.transition = 'transform 0.28s cubic-bezier(.22,1,.36,1)'` and `node.style.transform = 'translateX(0)'` (row never removed — drop swipeRemove's removal/onremove and its FADE_DISTANCE/opacity fade). Return `{ update(newOpts), destroy() }` removing all listeners. Do NOT call haptics here — the consuming component fires `haptics.tick()` inside onSwipeRight/onSwipeLeft so the gesture stays a pure DOM action (PATTERNS.md §3.3). Add `src/lib/actions/swipeAction.test.ts` mirroring swipeRemove.test.ts: simulate pointerdown→move→up for right-commit, left-commit, sub-slop-no-commit, vertical-yield, and flick-commit cases.</action>
  <verify>
    <automated>PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm vitest run src/lib/actions/swipeAction.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/actions/swipeAction.ts exports a default action accepting `onSwipeRight` and `onSwipeLeft` opts
    - swipeAction.ts sets `touchAction = 'pan-y'` and contains NO `setPointerCapture` call inside the pointerdown handler (capture is in move only)
    - swipeAction.ts always restores `translateX(0)` after commit (no removal branch, no FADE_DISTANCE)
    - swipeAction.test.ts asserts right-commit calls onSwipeRight, left-commit calls onSwipeLeft, sub-slop does not commit, and tests pass
  </acceptance_criteria>
  <done>swipeAction exists with verbatim swipeRemove invariants and spring-back commit; node tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: focusTrap hand-rolled Svelte action (UX-06 §7.3)</name>
  <files>src/lib/actions/focusTrap.ts</files>
  <read_first>
    - src/lib/actions/longpress.ts (canonical hand-rolled Action shape: add listeners on attach, return { destroy() } removing them)
    - src/lib/components/TrackMenu.svelte (the overlay `$effect` history invariant ~lines 180-208 — focusTrap manages focus ONLY, never open/close state)
    - .planning/phases/23-ux-audit-homepage-artist-polish/23-UI-SPEC.md §7.3
  </read_first>
  <action>Create `src/lib/actions/focusTrap.ts` as `Action<HTMLElement>` following the longpress.ts hand-rolled posture (no new dependency). On mount: capture `document.activeElement` as the return target, then focus the first focusable element inside the node (query `a[href], button:not([disabled]), input, select, textarea, [tabindex]:not([tabindex="-1"])`; fall back to focusing the node itself with `tabindex=-1`). Add a `keydown` listener: on `Tab`/`Shift+Tab` cycle focus within the focusable set (wrap from last→first and first→last); on `Escape` do NOT manage open/close — leave dismissal to the host overlay (the action manages focus only, per §7.3 and the Phase 19 overlay invariant). Return `{ destroy() }` that removes the keydown listener and restores focus to the captured return target. Keep it pure of any store/state.</action>
  <verify>
    <automated>PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH" pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/actions/focusTrap.ts exports a default Svelte action returning an object with a `destroy` method
    - focusTrap.ts queries focusable elements and adds a `keydown` listener handling Tab/Shift+Tab cycling
    - focusTrap.ts captures and restores `document.activeElement` (focus restore on destroy)
    - focusTrap.ts contains no open/close state mutation (focus-only; no `open =` assignment)
    - `pnpm check` exits 0
  </acceptance_criteria>
  <done>focusTrap action exists, traps + cycles + restores focus, manages no open/close state; pnpm check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer events → swipe commit | user gesture triggers queue/like actions on commit |
| keyboard → focus cycle | Tab handling inside a trap could strand focus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Denial of Service | focusTrap keydown | mitigate | Trap restores focus on destroy and falls back to the container if no focusable child exists, so focus is never permanently lost. Acceptance: destroy() restores prior activeElement. |
| T-23-05 | Tampering | swipeAction commit | accept | Gesture only invokes caller-supplied callbacks (queue/like) which themselves act on the user's own library/queue; no privilege boundary crossed. Spring-back guarantees no destructive removal. |
</threat_model>

<verification>
- `pnpm check` exits 0.
- swipeAction.test.ts passes (right/left/sub-slop/vertical/flick cases).
- swipeAction.ts contains no `setPointerCapture` in the pointerdown handler and no removal branch.
</verification>

<success_criteria>
- swipeAction commits per-direction past threshold/flick and always springs back, preserving tap + longpress + vertical-scroll.
- focusTrap focuses-first, cycles Tab, and restores focus on destroy without touching open/close state.
</success_criteria>

<output>
Create `.planning/phases/23-ux-audit-homepage-artist-polish/23-02-SUMMARY.md` when done.
</output>
