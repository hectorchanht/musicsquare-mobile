---
phase: 21-search-cover-pipeline-polish
plan: 03
type: tdd
wave: 2
depends_on: ["21-02"]
files_modified:
  - src/lib/stores/player.svelte.ts
  - src/lib/stores/player.svelte.test.ts
  - src/lib/components/NowPlaying.svelte
  - src/lib/components/Nowbar.svelte
autonomous: false
requirements: [COVER-01]
must_haves:
  truths:
    - "The player exposes a single resolvedCover field set synchronously on play() entry from track.cover ?? uid-cache ?? name-cache ?? null (D-09)"
    - "When resolvedCover is null after the sync read, the async tier chain runs and, generation-guarded, sets resolvedCover and re-fires MediaSession metadata on a SOLID land (D-09)"
    - "NowPlaying, Nowbar, and buildArtwork (MediaSession) all read resolvedCover — one field, three surfaces (D-09)"
    - "MediaSession artwork repaints by assigning a NEW MediaMetadata object on async land, not by mutating artwork in place (Pitfall 4)"
    - "A superseded play() (newer playGen) never paints stale cover or stale MediaSession art (generation guard)"
    - "On total miss, surfaces keep their seeded-gradient fallbackCover and MediaSession keeps /favicon.svg (D-12)"
  artifacts:
    - path: "src/lib/stores/player.svelte.ts"
      provides: "resolvedCover $state field + sync-set on play + async tier-chain land + MediaSession re-fire"
      contains: "resolvedCover"
    - path: "src/lib/components/NowPlaying.svelte"
      provides: "cover render reads player.resolvedCover with gradient fallback"
      contains: "resolvedCover"
    - path: "src/lib/components/Nowbar.svelte"
      provides: "cover render reads player.resolvedCover with gradient fallback"
      contains: "resolvedCover"
  key_links:
    - from: "src/lib/stores/player.svelte.ts"
      to: "src/lib/services/cover-cache.ts"
      via: "getCachedCoverByUid → getCachedCover sync read on play entry"
      pattern: "getCachedCoverByUid"
    - from: "src/lib/stores/player.svelte.ts"
      to: "src/lib/services/cover-backfill.ts"
      via: "single-item resolve helper for the async land (generation-guarded)"
      pattern: "resolveCover"
    - from: "src/lib/stores/player.svelte.ts"
      to: "src/lib/services/media-session.ts"
      via: "buildArtwork(this.resolvedCover) on a new MediaMetadata"
      pattern: "buildArtwork\\(.*resolvedCover"
---

<objective>
Guarantee the playing track always has artwork: add a single `resolvedCover` field to the player store, set it synchronously from track/cache on play and asynchronously from the tier chain on a miss, and repoint NowPlaying, Nowbar, and MediaSession all at that one field — with a generation-guarded MediaSession re-fire on async land.

Purpose: COVER-01 — the playing track's cover always renders in now-playing, the nowbar, and MediaSession even when the source returns none. Depends on Plan 02's uid cache helpers + single-item resolve helper.
Output: `resolvedCover` on `player.svelte.ts` (sync + async + MediaSession re-fire), NowPlaying/Nowbar repointed, player tests extended.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

ENVIRONMENT: Shell default node is v16 and breaks vitest/svelte-check. EVERY node command MUST be prefixed with:
`export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"` and use pnpm.

DEPENDS ON Plan 02: uses `getCachedCoverByUid`/`getCachedCover` (cover-cache.ts) and the single-item resolve helper exported from cover-backfill.ts. Read 21-02-SUMMARY.md for the exact helper name/signature before starting.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-search-cover-pipeline-polish/21-CONTEXT.md
@.planning/phases/21-search-cover-pipeline-polish/21-RESEARCH.md
@.planning/phases/21-search-cover-pipeline-polish/21-PATTERNS.md
@.planning/phases/21-search-cover-pipeline-polish/21-02-SUMMARY.md

<interfaces>
From src/lib/stores/player.svelte.ts (existing cover/MediaSession path):
  // play() builds MediaMetadata at the offline-blob path (~1292-1298) AND the network path (~1351-1357),
  //   currently artwork: buildArtwork(track.cover) / buildArtwork(resolved.cover)
  // playGen monotonic generation snapshot+recheck after every await: `const myGen = this.playGen; ... if (myGen !== this.playGen) return;` (~1264, 1282, 1322)
  // library.adoptCover(resolved) at ~1333 already propagates landed covers
  // preloadNextCover (~1151) is the new Image() precedent
  // state fields declared ~149-181 (pendingTrack ~151, playGen ~181)

From src/lib/services/media-session.ts:
  export function buildArtwork(cover: string | null): MediaImage[]   // null-safe; /favicon.svg final fallback (D-12) — NO signature change

From src/lib/services/cover-cache.ts (Plan 02):
  getCachedCoverByUid(uid) / getCachedCover(artist, title)

From src/lib/components/Nowbar.svelte:
  const np = $derived(player.current ?? player.pendingTrack);  // cover render uses np cover + fallbackCover()
From src/lib/components/NowPlaying.svelte:
  const t = player.current; // cover render uses fallbackCover(t)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolvedCover field — sync set/clear on play + async tier-chain land + MediaSession re-fire</name>
  <files>src/lib/stores/player.svelte.ts, src/lib/stores/player.svelte.test.ts</files>
  <read_first>
    - src/lib/stores/player.svelte.ts (the state field block ~149-181; the offline-blob MediaMetadata write ~1290-1299; the network-path MediaMetadata write ~1350-1358; the playGen snapshot+recheck idiom ~1264/1282/1322; library.adoptCover ~1333; preloadNextCover ~1151-1166)
    - src/lib/stores/player.svelte.test.ts (existing test structure + how it constructs the store / fakes audio + MediaSession)
    - src/lib/services/cover-cache.ts (getCachedCoverByUid + getCachedCover from Plan 02)
    - src/lib/services/cover-backfill.ts (the single-item resolve helper from Plan 02 — confirm name/signature in 21-02-SUMMARY.md)
    - src/lib/services/media-session.ts (buildArtwork — feed it resolvedCover; no signature change)
  </read_first>
  <behavior>
    - Test: on play() entry with a track that HAS a cover, resolvedCover === track.cover synchronously (no await).
    - Test: on play() with no track cover but a uid-cache hit, resolvedCover === the uid-cached URL.
    - Test: on play() with no cover and no cache, resolvedCover is null synchronously, then after the async chain resolves a SOLID URL, resolvedCover === that URL.
    - Test (Pitfall 4): the async land assigns a NEW MediaMetadata (not in-place artwork mutation) — assert ms.metadata is a fresh object whose artwork derives from resolvedCover.
    - Test (generation guard): a superseded play() (playGen bumped) whose async cover lands does NOT overwrite the newer track's resolvedCover or MediaSession.
    - Test: switching tracks clears/repoints resolvedCover (no stale cover from the prior track).
  </behavior>
  <action>
    Add `resolvedCover = $state<string | null>(null);` near the existing state fields (~149-181), per D-09.

    On play() entry (and the playStub fresh-play path that builds pendingTrack), set it synchronously:
    `this.resolvedCover = track.cover ?? getCachedCoverByUid(track.uid) ?? getCachedCover(track.artist, track.title) ?? null;`
    Do this BEFORE the first MediaMetadata write so the OS gets the best-known art immediately.

    Repoint BOTH MediaMetadata writes (offline-blob path ~1296 and network path ~1355) from `buildArtwork(track.cover)` / `buildArtwork(resolved.cover)` to `buildArtwork(this.resolvedCover)`.

    If `this.resolvedCover` is still null after the sync read, fire the Plan-02 single-item resolve helper (never-throw), generation-guarded with the existing `myGen`/`playGen` idiom: snapshot `myGen = this.playGen` before the await; after it lands, `if (myGen !== this.playGen) return;` (Pitfall 4 + CR-02). On a SOLID result: set `this.resolvedCover = url`, call `library.adoptCover` (reuse the existing ~1333 propagation), and re-assign `this.ms.metadata = new MediaMetadata({ title, artist, album, artwork: buildArtwork(this.resolvedCover) })` — a FRESH object so the OS repaints (never mutate artwork in place, A2/Pitfall 4). Keep `ms.playbackState` correct.

    On a total miss, resolvedCover stays null; surfaces fall back to their gradient and MediaSession keeps /favicon.svg via buildArtwork (D-12). Do not block playback on the cover resolve — it is best-effort, off the audio critical path.

    Extend player.svelte.test.ts with the six behaviors.
  </action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm test -- player media-session 2>&1 | tail -15; export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- player media-session` green with the six behaviors
    - `grep -n 'resolvedCover = \$state' src/lib/stores/player.svelte.ts` matches
    - Both MediaMetadata writes use `buildArtwork(this.resolvedCover)` (grep: `buildArtwork(this.resolvedCover)` appears at both former sites; `grep -c 'buildArtwork(track.cover)\|buildArtwork(resolved.cover)' src/lib/stores/player.svelte.ts` returns 0)
    - The async land is generation-guarded (`myGen !== this.playGen` recheck after the resolve await) and assigns a NEW MediaMetadata
    - Sync set reads uid-cache before name-cache before null (grep order in play entry)
    - `pnpm check` clean
  </acceptance_criteria>
  <done>resolvedCover is set sync from track/cache and async from the tier chain (generation-guarded), feeds both MediaMetadata writes via a fresh object on land, and degrades to gradient/favicon on miss; player tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repoint NowPlaying + Nowbar cover render at player.resolvedCover</name>
  <files>src/lib/components/NowPlaying.svelte, src/lib/components/Nowbar.svelte</files>
  <read_first>
    - src/lib/components/NowPlaying.svelte (the cover render site ~line 84 reading np/current.cover; the local fallbackCover(t) helper ~line 59-66)
    - src/lib/components/Nowbar.svelte (the np $derived ~line 27 + cover render ~line 84; the local fallbackCover() ~line 39)
    - src/lib/stores/player.svelte.ts (resolvedCover field from Task 1)
  </read_first>
  <behavior>
    - Manual (UI): playing a no-cover-source track shows resolved art in both the nowbar and full-screen now-playing once the chain lands; until then the seeded gradient shows (no flash to a broken image).
  </behavior>
  <action>
    In NowPlaying.svelte and Nowbar.svelte, change the cover render to read `player.resolvedCover` as the primary source, falling back to each component's existing seeded-gradient `fallbackCover(...)` when resolvedCover is null (D-12 placeholder). Keep the existing `style:background-image` mechanism for the now-playing/nowbar artwork (these are NOT the lazyCover-list surfaces; they read the single player field). The resolvedCover value is already https-guarded upstream (T-0bb-01) — do not introduce any new URL path. Preserve all marquee/skeleton behavior on the surrounding rows per project UI rules.

    No new state in these components — they read `player.resolvedCover` reactively (it is a $state field, so the surfaces repaint when the async land sets it).
  </action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'resolvedCover' src/lib/components/NowPlaying.svelte` matches; `grep -n 'resolvedCover' src/lib/components/Nowbar.svelte` matches
    - Each surface still references its local `fallbackCover` gradient as the null fallback (grep both)
    - `pnpm check` clean (no type errors)
  </acceptance_criteria>
  <done>NowPlaying and Nowbar render player.resolvedCover with the existing gradient as the null fallback; one field drives all three surfaces (incl. MediaSession from Task 1).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify playing-track cover guarantee across surfaces + lock screen</name>
  <what-built>resolvedCover on the player store feeding NowPlaying, Nowbar, and MediaSession; async tier-chain resolve with a generation-guarded MediaSession re-fire and gradient/favicon fallback.</what-built>
  <how-to-verify>
    1. Run the dev server: `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm dev` and open the app.
    2. Search and play a track from a source that returns NO cover (e.g. a QQ result — QQ search rows have cover: null). Confirm:
       a. The nowbar mini-player shows resolved artwork (not a permanent gradient) once the chain lands.
       b. Expanding to full-screen now-playing shows the same resolved artwork (sharp — iTunes tier is now 1200px).
       c. The lock-screen / OS media notification artwork shows the resolved cover (Android; iOS where supported). It should NOT stay on the favicon if a real cover resolved.
    3. Play a track that genuinely has no resolvable cover anywhere — confirm the gradient shows on-surface and the favicon shows on the lock screen (graceful miss, no broken image).
    4. Skip quickly between two tracks — confirm a slow cover resolve for the first never paints onto the second (no stale art).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what rendered wrong (which surface, gradient-vs-art, stale art on skip).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| async resolved cover → MediaSession / surfaces | a cover URL crosses into OS MediaSession + DOM render; must be https-only and generation-guarded against supersede |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 (T-0bb-01) | Tampering / XSS | resolvedCover render + MediaSession | mitigate | resolvedCover only ever holds a SOLID https URL (the Plan-02 helper gates with isSolidCover) or null; surfaces render it via the existing background-image / buildArtwork paths, no new injection surface for resolved values |
| T-21-06 | Tampering | stale-cover paint on supersede | mitigate | generation guard (`myGen !== this.playGen` recheck) discards a superseded resolve before it paints resolvedCover or MediaSession (Pitfall 4) |
| T-21-07 | DoS | cover resolve blocking playback | accept | resolve is best-effort, off the audio critical path, never-throw; a miss leaves the gradient — playback never waits on it |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (zero new deps — RESEARCH.md audit "Not applicable"); no install task to gate |
</threat_model>

<verification>
- `pnpm test -- player media-session` green; `pnpm check` clean
- Full suite green at wave merge
- Manual checkpoint (Task 3): no-cover-source track shows resolved art in nowbar + now-playing + lock screen; total-miss shows gradient/favicon; no stale art on fast skip
</verification>

<success_criteria>
- COVER-01: one resolvedCover field guarantees artwork in NowPlaying, Nowbar, and MediaSession even when the source returns none
- D-09 (single field, three surfaces, MediaSession refresh on land), D-12 (gradient + favicon fallback), Pitfall-4 (new MediaMetadata object), generation guard — all satisfied
</success_criteria>

<output>
Create `.planning/phases/21-search-cover-pipeline-polish/21-03-SUMMARY.md` when done. Record where resolvedCover is set/cleared in the play flow and how the MediaSession re-fire is sequenced.
</output>
