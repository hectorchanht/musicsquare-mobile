---
phase: 08-last-fm-read-foundation-metadata-enrichment
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/routes/(app)/artist/[name]/+page.svelte
autonomous: true
requirements: [ENRICH-01, ENRICH-02]

must_haves:
  truths:
    - "Opening an artist page shows a Last.fm artist-bio snippet, top-tag chips, and (when better) a higher-res artist image, augmenting — never replacing — the existing derived track-list"
    - "The 'Read more on Last.fm' attribution link always renders whenever a bio is shown (Last.fm ToS)"
    - "An artist with no Last.fm match still renders the existing derived track list exactly as before — enrichment is silent best-effort and never blocks the searchAll load"
  artifacts:
    - path: "src/routes/(app)/artist/[name]/+page.svelte"
      provides: "Artist page augmented with bio + tags + hero image enrichment below the existing hero, without changing the existing searchAll track-list behavior"
      contains: "enrichArtist"
  key_links:
    - from: "src/routes/(app)/artist/[name]/+page.svelte"
      to: "src/lib/services/lastfm.ts"
      via: "second $effect keyed on name calls enrichArtist async (separate from the searchAll $effect)"
      pattern: "enrichArtist"
    - from: "src/routes/(app)/artist/[name]/+page.svelte"
      to: "src/lib/components/TagChips.svelte"
      via: "reuse the TagChips component for artist tags"
      pattern: "TagChips"
---

<objective>
Enrich the existing `/artist/[name]` page with Last.fm artist data — a bio snippet (English, HTML-stripped, ~2-3 sentences) with the always-present "Read more on Last.fm" attribution link, top-tag chips (reusing TagChips), and a higher-res artist image for the hero when available and non-placeholder — layered on without touching the page's existing derived `searchAll` track-list behavior.

Purpose: Deliver D-01b (artist page bio + tags + better image) using the Plan-01 enrichment service + TagChips, satisfying ENRICH-01/02 on the artist surface and the D-07/D-08 bio/attribution locks.
Output: An augmented artist page that shows Last.fm bio/tags/image when matched and degrades silently to the existing track-list-only view otherwise.
</objective>

<phase_goal>
**As a** listener, **I want to** see an artist's Last.fm bio, tags, and a better image on the artist page, **so that** I get context about who I'm listening to without losing the existing hit-songs/albums view.

(Derived from the ROADMAP Phase 8 `**Goal:**` line — outcome-shaped, not in canonical user-story form. Capability and outcome taken from that goal; no scope invented.)
</phase_goal>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-last-fm-read-foundation-metadata-enrichment/08-CONTEXT.md
@.planning/phases/08-last-fm-read-foundation-metadata-enrichment/08-PATTERNS.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Contracts from Plan 01 (already shipped in wave 1). Use directly. -->

From src/lib/services/lastfm.ts (Plan 01):
```typescript
export type EnrichResult = { tags: string[]; bio: string | null; bioUrl: string | null; lastfmArt: string | null; listeners?: number | null; playcount?: number | null };
export function enrichArtist(name: string): Promise<EnrichResult>; // never throws; all-empty on failure/absent-key
```

From src/lib/components/TagChips.svelte (Plan 01):
```svelte
<!-- props: tags: string[]; onTagClick?: (tag: string) => void  (omit onTagClick for display-only) -->
<TagChips tags={...} />
```

From src/routes/(app)/artist/[name]/+page.svelte (existing — the analog to extend):
```typescript
const name = $derived(decodeURIComponent(page.params.name ?? ''));
let songs = $state<Track[]>([]); let loading = $state(true); let loadedFor = '';
const hero = $derived(songs.find((t) => t.cover)?.cover ?? null);
$effect(() => { /* keyed on name + loadedFor guard → searchAll(n,1) → dedupeBest → songs */ });
// hero markup: <div class="herocover" style:background-image={hero ? `url(${hero})` : '...'}></div>
// <h1>{names.dn(name)}</h1> <p class="note">{t('artist.derived', {count})}</p>
```

From src/lib/i18n/index.ts: `t()` + `TranslationKey = keyof typeof en` — add new keys to en.ts, zh-Hant.ts, zh-Hans.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add artist bio + tags + hero-image enrichment to the artist page (augment, never replace)</name>
  <files>src/routes/(app)/artist/[name]/+page.svelte</files>
  <read_first>
    - src/routes/(app)/artist/[name]/+page.svelte (the full existing page — its name-keyed $effect with loadedFor guard, the hero/h1/note markup, the .note/.muted/section styles to reuse)
    - src/lib/services/lastfm.ts (Plan 01 — enrichArtist contract; never throws)
    - src/lib/components/TagChips.svelte (Plan 01 — reuse for artist tags)
    - src/lib/stores/names.svelte.ts usage in the page (names.dn — bio is English-as-is, NOT run through dn; the artist name still uses dn)
    - src/lib/i18n/en.ts (CONSUME the keys Plan 01 already added: `lastfm.readMore`, `lastfm.about` — do NOT add/edit i18n keys; en defines TranslationKey)
    - .planning/research/FEATURES.md (artist.getInfo bio.summary + placeholder-image trap — artist images are frequently the grey star)
  </read_first>
  <action>
    In `src/routes/(app)/artist/[name]/+page.svelte`, add enrichment state alongside the existing `songs`/`loading`/`loadedFor`: `let enrich = $state<EnrichResult | null>(null)` and `let enrichedFor = ''`. Import `enrichArtist`, `EnrichResult` from `$lib/services/lastfm` and `TagChips` from `$lib/components/TagChips.svelte`.
    Add a SECOND `$effect` (separate from the existing `searchAll` effect — do NOT merge them; the bio load must never block or replace the track-list load, D-02) keyed on `name` with its own `enrichedFor` guard: when `name` changes, set `enrichedFor = name`, `enrich = null`, then `void enrichArtist(name).then((r) => { if (enrichedFor === name) enrich = r })`. Best-effort, async, never awaited.
    Render BELOW the existing hero block (after the `.note` line), inside the hero or a new section using the existing `.note`/`section`/`.muted` styles:
    - The bio paragraph: `{enrich?.bio}` rendered as plain text (already HTML-stripped by the service, English-as-is per D-07 — do NOT run through `names.dn`). Render the bio block only when `enrich?.bio` is non-null.
    - The "Read more on Last.fm" attribution link: ALWAYS render it whenever the bio is shown (D-08 / ToS) — an `<a href={enrich.bioUrl} target="_blank" rel="noopener noreferrer">{t('lastfm.readMore')}</a>`. If `bioUrl` is null but a bio exists, fall back to the artist's last.fm page URL pattern is NOT allowed — only render the link when `bioUrl` is present (and gate the bio render on having both bio AND bioUrl so the attribution is never missing).
    - `<TagChips tags={enrich?.tags ?? []} />` for the artist's top tags (display-only).
    Hero image: prefer the Last.fm artist image for the hero ONLY when `enrich?.lastfmArt` is present (the service already placeholder-filtered it) — compute `const heroImg = $derived(enrich?.lastfmArt ?? hero)` and use `heroImg` in the `.herocover` background-image; otherwise keep the existing derived `hero`. Never regress to placeholder (ENRICH-02 — lastfmArt is null when only placeholder/empty, so the fallback keeps the derived hero).
    Use the i18n keys Plan 01 already added (`t('lastfm.readMore')`, `t('lastfm.about')`) — this plan does NOT add or edit any i18n keys.
    Do NOT modify the existing `searchAll` effect, the albums derivation, or the hit-songs list — they must behave exactly as before.
  </action>
  <verify>
    <automated>pnpm check && pnpm test -- --run</automated>
    <human-check>Open `/artist/<a Western artist with a Last.fm bio>` on a `wrangler dev`/deployed build with LASTFM_KEY set: confirm a bio snippet, the "Read more on Last.fm" link, top-tag chips, and (if available) a better hero image appear below the hero, while the existing albums + hit-songs lists still render. Then open `/artist/<a CN artist not on Last.fm>` and confirm the page renders exactly as before with no bio/tags/attribution and no broken hero.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/routes/(app)/artist/[name]/+page.svelte` imports and calls `enrichArtist` in a SECOND `$effect` separate from the existing `searchAll` effect (the searchAll effect is unmodified).
    - The "Read more on Last.fm" attribution link renders whenever a bio is shown (bio render is gated on having both `bio` AND `bioUrl`), with `rel="noopener noreferrer"`.
    - `<TagChips>` is reused for artist tags (no duplicate chip implementation).
    - The hero falls back to the existing derived `hero` when `lastfmArt` is null (no placeholder regression — ENRICH-02).
    - Bio text is NOT passed through `names.dn` (English-as-is, D-07).
    - `pnpm check` clean; all existing tests pass; this plan consumes `lastfm.readMore`/`lastfm.about` (added by Plan 01) and does NOT modify any i18n file.
  </acceptance_criteria>
  <done>Artist page shows bio + attribution link + tag chips + better hero when matched; degrades silently to the existing track-list view otherwise; attribution always present with the bio; check + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → edge (`/api/lastfm/info`) | The artist page calls the Plan-01 edge route via `enrichArtist`; LASTFM_KEY stays edge-only (inherited posture) |
| rendered bio HTML | Last.fm bio content is HTML-stripped by the Plan-01 service before reaching the page; rendered as plain text |
| external attribution link | The Last.fm bioUrl opens cross-origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Information Disclosure | enrichArtist → /api/lastfm/info | mitigate | Inherits Plan-01 T-08-01: LASTFM_KEY edge-only, never in body/headers; the client `enrichArtist` only ever sees the clean shape |
| T-08-08 | Tampering (XSS via bio HTML) | bio render | mitigate | Bio is HTML-stripped server-side by the Plan-01 service and rendered as Svelte text interpolation (`{enrich.bio}`), never `{@html}` — no injection surface |
| T-08-09 | Spoofing (reverse-tabnabbing) | attribution `<a target="_blank">` | mitigate | Attribution link uses `rel="noopener noreferrer"` |
| T-08-10 | DoS-of-UX | enrichArtist load | mitigate | Enrichment runs in a SEPARATE `void`-fired `$effect` from the track-list `searchAll` load; never awaited, never blocks the existing page; `enrichArtist` never throws (Plan-01 contract) |
| T-08-11 | Information Disclosure (UX regression) | hero image | mitigate | Hero falls back to the existing derived cover when `lastfmArt` is null (placeholder already filtered upstream) — no placeholder regression (ENRICH-02) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (reuses Plan-01 service + existing TagChips). No legitimacy checkpoint required |
</threat_model>

<verification>
- `pnpm check` clean; `pnpm test -- --run` green (no regressions; this plan adds no new tests but must not break existing suites).
- Human-check: bio + always-present attribution link + tag chips + better hero appear for a matched artist; a non-matched/absent-key artist page renders exactly as before with no broken hero and no orphan attribution.
</verification>

<success_criteria>
- ENRICH-01: artist page shows Last.fm bio + tags + (better) image merged onto the existing derived view.
- ENRICH-02: artist hero never regresses to a placeholder image.
- D-07/D-08: bio is English-as-is, HTML-stripped, ~2-3 sentences, with the always-present "Read more on Last.fm" attribution link.
- The existing `searchAll` track-list behavior is unchanged (D-02: augment, not replace).
</success_criteria>

<output>
Create `.planning/phases/08-last-fm-read-foundation-metadata-enrichment/08-02-SUMMARY.md` when done.
</output>
