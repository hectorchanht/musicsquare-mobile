---
phase: 22-lyrics-polish
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - src/lib/components/NowPlaying.svelte
autonomous: false
requirements: [LYR-01, LYR-02, LYR-03]
nyquist_compliant: true

must_haves:
  truths:
    - "The lyrics `lines` derived composes parse → reorder → split: splitParenLines(reorderPairs(parseLRC(player.current.lrc))) (D-04 pipeline order)"
    - "Every rendered lyric <p> (main, paren clause, translation pair) is a tap target that seeks to its line's timestamp via seekFraction(time/duration); tapping while paused also starts playback (D-01/D-03)"
    - "A tap re-enables auto-scroll immediately and clears the idle timer, overriding the pointerdown suspend its own touch fired — the tapped line smooth-centers (D-02)"
    - "While touching/holding/scrolling the lyrics panel, auto-scroll is suspended; it resumes ~3s after activity stops, including iOS momentum glide (D-10/LYR-02)"
    - "An end spacer ≈ half the visible band lets the last lines center in half AND full sheet modes; NO top spacer added (D-11/D-12/LYR-03)"
    - "The first-of-group scroll anchor (activeIndexAndTime) is unchanged — after reorder, first-of-group is the original, so the original highlights as current (D-04/LYR-04)"
    - "Highlight styling is unchanged: both lines of the current same-timestamp group keep the .active treatment — no dimming of translation lines, no CSS changes to .lyrics p.active (D-06)"
  artifacts:
    - path: "src/lib/components/NowPlaying.svelte"
      provides: "Composed pipeline, per-line onclick seekToLine, 3s+momentum idle resume, end spacer element"
      contains: "reorderPairs("
  key_links:
    - from: "src/lib/components/NowPlaying.svelte lines $derived"
      to: "src/lib/services/lrc.ts reorderPairs + splitParenLines"
      via: "composed pure pipeline"
      pattern: "splitParenLines\\(reorderPairs\\(parseLRC"
    - from: "src/lib/components/NowPlaying.svelte lyric <p> onclick"
      to: "player.seekFraction"
      via: "seekToLine(line) using lineSeekFraction(line.time, player.duration)"
      pattern: "seekFraction\\("
    - from: "src/lib/components/NowPlaying.svelte anchor $effect"
      to: "lyrics-end-spacer height"
      via: "spacerH = Math.round(visHeight / 2)"
      pattern: "visHeight"
---

<objective>
Wire the pure lrc.ts functions from 22-01 into the `NowPlaying.svelte` lyrics tab and deliver the device-facing behaviors: compose the parse→reorder→split pipeline (LYR-04 anchor fix), per-line tap-to-seek with instant re-center and paused-resume (LYR-01), the 600ms→~3s + momentum-safe auto-scroll resume (LYR-02), and an end spacer so the last lines center (LYR-03).

Purpose: Make the lyrics view dependable — tap jumps and re-centers like YouTube Music, manual scroll holds ~3s before drifting back (fixing the live-broken LYR-02), and end lines reach center in every sheet mode.
Output: Updated `NowPlaying.svelte`; full suite + svelte-check green; a blocking human-verify checkpoint for the three device-only contracts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

ENVIRONMENT: shell default node is v16 and breaks vitest/svelte-check/vite. ALWAYS prefix every node/pnpm command with:
`export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"` and use pnpm.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-lyrics-polish/22-CONTEXT.md
@.planning/phases/22-lyrics-polish/22-RESEARCH.md
@.planning/phases/22-lyrics-polish/22-PATTERNS.md
@.planning/phases/22-lyrics-polish/22-VALIDATION.md
@.planning/phases/22-lyrics-polish/22-01-SUMMARY.md

<interfaces>
<!-- Exports from 22-01 this plan consumes. Use directly — no codebase exploration needed. -->
From src/lib/services/lrc.ts (after 22-01):
```typescript
export function parseLRC(txt: string): LyricLine[];
export function reorderPairs(lines: LyricLine[]): LyricLine[];     // D-04/D-05 — call BETWEEN parse and split
export function splitParenLines(lines: LyricLine[]): LyricLine[];  // widened D-07/D-08
export function lineSeekFraction(time: number, duration: number): number | null; // LYR-01 seek math
export interface LyricLine { time: number; text: string; fromParen?: boolean; }
```

<!-- Current NowPlaying.svelte anchors the executor must edit, NOT replace. Extracted from src/lib/components/NowPlaying.svelte. -->
- :25  import { parseLRC, splitParenLines, type LyricLine } from '$lib/services/lrc';  ← add reorderPairs, lineSeekFraction
- :67  function seek(e: MouseEvent) { ... player.seekFraction((e.clientX - r.left) / r.width); }  ← seek-call-site analog
- :83  const lines = $derived<LyricLine[]>(player.current?.lrc ? splitParenLines(parseLRC(player.current.lrc)) : []);  ← compose reorderPairs
- :92  const activeIndexAndTime = $derived.by(...)  ← DO NOT TOUCH (first-of-group anchor; D-04 needs zero change)
- :109 let autoScroll = $state(true); let idleTimer ...
- :121 const pressedPointers = new Set<number>(); function lyricsTouched(e) { autoScroll=false; ... }  ← NO setPointerCapture (keep)
- :144 function lyricsReleased() { ... idleTimer = setTimeout(() => (autoScroll = true), 600); }  ← raise 600 → ~3000
- :148 function lyricsWheel() { ... lyricsReleased(); }
- :154 anchor $effect: computes visHeight (:180), container.scrollTo (:187) — mode-aware visible-band center
- :897 <div class="lyrics" ... onpointerdown={lyricsTouched} onwheel={lyricsWheel}>  ← add onscroll re-arm
- :898 {#each lines as l, i (i)}  ← index key stays safe (reorder is per-LRC deterministic)
- :901 <p class:active={...} class:paren={l.fromParen}>  ← add onclick={() => seekToLine(l)}
- :911 </div>  ← insert end spacer element after the {#each}, inside .lyrics

player store (READ ONLY — no edit): player.seekFraction(frac) clamps to [0,1] and auto-plays when audio.paused (D-03 free). player.duration available.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose pipeline + per-line tap-to-seek with instant re-center (D-01/D-02/D-03/D-04, LYR-01/LYR-04)</name>
  <files>src/lib/components/NowPlaying.svelte</files>
  <read_first>
    - src/lib/components/NowPlaying.svelte (lines 25, 67-71, 83-107, 109-152, 897-911 — imports, seek analog, lines derived, anchor, touch handlers, render block)
    - src/lib/services/lrc.ts (after 22-01 — reorderPairs, lineSeekFraction signatures and behavior)
    - .planning/phases/22-lyrics-polish/22-RESEARCH.md (Pattern 1 compose, Pattern 5 tap-overrides-suspend, Pitfall 2 translation alignment verified-safe, Don't-Hand-Roll seek+resume)
    - .planning/phases/22-lyrics-polish/22-PATTERNS.md (NowPlaying section items 1-4 — compose seam, anchor do-not-touch, seek-call-site, onclick wiring)
  </read_first>
  <action>
    Update the import at NowPlaying.svelte:25 to add `reorderPairs` and `lineSeekFraction` to the existing `$lib/services/lrc` import. Change the `lines` $derived (:83-85) to compose `splitParenLines(reorderPairs(parseLRC(player.current.lrc)))` — reorder MUST sit between parse and split (D-04 pipeline order). Do NOT modify `activeIndexAndTime` (:92-107); after reorder, first-of-group is the original, so the existing first-of-group anchor delivers LYR-04 with zero anchor changes.

    Add a `seekToLine(line: LyricLine)` function modeled on the progress-bar `seek()` analog (:67): compute the fraction with `lineSeekFraction(line.time, player.duration)`; if the result is non-null call `player.seekFraction(frac)` (which auto-plays when paused — D-03 is free, no explicit play() call). Then clear `idleTimer` and set `autoScroll = true` (D-02: override the suspend that this tap's own `lyricsTouched` pointerdown just set; the anchor $effect re-runs on the autoScroll flip and smooth-centers the now-active line). Do NOT add setPointerCapture anywhere — a sub-slop tap must still produce the native click that reaches onclick (project tap-vs-scroll idiom; a real drag fires pointercancel/no click and the suspend correctly holds).

    Add `onclick={() => seekToLine(l)}` to the lyric `<p>` (:901). Keep the index key `(i)` — reorder is deterministic per-LRC, alignment with translated[i] holds (Pitfall 2 verified safe). Do not touch the translation $effect or the showTr/translated[i] render.
  </action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm check && pnpm vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "splitParenLines(reorderPairs(parseLRC" src/lib/components/NowPlaying.svelte` returns a match (pipeline composed in correct order)
    - `grep -n "reorderPairs" src/lib/components/NowPlaying.svelte` shows it in BOTH the import (:25 region) and the lines derived
    - `grep -n "function seekToLine" src/lib/components/NowPlaying.svelte` returns a match; the function body references `lineSeekFraction` and `player.seekFraction` and sets `autoScroll = true`
    - `grep -n "onclick={() => seekToLine" src/lib/components/NowPlaying.svelte` returns a match on the lyric <p>
    - activeIndexAndTime block (:92-107) is byte-unchanged — `git diff src/lib/components/NowPlaying.svelte` shows no edits inside the activeIndexAndTime $derived
    - No `setPointerCapture` added — `grep -c "setPointerCapture" src/lib/components/NowPlaying.svelte` equals its pre-edit count (unchanged)
    - `pnpm check` reports 0 errors / 0 warnings; `pnpm vitest run` full suite passes
  </acceptance_criteria>
  <done>Pipeline composes parse→reorder→split; every lyric <p> taps to seek with instant re-center and paused-resume; anchor logic untouched; suite + svelte-check green.</done>
</task>

<task type="auto">
  <name>Task 2: Momentum-safe ~3s idle resume + end spacer (D-10/D-11/D-12, LYR-02/LYR-03)</name>
  <files>src/lib/components/NowPlaying.svelte</files>
  <read_first>
    - src/lib/components/NowPlaying.svelte (lines 109-152 touch/idle handlers, 154-188 anchor $effect with visHeight, 897-911 render block + .lyrics container; plus the <style> block for .lyrics)
    - .planning/phases/22-lyrics-polish/22-RESEARCH.md (Pitfall 1 iOS momentum/early-resume root cause + momentum-safe onscroll re-arm, Pitfall 3 end spacer must grow scrollHeight, Code Examples bumpResume + spacer)
    - .planning/phases/22-lyrics-polish/22-PATTERNS.md (NowPlaying items 5-6 idle-resume tune + end spacer sizing from visHeight)
  </read_first>
  <action>
    Raise the idle-resume delay in `lyricsReleased` (:144-146) from 600 to 3000ms (D-10). Add a momentum-safe re-arm: introduce a `RESUME_MS = 3000` constant and a `bumpResume()` handler that, only while `autoScroll === false`, clears and re-sets `idleTimer` to fire `autoScroll = true` after RESUME_MS — so resume fires ~3s after scrolling truly STOPS, covering iOS momentum glide which emits no further pointer/wheel events (Pitfall 1). Wire `onscroll={bumpResume}` onto the `.lyrics` scroller `<div>` (:897), alongside the existing `onpointerdown={lyricsTouched}` and `onwheel={lyricsWheel}`. Have `lyricsReleased` and `lyricsWheel` use the same RESUME_MS. Do NOT add setPointerCapture; do NOT rewrite the pressedPointers Set / window-capture pointerup mechanism (:121-143) — it is intact and LYR-02 will be device-verified.

    Add the end spacer (D-11): declare `let spacerH = $state(0)`. Inside the anchor $effect, after `visHeight` is computed (~:180), set `spacerH = Math.round(visHeight / 2)`. Insert a real trailing element `<div class="lyrics-end-spacer" style:height="{spacerH}px" aria-hidden="true"></div>` INSIDE the `.lyrics` container, after the `{#each}` block (after :910, before the closing `</div>` at :911) — a real element is required because browsers clamp scrollTo to content bounds, so the spacer must grow scrollHeight (Pitfall 3). Do NOT add any top spacer (D-12). Add a minimal `.lyrics-end-spacer { width: 100%; }` rule if the style block requires it (height is inline).
  </action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm check && pnpm vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "3000" src/lib/components/NowPlaying.svelte` shows the raised resume delay (RESUME_MS or inline) replacing the old 600 in the lyrics resume path; `grep -c "(autoScroll = true), 600" src/lib/components/NowPlaying.svelte` returns 0
    - `grep -n "function bumpResume" src/lib/components/NowPlaying.svelte` returns a match; bumpResume early-returns when autoScroll is true and re-arms idleTimer
    - `grep -n "onscroll={bumpResume}" src/lib/components/NowPlaying.svelte` returns a match on the .lyrics container
    - `grep -n "lyrics-end-spacer" src/lib/components/NowPlaying.svelte` returns a match; the element sits inside .lyrics after the {#each}
    - `grep -n "spacerH = Math.round(visHeight / 2)" src/lib/components/NowPlaying.svelte` returns a match inside the anchor $effect
    - No top spacer added; no setPointerCapture added; pressedPointers/window-pointerup block (:121-143) byte-unchanged in `git diff`
    - `pnpm check` 0/0; `pnpm vitest run` full suite passes
  </acceptance_criteria>
  <done>Idle resume raised to ~3s with onscroll momentum re-arm; end spacer ≈ half visible band mounted inside .lyrics; no top spacer; touch-suspend mechanism untouched; suite + svelte-check green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device verification — tap-seek, touch-suspend (live-broken), end-spacer centering</name>
  <files>src/lib/components/NowPlaying.svelte</files>
  <action>Pause for human device verification AFTER Tasks 1-2 are automated and committed. This is a verification-only checkpoint — no code is written here. The executor runs the dev server and the human exercises the three device-only contracts (tap-seek, momentum touch-suspend, end-spacer centering) on a real iOS Safari device. See <what-built>/<how-to-verify> below.</action>
  <what-built>
    Composed parse→reorder→split lyrics pipeline (LYR-04 original-line highlight), per-line tap-to-seek with instant re-center + paused-resume (LYR-01), ~3s momentum-safe auto-scroll resume (LYR-02 — the previously live-broken behavior), and an end spacer so last lines center (LYR-03). These three contracts cannot be unit-tested (pointer interplay + iOS momentum scroll + layout measurement under sheet modes).
  </what-built>
  <how-to-verify>
    Run the dev server: `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm dev` and open on a real iOS Safari device (the LYR-02 break is iOS-momentum-specific). Verify each:

    1. LYR-01 tap-to-seek + instant re-center: Open a playing track's lyrics. Tap any line → playback jumps to that line's timestamp and the tapped line smooth-centers immediately. Tap a paren-clause line and a translation line too — all are tap targets.
    2. LYR-01/D-03 paused-resume: Pause playback, tap a different line → playback resumes from that line.
    3. LYR-02 touch-suspend (PRIMARY — reported broken): Flick-scroll the lyrics hard. During the momentum glide AND for ~3s after it stops, the view must NOT snap back to the current line. After ~3s idle it smoothly re-centers. Repeat holding a finger down — auto-scroll stays suspended the whole time.
    4. LYR-02/D-02 tap overrides suspend: Immediately after scrolling away, tap a line → it seeks AND re-centers instantly (does not wait the 3s).
    5. LYR-04 CN ordering: On a CN LRC that ships translation-above-original, confirm the ORIGINAL line (not the translation) is the highlighted/anchored current line.
    6. LYR-03 end spacer: Seek near the song's end. In HALF sheet mode and FULL sheet mode, the last 2-3 lines must reach the vertical center (not pin to the bottom).
  </how-to-verify>
  <resume-signal>Type "approved" once all six pass on a real iOS device, or describe which contract failed (esp. if LYR-02 still snaps back during momentum — that indicates the pressedPointers Set / window listener itself, not early-resume, per RESEARCH Open Question 1 / A2).</resume-signal>
  <verify><human-check>All six device contracts (LYR-01 tap+paused, LYR-02 momentum-suspend + tap-override, LYR-04 CN ordering, LYR-03 spacer in half AND full) pass on a real iOS Safari device.</human-check></verify>
  <done>Human approves all six device contracts on a real iOS Safari device, OR logs the specific failing contract for gap closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM event → player store seek | A tap on a lyric <p> writes a seek fraction to the player store. Input is a derived numeric time/duration, not user-supplied text; no injection surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02 | Input Validation (V5) | lyric <p> text + translated[i] render | accept | Lyric text and translations render via Svelte auto-escaped text interpolation ({l.text} / {translated[i]}); no {@html}. ASVS V5 satisfied by default escaping — no change to escaping posture in this plan. |
| T-22-03 | Tampering | seekToLine fraction write | mitigate | lineSeekFraction guards duration <= 0 / non-finite → null; player.seekFraction clamps to [0,1]. No unbounded value reaches the audio element. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (component wiring only). RESEARCH § Package Legitimacy Audit is N/A. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm vitest run` — full suite green (lrc fixtures from 22-01 still pass; no regressions)
- `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm check` — svelte-check 0 errors / 0 warnings
- Device pass on the Task 3 checkpoint (LYR-01 tap+paused, LYR-02 momentum-suspend, LYR-03 spacer in half AND full) — REQUIRED before `/gsd:verify-work`
</verification>

<success_criteria>
- lines derived composes parse→reorder→split; activeIndexAndTime untouched (LYR-04 via anchor)
- Every lyric <p> taps to seek with instant re-center and paused-resume (LYR-01)
- Idle resume raised to ~3s with onscroll momentum re-arm; touch-suspend mechanism intact (LYR-02)
- End spacer ≈ half visible band inside .lyrics; no top spacer (LYR-03)
- Full suite + svelte-check green; device checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/22-lyrics-polish/22-02-SUMMARY.md` when done.
</output>
