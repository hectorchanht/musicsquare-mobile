---
phase: 21-search-cover-pipeline-polish
plan: 05
type: execute
wave: 2
depends_on: ["21-02"]
files_modified:
  - src/routes/(app)/library/+page.svelte
  - src/routes/(app)/album/[name]/+page.svelte
  - src/routes/(app)/artist/[name]/+page.svelte
autonomous: false
requirements: [COVER-02]
must_haves:
  truths:
    - "Track rows on the library page (liked, playlist, downloads list sites), album tracklist, and artist tracklist carry use:lazyCover (D-14 — all track lists this phase)"
    - "Each surface resolves empty/broken covers on scroll-into-view and repaints via a reactive resolved-cover map keyed by uid, reusing the Plan-02 action — no new resolver"
    - "Album tracklist rows (which currently render ONLY the gradient, no track.cover) resolve and paint a real cover lazily"
    - "Each surface keeps its own seeded-gradient fallbackCover as the null placeholder (D-12) and its existing use:longpress + onclick handlers intact"
    - "Resolved covers are SOLID https URLs only — no widening of the injection surface (T-0bb-01)"
  artifacts:
    - path: "src/routes/(app)/library/+page.svelte"
      provides: "use:lazyCover on all track-row .art sites + reactive resolved-cover map"
      contains: "use:lazyCover"
    - path: "src/routes/(app)/album/[name]/+page.svelte"
      provides: "use:lazyCover on the album tracklist rows (gradient→resolved cover)"
      contains: "use:lazyCover"
    - path: "src/routes/(app)/artist/[name]/+page.svelte"
      provides: "use:lazyCover on the artist tracklist rows"
      contains: "use:lazyCover"
  key_links:
    - from: "src/routes/(app)/library/+page.svelte"
      to: "src/lib/actions/lazyCover.ts"
      via: "use:lazyCover on each .art span with an onResolved reactive-map bump"
      pattern: "use:lazyCover"
    - from: "src/routes/(app)/album/[name]/+page.svelte"
      to: "src/lib/actions/lazyCover.ts"
      via: "use:lazyCover resolves the gradient-only album rows"
      pattern: "use:lazyCover"
---

<objective>
Wire the Plan-02 `lazyCover` action into the remaining track-list surfaces — library (liked / playlist / downloads), album tracklist, and artist tracklist — so empty/broken covers resolve on scroll-into-view and cache (uid-first/name), never refetching.

Purpose: COVER-02 D-14 — the reusable lazyCover action must be wired into ALL track lists this phase, not just search. Depends on Plan 02 (lazyCover action). Independent file set from Plan 04 (search page) so it runs in parallel within Wave 2.
Output: edited library, album, and artist route components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

ENVIRONMENT: Shell default node is v16 and breaks vitest/svelte-check. EVERY node command MUST be prefixed with:
`export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"` and use pnpm.

DEPENDS ON: Plan 02 (lazyCover action). Read 21-02-SUMMARY.md for the action signature. Mirror Plan 04's reactive-map + render-expression idiom on the search page for consistency (read 21-04-SUMMARY.md if available, else mirror the pattern described below).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-search-cover-pipeline-polish/21-CONTEXT.md
@.planning/phases/21-search-cover-pipeline-polish/21-RESEARCH.md
@.planning/phases/21-search-cover-pipeline-polish/21-PATTERNS.md
@.planning/phases/21-search-cover-pipeline-polish/21-02-SUMMARY.md

<interfaces>
From src/lib/actions/lazyCover.ts (Plan 02):
  export const lazyCover: Action<HTMLElement, { track: Track; onResolved: (uid: string, url: string) => void }>

Current render sites (re-verify line numbers at edit time):
  library/+page.svelte:
    - liked list:     row ~160 (use:longpress + onclick), .art ~161  `style:background-image={track.cover ? url(...) : fallbackCover(track)}`
    - playlist list:  row ~185, .art ~186
    - downloads list: row ~202, .art ~203
    - (another track list) row ~233, .art ~234
    - local fallbackCover(t) ~111
  album/[name]/+page.svelte:
    - tracklist .art ~503  `style:background-image={fallbackCover(track.artist + track.title)}`  ← GRADIENT ONLY, no track.cover today; lazyCover must resolve + paint
    - local fallbackCover(seed) ~63
  artist/[name]/+page.svelte:
    - tracklist .art ~378  `style:background-image={track.cover ? url(...) : fallbackCover(track)}`
    - local fallbackCover(t) ~89
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: use:lazyCover on all library track-row .art sites</name>
  <files>src/routes/(app)/library/+page.svelte</files>
  <read_first>
    - src/routes/(app)/library/+page.svelte (the four track-row .art sites ~161/186/203/234, each with use:longpress + onclick on the row; the favCovers reactive-map pattern ~220 as a reactive-cover precedent; fallbackCover ~111)
    - src/lib/actions/lazyCover.ts (action signature from Plan 02)
  </read_first>
  <action>
    Add a reactive resolved-cover map `let resolvedCovers = $state<Record<string, string>>({});` (or the repo's reactive-collection idiom — note this page already uses a `favCovers` map, mirror its shape). Attach `use:lazyCover={{ track, onResolved: (uid, url) => { resolvedCovers = { ...resolvedCovers, [uid]: url }; } }}` to each of the four track-row `.art` spans (~161, 186, 203, 234). Change each `.art` background-image to prefer the resolved cover: `style:background-image={ (resolvedCovers[track.uid] ?? track.cover) ? `url(${resolvedCovers[track.uid] ?? track.cover})` : fallbackCover(track) }`. Keep each row's `use:longpress` + onclick handlers untouched (lazyCover stacks on the `.art` span). The resolved value is a SOLID https URL (Plan 02 gate) so the existing background-image render is safe — do not widen the injection surface (T-0bb-01). Do NOT touch the artist-avatar / favCovers rows (those are artist-image rows, not track covers).
  </action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'use:lazyCover' src/routes/(app)/library/+page.svelte` returns at least 4 (one per track-row .art site)
    - A reactive `resolvedCovers` state exists and is reassigned in onResolved (grep both)
    - Each touched `.art` prefers `resolvedCovers[track.uid] ?? track.cover` (grep the render expression)
    - The four rows' `use:longpress` + onclick remain (grep count unchanged)
    - `pnpm check` clean
  </acceptance_criteria>
  <done>All four library track-row .art sites resolve covers lazily on scroll via the Plan-02 action with a reactive repaint; longpress/onclick intact; artist-avatar rows untouched.</done>
</task>

<task type="auto">
  <name>Task 2: use:lazyCover on album + artist tracklist rows</name>
  <files>src/routes/(app)/album/[name]/+page.svelte, src/routes/(app)/artist/[name]/+page.svelte</files>
  <read_first>
    - src/routes/(app)/album/[name]/+page.svelte (the tracklist .art ~503 — GRADIENT ONLY today, no track.cover; fallbackCover(seed) ~63; the {#each} over tracks)
    - src/routes/(app)/artist/[name]/+page.svelte (the tracklist .art ~378 with track.cover ? url : fallbackCover; fallbackCover(t) ~89; the al-cover sites ~345/410 are ALBUM-image rows — do NOT touch)
    - src/lib/actions/lazyCover.ts (action signature)
  </read_first>
  <action>
    Album page: add `let resolvedCovers = $state<Record<string, string>>({});`. The album tracklist `.art` (~503) currently renders ONLY the gradient (`fallbackCover(track.artist + track.title)`) — wire `use:lazyCover={{ track, onResolved: (uid, url) => { resolvedCovers = { ...resolvedCovers, [uid]: url }; } }}` on it and change the render to `style:background-image={ resolvedCovers[track.uid] ? `url(${resolvedCovers[track.uid]})` : fallbackCover(track.artist + track.title) }` so a resolved cover paints over the gradient. (Album track objects expose `track.uid`, `track.artist`, `track.title` — confirm at edit time; lazyCover reads cache uid-first then name.)

    Artist page: add the same reactive map and attach `use:lazyCover` to the tracklist `.art` (~378), preferring `resolvedCovers[track.uid] ?? track.cover` over the gradient (mirror the library Task-1 expression). Do NOT touch the `al-cover` album-image rows (~345, 410) — those are album artwork, not track covers, and out of scope.

    Keep each page's own `fallbackCover` gradient as the null placeholder (D-12) and any existing row handlers intact. Resolved covers are SOLID https only (T-0bb-01).
  </action>
  <verify>
    <automated>export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'use:lazyCover' src/routes/(app)/album/[name]/+page.svelte` matches on the tracklist row
    - `grep -n 'use:lazyCover' src/routes/(app)/artist/[name]/+page.svelte` matches on the tracklist row
    - Album tracklist render prefers `resolvedCovers[track.uid]` over the gradient (grep the expression at ~503)
    - The artist `al-cover` album-image sites (~345/410) are NOT modified (no use:lazyCover added there)
    - `pnpm check` clean
  </acceptance_criteria>
  <done>Album and artist tracklist rows resolve covers lazily on scroll (album rows now paint a real cover over the gradient); album-image rows untouched; gradient fallback preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify lazy covers across library, album, and artist surfaces</name>
  <what-built>use:lazyCover wired into the library (liked/playlist/downloads), album tracklist, and artist tracklist rows with reactive resolved-cover repaint, reusing the Plan-02 action + cache.</what-built>
  <how-to-verify>
    1. `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"; pnpm dev`.
    2. Library: open the Library tab with liked tracks / a playlist / downloads that include cover-less or QQ tracks → scroll; covers resolve as rows enter view. Scroll back → no refetch (network tab shows no repeat cover fetches for resolved uids).
    3. Album: open an album page whose tracklist rows show only the gradient → scroll; real covers resolve and paint over the gradient.
    4. Artist: open an artist page tracklist with cover-less tracks → scroll; covers resolve. Confirm the album-artwork tiles (al-cover) are unchanged.
    5. Confirm no broken-image flashes; rows with no resolvable cover keep the gradient; longpress/tap still open the track menu / play.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what failed (surface, cover not resolving, refetch on re-scroll, broken handler, wrong row type touched).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolved cover URL → list row render | a resolved cover string crosses into the row background-image; must be https-only, no new injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 (T-0bb-01) | Tampering / XSS | lazyCover resolved cover render on 3 surfaces | mitigate | resolvedCovers holds SOLID https URLs only (Plan 02 isSolidCover gate); rendered via the existing background-image path used for source covers — resolved values not routed through a less-safe sink |
| T-21-05 | DoS | lazyCover fan-out on scroll | mitigate | per-row action: unobserve-after-first + in-flight de-dupe + cache-first read (Plan 02); viewport-bounded, no Promise.all over the list |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (zero new deps — RESEARCH.md audit "Not applicable"); no install task to gate |
</threat_model>

<verification>
- `pnpm check` clean after each task
- Full suite green at wave merge
- Manual checkpoint (Task 3): covers resolve on scroll across library/album/artist, cache (no refetch), gradient fallback + handlers intact, album-image rows untouched
</verification>

<success_criteria>
- COVER-02 D-14 complete: lazyCover wired into ALL track lists this phase (search via Plan 04; library/album/artist here)
- Album gradient-only rows now paint resolved covers; album-image / artist-avatar rows untouched; D-12 gradient fallback + T-0bb-01 https-only preserved
</success_criteria>

<output>
Create `.planning/phases/21-search-cover-pipeline-polish/21-05-SUMMARY.md` when done. Record which list sites got lazyCover and confirm the album tracklist now resolves real covers over the gradient.
</output>
