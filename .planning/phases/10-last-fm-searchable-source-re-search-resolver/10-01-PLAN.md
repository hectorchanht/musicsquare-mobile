---
phase: 10-last-fm-searchable-source-re-search-resolver
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/services/score-match.ts
  - src/lib/services/score-match.test.ts
  - src/lib/services/discovery.ts
  - src/lib/services/discovery.test.ts
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [LFSRC-03]
# Requirement disposition (CONTEXT D-01 — supersedes pre-rescope ROADMAP criteria):
#   LFSRC-02 = DONE in Phase 9 (resolveStub) — no new work; mark Complete in traceability (Task 3).
#   LFSRC-01 = DROPPED / satisfied-by-pattern (resolveStub IS the resolver; no formal 'lastfm'
#              SourceId — avoids widening SourceId/SOURCES) — mark dropped/backlog (Task 3).
#   LFSRC-03 = the ONLY live requirement this phase (best-match scoring upgrade).

must_haves:
  truths:
    - "(D-02) resolveStub scores searchAll candidates instead of blindly taking dedupeBest[0]: a clean title match outranks a cover/karaoke/live/instrumental/remix variant of the same song"
    - "(D-02) a candidate whose normalized artist+title (via matchKey) closely matches the Last.fm {artist,title} outranks a loose/unrelated candidate"
    - "(D-02) when the Last.fm query itself asks for a variant (e.g. title contains 'live'), matching variant candidates are NOT penalized"
    - "(D-02) among similarly-scored candidates the final tie-break is the existing dedupeBest preferredSource + quality ranking"
    - "(D-03) resolveStub ALWAYS returns the top-scored candidate when searchAll yields ≥1 result — no score threshold ever nulls a found result"
    - "(D-03) resolveStub returns null ONLY when searchAll yields zero results, and never throws (best-effort posture preserved)"
    - "(D-04) NO duration sanity check is added (out of scope — would need Last.fm duration plumbing)"
    - "(D-05) NO GD Studio ytmusic / new source / new endpoint / registry change — resolveStub remains the single CN-source re-search resolver and the single change point"
    - "(D-01) ROADMAP Phase 10 + REQUIREMENTS traceability updated: LFSRC-02 Complete (Phase 9), LFSRC-01 dropped/backlog, LFSRC-03 implemented"
  artifacts:
    - path: "src/lib/services/score-match.ts"
      provides: "Pure scoreMatch(query, candidate) helper + exported variant-keyword list"
      contains: "export function scoreMatch"
    - path: "src/lib/services/score-match.test.ts"
      provides: "Pure-logic vitest coverage for the scorer (variant penalty, similarity, query-asked-for-variant, tie-break inputs)"
      contains: "scoreMatch"
    - path: "src/lib/services/discovery.ts"
      provides: "resolveStub upgraded to a scored pick over searchAll candidates"
      contains: "scoreMatch"
  key_links:
    - from: "src/lib/services/discovery.ts"
      to: "src/lib/services/score-match.ts"
      via: "import + sort searchAll candidates by scoreMatch, dedupeBest as final tie-break"
      pattern: "scoreMatch"
    - from: "src/lib/services/score-match.ts"
      to: "src/lib/services/match-key.ts"
      via: "reuse matchKey normalization for artist+title similarity"
      pattern: "match-key|matchKey"
---

<objective>
Upgrade `resolveStub(artist, title)` in `src/lib/services/discovery.ts` from a blind
`dedupeBest(...)[0]` pick to a SCORED pick over the `searchAll` candidates, so tapping a
Last.fm-discovered track plays the REAL song — not a karaoke / cover / live / instrumental
variant (LFSRC-03 / Pitfall 7 wrong-song resolution).

Purpose: Phase 9 already made Last.fm discovery items tap-to-play via `resolveStub` (LFSRC-02
DONE). The remaining defect is resolution QUALITY: `dedupeBest[0]` can surface a cover/karaoke
variant first. This phase fixes that with a pure, unit-testable `scoreMatch` helper that
re-ranks candidates, with `dedupeBest`'s preferredSource/quality order as the final tie-break.

Output: a pure `scoreMatch` helper + tests, an upgraded `resolveStub`, and a traceability
update (D-01) reconciling REQUIREMENTS/ROADMAP with the rescope.

Requirement disposition (CONTEXT D-01 — read this before the requirements-coverage gate
flags "LFSRC-01/02 unplanned"):
- LFSRC-02 is DONE (Phase 9 `resolveStub`) — Task 3 marks it Complete; no implementation here.
- LFSRC-01 is DROPPED / satisfied-by-pattern (no formal `'lastfm'` SourceId; `resolveStub` IS
  the resolver) — Task 3 marks it dropped/backlog; no implementation here.
- LFSRC-03 is the ONLY live requirement — Tasks 1 + 2 deliver it.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-last-fm-searchable-source-re-search-resolver/10-CONTEXT.md
@.planning/phases/09-discovery-hot-picks-tab/09-01-SUMMARY.md

# The change surface + the reuse primitives (read before touching anything):
@src/lib/services/discovery.ts
@src/lib/services/dedupe.ts
@src/lib/services/match-key.ts
@src/lib/services/catalog.ts
@src/lib/stores/settings.svelte.ts

<interfaces>
<!-- Contracts the executor needs — already in the codebase, do NOT re-derive. -->

From src/lib/sources/types.ts (the candidate shape the scorer reads):
- `Track` has: `title: string`, `artist: string`, `album: string`, `source: SourceId`,
  `quality: string | null`, `qualityLabel: string | null`, `uid`, `songid`, … .
  The scorer reads `title` (variant-keyword penalty) and `artist`+`title` (similarity).
- `SourceId = 'netease' | 'qq' | 'kuwo' | 'joox'`.

From src/lib/services/match-key.ts (reuse for similarity — do NOT reimplement normalization):
- `matchKey(artist: string, title: string): string` → `${norm(artist)}|${norm(title)}`
  (artist-first; lowercases, drops bracketed/feat./remaster/live suffixes, strips all
  punctuation/space, keeps CJK+latin+digits). Empty inputs yield the empty component
  (`matchKey('', '') === '|'`). NOTE: `norm` itself is NOT exported — only `matchKey` is.

From src/lib/services/dedupe.ts (the FINAL tie-break — keep, do NOT replace):
- `dedupeBest(tracks: Track[], preferred?: SourceId): Track[]` → collapses
  same-song-different-source dupes keeping best quality, preferred source wins ties,
  order preserved by first appearance.

From src/lib/services/catalog.ts:
- `searchAll(keyword, page=1, prefs?, signal?): Promise<SearchResult>` where
  `SearchResult.interleaved: Track[]` is the candidate list.

From src/lib/stores/settings.svelte.ts:
- `settings.preferredSource: SourceId | undefined` (the dedupeBest tie-break input).

Current resolveStub (the body to upgrade — discovery.ts ~line 23):
- `searchAll(`${artist} ${title}`, 1)` → `dedupeBest(r.interleaved, settings.preferredSource)[0] ?? null`,
  wrapped in try/catch returning null. NEVER throws. The strings, the page=1, the
  try/catch-null posture, and the import set must all be preserved.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Pure scoreMatch helper (RED → GREEN)</name>
  <files>src/lib/services/score-match.ts, src/lib/services/score-match.test.ts</files>
  <read_first>
    - src/lib/services/match-key.ts — reuse `matchKey(artist,title)` for the similarity term; do NOT reimplement `norm` (it is not exported — call `matchKey` and compare keys, or normalize the title for keyword scanning with the SAME spirit but a separate local lowercase pass for keyword detection).
    - src/lib/services/dedupe.ts — note `dedupeBest` is the FINAL tie-break (Task 2 wires it); the scorer must NOT duplicate quality/source ranking.
    - src/lib/services/discovery.test.ts — mirror the `mk(source, songid, artist, extra)` Track factory + zh fixtures (`周杰伦` / `稻香`) and the vitest style for the new test file.
    - src/lib/sources/types.ts — `Track` field names the scorer reads (`title`, `artist`).
  </read_first>
  <behavior>
    - Variant-keyword penalty (D-02a): a candidate whose `title` contains a variant keyword (cover / karaoke / live / instrumental / remix / "sped up" / "8d" / tribute / re-recorded / acoustic — plus common CJK variant terms: 翻唱 / 卡拉ok|卡拉OK / 现场|現場 / 伴奏 / 纯音乐|純音樂 / live版|現場版 / 重制|重製) scores LOWER than an otherwise-identical clean-title candidate.
    - "Query asked for the variant" exception (D-02a): if the QUERY (artist+title the user tapped) already contains that variant keyword, that keyword is NOT penalized for that candidate (a user who tapped a "Live" track should get the live take). Scan the query against the same keyword list and exclude matched keywords from the penalty for that resolve.
    - Artist+title similarity reward (D-02b): a candidate whose normalized artist+title (via `matchKey`) equals/closely matches the query's `matchKey(artist,title)` scores HIGHER than a loosely-matching one. Exact key match = max similarity; otherwise a graded similarity (planner discretion on the metric — e.g. token overlap or per-component equality; keep it pure + deterministic).
    - NO threshold and NO source/quality logic in the scorer (D-03 + separation): the scorer only RE-RANKS by (similarity − variant penalty); it never returns null and never reads `source`/`quality`/`preferredSource` (that is dedupeBest's job in Task 2).
    - Determinism: same inputs → same number; pure (no `$state`, no `$app/*`, no I/O) — node-vitest-testable like match-key.ts / dedupe.ts.
  </behavior>
  <action>
    Create `src/lib/services/score-match.ts` exporting a PURE
    `scoreMatch(query: { artist: string; title: string }, candidate: Track): number`
    (higher = better) and an exported `VARIANT_KEYWORDS: string[]` (or RegExp set) holding
    the English + CJK variant terms listed in `<behavior>` so the list is editable and the
    test can assert against it. Implement scoring as `similarity(query, candidate) − variantPenalty(query, candidate)`:
    similarity reuses `matchKey` (artist-first normalization) for the query and the candidate
    and grades how close they are; variantPenalty scans the candidate `title` (lowercased) for
    any `VARIANT_KEYWORDS` term that is NOT also present in the query string, subtracting a
    fixed weight per offending keyword. Keep weights as named consts so the similarity term can
    out-weigh a single variant hit when the variant is clearly the intended song but loses to a
    clean exact match otherwise. Do NOT add a threshold, do NOT read `candidate.source`/`quality`,
    do NOT call `searchAll`/`dedupeBest`/`settings` — this file is pure and import-light
    (only `matchKey` from match-key.ts + the `Track` type). NO fenced code in the action; the
    signature + keyword list above are the contract.
    Create `src/lib/services/score-match.test.ts` (RED first — write failing assertions before
    the implementation passes): cases for (1) clean title outranks karaoke/cover/live variant of
    the same song; (2) CJK variant term (翻唱 / 伴奏) is penalized; (3) query containing 'live'
    does NOT penalize a 'Live' candidate; (4) exact `matchKey` match outranks a loose match;
    (5) `VARIANT_KEYWORDS` is a non-empty editable list containing both an English term (e.g.
    'karaoke') and a CJK term (e.g. '翻唱'); (6) determinism (same inputs → same score).
  </action>
  <verify>
    <automated>pnpm test -- score-match</automated>
  </verify>
  <acceptance_criteria>
    - `scoreMatch({artist:'周杰伦',title:'稻香'}, cleanCandidate) > scoreMatch({artist:'周杰伦',title:'稻香'}, karaokeCandidate)` where karaokeCandidate.title contains 'karaoke' (or 翻唱).
    - `scoreMatch({artist:'X',title:'Song (Live)'}, liveCandidate)` does NOT apply the 'live' penalty (query asked for live).
    - A candidate whose `matchKey` equals the query key scores higher than one that does not.
    - `VARIANT_KEYWORDS` exported, non-empty, contains at least one English + one CJK term.
    - `scoreMatch` never returns null/NaN; same inputs return the same number.
    - `pnpm test -- score-match` green; `pnpm check` 0 errors / 0 warnings.
  </acceptance_criteria>
  <done>scoreMatch is pure, exported, tested (variant penalty, query-asked-for-variant exception, similarity ordering, determinism), and check + the score-match suite are green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Wire scoreMatch into resolveStub (scored pick, dedupeBest tie-break)</name>
  <files>src/lib/services/discovery.ts, src/lib/services/discovery.test.ts</files>
  <read_first>
    - src/lib/services/discovery.ts — `resolveStub` body (~line 23) to upgrade; preserve the `searchAll(`${artist} ${title}`, 1)` call, the import set, and the try/catch → null posture.
    - src/lib/services/score-match.ts — `scoreMatch` + `VARIANT_KEYWORDS` from Task 1.
    - src/lib/services/dedupe.ts — `dedupeBest(tracks, preferred)` stays the FINAL tie-break among similarly-scored candidates.
    - src/lib/services/discovery.test.ts — existing 4 resolveStub cases (top hit / first-of-many / null on miss / null on throw) MUST stay green; add the scoring cases alongside them using the same `mk`/`result` helpers and zh fixtures.
    - src/lib/stores/settings.svelte.ts — `settings.preferredSource` is the dedupeBest input (already imported in discovery.ts).
  </read_first>
  <behavior>
    - (D-03) Non-empty `searchAll` result → resolveStub ALWAYS returns a Track (the top-scored one); it NEVER returns null for a "low score".
    - (D-02) Among the `searchAll` candidates, the returned Track is the one maximizing `scoreMatch({artist,title}, candidate)`; a clean match is chosen over a karaoke/cover/live variant of the same song that `dedupeBest[0]` would previously have surfaced.
    - (D-02 tie-break) When two candidates score equally (e.g. same song, same cleanliness, different sources), the existing `dedupeBest(..., settings.preferredSource)` preferredSource + quality ordering decides — i.e. scoring re-orders, dedupeBest still collapses dupes + breaks ties.
    - (D-03) Zero `searchAll` results → null (unchanged graceful-miss path). Any throw → null (never throws — unchanged).
  </behavior>
  <action>
    Upgrade `resolveStub` in `src/lib/services/discovery.ts` so that, after `searchAll`, it
    selects the best candidate by `scoreMatch` BEFORE/AROUND `dedupeBest` rather than taking
    `dedupeBest(...)[0]` blindly. Concretely: keep `dedupeBest(r.interleaved, settings.preferredSource)`
    as the de-duplicated, quality/preferred-ordered candidate list (this preserves the final
    tie-break for free — `dedupeBest` already orders by quality + preferredSource and collapses
    same-song dupes), then pick the candidate with the highest `scoreMatch({ artist, title }, c)`
    from THAT list using a STABLE selection (a stable max that, on equal scores, keeps the
    earlier dedupeBest position — so the dedupeBest order IS the tie-break, satisfying D-02's
    "final tie-break = existing ranking"). Return that candidate, or `null` only when the
    deduped list is empty. Import `scoreMatch` from `./score-match`. Preserve VERBATIM: the
    `searchAll(`${artist} ${title}`, 1)` call, the `try { … } catch { return null }` wrapper,
    and the never-throws/null-on-miss posture. Do NOT add a score threshold, do NOT touch
    catalog.ts / dedupe.ts / match-key.ts, do NOT change any caller (player.svelte.ts `playStub`
    and the album page already consume `Track | null`). NO fenced code in the action.
    In `src/lib/services/discovery.test.ts` (RED first for the new behavior): add cases asserting
    (a) given a searchAll result of [karaoke-variant-first, clean-second] for `稻香`, resolveStub
    returns the CLEAN track (proves scoring beats dedupeBest position); (b) equal-score candidates
    fall back to dedupeBest order (preferredSource/quality); (c) the existing zero-results → null
    and throws → null cases STILL pass unchanged. Mock `catalog.searchAll` exactly as the existing
    tests do (spy + `result([...])`).
  </action>
  <verify>
    <automated>pnpm test -- discovery score-match</automated>
  </verify>
  <acceptance_criteria>
    - With searchAll returning a karaoke/cover variant ordered before a clean match of the same `{artist,title}`, `resolveStub` returns the CLEAN match's uid (scoring beats raw dedupeBest[0]).
    - Equal-scored candidates resolve to the dedupeBest-preferred one (preferredSource/quality tie-break intact).
    - `resolveStub('Nobody','Nothing')` with zero searchAll hits → null; `searchAll` throwing → null (never throws). Existing 4 cases remain green.
    - No caller file (`player.svelte.ts`, album page) is modified; `pnpm check` 0 errors / 0 warnings.
    - `pnpm test -- discovery score-match` green.
  </acceptance_criteria>
  <done>resolveStub returns the top-scored candidate (clean over variant), dedupeBest remains the final tie-break, the graceful-miss/never-throws posture is preserved, callers untouched, and check + the discovery/score-match suites are green.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile REQUIREMENTS + ROADMAP traceability for the rescope (D-01)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/10-last-fm-searchable-source-re-search-resolver/10-CONTEXT.md — D-01 disposition (LFSRC-02 done, LFSRC-01 dropped/satisfied-by-pattern, LFSRC-03 live).
    - .planning/REQUIREMENTS.md — the v1.1 traceability table (Phase 10 rows for LFSRC-01/02/03 near line 189) + the LFSRC-03 wording (line 77, which still mentions a duration check — see note below).
    - .planning/ROADMAP.md — Phase 10 section (line 226) goal + success criteria + the existing CONTEXT-D-01 surface note pattern used for Phase 9 (line 199) as the style to mirror.
  </read_first>
  <action>
    Update `.planning/REQUIREMENTS.md` v1.1 traceability table (Phase 10 rows): set
    **LFSRC-02 → Complete** (note: shipped in Phase 9 as `resolveStub`), **LFSRC-01 → Dropped /
    satisfied-by-pattern** (note: `resolveStub` is the de-facto resolver; no formal `'lastfm'`
    SourceId — moved to backlog, parity-with-adapter no longer required for v1.1), and
    **LFSRC-03 → Complete** (this phase). Add a one-line footnote under the table recording the
    D-01 rescope and that LFSRC-03's duration-sanity clause was explicitly dropped (D-04). Keep
    the v1.1 coverage counts honest — if you change the "mapped" wording, reflect that LFSRC-01
    is now satisfied-differently/backlog rather than an unmapped gap (it is NOT an unplanned
    item — it is a deliberate drop per D-01).
    In `.planning/ROADMAP.md` Phase 10 section, add a **CONTEXT D-01 surface note** (mirror the
    Phase 9 line-199 style) immediately under the Phase 10 success criteria stating: the phase
    was rescoped to LFSRC-03 scoring only; LFSRC-02 shipped in Phase 9 (`resolveStub`); LFSRC-01
    (formal `'lastfm'` SourceId) is dropped/backlog (resolveStub is the resolver — avoids
    widening `SourceId`/`SOURCES`); success-criterion #2's duration sanity check is dropped (D-04);
    criterion #4 (registry parity) is N/A under the drop. Do NOT delete the original criteria —
    annotate that D-01 supersedes them, same as the Phase 9 note does.
  </action>
  <verify>
    <automated>grep -n "LFSRC-0" .planning/REQUIREMENTS.md && grep -in "D-01" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md Phase 10 rows read: LFSRC-01 dropped/backlog, LFSRC-02 Complete (Phase 9), LFSRC-03 Complete.
    - A footnote records the D-01 rescope + the D-04 duration-check drop.
    - ROADMAP.md Phase 10 has a CONTEXT-D-01 surface note (mirroring the Phase 9 line-199 style) explaining LFSRC-01 drop, LFSRC-02-in-Phase-9, the duration-check drop, and that criterion #4 registry parity is N/A.
    - `grep` confirms the LFSRC rows and the D-01 note exist; no requirement is left silently unmapped.
  </acceptance_criteria>
  <done>REQUIREMENTS + ROADMAP reflect the D-01 rescope so the requirements-coverage gate sees LFSRC-01 (dropped), LFSRC-02 (done), LFSRC-03 (implemented this phase) with no silent gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Last.fm stub → player | A discovered `{artist,title}` (untrusted-shape metadata) is re-searched and resolved to a playable Track; the wrong candidate = wrong-song playback. |
| resolveStub → shared searchAll fan-out | resolveStub runs inside the same best-effort CN-source path the working 4-source experience uses; a hang/throw here must not stall or break it. |

Surface note (per security brief): Phase 10 adds NO new endpoint, NO new secret, NO new
external call. `scoreMatch` is a pure in-memory ranking function; `resolveStub` continues to
reuse the EXISTING `searchAll` (already-deployed CN proxies). No new network or secret surface
is introduced — so no new HIGH-severity threat is created. The two threats below are the
pre-existing Pitfall-7 posture this phase HARDENS (T-10-01) and PRESERVES (T-10-02).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Spoofing (wrong-song / mismatched content) | `resolveStub` / `scoreMatch` candidate selection | mitigate | This phase's deliverable: `scoreMatch` penalizes cover/karaoke/live/instrumental/CJK variant titles and rewards normalized artist+title (matchKey) similarity so a tap plays the real song, not a variant (Pitfall 7 "mismatched audio"). Covered by Task 1 + Task 2 tests (clean beats karaoke/翻唱). |
| T-10-02 | Denial of Service (resolver stalling the shared fan-out) | `resolveStub` inside the searchAll best-effort path | accept (preserve existing control) | resolveStub keeps its `try/catch → null`, never-throws, lazy-per-tap posture (Phase 9 D-03); it reuses `searchAll`'s existing `Promise.allSettled` + per-adapter `AbortSignal` isolation. No inline blocking added. No new timeout needed — the scorer is synchronous/in-memory and adds no I/O. Pitfall 7 isolation requirement already satisfied by the unchanged searchAll path. |
| T-10-03 | Tampering (supply chain — package installs) | npm/pnpm installs | mitigate (N/A this phase) | No new dependency is added (pure helper reusing matchKey). No `[ASSUMED]`/`[SUS]` package, no install task — supply-chain checkpoint not triggered. |
</threat_model>

<verification>
- `pnpm check` → 0 errors / 0 warnings (TS strict, svelte-check).
- `pnpm test` → full suite green (existing 222+ tests still pass; new score-match + extended discovery cases added).
- `pnpm test -- score-match discovery` → the two touched suites green in isolation.
- Behavioral proof: a searchAll result with a karaoke/翻唱 variant ordered before a clean match resolves to the CLEAN track via `resolveStub` (T-10-01 mitigation evidence).
- Graceful-miss proof: zero searchAll hits → `resolveStub` returns null; searchAll throwing → null (never throws) — existing cases remain green (T-10-02 posture preserved).
- Traceability proof: `grep "LFSRC-0" .planning/REQUIREMENTS.md` shows LFSRC-02 Complete / LFSRC-01 dropped / LFSRC-03 Complete; ROADMAP Phase 10 carries the D-01 surface note.
</verification>

<success_criteria>
- LFSRC-03 delivered: `resolveStub` picks the best `{artist,title}` match by normalized scoring
  (variant-keyword penalty + matchKey similarity), with dedupeBest preferredSource/quality as the
  final tie-break — not a blind first result (ROADMAP Phase 10 criterion #2, minus the D-04 duration clause).
- Graceful miss preserved: zero results → "no playable source" (null), never breaks search/queue/other
  sources, never throws (ROADMAP Phase 10 criterion #3).
- Single change point honored: only `resolveStub` + a new pure `scoreMatch` helper change; no caller,
  no endpoint, no registry, no new dependency (D-05).
- `scoreMatch` is pure and fully unit-tested (variant penalty, query-asked-for-variant exception,
  similarity ordering, determinism).
- Traceability reconciled (D-01): LFSRC-02 Complete, LFSRC-01 dropped/backlog, LFSRC-03 implemented.
- `pnpm check` + `pnpm test` green.
</success_criteria>

<output>
Create `.planning/phases/10-last-fm-searchable-source-re-search-resolver/10-01-SUMMARY.md` when done.
</output>
