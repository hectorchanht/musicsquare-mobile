---
phase: 08-last-fm-read-foundation-metadata-enrichment
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/routes/(app)/album/[name]/+page.svelte
autonomous: true
requirements: [ENRICH-01, ENRICH-02]

must_haves:
  truths:
    - "Opening an album page shows a higher-res Last.fm album cover (when strictly better) and album info (listeners / playcount), augmenting — never replacing — the existing derived track list (D-01, D-02, D-03)"
    - "An album with no Last.fm match still renders the existing derived track list exactly as before — enrichment is silent best-effort and never blocks the searchAll load"
    - "The album cover never regresses to a Last.fm placeholder/empty image (D-04)"
  artifacts:
    - path: "src/routes/(app)/album/[name]/+page.svelte"
      provides: "Album page augmented with hi-res cover + listeners/playcount info, without changing the existing searchAll track-list behavior"
      contains: "enrichAlbum"
  key_links:
    - from: "src/routes/(app)/album/[name]/+page.svelte"
      to: "src/lib/services/lastfm.ts"
      via: "second $effect keyed on name calls enrichAlbum async (separate from the searchAll $effect)"
      pattern: "enrichAlbum"
---

<objective>
Enrich the existing `/album/[name]` page with Last.fm album data — a higher-res album cover (from `album.getInfo`, the most-reliable Last.fm art source, swapped only when strictly better and never a placeholder) and an album info line (listeners / playcount) — layered on without touching the page's existing derived `searchAll` track-list behavior.

Purpose: Deliver D-01c (album page art + info) using the Plan-01 enrichment service, satisfying ENRICH-01/02 on the album surface.
Output: An augmented album page that shows a better cover + listeners/playcount when matched and degrades silently to the existing track-list-only view otherwise.
</objective>

<phase_goal>
**As a** listener, **I want to** see a crisper album cover and how many people listen to it on the album page, **so that** the album view feels richer without losing the existing track list.

(Derived from the ROADMAP Phase 8 `**Goal:**` line — outcome-shaped, not in canonical user-story form. Capability and outcome taken from that goal; no scope invented.)
</phase_goal>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-last-fm-read-foundation-metadata-enrichment/08-CONTEXT.md
@.planning/phases/08-last-fm-read-foundation-metadata-enrichment/08-PATTERNS.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Contracts from Plan 01 (already shipped in wave 1). Use directly. -->

From src/lib/services/lastfm.ts (Plan 01):
```typescript
export type EnrichResult = { tags: string[]; bio: string | null; bioUrl: string | null; lastfmArt: string | null; listeners?: number | null; playcount?: number | null };
export function enrichAlbum(album: string, artist: string): Promise<EnrichResult>; // never throws; all-empty on failure/absent-key
```

From src/lib/i18n (Plan 01 already added these — CONSUME only, do NOT edit i18n):
```typescript
t('lastfm.listeners')  // album info label
t('lastfm.playcount')  // album info label
```

From src/routes/(app)/album/[name]/+page.svelte (existing — the analog to extend):
```typescript
const name = $derived(decodeURIComponent(page.params.name ?? ''));
let tracks = $state<Track[]>([]); let loading = $state(true); let loadedFor = '';
const hero = $derived(tracks.find((t) => t.cover)?.cover ?? null);
$effect(() => { /* keyed on name + loadedFor → searchAll(n,1) → dedupeBest → prefer exact-album → tracks */ });
// hero markup: <div class="cover" style:background-image={hero ? `url(${hero})` : '...'}></div>
// <h1>{names.dn(name)}</h1> <p class="note">{t('album.derived', {count})}</p>
// NOTE: the album page has NO artist in scope by default — derive an album artist from the
// resolved tracks (e.g. tracks[0]?.artist) to pass to enrichAlbum(name, artist).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add album hi-res cover + listeners/playcount info to the album page (augment, never replace)</name>
  <files>src/routes/(app)/album/[name]/+page.svelte</files>
  <read_first>
    - src/routes/(app)/album/[name]/+page.svelte (the full existing page — its name-keyed $effect with loadedFor guard, the .cover hero bg-image at L53, the .note/.muted styles to reuse)
    - src/lib/services/lastfm.ts (Plan 01 — enrichAlbum contract; never throws; album art from album.getInfo)
    - src/lib/stores/names.svelte.ts usage in the page (names.dn — album name uses dn; numeric info is formatted plainly)
    - .planning/research/FEATURES.md (album.getInfo = most-reliable cover art; image-array placeholder trap)
    - .planning/research/PITFALLS.md (Pitfall 8 — placeholder never overwrites real art; ENRICH-02 non-negotiable)
  </read_first>
  <action>
    In `src/routes/(app)/album/[name]/+page.svelte`, add enrichment state alongside the existing `tracks`/`loading`/`loadedFor`: `let enrich = $state<EnrichResult | null>(null)` and `let enrichedFor = ''`. Import `enrichAlbum`, `EnrichResult` from `$lib/services/lastfm`.
    Derive the album artist from the resolved track list: `const albumArtist = $derived(tracks[0]?.artist ?? '')` (the album page has no artist param; album.getInfo needs both album + artist).
    Add a SECOND `$effect` (separate from the existing `searchAll` effect — do NOT merge; enrichment must never block or replace the track-list load, D-02) keyed on `name` + `albumArtist` with its own `enrichedFor` guard: when the key changes and `albumArtist` is non-empty, set `enrichedFor`, `enrich = null`, then `void enrichAlbum(name, albumArtist).then((r) => { if (still current) enrich = r })`. Best-effort, async, never awaited. (Because albumArtist comes from the searchAll results, enrichment naturally runs after the track list resolves — keeping it off the initial render path.)
    Cover swap (D-03/D-04, ENRICH-02 overrides D-03): compute the effective hero as `const heroImg = $derived(enrich?.lastfmArt ?? hero)` and use it in the `.cover` background-image. Because `enrichAlbum` already placeholder-filters (lastfmArt is null when only grey-star/empty), the fallback keeps the existing derived `hero` — a real cover never regresses (ENRICH-02). Optionally preload before swap (mirror the NowPlaying preload guard from Plan 01) if a flash is observed; for the album page a direct `$derived` swap is acceptable since it is not the playback background.
    Album info line: render listeners / playcount near the existing `.note` line when present, using `t('lastfm.listeners')` / `t('lastfm.playcount')` (Plan-01 keys) and a plain number format (e.g. `Intl.NumberFormat`). Render the info line only when `enrich?.listeners` or `enrich?.playcount` is present.
    Do NOT modify the existing `searchAll` effect, the exact-album filtering, or the track list — they must behave exactly as before. Do NOT add or edit any i18n keys (consume Plan-01 keys only).
  </action>
  <verify>
    <automated>pnpm check && pnpm test -- --run</automated>
    <human-check>Open `/album/<a well-known album>` on a `wrangler dev`/deployed build with LASTFM_KEY set: confirm a higher-res cover (if better) and a listeners/playcount line appear, while the existing derived track list still renders. Then open `/album/<an obscure CN album not on Last.fm>` and confirm the page renders exactly as before with no info line and no broken/placeholder cover.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/routes/(app)/album/[name]/+page.svelte` imports and calls `enrichAlbum` in a SECOND `$effect` separate from the existing `searchAll` effect (the searchAll effect is unmodified).
    - The album artist passed to `enrichAlbum` is derived from the resolved tracks (e.g. `tracks[0]?.artist`).
    - The effective cover falls back to the existing derived `hero` when `lastfmArt` is null (no placeholder regression — ENRICH-02).
    - The listeners/playcount info line renders only when present and uses the Plan-01 `lastfm.listeners`/`lastfm.playcount` keys (this plan does NOT modify any i18n file).
    - `pnpm check` clean; all existing tests pass.
  </acceptance_criteria>
  <done>Album page shows a higher-res cover + listeners/playcount when matched; degrades silently to the existing track-list view otherwise; no placeholder cover regression; consumes Plan-01 i18n keys only; check + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → edge (`/api/lastfm/info`) | The album page calls the Plan-01 edge route via `enrichAlbum`; LASTFM_KEY stays edge-only (inherited posture) |
| Last.fm image URL | Last.fm-supplied cover URL is loaded as a CSS background-image after placeholder filtering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Information Disclosure | enrichAlbum → /api/lastfm/info | mitigate | Inherits Plan-01 T-08-01: LASTFM_KEY edge-only, never in body/headers; the client `enrichAlbum` only sees the clean shape |
| T-08-13 | DoS-of-UX | enrichAlbum load | mitigate | Enrichment runs in a SEPARATE `void`-fired `$effect` (gated on the album artist derived from results), never awaited, never blocks the existing page; `enrichAlbum` never throws (Plan-01 contract) |
| T-08-14 | Information Disclosure (UX regression) | album cover | mitigate | Effective cover falls back to the existing derived cover when `lastfmArt` is null (placeholder already filtered upstream) — no placeholder regression (ENRICH-02) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (reuses Plan-01 service). No legitimacy checkpoint required |
</threat_model>

<verification>
- `pnpm check` clean; `pnpm test -- --run` green (no regressions; this plan adds no new tests but must not break existing suites).
- Human-check: hi-res cover + listeners/playcount appear for a matched album; a non-matched/absent-key album page renders exactly as before with no broken/placeholder cover and no orphan info line.
</verification>

<success_criteria>
- ENRICH-01: album page shows a higher-res Last.fm cover + album info merged onto the existing derived view.
- ENRICH-02: album cover never regresses to a placeholder image.
- D-01c: album art + info (listeners/playcount) surfaced on the album page.
- The existing `searchAll` track-list behavior is unchanged (D-02: augment, not replace).
</success_criteria>

<output>
Create `.planning/phases/08-last-fm-read-foundation-metadata-enrichment/08-03-SUMMARY.md` when done.
</output>
